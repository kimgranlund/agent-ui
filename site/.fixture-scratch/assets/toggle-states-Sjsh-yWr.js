import"./super-shell-D76CLu9A.js";import{t as e}from"./src-aOW07B5_.js";import"./phosphor-2R9oNMbL.js";import{n as t}from"./_page-DIBN49D1.js";/* empty css               */var{content:n}=t({title:`Toggle — interaction states`,intro:`The live <ui-toggle> below, staged in each interaction state. Every state here is authored by the control itself in toggle.css — this page only stages and labels, never restyling a toggle. Hover and active step the pressed/idle fill, :focus-visible draws the shared fleet focus ring, and a disabled toggle is fully inert — out of the tab order and pointer-dead.`});function r(t){let n=document.createElement(`ui-toggle`);if(t.icon){let r=e(t.icon);r.setAttribute(`slot`,`icon`),n.append(r)}return n.append(document.createTextNode(t.label)),t.pressed&&n.setAttribute(`pressed`,``),t.disabled&&n.setAttribute(`disabled`,``),n}function i(e,t){let n=document.createElement(`section`);n.className=`state-section`;let r=document.createElement(`h2`);r.textContent=e;let i=document.createElement(`p`);return i.className=`state-instruction`,i.innerHTML=t,n.append(r,i),n}function a(...e){let t=document.createElement(`div`);return t.className=`state-row`,t.append(...e),t}function o(e){let t=document.createElement(`span`);return t.className=`state-caption`,t.textContent=e,t}var s=document.createElement(`ul`);s.className=`activation-log`,s.setAttribute(`aria-live`,`polite`);var c=0;function l(e,t){e.addEventListener(`click`,e=>{let n=e.detail,r=n===0?`keyboard`:`pointer`;c+=1;let i=document.createElement(`li`);i.dataset.source=r,i.textContent=`#${String(c).padStart(2,`0`)}  ${t.padEnd(20)}${r.padEnd(10)}(detail=${n})`,s.append(i),s.scrollTop=s.scrollHeight})}var u=i(`Hover — idle and pressed fill`,`Move your pointer over each enabled toggle. The idle (unpressed) pill is a ghost-like transparent background with a hover wash; the pressed pill is a tonal <code>primary-container</code> fill that gains its own hover step. The cursor also becomes a pointer. The disabled toggle holds its muted colour: it is pointer-inert, so <code>:hover</code> never matches it.`);u.append(a(r({label:`Unpressed`}),r({label:`Pressed`,pressed:!0}),o(`hover each — the fill steps`),r({label:`Disabled`,disabled:!0}),o(`disabled — holds (pointer-inert, :hover never matches)`)));var d=i(`:focus-visible — the shared focus ring`,`Toggles are keyboard-focusable by default (the <code>tabbable</code> trait sets <code>tabindex=0</code> — this page adds nothing). Press <strong>Tab</strong> to move focus onto a toggle: the control draws the shared fleet focus ring (ADR-0009). Now click a toggle with the mouse instead — <strong>no</strong> ring appears, because <code>:focus-visible</code> matches keyboard focus, not a pointer click.`);d.append(a(r({label:`Unpressed`}),r({label:`Pressed`,pressed:!0}),o(`Tab → identical ring · mouse-click → no ring`)));var f=i(`:active — pressed down`,`Press and <strong>hold</strong> the mouse button down on a toggle: while held, the fill steps to its deepest ladder step. Release to return to the hover/idle fill. With keyboard focus, holding <strong>Space</strong> holds it active until you release.`);f.append(a(r({label:`Unpressed`}),r({label:`Pressed`,pressed:!0}),o(`mouse-down / Space-held → deepest step`)));var p=i(`Keyboard activation — the press-activation trait`,`Tab to a toggle below, then press <strong>Space</strong> or <strong>Enter</strong> — unlike the Indicator class (checkbox/switch/radio), <strong>both</strong> activate (role="button", not role="checkbox"). Every activation — keyboard or mouse — appends a line to the log below (it listens for the real <code>click</code> event). Trait clicks arrive with <code>detail: 0</code>, pointer clicks with <code>detail ≥ 1</code>, so the log tags the source.`),m=r({label:`Chat`,icon:`chats-circle`}),h=r({label:`Settings`,icon:`gear-six`});l(m,`Chat`),l(h,`Settings`),p.append(a(m,h,o(`Tab to each, then Space or Enter`)),s);var g=i(`Disabled — out of the tab order and pointer-inert`,`A <code>disabled</code> toggle is fully inert. <strong>Tab</strong> from the first enabled toggle below — focus jumps straight to the last one, <strong>skipping</strong> Unavailable: the <code>tabbable</code> trait removes the disabled host from the tab order, so keyboard focus never lands on it and Space/Enter can never reach it. toggle.css repoints it to muted neutral roles and sets <code>pointer-events: none</code>.`),_=r({label:`Before`}),v=r({label:`Unavailable`,disabled:!0}),y=r({label:`After`});l(v,`Unavailable (disabled)`),g.append(a(_,o(`enabled — Tab reaches this`),v,o(`disabled — Tab skips it; cursor + click inert`),y,o(`enabled — focus jumps here, over Unavailable`))),n.append(u,d,f,p,g);