import"./super-shell-D76CLu9A.js";import{n as e}from"./_page-DIBN49D1.js";import{a as t,r as n}from"./doc-page-H_CmxYv1.js";import{j as r}from"./frontmatter-D6AIzGjv.js";/* empty css                 */import"./markdown-BGzpPrJN.js";var{descriptor:i,body:a}=r(),{content:o}=e({title:`ui-markdown — API`,intro:`A Display-class, zero-dependency renderer for a small, hand-rolled markdown subset (ADR-0119 fork F4) — never a vendored markdown library. Parses into an AST of KNOWN node kinds only (no raw-HTML node kind exists at all, so sanitization is by construction, not a filter pass) and replaces its own children with real fleet DOM: ui-text (headings, paragraphs, links, blockquotes), native ol/ul/li, ui-code (fenced blocks), and ui-table (GFM tables) — never innerHTML. Syntax highlighting inside a fenced code block is a separate, opt-in concern — see the Highlighting guide.`});n(o,i,a,s());function s(){let e=document.createElement(`section`);e.append(t(2,`Examples`));let n=c('# Getting started\n\nA **bold** claim, an *emphasized* aside, and some `inline code` — all inline runs share one left-to-right scan, no recursive regex.\n\nA [link](https://example.com) renders as `ui-text[as="a"]`; a raw `<script>` tag in the source is just literal text — the block pass never opens an HTML lane.'),r=c(`## Supported block types

- ATX headings (\`#\` through \`######\`)
- Ordered and unordered lists, with indent-depth nesting
- Blockquotes (\`>\`)
- GFM tables
- Fenced code blocks
`),i=c('> A blockquote renders as `ui-text[as="blockquote"]` — a real fleet element, not a native `<blockquote>`.\n\n| Pack | Zero-dep | Notes |\n| --- | --- | --- |\n| `./markdown` | yes | this element — a hand-rolled subset renderer |\n| `./highlight` | yes | seven bundled tokenizers, opt-in registration |\n| `./editor` | **no** | the one ruled exception — CodeMirror 6, lazy-loaded (ADR-0139) |\n'),a=c("## A fenced block\n\n```ts\nconst greet = (name: string): string => `Hello, ${name}!`\n```\n\nRenders as a real `ui-code` element — plain by default; see the Highlighting guide to turn on real syntax coloring.");return e.append(l(`Headings, emphasis, inline code, links`,n),l(`Lists`,r),l(`Blockquote + a GFM table`,i),l(`A fenced code block`,a)),e}function c(e){let t=document.createElement(`ui-markdown`);return t.markdown=e,t}function l(e,n){let r=document.createElement(`div`);return r.style.cssText=`margin:0.5rem 0 1.5rem; max-inline-size:42rem;`,r.append(t(3,e),n),r}