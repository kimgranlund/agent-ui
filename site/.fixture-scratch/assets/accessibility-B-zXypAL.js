import"./super-shell-D76CLu9A.js";import{n as e,r as t}from"./_page-DIBN49D1.js";import{t as n}from"./code-block-DEt2Scp8.js";import{a as r}from"./doc-page-H_CmxYv1.js";/* empty css                   */import{a as i,i as a,s as o}from"./specimens-BSFejhGR.js";var{content:s}=e({title:`Accessibility conventions`,intro:`How the fleet exposes roles and state, manages focus, and announces change — the conventions every control follows, cited to their source rather than restated, plus one small live example.`}),c=e=>document.createTextNode(e),l=e=>a(`code`,{},[c(e)]),u=(...e)=>a(`p`,{},e.map(e=>typeof e==`string`?c(e):e));s.append(t(`Three load-bearing rules, stated once in the repo’s CLAUDE.md "Conventions" section and never repeated here: components are light-DOM by default; ARIA rides `,l(`ElementInternals`),`, never a host attribute; and there are no native form elements underneath a control — a ui-text-field is a real, form-associated custom element, not an <input> wrapper (the same two-sentence framing `,l(`getting-started.html`),` opens with). What follows is how that shows up: roles/state, focus, landmarks, live regions, motion, and labelling — each cited to its owning source.`)),s.append(r(2,`1 · Roles and state — via ElementInternals only`)),s.append(u(`A control sets its own role and AX state on `,l(`this.internals`),` (a protected accessor a subclass alone can reach) — never `,l(`setAttribute('role', …)`),` or `,l(`setAttribute('aria-*', …)`),` on the host. The disabled contract is the canonical worked example (`,l(`.claude/docs/references/interaction-states.md`),` §3): a reusable `,l(`tabbable`),` trait (`,l(`packages/agent-ui/components/src/traits/tabbable.ts`),`) owns focusability — `,l(`tabindex=0`),` by default, removed (not set to `,l(`-1`),`) when disabled, native-parity — while the AX announcement is a control-level effect, because `,l(`internals`),` is protected and a trait only ever receives `,l(`host: UIElement`),`:`)),s.append(n([`this.internals.role = '{role}'`,`tabbable(this,       { disabled: () => this.disabled })`,`pressActivation(this, { disabled: () => this.disabled })`,`this.effect(() => { this.internals.ariaDisabled = this.disabled ? 'true' : null })`].join(`
`),`ts`)),s.append(u(`The split is the reusable lesson: focusability recurs on every interactive control → a trait; `,l(`ariaDisabled`),` cannot be a trait (protected `,l(`internals`),`) → a one-line control-level effect any control with a `,l(`disabled`),` prop copies (interaction-states.md §3). A `,l(`UIFormElement`),`-based control (ui-text-field, …) is form-associated and gains a real platform `,l(`disabled`),` state instead — confirm the disabled channel per family before copying the `,l(`UIElement`),` shape onto a form control.`)),s.append(r(2,`2 · Focus management`)),s.append(u(`The keyboard focus ring is a fleet CONSTANT, not a per-control opinion (interaction-states.md §2): every control’s `,l(`@scope`),` block applies the identical rule, reading three shared tokens from `,l(`@agent-ui/shared`),`:`)),s.append(n([`:scope:focus-visible {`,`  outline: var(--ui-focus-ring-width) solid var(--md-sys-color-focus-ring);`,`  outline-offset: var(--ui-focus-ring-offset);`,`}`].join(`
`),`css`)),s.append(u(l(`outline`),` (not `,l(`box-shadow`),`) so it paints outside the box without affecting layout and survives `,l(`forced-colors`),`; `,l(`:focus-visible`),` (not `,l(`:focus`),`) so the ring is keyboard-only — no ring on a mouse click.`)),s.append(r(3,`Roving tabindex`)),s.append(u(`A set of items with one moving focus stop (a nav rail, a tablist, a listbox) uses the shared `,l(`rovingFocus`),` trait (`,l(`packages/agent-ui/components/src/traits/roving-focus.ts`),`): exactly one item holds `,l(`tabindex=0`),`, the rest `,l(`-1`),`; Arrow keys (+ Home/End, + optional type-ahead) move the roving index AND transfer real focus; `,l(`loop`),` wraps at the ends. Two real consumers on this site: `,l(`ui-nav-rail`),` (`,l(`role="tablist"`),` when every item is bare, `,l(`role="navigation"`),` otherwise — `,l(`nav-rail.ts:158`),`) and `,l(`ui-tabs`),` (the trait’s original extraction site, `,l(`tabs.ts`),`).`)),s.append(u(`Where an item can be REORDERED rather than merely focused, a pointer-drag mechanism alone fails WCAG 2.2 SC 2.5.7 (Dragging Movements) — the reusable `,l(`list-reorder`),` trait (`,l(`packages/agent-ui/components/src/traits/list-reorder.ts`),`) pairs pointer-capture drag with a keyboard fallback: Up/Down while a handle is armed and focused commits the same move as a drag, converging on one `,l(`onCommit(from, to)`),` + one `,l(`change`),` event either way. The trait writes no ARIA of its own (the fleet’s ElementInternals-only law again) — the accessible story is the consumer’s own labelled handle, e.g. a real `,l(`aria-label="Move X up"`),` button.`)),s.append(r(2,`3 · Landmarks in the app-surface shells`)),s.append(u(`The same `,l(`this.internals.role`),` mechanism (§1) is how a shell composes real ARIA landmarks — no host attribute, ever:`));{let e=[{control:`ui-nav-rail`,role:`navigation (tablist when every item is bare)`,source:`nav-rail.ts:158`},{control:`ui-pagination`,role:`navigation`,source:`pagination.ts:44`},{control:`ui-drill`,role:`group (a labelled navigation region)`,source:`drill.ts:86`},{control:`ui-drill-panel`,role:`region`,source:`drill-panel.ts:45`},{control:`ui-surface-host`,role:`region`,source:`surface-host.ts:155`}],t=document.createElement(`table`),n=document.createElement(`thead`),r=document.createElement(`tr`);for(let e of[`Control`,`Role`,`Source`]){let t=document.createElement(`th`);t.textContent=e,r.append(t)}n.append(r);let i=document.createElement(`tbody`);for(let t of e){let e=document.createElement(`tr`),n=document.createElement(`td`);n.append(l(t.control));let r=document.createElement(`td`);r.textContent=t.role;let a=document.createElement(`td`);a.append(l(t.source)),e.append(n,r,a),i.append(e)}t.append(n,i),s.append(t)}s.append(r(2,`4 · Live regions`)),s.append(u(l(`ui-status-stream`),` and `,l(`ui-app/conversation-dialog`),` both set `,l(`internals.role = 'log'`),` (a POLITE live region — ARIA’s `,l(`role=log`),` carries an implicit `,l(`aria-live="polite"`),`) — `,l(`status-stream.ts:366`),` and `,l(`conversation-dialog.ts:45`),`. `,l(`ui-toast`),` sets `,l(`role="status"`),` in its CONSTRUCTOR, not `,l(`connected()`),` — the live-region semantics must exist BEFORE the element is inserted, since content present at append time is what announces (`,l(`toast.ts`),`, SPEC-R15 AC2); `,l(`connected()`),` may later flip `,l(`status`),` → `,l(`alert`),` for an urgent toast.`)),s.append(u(`A soft-drift note, not a claim this page makes structural: `,l(`ui-combo-box`),`’s "No matches" empty row (`,l(`combo-box.ts:427`),`) is `,l(`role="presentation"`),` with no `,l(`aria-live`),` anywhere in the control — it renders visually but is not announced to assistive tech when a filter empties the list. Flagged below in Findings rather than fixed here (this page derives from the shipped behaviour, it does not change it).`)),s.append(r(2,`5 · Reduced motion`)),s.append(u(`State-paint transitions (never geometry, never `,l(`all`),`) are gated behind a post-first-paint `,l(`:state(ready)`),` custom state, and every control zeroes them under the media query — non-negotiable (interaction-states.md §4c):`)),s.append(n([`@media (prefers-reduced-motion: reduce) {`,`  :scope:state(ready) { transition: none; }`,`}`].join(`
`),`css`)),s.append(r(2,`6 · How a consumer labels a control`)),s.append(u(`A wrapper composes its own accessible-name delivery rather than asking the wrapped control to read a plain <label for>. Two shapes, same mechanism (a real DOM id wired through `,l(`aria-labelledby`),`):`)),s.append(u(`· `,l(`ui-field`),` (`,l(`packages/agent-ui/components/src/controls/field/field.ts`),`) wraps ONE control and supplies its `,l(`label`),`/`,l(`description`),` as real DOM parts; the ADR-0051 labelling seam sets `,l(`aria-labelledby`),` on the WRAPPED control’s own ElementInternals to the label part’s id — so the accessible name is correct even though the control never received a label attribute directly.`)),s.append(u(`· `,l(`ui-drawer`),` (`,l(`packages/agent-ui/components/src/controls/drawer/drawer.ts:159`),`) FORWARDS an author-supplied `,l(`aria-label`),`/`,l(`aria-labelledby`),` off the HOST onto the internal `,l(`<dialog data-part="dialog">`),` part, then strips it from the host (ADR-0017 cl.5) — the host stays role/aria-clean while the real top-layer surface carries the name.`)),s.append(r(2,`7 · Live example`)),s.append(u(`A real `,l(`ui-field`),`-wrapped `,l(`ui-text-field`),` (§6’s labelling seam) beside a real `,l(`ui-toast-region`),` (§4’s `,l(`role="status"`),` live region). Save announces through the live region — inspect either with a screen reader or the accessibility tree in devtools; both are the real controls, not a mock.`));var d=document.createElement(`ui-toast-region`),f=a(`ui-field`,{label:`Display name`,description:`Shown on your public profile`},[a(`ui-text-field`,{name:`display-name`,placeholder:`Ada Lovelace`})]);f.style.cssText=`max-inline-size:24rem;`;var p=o(`Save`,`solid`);p.addEventListener(`click`,()=>{let e=f.querySelector(`ui-text-field`)?.value??``;d.show({message:e===``?`Enter a display name first.`:`Saved “${e}”.`,urgent:e===``})}),s.append(i(`Labelled field + status live region`,f,p,d)),s.append(a(`p`,{class:`a11y-crosslinks`},[c(`See also: `),a(`a`,{href:`./getting-started.html`},[c(`Getting started`)]),c(` (the light-DOM/ElementInternals framing this page expands on) and `),a(`a`,{href:`./forms.html`},[c(`Forms`)]),c(` (the ui-field labelling seam §6 cites, walked end-to-end).`)]));