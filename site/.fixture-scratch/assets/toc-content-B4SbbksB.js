import"./super-shell-D76CLu9A.js";import"./nav-rail-sXDz1Xz6.js";import{n as e,r as t}from"./_page-DIBN49D1.js";import{a as n}from"./doc-page-H_CmxYv1.js";var r=`0px 0px -80% 0px`;function i(e,t,n){let r=null;for(let n of e)t.get(n)&&(r=n);if(r)return r;let i=n?.getBoundingClientRect().top??0,a=-1/0;for(let t of e){let e=t.getBoundingClientRect().top;e<=i&&e>a&&(a=e,r=t)}return r}function a(e,t){let n=t.enabled??(()=>!0),a=t.rootMargin??r,o,s=e.effect(()=>{if(!n()){o!==null&&(o=null,t.onActiveChange(null));return}let e=t.headings;if(e.length===0)return;let r=new Map,s=t.root??null,c=()=>{let n=i(e,r,s)?.id||null;n!==o&&(o=n,t.onActiveChange(n))};if(typeof IntersectionObserver>`u`)return;let l=new IntersectionObserver(e=>{for(let t of e)r.set(t.target,t.isIntersecting);c()},{root:s,rootMargin:a,threshold:0});for(let t of e)l.observe(t);return()=>l.disconnect()});return()=>s()}var{content:o}=e({title:`Sticky TOC content layout`,intro:`A long-form article + a scroll-spy’d ui-nav-rail sidebar, swapped to a ui-select below the ADR-0150 compact line — the composition recipe for GH #964’s "content corpus + sticky nav" pattern. The TOC below is derived from this very page’s own headings; scroll it and watch the active item track your position.`});o.append(t(`Every ingredient already shipped — ui-nav-rail, ui-select, real ui-text-style headings, and shell-breakpoint.ts’s named compact line. What was missing was the scroll-spy wiring and the responsive swap between them; this recipe supplies both without a new control.`));function s(e){return e.toLowerCase().replace(/[^a-z0-9]+/g,`-`).replace(/(^-|-$)/g,``)}function c(e){let t=n(2,e);return t.id=s(e),t}function l(e){let t=document.createElement(`p`);return t.textContent=e,t}var u=document.createElement(`article`);u.className=`toc-content-article`,u.append(c(`Overview`),l(`A reference corpus — docs, a knowledge base, a long settings/help page — reads best with a content column plus a sticky side nav that always shows where you are. Below a certain width there is no room for a sidebar at all, so the same navigation collapses into a single control pinned at the top: MDN and most SaaS docs sites take this shape (the brief’s canonical pattern).`),c(`Why not a scroll listener`),l("The obvious first instinct is a `scroll` event handler that re-measures every heading’s `getBoundingClientRect()` on every frame. That works, but it runs continuously whether anything changed or not. An IntersectionObserver only fires when a heading actually crosses a named band near the top of the viewport — the browser does the continuous work, the trait only reacts to real crossings."),c(`The scroll-spy trait`),l("`scrollSpy(host, opts)` (traits/ tier, `@agent-ui/components/traits/scroll-spy`) takes the list of headings to watch and an `onActiveChange(id)` callback. It keeps its own live map of every heading’s intersecting state — IntersectionObserver callbacks are DELTA-only, reporting just the targets that changed — and reports the last (topmost-crossed) intersecting heading in document order, falling back to the nearest already-passed heading so the active id never blanks out mid-article."),c(`Composing the wide rail`),l('The sidebar is a plain `ui-nav-rail` with `collapse="none"` (the family’s own escape hatch for "a consumer whose shell owns the narrow behavior" — this page does, not the rail). One `ui-nav-rail-item` per `h2`, each with a real `href="#slug"` — link-shaped, so it renders a real `<a>`: native Tab order, status-bar preview, ctrl/cmd-click-new-tab, and the browser’s own anchor jump all come free, no click handler needed for navigation itself.'),c(`The compact swap`),l("Below the ADR-0150 compact line (52.5rem / 840px — shell-breakpoint.ts’s `SHELL_COMPACT_BREAKPOINT_REM`, the SAME line ui-settings’ rail→select swap uses, GH #962) the rail hides and a `ui-select` holding the identical set of sections takes its place, pinned above the article. A `ResizeObserver` on the layout’s own box stamps `data-compact` — the exact idiom #962 shipped, reused rather than re-invented — against the IMPORTED constant (`@agent-ui/app/shell-breakpoint`, the package’s own named export), never a re-minted literal: one source of truth for the line, so it cannot drift from the shell family’s."),c(`Wiring active-state sync`),l("One `scrollSpy` call drives BOTH halves: its `onActiveChange` callback marks the matching `ui-nav-rail-item`’s `selected` attribute (native ARIA/indicator handling, zero extra work) and writes the compact `ui-select`’s `.value` in lock-step — a silent, non-emitting reflect (ADR-0019), so keeping both synced risks no doubled event. Choosing a section from the select scrolls the matching heading into view; scrolling the article updates the rail’s indicator. Either surface stays correct regardless of which one is currently visible."),c(`Accessibility notes`),l("Link-shaped `ui-nav-rail-item`s carry native anchor semantics, so no ARIA is hand-rolled for the rail itself. `scroll-margin-block-start` on every heading keeps an anchor jump (or a `scrollIntoView()` call from the compact select) from landing UNDER the page’s own sticky header. The active-item indication is a real `selected` attribute driving a `border-inline-start` bar — not color alone — so it survives forced-colors mode, the same law the rest of the nav-rail family already carries."),c(`Anti-patterns`),l("Do not fork a second compact-breakpoint mechanism: reuse the named ADR-0150 line, never a bespoke width guess. Do not run the scroll-spy decision off a raw `scroll` listener when IntersectionObserver is available — the continuous-cost trade-off above. Do not show the rail AND the select at the same width — exactly one navigation surface is visible at any given band, matching ui-settings’ own rail/select posture."),c(`Mint-last verdict: no ui-toc`),l("This recipe carries the whole pattern — heading-derived entries, sticky positioning, scroll-spy sync, and the responsive swap — in one page of composition over shipped controls, with no bespoke rendering, no new ARIA contract, and no new CSS component. A `ui-toc` control would duplicate exactly what `ui-nav-rail` + `ui-select` + `scrollSpy` already do; the mint bar (recipe too heavy to ask of consumers) is not met. Verdict: DO NOT MINT — recorded in the issue’s Findings, reconsider only if a second consumer needs this composition and finds the wiring genuinely too heavy to repeat."));var d=[...u.querySelectorAll(`h2`)].map(e=>({id:e.id,label:e.textContent??``})),f=document.createElement(`ui-nav-rail`);f.className=`toc-content-nav`,f.setAttribute(`collapse`,`none`),f.setAttribute(`aria-label`,`Table of contents`);var p=document.createElement(`ui-select`);p.className=`toc-content-select`,p.label=`Table of contents`;var m=new Map;for(let e of d){let t=document.createElement(`ui-nav-rail-item`);t.setAttribute(`href`,`#${e.id}`),t.textContent=e.label,m.set(e.id,t),f.append(t);let n=document.createElement(`div`);n.setAttribute(`role`,`option`),n.setAttribute(`value`,e.id),n.textContent=e.label,p.append(n)}p.addEventListener(`select`,()=>{let e=p.value;e&&document.getElementById(e)?.scrollIntoView({behavior:`smooth`,block:`start`})});function h(e){for(let[t,n]of m)n.toggleAttribute(`selected`,t===e);e&&p.value!==e&&(p.value=e)}var g=document.createElement(`div`);g.className=`toc-content-layout`,g.append(p,u,f),o.append(g);function _(e){let t=parseFloat(getComputedStyle(document.documentElement).fontSize)||16,n=e>0&&e<52.5*t;g.toggleAttribute(`data-compact`,n)}typeof ResizeObserver>`u`?_(g.clientWidth):new ResizeObserver(e=>{let t=e[0];t!==void 0&&_(t.contentBoxSize?.[0]?.inlineSize??t.contentRect.width)}).observe(g,{box:`content-box`});var v=document.querySelector(`.page-header`);if(v instanceof HTMLElement){let e=()=>{g.style.setProperty(`--toc-sticky-top`,`${v.offsetHeight+16}px`)};typeof ResizeObserver>`u`?e():new ResizeObserver(e).observe(v)}a(f,{headings:[...u.querySelectorAll(`h2`)],onActiveChange:h});