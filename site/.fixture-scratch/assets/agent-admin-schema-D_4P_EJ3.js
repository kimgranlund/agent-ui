import{a as e,i as t,r as n}from"./src-BCjsGt6c.js";var r=[{id:`claude-haiku-4-5-20251001`,label:`Haiku 4.5`,provider:`Anthropic`,includedByDefault:!0},{id:`claude-sonnet-5`,label:`Sonnet 5`,provider:`Anthropic`,includedByDefault:!0},{id:`gpt-4.1-mini`,label:`GPT-4.1 mini`,provider:`OpenAI`,includedByDefault:!1},{id:`gpt-4.1`,label:`GPT-4.1`,provider:`OpenAI`,includedByDefault:!1},{id:`gemini-2.5-flash`,label:`Gemini 2.5 Flash`,provider:`Google`,includedByDefault:!1},{id:`gemini-2.5-pro`,label:`Gemini 2.5 Pro`,provider:`Google`,includedByDefault:!1}],i=`claude-haiku-4-5-20251001`,a=`modelsIncluded`;function o(){return[...r]}function s(e,t){if(typeof e==`object`&&e){let n=e[t.id];if(typeof n==`boolean`)return n}return t.includedByDefault}function c(e,t){return typeof e==`string`&&t.some(t=>t.id===e)?e:i}var l=`claude-sonnet-5`;function u(e,t){return typeof e==`string`&&t.some(t=>t.id===e)?e:t.some(e=>e.id===`claude-sonnet-5`)?l:i}function d(e){return r.find(t=>t.id===e)?.label??e}function f(){return{version:1,sections:[{id:`agent`,label:`Agent`,description:`The identity and generation behavior this preview reads before every turn.`,fields:[{key:`name`,type:`text`,label:`Name`,description:`The agent's display name.`,default:`Untitled agent`,validation:{required:!0}},{key:`temperature`,type:`slider`,label:`Temperature`,description:`How much the stub reply varies its framing (0 = terse, 1 = expansive).`,default:.5,validation:{min:0,max:1,step:.1}}]}]}}var p=f(),m=`agentEnabled`;function h(e){return`${e}sEnabled`}function g(e){return e!==!1}var _=`surfaceMarkdown`,v=`surfaceA2ui`,y=`surfaceGenui`;function b(e){return e===!0}var x=`surfaceGenuiDogfood`;function S(e){return e===!0}var C=`surfacePlanner`;function w(e){return e===!0}var T=`surfaceAuthoring`;function E(e){return e===!0}var D=`a2uiCatalog`,O=[{id:`agent-ui`,label:`Default (agent-ui)`,description:`The fleet's own catalog — every ui-* control the renderer paints, mapped 1:1.`},{id:`a2ui-basic`,label:`A2UI Basic (upstream v0.9.1)`,description:`Upstream A2UI's own core component set, rendered onto fleet controls (ADR-0169).`}],k=`agent-ui`,A=new Set(e(n));function j(e){return typeof e==`string`&&(O.some(t=>t.id===e)||A.has(e))?e:k}var M=`a2uiLocalPatterns`;function N(e){return typeof e==`string`&&n.some(t=>t.personaId===e)?e:void 0}function P(e,n){let r=N(n);if(r===void 0)return e;let i=t(e,r);return A.has(i)?i:e}var F=p.sections[0],I=F?.description??`The identity and generation behavior this preview reads before every turn.`,L=(F?.fields??[]).filter(e=>(e.description??``).trim().length>0).map(e=>({term:e.label,detail:e.description??``})),R={"surface-options":{title:`Surface Options`,summary:`Which output modalities this agent may use, and how each one is configured`,body:[`Each row here is one output modality — a way this agent is allowed to answer. A modality that is switched off composes no teaching for itself, so the agent is never told to produce something this client would refuse to show.`,`Markdown and A2UI ship on; GenUI, Planner and Authoring are opt-in and ship off. Switching one off degrades the answer rather than breaking it: anything the model volunteers for a disabled modality is simply never consumed, and any surface left over from an earlier turn goes inert instead of starting a hidden turn.`],facts:[{term:`Applies`,detail:`from the next turn — every switch is read fresh at turn time, never cached`}]},markdown:{title:`Markdown`,summary:`Rendered as rich text — simple text is the fallback`,body:[`On: the agent's notes and the system bubbles render as rich text — headings, lists, emphasis, links and code paint as real elements, sanitized by construction.`,`Off: the very same text renders verbatim, character for character. Nothing is lost — the formatting is simply not interpreted.`],facts:[{term:`Default`,detail:`On`}]},a2ui:{title:`A2UI`,summary:`Structured generative UI against the picked catalog`,body:[`On: a turn may stream A2UI wire lines, which this client renders into real controls — forms, cards, choices the reader can act on. Acting on one sends the agent a follow-up turn, so the surface is a conversation, not a picture.`,`Off: no A2UI grammar is taught at all, and a surface from an earlier turn stops accepting clicks. A surface the agent tries to render while this is off is announced in the log rather than silently dropped.`],facts:[{term:`Default`,detail:`On`},{term:`Configured by`,detail:`the catalog picked directly below this row`}]},"a2ui-catalog":{title:`Catalog`,summary:`The component vocabulary an A2UI surface is rendered against`,body:[`Exactly one catalog is active. It decides both which components the agent is taught to emit and which ones this client will paint, so switching catalogs changes the vocabulary of every surface from the next turn on.`,`Use "From library" to add a registered catalog to this roster. An entry already in the list is offered disabled rather than hidden, so it is visible why a second copy cannot be added.`],facts:O.map(e=>({term:e.label,detail:e.description??``}))},genui:{title:`GenUI`,summary:`Sandboxed free-form generative UI — a pattern source, picked below, conditions it`,body:[`On: a turn may compose a free-form surface that mounts inside a sandboxed frame — no catalog, no fixed component set, and no access to this page. The pattern source picked under "Pattern sources" is what conditions the structure and style the agent aims for.`,`Off: no GenUI teaching composes, and a frame left over from an earlier turn is inert — its actions start no hidden turn.`],facts:[{term:`Default`,detail:`Off — an explicit opt-in per agent`}]},"genui-dogfood":{title:`Use agent-ui components`,summary:`Serve the fleet's own components into the GenUI frame`,body:[`On: the sandboxed frame is served this fleet's own component and token assets, and the turn is taught to build with those elements instead of hand-rolled markup — so a generated surface looks like the rest of this app rather than a stranger inside it.`,`Only meaningful while GenUI itself is on. With the modality off this setting composes nothing and mounts nothing, even if it was left on in an earlier session.`],facts:[{term:`Default`,detail:`Off`},{term:`Requires`,detail:`the GenUI modality above, switched on`}]},planner:{title:`Planner`,summary:`Sequential plan → execute → synthesize host loop — opt-in`,body:[`On: a turn may run as a sequence — the host asks for a plan, executes its steps, then asks for a synthesis — instead of one single dispatch. The stages stay internal: what changes is how the answer is produced, not what the reader sees.`,`Off: every turn runs the single-dispatch path. A model that volunteers a plan anyway is not stopped from saying so — the host simply never acts on it.`],facts:[{term:`Default`,detail:`Off — an explicit opt-in per agent`}]},authoring:{title:`Authoring`,summary:`Let this agent propose edits to a draft agent’s own configuration — opt-in`,body:[`On: the turn is taught how to declare a configuration patch, and a patch it declares may be applied to the draft agent's own settings. This is the seam the conversational agent-builder runs on.`,`This switch teaches; it never authorizes on its own. A declared patch is still filtered key by key, sanitized, and validated before anything is written, and a patch declared outside the dedicated authoring conversation is ignored outright.`],facts:[{term:`Default`,detail:`Off — an explicit opt-in per agent`}]},"pattern-source":{title:`Pattern sources`,summary:`The style and structure reference a GenUI turn composes against`,body:[`A pattern source is a body of reference material — markup conventions, a design language, a worked example — handed to a GenUI turn to compose against. Exactly the one picked here rides the turn; the rest stay parked in the list.`,`"Add pattern source" authors one by hand: a name, an optional description, and the content itself. "From library" adds a shipped source with the typing already done. Both paths commit through the same validation, so a duplicate name is refused identically either way.`],facts:[{term:`Read by`,detail:`the GenUI modality — with GenUI off, no source composes at all`}]},agent:{title:`Agent`,summary:I,body:[`This section is the agent itself: the name it answers to, and the dials that shape how a turn is generated. Every field is read FRESH at turn time, so an edit here applies to the next message without a reload and without a save step.`,`The master switch on this heading row is a different thing from the fields below it: it says whether this agent is available at all. Switched off, the composer stops accepting turns while everything here stays editable — it is a pause, not a delete.`],facts:L},model:{title:`Model`,summary:`Which models this agent may run on, and which one it starts with`,body:[`Each row is one model, grouped by provider. Its switch decides whether the model is OFFERED for this agent at all; the radio beside it picks the one a fresh conversation starts on. Excluding a model never rewrites history — turns already taken keep the model they were taken with.`,`A model listed here is not necessarily reachable: the roster is this client’s, and whether a given provider actually answers depends on the host it is pointed at. An unreachable pick degrades visibly in the turn log rather than silently falling back to another model.`],facts:[{term:`Reset Agent`,detail:`clears this agent's whole configuration back to its seeded state — the roster included`}]},bankroll:{title:`Bankroll`,summary:`The running figure a games persona carries between sessions`,body:[`Some personas keep a score that must survive a reload — a chip count, a stake, a running total. When one of them plays a turn, the figure its surface holds is mirrored into this agent’s own storage, and the next session is told the stored figure instead of starting from a fresh seed stake.`,`"Reset" clears the stored figure and nothing else. The next game then opens on its own seed value, exactly as a first-ever session would.`],facts:[{term:`Shown`,detail:`only for a persona that opts into keeping a bankroll — there is nothing to store otherwise`}]},"prompt-section":{title:`Instructions`,summary:`The composed system prompt, one titled section at a time`,body:[`The agent’s system prompt is not one blob of text: it is this ordered list of titled sections, joined in order at turn time. Writing them separately is what makes one instruction editable, reorderable or switchable off without disturbing the rest.`,`A section’s switch decides whether it composes at all. Switched off it stays here, fully editable, and simply contributes nothing to the next turn — the way to try an instruction’s absence without losing the words.`],facts:[{term:`Applies`,detail:`from the next turn — the prompt is composed fresh every time, never cached`}]},skill:{title:`Skills`,summary:`Named procedures the agent is taught it knows how to carry out`,body:[`A skill is a piece of know-how written as prose: how to run a review, how to draft a release note, what "done" means for a recurring task. Enabled skills are named to the agent in its composed prompt, so it can recognise when one applies.`,`Each row carries a mode. "In context" means the agent is told about the skill every turn, which is what you want for the handful it should always have in mind. "Invocable" keeps it out of the standing prompt and reachable on demand — the way to keep a long shelf without spending the whole prompt on it.`],facts:[{term:`Master switch`,detail:`off ⇒ no skill composes at all, whatever the individual rows say`}]},workflow:{title:`Workflows`,summary:`Multi-step procedures with an order the agent should keep`,body:[`A workflow is a skill with a sequence: the steps, their order, and what has to be true before moving on. Enabled workflows are named to the agent the same way skills are, so it can follow one rather than improvising an order of its own.`,`The same per-row mode applies: keep the few it should always follow in context, and leave the rest invocable so a long shelf costs nothing until it is actually asked for.`],facts:[{term:`Master switch`,detail:`off ⇒ no workflow composes at all, whatever the individual rows say`}]},resource:{title:`Resources`,summary:`Reference material the agent may consult while answering`,body:[`A resource is knowledge rather than know-how: a policy, a glossary, a specification, a page of house style. Enabled resources are made available to the agent so an answer can be grounded in them instead of in a guess.`,`Resources are usually the biggest thing on this tab, which is exactly what the per-row mode is for: keep the one or two that belong in every answer in context, and mark the reference shelf invocable.`],facts:[{term:`Master switch`,detail:`off ⇒ no resource composes at all, whatever the individual rows say`}]},tool:{title:`Tools`,summary:`Actions the agent may ask the host to take on its behalf`,body:[`A tool is something the agent can DO rather than something it knows — look a record up, call a service, fetch a live figure. Enabled tool names ride the turn, and the host decides which of them it actually recognises and is willing to run.`,`A tool named here that the host does not recognise is ignored rather than failed: this list is the agent’s side of the contract, never the host’s registry. That is also why renaming a row is safe — the underlying identifier the host matches on is never rewritten.`],facts:[{term:`Master switch`,detail:`off ⇒ no tool rides the turn at all, whatever the individual rows say`}]},"context-agent":{title:`Agent`,summary:`The compiled record a turn is actually built from — read-only`,body:[`This is the whole configuration as the next turn will read it: the identity and generation dials, every surface modality’s live state, and the fully composed system prompt itself, verbatim. It is derived, never edited — the tabs to the left are where these values are set.`,`It updates the moment anything it reads changes, which makes it the place to answer "did that setting actually reach the agent?" without taking a turn to find out.`],facts:[{term:`Read-only`,detail:`a projection of the store, rebuilt on every change`}]},"context-turn":{title:`Dialog turn`,summary:`One exchange, recorded exactly as it went over the wire`,body:[`Each entry is a single turn: which arm ran it, the request that was sent — system prompt, model, history and all — and the response that came back. Newest first, and the newest one opens by default.`,`This is a session-local trace kept to help you see what actually happened; it is bounded, so the oldest turns fall off as new ones arrive, and none of it is persisted with the agent’s configuration.`],facts:[{term:`Retention`,detail:`the last 20 turns of this session — never saved with the agent`}]}};function z(e){return B[e]}var B={"prompt-section":`prompt-section`,skill:`skill`,workflow:`workflow`,resource:`resource`,tool:`tool`,"pattern-source":`pattern-source`,catalog:`a2ui-catalog`},V=`bankrollCapable`;function H(e){return e===!0}var U=`bankroll`;function W(e){return typeof e==`number`&&Number.isFinite(e)&&e>=0?e:void 0}function G(e){return e.length>0?e.join(`, `):`none`}function K(e,t){let n=t.systemPrompt.length>60?`${t.systemPrompt.slice(0,60)}…`:t.systemPrompt,r=t.toolsEnabled?` Tools are enabled.`:``;return`[stub preview — no live model call] ${t.name} (${d(t.model)}, temp ${t.temperature.toFixed(1)}): instructed as "${n}".${r} Skills: ${G(t.skills)}. Workflows: ${G(t.workflows)}. Resources: ${G(t.resources)}. Tools: ${G(t.tools)}. You said: ${e}`}export{K as A,S as C,h as D,w as E,c as F,W as M,j as N,o as O,N as P,g as S,s as T,f as _,m as a,E as b,k as c,v as d,T as f,C as g,_ as h,R as i,u as j,P as k,i as l,y as m,O as n,V as o,x as p,M as r,U as s,D as t,a as u,p as v,b as w,H as x,z as y};