import"./super-shell-D76CLu9A.js";import{n as e,r as t}from"./_page-DIBN49D1.js";import{t as n}from"./code-block-DEt2Scp8.js";import{a as r}from"./doc-page-H_CmxYv1.js";import{i}from"./specimens-BSFejhGR.js";var{content:a}=e({title:`Theming`,intro:`One theming subtree, three live axes: scheme (light/dark), scale, and density. This page shows each in a real, running subtree — never a static screenshot.`});a.append(t(`ui-theme-provider is a real, shipped control (@agent-ui/components/controls/theme-provider) — a pure coordination/carrier element with no visual voice of its own. scheme is the one axis with a JS-side effect (it maps to the element’s own color-scheme, which every light-dark() token resolves against per subtree via inheritance — an unset scheme imposes no override and inherits the ambient scheme). scale and density are pure attribute carriers: dimensions.css’s [scale]/[density] selectors key off them in CSS alone.`));function o(e){let t=document.createElement(`code`);return t.textContent=e,t}a.append(r(2,`1 · scheme — light and dark, side by side`)),a.append(i(`p`,{},[document.createTextNode(`Each panel below is its own `),o(`<ui-theme-provider scheme="…">`),document.createTextNode(`, independent of your OS or browser preference — the same live specimens, rendered under both schemes at once so you can compare them directly.`)]));function s(e){let t=document.createElement(`ui-theme-provider`);t.setAttribute(`scheme`,e),t.className=`theming-panel`;let n=i(`p`,{class:`theming-panel-label`},[document.createTextNode(`scheme="${e}"`)]),r=document.createElement(`ui-button`);r.setAttribute(`variant`,`solid`),r.textContent=`Solid`;let a=document.createElement(`ui-button`);a.setAttribute(`variant`,`soft`),a.textContent=`Soft`;let o=document.createElement(`ui-text-field`);o.setAttribute(`label`,`Name`),o.setAttribute(`placeholder`,`Ada Lovelace`);let s=i(`div`,{class:`theming-panel-row`},[r,a]);return t.append(n,s,o),t}a.append(i(`div`,{class:`theming-scheme-row`},[s(`light`),s(`dark`)])),a.append(r(2,`2 · scale & density`)),a.append(i(`p`,{},[document.createTextNode(`scale re-tables a control’s frame (height + font + icon) to a different §1 row; density multiplies only the icon-to-label rhythm gap, never the frame. Both are plain attributes on any ancestor — ui-theme-provider carries them for free, but any wrapper element works identically.`)]));function c(e,t){let n=i(`div`,{class:`theming-axis-cluster`});n.setAttribute(e,t);let r=i(`span`,{class:`theming-axis-label`},[document.createTextNode(`[${e}="${t}"]`)]),a=document.createElement(`ui-button`);return a.setAttribute(`variant`,`soft`),a.textContent=`Sample`,n.append(r,a),n}a.append(i(`div`,{class:`theming-axis-row`},[c(`scale`,`ui-sm`),c(`scale`,`ui-md`),c(`scale`,`content-lg`)])),a.append(i(`div`,{class:`theming-axis-row`},[c(`density`,`compact`),c(`density`,`comfortable`),c(`density`,`spacious`)])),a.append(i(`p`,{class:`theming-note`},[document.createTextNode(`The full [scale] × [size] stepping — every combination, tabulated — lives on the `),(()=>{let e=document.createElement(`a`);return e.href=`./sizing.html`,e.textContent=`sizing guide`,e})(),document.createTextNode(`; the type scale the -size leg rescales is rendered live on the `),(()=>{let e=document.createElement(`a`);return e.href=`./tokens.html#type-scale`,e.textContent=`token reference`,e})(),document.createTextNode(`.`)])),a.append(r(2,`3 · the role system`)),a.append(i(`p`,{},[document.createTextNode(`Every colour a control paints is a --md-sys-color-{family}-{role} custom property — never a bare hex or an oklch() literal in a control’s own CSS. family is the semantic channel (neutral · primary · secondary · tertiary · info · success · warning · danger, plus the dedicated focus-ring utility role); role is the usage (on-surface, container, outline-variant, …). Each role resolves through `),o(`light-dark(lightValue, darkValue)`),document.createTextNode(`, so a role automatically repaints when its subtree’s color-scheme flips — no separate dark-mode stylesheet, ever. The full derived table of every role, live in both schemes, is the `),(()=>{let e=document.createElement(`a`);return e.href=`./tokens.html`,e.textContent=`token reference`,e})(),document.createTextNode(`.`)])),a.append(r(2,`4 · overriding a token at a subtree`)),a.append(i(`p`,{},[document.createTextNode(`A role is a custom property, so a consumer repoints one the ordinary CSS way: declare it on an ancestor, and every descendant control that reads that role repaints — no control-specific override API. Below, a wrapper repoints --md-sys-color-primary; the solid button on the right reads it live, the one on the left does not.`)])),a.append(n([`.brand-accent {`,`  --md-sys-color-primary: oklch(0.6 0.18 25); /* a custom hue, both schemes */`,`}`].join(`
`),`css`));{let e=document.createElement(`ui-button`);e.setAttribute(`variant`,`solid`),e.textContent=`Default`;let t=i(`div`,{class:`theming-override`});t.style.setProperty(`--md-sys-color-primary`,`oklch(0.6 0.18 25)`);let n=document.createElement(`ui-button`);n.setAttribute(`variant`,`solid`),n.textContent=`Overridden`,t.append(n),a.append(i(`div`,{class:`theming-override-row`},[e,t]))}a.append(r(2,`5 · theme packs`)),a.append(i(`p`,{},[document.createTextNode(`ui-theme-provider also carries a theme attribute — swapping whole token PACKAGES (a distinct palette, not just a subtree override). A pack is generated out-of-band from Ultimate Tokens' export, then wrapped under a [theme='name'] selector; it only needs to declare the roles it actually changes — anything it omits (the focus ring, the status colors) inherits the default from :root via ordinary CSS cascade. Pick a theme from this site's own header control to see it live: the shell around this very page is one ui-theme-provider, and the picker lazy-loads each pack's stylesheet on first selection.`),document.createTextNode(` `),(()=>{let e=document.createElement(`a`);return e.href=`./adr-index.html`,e.textContent=`Decision records`,e})(),document.createTextNode(`.`)]));