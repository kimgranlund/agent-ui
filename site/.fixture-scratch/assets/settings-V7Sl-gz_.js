import"./super-shell-D76CLu9A.js";import{n as e,r as t}from"./_page-DIBN49D1.js";import{t as n}from"./code-block-DEt2Scp8.js";import{a as r,d as i,o as a}from"./doc-page-H_CmxYv1.js";import{Ct as o}from"./frontmatter-D6AIzGjv.js";import"./master-detail-88sC5suq.js";import"./settings-Aka6YaIs.js";import{t as s}from"./memory-store-BOa7Z4oO.js";/* empty css                      *//* empty css                 */import{t as c}from"./store-BQcz4KvH.js";var l="---\n# settings.md frontmatter — the attributes-as-API descriptor for ui-settings (ADR-0004;\n# app-surfaces-m4.lld.md LLD-C12). The `attributes[]` block MUST mirror settings.ts `settingsProps` — the\n# contract↔props trip-wire (settings.test.ts) targets this fence.\ntag: ui-settings\ntier: layout            # geometry size-class (Container/layout band — a composition over ui-master-detail, the master-detail.md precedent; no control height of its own)\nextends: UIElement      # a plain structural base — composes ui-master-detail rather than extending it (LLD-C12, LLD F8)\n# marginal: measured at the @agent-ui/app integration slice (scripts/measure-size.mjs, LLD-C9/C16)\n\nattributes:              # attributes-as-API — mirrors settings.ts `settingsProps`\n  - name: schema\n    type: json            # a JS object (SettingsSchema), not an attribute — too structured to reflect (the ui-split `sizes` precedent)\n    default: undefined    # undefined ⇒ no schema yet: the rail/panel render empty, never throw (SPEC-R10)\n    reflect: false         # NEVER reflected — `attribute: false` in source, a JS property only\n  - name: store\n    type: json             # a JS object (SettingsStore: get/set/subscribe?/save?) — functions cannot round-trip through JSON.stringify, safe only because `attribute: false` means the codec never actually runs\n    default: undefined     # undefined ⇒ no store: fields render from `field.default`, never throw (SPEC-R12 AC2)\n    reflect: false\n  - name: section\n    type: string\n    default: ''\n    reflect: true          # reflects so a JS-set value applies identically to an author-set attribute (the ui-master-detail `selected` precedent); '' ⇒ no section resolved yet\n\nproperties:\n  - name: schema\n    description: The typed, versioned `SettingsSchema` (`{ version: 1, sections: [...] }`, schema.ts) driving the generated panels. Reactive — a reassignment (e.g. an async-loaded schema landing after mount) rebuilds the rail + every section's form from scratch; a bare reconnect with the SAME schema object skips the rebuild (preserving live field values) but still re-arms every per-connection reactive seam a disconnect tore down — the rail's `select` listener AND every generated field's validation, re-shows the current section. Absent ⇒ an empty rail/panel, never a throw.\n  - name: store\n    description: An optional `SettingsStore` adapter (store.ts — `get`/`set`/optional `subscribe`/`save`) the surface reads initial values from and writes changes back to, per-field-on-change. A supplied `subscribe` is WIRED (TKT-0021) — an external `set(key, value)` (another tab, a remote push) reflects into the matching field's control, with no echo back into `store.set` (an Object.is cutoff against the control's own current value). Reactive the same way as `schema` (a reassignment rebuilds; a reconnect with the same object skips the rebuild but still re-arms per-connection wiring, subscribe included). Absent ⇒ every field renders from its own schema `default` and changes are never persisted (SPEC-R12 AC2).\n  - name: section\n    description: The active section id. A reactive effect derives which generated panel shows + the rail's active marker from it, and emits `select`/`change` on every change AFTER the first (the initial/resolved-default state at connect does not fire — the `ui-master-detail` `selected` precedent).\n\nevents:\n  - name: select\n    detail: 'string'\n    description: Fired after `section` changes to a new value (post-connect only — the initial/resolved-default state at connect does not fire). Detail is the new `section` id.\n  - name: change\n    detail: 'string'\n    description: Fired alongside `select`, same timing, same detail (the `ui-master-detail` select/change-pair convention).\n\nslots: []                 # no authored children at all — the rail + every section's form are GENERATED from `schema` (unlike ui-master-detail, which docks AUTHORED panes)\n\nparts:                     # NOT shadow-DOM ::part() (light-DOM only) — documented for completeness (compareDescriptorToSource does not mechanically check `parts:`, the split.md/master-detail.md precedent)\n  - name: panel\n    description: A control-rendered `<div data-part=\"panel\">` — the mount point the active section's generated `ui-form-provider` is attached into (detached, never destroyed, when the section changes).\n  - name: section-select\n    description: (GH #962) A control-rendered `<ui-select data-part=\"section-select\">`, pinned above the panel inside the detail pane — holds ONE `[role=option]` per section (the same set `#rail`'s `ui-nav-rail-item`s hold), kept in lock-step with the active `section`. Hidden except under `data-compact` (and never in the `data-single-section` posture).\n\ncustomStates: []          # no :state() hooks — the active rail item rides the composed ui-nav-rail-item's own `selected` reflected attribute, not a custom state of ui-settings' own (the ui-master-detail `data-view` precedent). Two DERIVED host markers (never author-settable API, the same `data-view` class): `data-single-section` — stamped per build when the supported schema has exactly ONE section; settings.css keys the GH #50 detail-only posture off it (list pane, separator, and the narrow drill-in's Back all hide — with one section there is nothing to navigate). `data-compact` (GH #962) — stamped by a ResizeObserver band watcher at the shell family's named COMPACT line (`SHELL_COMPACT_BREAKPOINT_REM`, shell-breakpoint.ts, the SAME constant super-shell.css's own compact band reads — the shared responsive spine, not a second breakpoint mechanism); settings.css keys the SAME detail-only posture off it PLUS shows `section-select` in the rail's place.\n\nface:\n  formAssociated: false    # NOT a FACE form control — a layout composition; the GENERATED per-field controls are each their own FACE participant\n\naria:\n  role: none               # this element carries no ARIA of its own — the rail is a composed ui-nav-rail collapse=\"drill-in\" (its own role/AX contract, ADR-0130), and every generated field rides its own control's ARIA (inherited, unchanged)\n  roleSource: none\n\nkeyboard: []                # no bespoke keyboard handling of this element's own — the composed ui-nav-rail-item's activator is a real <a>/<button> (Enter/Space activate natively); the composed ui-master-detail's own drill-in/back-affordance keyboard contract is inherited unchanged; each generated field's keyboard contract is its own control's\n\ngeometry:\n  sizeClass: layout          # Container/layout — NO control height\n  blockSize: auto             # fills its flex parent (flex:1 1 auto on the host is the CONSUMER's job — the master-detail.md precedent)\n  paddingBlock: 0             # no padding of its own — the rail/panel own their own inset\n  narrowThreshold: inherited  # the drill-in threshold is entirely the composed ui-master-detail's own (40rem) — this element declares none of its own\n  compactThreshold: 52.5rem   # GH #962 — this element's OWN band (SHELL_COMPACT_BREAKPOINT_REM, shell-breakpoint.ts): below it, `data-compact` swaps the rail for `section-select` (settings.css) — wider than, and independent of, the composed ui-master-detail's own 40rem narrow drill-in above\n\nforcedColors: The active rail item's marker is entirely the composed ui-nav-rail-item's own (SPEC-R4, nav-rail.css) — a real `border-inline-start` that survives forced-colors, inherited unchanged; ui-settings contributes no forced-colors rule of its own.\n---\n\n# ui-settings\n\n`ui-settings` is the **app-tier settings surface** (`@agent-ui/app`) — a sections rail + per-section panel,\ncomposing the shipped `ui-master-detail` for the rail|panel drill-in, with every panel **generated from a\ntyped schema** over the fleet's own form spine (`ui-form-provider`/`ui-field`).\n\n```html\n<ui-settings id=\"app-settings\"></ui-settings>\n<script type=\"module\">\n  import { createMemoryStore } from '@agent-ui/app/settings-memory-store'\n\n  const el = document.getElementById('app-settings')\n  el.schema = {\n    version: 1,\n    sections: [\n      {\n        id: 'general', label: 'General', fields: [\n          { key: 'displayName', type: 'text', label: 'Display name', default: '' },\n          { key: 'darkMode', type: 'boolean', label: 'Dark mode', default: false },\n        ],\n      },\n    ],\n  }\n  el.store = createMemoryStore({ persistKey: 'demo-settings' })\n<\/script>\n```\n\n## Composition — a generated tree over `ui-master-detail`\n\nUnlike `ui-master-detail` (which docks *authored* panes), `ui-settings` takes **no light-DOM children at\nall**: at connect it composes one `ui-master-detail` (a rail `ui-master-detail-pane` + a panel\n`ui-master-detail-pane`) whose rail is a composed `ui-nav-rail collapse=\"drill-in\"` (ADR-0130, nav-rail\nPhase 2 — the shared sections-rail primitive), and **generates** the rail's `ui-nav-rail-item`s + every\nsection's form from `schema` (`schema.ts`/`generate.ts`) — the rail|panel drill-in behaviour and the\ncomposed `ui-master-detail`'s own keyboard contract are inherited unchanged. `schema`/`store` are reactive:\nsetting them after mount (an async-loaded schema, a swapped store) rebuilds the rail + every section's form;\na relocation reconnect with the SAME schema/store objects never rebuilds (live field VALUES survive an\nisolated-shell relocation untouched) but still re-arms every per-connection reactive seam a disconnect tore\ndown — the rail's `select` listener AND every generated field's validation, and re-shows the current\nsection.\n\n## Schema-driven fields\n\nEach `SettingsField.type` maps to a fleet control: `text`/`number`/`date` → `ui-text-field` (+ its own\n`type`), `boolean` → `ui-switch`, `select` → `ui-select`, `slider` → `ui-slider`. Every field renders inside\na `ui-field` (label/description from the schema) inside one `ui-form-provider` per section — validation\n(`Field.validation`) wires onto the generated control's **own** validity, so errors render through its\nexisting `user-invalid` timing, never a second observation path (ADR-0051).\n\n## Persistence — the `SettingsStore` seam\n\n`store` is optional (`store.ts`'s `SettingsStore` interface: `get`/`set`/optional `subscribe`/`save`). A\nfield reads `store.get(key) ?? field.default` at generation time and commits `store.set(key, value)` on the\nmapped control's OWN documented commit event (per-field-on-change, immediately per field, never batched) —\n`change` for every v1 type except `select`, which commits on its own `select` event (`ui-select` never\nemits `change` — a per-type table, not a universal `change` listener, TKT-0021). No store supplied ⇒ every\nfield still renders from its schema `default`, and changes are simply not persisted. `memory-store.ts`\nships a reference adapter for demos/tests — `ui-settings` itself never imports a concrete store, only the\ninterface.\n\n**External sync (TKT-0021 — realizes the M4 LLD §8 Fork F7 optional-`subscribe` arm).** A supplied\n`store.subscribe` is wired: an external `store.set(key, value)` — another tab, a remote push — reflects\ninto the matching field's control via the registry's `setValue`. No echo loop: the suppression is the\nkernel's Object.is precedent (reactive/index.ts), not a flag — a notification whose value already equals\nthe control's own `getValue()` is a silent no-op, which is also how the store's own re-notification of a\ncommit the field just made resolves to nothing. The two `ui-text-field` codec types (`number`/`date`)\nreflect the raw value visibly, but their internal display↔canonical codec only resyncs on a real blur — a\npre-existing `ui-text-field` limitation (schema.test.ts documents it), not something this seam changes. A\nstore without `subscribe` behaves byte-identically to before (no external-change reactivity, as already\ndocumented above). The subscription is re-armed across a relocation reconnect the same way validation is.\n\n## Mobile posture — rail → select below the compact line (GH #962)\n\nBelow `52.5rem` — the shell family's named COMPACT line (`SHELL_COMPACT_BREAKPOINT_REM`,\n`shell-breakpoint.ts`; the same constant `ui-super-shell`'s own compact band reads, ADR-0150) — the rail\nsteps aside for `section-select`, a `ui-select` holding the same sections, pinned above the panel. A\nResizeObserver watching the composed `ui-master-detail`'s own container box stamps `data-compact` on the\nhost; settings.css keys the rail/separator/back-affordance hide + the full-width detail pane off it, the\nsame \"detail-only\" shape the single-section posture already uses. Selecting an option sets `section` the\nsame way activating a rail item does — both are just different front ends onto the one active-section\nstate.\n\n## Accessibility\n\nThe rail is a composed `ui-nav-rail collapse=\"drill-in\"` (`aria-label=\"Settings sections\"`) of\n`ui-nav-rail-item`s; every item is empty-`href` ⇒ button-shaped, so its activator carries `role=\"tab\"` +\n`aria-selected` (an in-page selection commit — the deliberate ADR-0130 cl.4 correction away from the older\n`aria-current=\"page\"` page-nav verb this surface used before the migration). `role: none` on `ui-settings`\nitself is unchanged — it carries no ARIA of its own. The composed `ui-master-detail`'s drill-in +\nits \"back\" affordance keep their own contract unchanged. Every generated field's ARIA rides its own\ncontrol — this element adds none of its own.\n",u="// memory-store.ts — the REFERENCE `SettingsStore` adapter (LLD-C15, SPEC-R12) for the demo/tests, not a\n// dependency of `ui-settings` (which imports only `store.ts`'s interface — SPEC-R12 AC3). Two flavours,\n// one factory: an in-process Map (the default — dies with the page, round-trips within a session) and an\n// optional `localStorage`-backed variant (the `persistKey` option) that round-trips ACROSS two separate\n// store instances (SPEC-R12 AC2's \"write → reload\" proof needs real persistence, not just object\n// identity).\n//\n// GH #959 (remaining slice, 2026-08-16) + GH #1077 (read-path migration, 2026-08-17) — the persisted\n// flavour goes through `@agent-ui/shared`'s localStorage `StorageAdapter` tier (ADR-0193) END-TO-END:\n// `createLocalStorageAdapter({ namespace: persistKey })` owns the `${persistKey}.${key}` key format, the\n// `JSON.stringify` encoding, AND the namespace enumeration — no code in this module touches\n// `localStorage` directly (parity-pinned in `memory-store.test.ts`). The seam's BASE contract is async by\n// ADR-0193 ruling, while `SettingsStore` is sync by SPEC-R12 fork F7 — the bridge is a SYNC READ-THROUGH\n// CACHE: the `values` Map answers every `get` synchronously, is WARMED at construction by the TIER's own\n// sync read surface — `keysSync()`/`getSync()`, `SyncReadableStorageAdapter` (ADR-0193 Amendment A1/A4;\n// same-tick over the live `localStorage`, so a construct-then-`get` sees the persisted value in the same\n// tick — `store.test.ts`'s persistKey suite pins it) — and is written through to the adapter on every\n// `set`. The localStorage\n// tier's `set()` performs its `setItem` synchronously before its promise settles (an `async` body runs to\n// its first `await`, and it has none), so a second instance constructed right after a `set` still sees the\n// write — the pre-existing cross-instance test is the trip-wire for that assumption. Consequence: this\n// store is pinned to the localStorage tier — an IndexedDB-backed `SettingsStore` would need an async\n// hydration handshake `SettingsStore` does not have (a planner call, not this module's).\n\nimport { createLocalStorageAdapter, type SyncReadableStorageAdapter } from '@agent-ui/shared'\nimport type { SettingsStore } from './store.ts'\n\nexport interface MemoryStoreOptions {\n  /** Seed values, keyed the same as the schema's field `key`s. */\n  initial?: Readonly<Record<string, unknown>>\n  /** When set, every read/write also round-trips through `localStorage` under `${persistKey}.${key}`\n   *  (JSON-encoded, via `@agent-ui/shared`'s localStorage `StorageAdapter` tier — ADR-0193) — the ONLY way\n   *  two separately-constructed stores can observe the same value (a plain in-memory Map is per-instance).\n   *  Omit for a pure in-memory store (the common demo/test case).\n   *\n   *  `${persistKey}.` is this store's whole NAMESPACE: construction rehydrates every key it holds (GH #409),\n   *  seeded or not, so nothing else should write under it. */\n  persistKey?: string\n}\n\n/** A reference `SettingsStore`: synchronous Map-backed get/set + `subscribe`, optionally mirrored into\n *  `localStorage` (through the shared `StorageAdapter` seam) for cross-instance persistence. */\nexport function createMemoryStore(options: MemoryStoreOptions = {}): SettingsStore {\n  const { initial, persistKey } = options\n  const values = new Map<string, unknown>(Object.entries(initial ?? {}))\n  const listeners = new Set<(key: string, value: unknown) => void>()\n\n  // The write-through seam (ADR-0193). Its own `typeof localStorage` guard makes every call a safe no-op\n  // where localStorage is unavailable, so no second guard is needed on the write path.\n  const adapter: SyncReadableStorageAdapter | undefined = persistKey ? createLocalStorageAdapter({ namespace: persistKey }) : undefined\n\n  if (adapter) {\n    // Seed from any previously-persisted values (a real reload, or a fresh store pointed at the same\n    // persistKey) — persisted values WIN over the constructor's `initial` seed, native-`localStorage`-parity.\n    //\n    // This is the sync read-through cache's warm-up (module banner): the TIER's own sync read surface,\n    // `keysSync()`/`getSync()` (ADR-0193 Amendment A1/A4) — same-tick over the live `localStorage`, so a\n    // `SettingsStore` answers the persisted value in the tick it is constructed; the adapter owns key\n    // format, JSON encoding, and enumeration end-to-end (its no-localStorage guard makes this a no-op\n    // where storage is unavailable). A corrupt/foreign value reads as `undefined` (the tier's fail-open\n    // idiom) and is skipped — the constructor's seed survives, never a throw.\n    //\n    // GH #409 — this is a PREFIX SCAN of the whole `${persistKey}.` namespace, not a walk of the seed's\n    // own keys. The old seed-key walk made the seed a hidden allowlist: a key the store had genuinely\n    // WRITTEN (`set` persisted it) but the seed never carried was invisible on the next construction, so\n    // it round-tripped inside one live instance and vanished on reload — which is exactly how\n    // agent-admin's Surface Options and capability master toggles (no preset seed carries them) persisted\n    // and then never came back. Scanning the namespace fixes the CLASS: whatever this persistKey holds is\n    // what this store answers, seeded or not.\n    //\n    // The namespace is the whole contract, so a caller that parks its OWN bookkeeping under a store's\n    // persistKey now has it rehydrated as a store key too (agent-admin-presets.ts's per-persona\n    // `agent-admin-app.<id>.seedVersion` marker is the one such neighbour in this repo — inert: nothing\n    // reads a `seedVersion` store key, and the persona file's exported key set is enumerated, never\n    // \"whatever the namespace holds\"). Keys are namespaced by the trailing `.`, so two persistKeys where\n    // one is a leading substring of the other (`…app.travel` vs `…app.travel-imported`) never cross.\n    for (const key of adapter.keysSync()) {\n      const value = adapter.getSync(key)\n      if (value !== undefined) values.set(key, value)\n    }\n  }\n\n  return {\n    get(key) {\n      return values.get(key)\n    },\n    set(key, value) {\n      values.set(key, value)\n      // Write-through to the seam. The localStorage tier's `setItem` runs synchronously inside this call\n      // (banner); the returned promise carries nothing this sync store can wait on, so it is released\n      // (a rejected write — quota, a locked-down embed that throws — surfaces as an unhandled rejection\n      // instead of the pre-migration synchronous throw: the ONE behavioural delta, error path only).\n      void adapter?.set(key, value)\n      for (const listener of listeners) listener(key, value)\n    },\n    subscribe(listener) {\n      listeners.add(listener)\n      return () => listeners.delete(listener)\n    },\n    save(next) {\n      for (const [key, value] of Object.entries(next)) this.set(key, value)\n    },\n  }\n}\n";function d(e,t){let n=`export interface ${t} {`,r=e.indexOf(n);if(r===-1)throw Error(`settings.ts: interface "${t}" not found — renamed or removed?`);let i=0,a=r;for(;a<e.length;a++)if(e[a]===`{`)i++;else if(e[a]===`}`&&(i--,i===0)){a++;break}return e.slice(r,a)}var{content:f}=e({title:`Composing a ui-settings surface`,intro:`ui-settings is a sections rail + panel, composing the shipped ui-master-detail for the rail|panel drill-in, with every panel generated from a typed schema over the fleet’s own form spine (ui-form-provider/ui-field). No app-authored form CSS — the generated tree is fleet controls only.`});f.append(t("Set `schema` (a versioned SettingsSchema) and an optional `store` (a SettingsStore adapter) as properties — ui-settings generates the rail buttons and every section’s form from them. Both are reactive: assigning a schema after mount (an async-loaded schema) rebuilds; a mere reconnect with the SAME objects does not."));function p(e,t,n){let r=document.createElement(e);return r.className=t,n!==void 0&&(r.textContent=n),r}function m(e){return r(2,e)}function h(e){return p(`code`,`as-code`,e)}var g={version:1,sections:[{id:`profile`,label:`Profile`,description:`How you appear to others.`,fields:[{key:`displayName`,type:`text`,label:`Display name`,description:`Shown on your posts and comments.`,default:`Ada Lovelace`,validation:{required:!0}},{key:`bio`,type:`text`,label:`Bio`,default:``}]},{id:`appearance`,label:`Appearance`,fields:[{key:`darkMode`,type:`boolean`,label:`Dark mode`,default:!1},{key:`density`,type:`select`,label:`Density`,default:`comfortable`,options:[{value:`compact`,label:`Compact`},{value:`comfortable`,label:`Comfortable`},{value:`spacious`,label:`Spacious`}]},{key:`fontScale`,type:`slider`,label:`Font scale`,description:`0.5× – 2×`,default:1,validation:{min:.5,max:2,step:.1}}]},{id:`notifications`,label:`Notifications`,fields:[{key:`emailDigest`,type:`boolean`,label:`Weekly email digest`,default:!0},{key:`quietHoursStart`,type:`number`,label:`Quiet hours start (24h)`,default:22,validation:{min:0,max:23,step:1}}]}]};function _(){let e=document.createElement(`ui-settings`);return e.className=`settings-demo`,e.schema=g,e.store=s({persistKey:`docs-settings-demo`}),e}f.append(m(`1 · Schema-driven fields`)),f.append(t(`Each field’s `,h(`type`),` maps to a fleet control: `,h(`text`),`/`,h(`number`),`/`,h(`date`),` → `,h(`ui-text-field`),`, `,h(`boolean`),` → `,h(`ui-switch`),`, `,h(`select`),` → `,h(`ui-select`),`, `,h(`slider`),` → `,h(`ui-slider`),`. Resize the frame below narrower than 40rem (ui-master-detail’s own container width) to see the drill-in.`));var v=p(`div`,`settings-resize`);v.append(_()),f.append(v,p(`p`,`as-caption`,`↑ Reload the page after changing a value — it persists via the store.`)),f.append(m(`2 · The SettingsStore seam`)),f.append(t(`A field reads `,h(`store.get(key) ?? field.default`),` at generation time and commits `,h(`store.set(key, value)`),` on its own `,h(`change`),` (per-field-on-change). No store supplied ⇒ every field still renders from its schema `,h(`default`),`, and changes are simply not persisted — `,h(`ui-settings`),` itself never imports a concrete store, only the interface.`)),f.append(p(`pre`,`as-snippet`,`const el = document.querySelector('ui-settings')
el.schema = {
  version: 1,
  sections: [{ id: 'general', label: 'General', fields: [
    { key: 'displayName', type: 'text', label: 'Display name', default: '' },
  ] }],
}
el.store = createMemoryStore({ persistKey: 'my-app-settings' })`)),f.append(m(`2a · @agent-ui/app/settings-store — the seam, derived from source`)),f.append(t(`A pure TypeScript interface, no implementation — `,h(`ui-settings`),` depends ONLY on this contract, never a concrete store (SPEC-R12 AC3, grep-guarded by the package’s own `,h(`store.test.ts`),`). `,h(`get`),`/`,h(`set`),` are SYNCHRONOUS by design (fork F7, LLD §8): a remote-backed store must resolve its own async work internally (an optimistic write + a background flush) — this seam models no pending/loading state. `,h(`subscribe`),` is the OPTIONAL external-change notification (another tab, a remote push) — absent ⇒ `,h(`ui-settings`),` is authoritative on read only at mount. `,h(`save`),` is an OPTIONAL batch write for a save-button flow — not called by `,h(`ui-settings`),` itself in this build (per-field-on-change is the only wired timing).`)),f.append(n(d(c,`SettingsStore`),`ts`)),f.append(m(`2b · @agent-ui/app/settings-memory-store — the reference adapter`)),f.append(t(`Not a dependency of `,h(`ui-settings`),` — a REFERENCE `,h(`SettingsStore`),` implementation for demos/tests (this page’s own live example above uses it). Two flavours, one factory: a plain in-process `,h(`Map`),` (the default — dies with the page), or an `,h(`localStorage`),`-backed variant (the `,h(`persistKey`),` option, used above) that round-trips ACROSS two separately-constructed store instances. The persisted flavour writes through `,h(`@agent-ui/shared`),`’s `,h(`StorageAdapter`),` seam (`,h(`createLocalStorageAdapter`),`, ADR-0193) rather than calling `,h(`localStorage`),` directly, fronted by a SYNCHRONOUS read-through cache — the adapter’s own seam is async (ADR-0193’s ruling, since IndexedDB is irreducibly async), while `,h(`SettingsStore`),` is sync (fork F7 above), so the cache is what bridges the two: warmed by a synchronous prefix scan of the whole `,h("${persistKey}."),` namespace at construction (persisted values WIN over the constructor’s `,h(`initial`),` seed), then written through on every `,h(`set`),`.`)),f.append(n(d(u,`MemoryStoreOptions`),`ts`)),f.append(t(`The seam itself — the async `,h(`StorageAdapter`),` interface, its localStorage/IndexedDB tiers, and how it differs from this sync `,h(`SettingsStore`),` — is its own guide: `,(()=>{let e=document.createElement(`a`);return e.href=`./persistence.html`,e.textContent=`Persistence`,e})(),`.`)),f.append(m(`API reference`)),f.append(t(`Read straight from the shipped descriptor (settings.md) through the same parser the package’s contract trip-wire validates.`));var y=o(l);y.descriptor.attributes.length>0&&f.append(a(y.descriptor.attributes,4));{let e=i(y.descriptor,4);e&&f.append(e)}