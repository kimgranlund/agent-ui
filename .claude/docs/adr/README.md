# agent-ui — Architecture Decision Records (ADR log)

> The log of ratified design changes. An ADR is the **record produced when the discovered-reality up-loop repairs an owning document** (a PRD goal, SPEC requirement, or LLD component). The up-loop is owned by the build team (the `orchestration-coordinator` seat + the spec family's change-propagation rule); this log owns the project's ADR *index* and its instance of the format/lifecycle, and the reusable discipline lives in the global `/adr-author` skill. · 2026-06-26

## When an ADR is written

When `system-builder` (or anyone) hits a constraint that the design can't satisfy, or a global pattern must change, the change is not patched at the symptom — the **owning** document is repaired and the decision is recorded here. One ADR per ratified change.

- **Trigger:** an escalation up the loop (typically from `system-builder`).
- **Author:** `system-planner` (ADRs are planning-owned, alongside PRD/SPEC/LLD).
- **Ratifier:** `orchestration-coordinator` (moves the ADR `proposed → accepted`).

## Numbering & files

- One file per ADR: `NNNN-short-kebab-title.md` (e.g. `0001-default-catalog-not-adapter.md`).
- `NNNN` is a zero-padded sequential integer, never reused. `0000-template.md` is the template — copy it.
- The decision's `Repairs:` field links the owning-doc IDs it changed (`PRD-G#` / `SPEC-R#` / `LLD-C#`), reference-by-ID — the ADR records *why*; the owning doc still holds the *fact*.

## Status lifecycle

`proposed` → `accepted` → (`superseded` | `deprecated`)

The `Status` frontmatter cell is **machine-readable**: it holds EXACTLY ONE of those four bare keywords, nothing
else — no trailing prose, ratification note, or `by ADR-NNNN` suffix. A lint gate (`site-adr-index.test.ts`)
enforces this over the whole corpus. Everything else lives in its own field: *when/how/by whom* it was ratified
goes in `Ratified by`; *which* ADR it supersedes or was superseded by (and why) goes in `Supersedes / Superseded
by`. `site/lib/adr.ts` reads the `Status` cell literally — there is no leading-word or ALL-CAPS heuristic.

- **proposed** — authored, not yet ratified.
- **accepted** — ratified by `orchestration-coordinator`; the repair propagates and dependents regenerate.
- **superseded / deprecated** — a later ADR replaced it (link forward in `Supersedes / Superseded by`), or the decision no longer applies. ADRs are append-only history; never edit an accepted decision's substance — write a new ADR that supersedes it. The one append-only exception is a **foreseen amendment** (see *[Amendment vs supersession](#amendment-vs-supersession)*): it *adds* a follow-through the decision already anticipated, without editing the original.

## Amendment vs supersession

A later decision can touch an earlier accepted ADR in two non-interchangeable ways. The test is whether the
original **Decision still stands**:

- **Foreseen amendment — amend in place.** The decision **stands**, and an extension it **already anticipated**
  lands. Record it as an append-only **`## Amendment`** section at the foot of the *same* ADR — never a new file.
  Example: **ADR-0008** booked, in its *Resolved on ratification* note, that "*only* if a ladder step collapses
  do we add token-layer dedicated `--md-sys-color-{f}-hover/-active` roles (a separate `tok-states` slice + **an amendment
  to this ADR**)". When the wave-2 smoke confirmed the collapse, the follow-through was appended as
  `## Amendment — dedicated primary hover/active roles`, **not** opened as a new ADR. The decision did not change;
  its anticipated branch resolved.
- **Supersession — a new ADR.** The decision **changes** — a reversal, a different choice, or a no-longer-applicable
  call. Write a **new** ADR that supersedes it: the *old* ADR's `Status` flips to `superseded` and its
  `Supersedes / Superseded by` row records `superseded by ADR-NNNN` (link forward); the old ADR's Decision is
  left intact as history. This is the existing lifecycle arrow.

**The append-only rule holds for both.** A `## Amendment` **adds** the foreseen follow-through; it does **not**
edit the original Context / Decision / Consequences — so it does not breach *never edit an accepted decision's
substance*. If you find yourself wanting to *change* a sentence inside the accepted Decision, that is a
supersession, not an amendment: open a new ADR.

| | the original Decision … | record as |
|---|---|---|
| **Foreseen amendment** | stands; an anticipated extension lands | append-only `## Amendment` in the same ADR |
| **Supersession** | is reversed / replaced | a new ADR, `superseded by` link forward |
| **Extension** | stands; a *separate, new* decision builds on it | a new ADR, `Extends` ↔ `extended by` cross-reference |

**Cross-reference convention.** A new ADR that builds on an earlier one **without reversing it** records the
link in its *Supersedes / Superseded by* field as **`Extends ADR-NNNN`**, and the earlier ADR is marked
**`Extended by ADR-NNNN`** (a two-way link) — e.g. **ADR-0012** *extends* **ADR-0006** (leading-icon anatomy →
the position×role family standard; ADR-0006's host-as-grid mechanism stays in force). An extension differs from
both neighbours: unlike an in-place amendment it is a *separate* ADR (a genuinely new decision, not a foreseen
branch of the old one), and unlike a supersession it leaves the earlier decision **standing**.

## How to add one

1. Copy [`0000-template.md`](./0000-template.md) to `NNNN-<title>.md` with the next number.
2. Fill Context · Decision · Consequences · Alternatives; set `Repairs:` to the owning-doc IDs.
3. Leave `Status: proposed` until ratified; on ratify, flip the cell to the bare `accepted` and record who/when in `Ratified by` + `Date` (never append detail to `Status` itself).
4. Add a row to the index below.

## Index

| ADR | Title | Status | Repairs |
|---|---|---|---|
| [0001](./0001-start-a1-validation-spine-ahead-of-g7.md) | Start A1 with the control-free validation spine ahead of G7 | accepted | (none — sequencing) |
| [0002](./0002-validator-parity-reconciliation.md) | Validator parity: missing-root, finalize granularity, syntactic-only pointer | accepted | renderer LLD-C11/C13 · corpus LLD-C5/C6 |
| [0003](./0003-single-file-component-css-barrels-host-page.md) | Single-file component CSS + barrels + host-page packaging | accepted | plan §8 · goals §G5 · process §1 · component-author |
| [0004](./0004-component-descriptor-md-frontmatter.md) | Component descriptor: `{name}.md` frontmatter replaces `{name}.api.json` | accepted | plan §10 · process §1/§4 · goals §G5 · component-author |
| [0005](./0005-lazy-upgrade-property-wins.md) | Lazy-property upgrade precedence: property-wins | accepted | goals §G2 · plan §5 |
| [0006](./0006-button-anatomy-optional-icon-slot-density-acceptance.md) | Button anatomy: optional leading icon slot + law-true `[density]` acceptance | accepted | goals §G5 |
| [0007](./0007-universal-selector-ramp-tokens.md) | Universal-selector ramp tokens: derived dimensions on `*` for subtree scale/density | accepted | shared `dimensions.css` (s6) · delivers ADR-0006 subtree `[density]` |
| [0008](./0008-interaction-state-styling-standard.md) | Control interaction-state styling standard (per-variant hover/active via role ladders) | accepted | goals §G5 · references/tokens.md · NEW references/interaction-states.md · button.css |
| [0009](./0009-focus-ring-token-standard.md) | The shared focus-ring standard (`--md-sys-color-focus-ring` + `:focus-visible` outline) | accepted *(amended by 0014: `:focus-within` text-entry variant)* | NEW shared tokens.css `--md-sys-color-focus-ring` · shared dimensions.css `--ui-focus-ring-*` · button.css · goals §G5 |
| [0010](./0010-tabbable-trait-aria-disabled.md) | Interactive-control focus + disabled a11y standard (the `tabbable` trait + `internals.ariaDisabled`) | accepted | NEW traits/tabbable.ts · button.ts · button.md · component-author · goals §G5 |
| [0011](./0011-canonical-action-prop-shape.md) | Canonical inbound `action`-prop shape (`{ action, context?, wantResponse? }`) | accepted | a2ui-catalog SPEC §5.1/§5.2 · catalog/default/catalog.json · renderer `readActionSpec` (LLD-C13) |
| [0012](./0012-button-anatomy-trailing-adornment-slot.md) | Button anatomy: position slots × `data-role` roles (the family adornment standard) | accepted | goals §G5 · button.css (role-driven sizing, `caret=font`) · button.md · button-doc.ts · component-author · sizing authority `geometry-sizing-spec.md` · **extends ADR-0006** |
| [0013](./0013-uiformelement-face-form-base.md) | UIFormElement, the FACE form base (form-associated participation over UIElement) | proposed | plan §5 · goals §G4 · NEW dom/form.ts · references/interaction-states.md · **extended by ADR-0014** |
| [0014](./0014-text-field-contenteditable-surface.md) | ui-text-field: the contenteditable editable surface + form-control interaction deviations | proposed | goals §G6 · NEW controls/text-field/* · NEW traits/track-user-invalid.ts · references/interaction-states.md · **extends ADR-0013/0006/0012** · **amends ADR-0009** |
| [0015](./0015-container-surface-space-token-model.md) | Container surface model (elevation × brightness) + `--ui-space` / `--ui-radius-base` tokens | proposed *(amended: ui-text-field adopts `--ui-radius-base`, #71)* | goals §G9 · references/tokens.md · references/geometry.md · NEW shared dimensions.css/tokens.css |
| [0016](./0016-a2ui-faithful-flex-layout-container-queries.md) | A2UI-faithful flex layout (Row/Column/List/Grid) + container-query responsiveness | proposed | goals §G9 · a2ui-catalog SPEC §5.2 · references/geometry.md · NEW controls/{row,column,list,grid}/* |
| [0017](./0017-native-dialog-modal.md) | ui-modal on native `<dialog>` showModal() (top-layer, not a form widget) | proposed *(cl.3 superseded by 0020)* | goals §G9 · a2ui-catalog SPEC §5.2 · plan §2 · NEW controls/modal/* |
| [0018](./0018-css-one-level-nested-radius.md) | CSS one-level nested radius (`--ui-card-child-radius`), JS controller rejected | proposed | goals §G9 · references/geometry.md · NEW controls/card/* |
| [0019](./0019-pull-renderer-lld-c8-two-way-binding.md) | Pull renderer LLD-C8 (two-way input binding) into the G9 milestone | proposed | goals §G9 · a2ui-renderer LLD-C8 · a2ui-catalog SPEC-R4/R7 · NEW a2ui renderer/input.ts |
| [0020](./0020-modal-persistent-dismissal-control.md) | ui-modal dismissal control: `persistent` (presence-boolean, default false) replaces `dismissable` | proposed | a2ui-catalog SPEC §5.2 · a2ui-catalog LLD · controls/modal/* · catalog/default/catalog.json · **supersedes ADR-0017 cl.3** |
| [0021](./0021-text-field-min-inline-size-floor.md) | ui-text-field intrinsic width: the entry-control `min-inline-size` floor (native `<input size>` parity), chars-width prop rejected | proposed | references/geometry.md (frame-law `min-inline-size` class-split) · controls/text-field/* · relates ADR-0014 |
| [0022](./0022-childpart-native-movebefore-reorder-focus.md) | `ChildPart.moveBefore` over native `Node.prototype.moveBefore` (atomic reorder — focus/selection preserved, identity-only fallback) | proposed | dom/template.ts `moveBefore` seam · dom/repeat.ts reorder contract · plan §6 · gates before A2UI LLD-C6 |
| [0023](./0023-components-mount-directive-host-public-seam.md) | Public `mount()` directive-host seam + the directive-authoring trio (imperative consumers drive kernel directives; `render`/`html` stay private) | proposed | NEW dom/template.ts `mount()` · dom/index.ts exports + dom-surface pin · a2ui-renderer LLD-C6 · plan §6 · user-ratified Option A |
| [0024](./0024-a2ui-v1-positional-dynamic-list.md) | A2UI v1.0 dynamic lists are positional (index-based, no key); `list.ts` uses a bespoke positional reconcile, not `repeat` | accepted *(amended: write-side itemScope, #139)* | a2ui-renderer LLD-C6 §5 + kernel-reuse note · NEW a2ui/protocol.ts ChildList union · NEW a2ui/renderer/list.ts · relates ADR-0022/0023 · user-ratified B2 · **(amendment)** LLD-C8 + a2ui/renderer/{input,widget,binding,types}.ts |
| [0025](./0025-ui-text-display-primitive-type-scale.md) | `ui-text` Display-class text primitive + the `--ui-type-*` typographic scale (+ the A2UI `Text` catalog type) | accepted *(all 3 forks settled: 1+2 ratified; 3 = real headings, user 2026-06-28)* | goals (NEW Display-control DoD) · references/geometry.md (Display size-class — type-scale lever) · NEW shared dimensions.css `--ui-type-*` · NEW controls/text/* · a2ui-catalog SPEC-R3/R8 + catalog/default/{catalog.json,factories.ts} (NEW `Text`) · relates ADR-0006/0015/0024 · superseded in part by ADR-0078 (prop schema / type scale / heading mechanism; leaf + content model + catalog type STAND) |
| [0026](./0026-a2ui-v1-function-call-bindings.md) | A2UI v1.0 function-call bindings: the `{call,args}` value kind + the `@index` system function (renderer LLD-C10) | accepted | a2ui-runtime SPEC §2/§5.1 (Binding fn-call arm) · a2ui-catalog SPEC-R5 (`required`/`email`/`regex`; `${…}` not `formatString`) · a2ui-renderer LLD-C5/C10 · a2ui-catalog LLD-C7 · NEW a2ui/renderer/functions.ts · NEW a2ui/catalog/functions.ts · a2ui/protocol.ts Binding union · a2ui/catalog/catalog.ts FunctionDef (named args) · a2ui/renderer/widget.ts · a2ui/catalog/conformance.ts · **relates ADR-0024** (settles its deferred `@index`-chain decision) |
| [0027](./0027-a2ui-v1-dynamicstring-interpolation.md) | A2UI v1.0 `${…}` DynamicString interpolation (path interpolation in any bindable string; function-expression grammar deferred) | accepted *(function-expression arm extended by 0028)* | a2ui-runtime SPEC §2 + SPEC-R10 (`${…}` clause normative) · a2ui-catalog SPEC-R5 (`${…}` delivered) · a2ui-renderer LLD-C5 §5 + LLD-C10 §7 · NEW a2ui/renderer/interpolate.ts · a2ui/renderer/functions.ts (resolveValue literal arm) · a2ui/renderer/widget.ts (isBinding extended — template string is a binding) · **extends ADR-0026** (settles its `${…}` follow-up) · relates ADR-0024 (relative-path/itemScope reuse) |
| [0028](./0028-a2ui-v1-function-expression-grammar.md) | A2UI v1.0 `${fn(arg:val)}` function-expression grammar — the in-string call form parses → `FunctionCall` → `evaluate` (no second evaluator; positional/unnamed-arg form deferred, nested named calls resolve) | accepted | a2ui-runtime SPEC-R10 (function-expression form resolves) · a2ui-renderer LLD-C10 §7 · NEW a2ui/renderer/fn-expr.ts · a2ui/renderer/interpolate.ts (classifier `(`-branch + signature) · a2ui/renderer/functions.ts (resolveValue threads deps) · **extends ADR-0027** (delivers its deferred function-expression arm) · relates ADR-0026 (parses into its `evaluate`) |
| [0029](./0029-a2ui-v1-checks-inline-validation.md) | A2UI v1.0 `checks` — client-side inline validation (custom-validity seam + reactive checks controller + button auto-disable; NO server error) | accepted | a2ui-catalog SPEC-R4/R5 (`checks` construct) · a2ui-renderer LLD-C10 §7 / LLD-C13 · goals §G4 (form-base custom-validity seam) · NEW a2ui/renderer/checks.ts · components/dom/form.ts (`setCustomValidity`) · components/controls/text-field/* (visible message) · a2ui/catalog/conformance.ts + a2ui/renderer/widget.ts (`checks` RESERVED) · **extends ADR-0026** (reuses `evaluate`) · **extends ADR-0014** (visible message node) |
| [0030](./0030-column-default-cross-axis-stretch.md) | ui-column default cross-axis align `start`→`stretch` (children fill width); row keeps start, list flips too (ratified), grid n/a | accepted | goals §G9 · components/controls/column/{column.ts,column.css,column.md} · components/controls/list/* · components/dom/container.ts (shared `flexProps.align` default stays `start`) · **extends ADR-0016** (flexProps grammar — a per-consumer default, the grammar stands) · relates ADR-0015/0005 |
| [0031](./0031-error-vocab-wire-mapping.md) | A2UI error-vocab: rich internal taxonomy (8 codes) mapped to the v1.0 two-code wire contract at the `#emit` boundary — every error → `VALIDATION_FAILED`+surfaceId this wave (FUNCTION = render-time binding-eval, not server-initiated); `INVALID_FUNCTION_CALL`+functionCallId reserved (#23); no wire `path` (folded into message) | accepted | a2ui-runtime SPEC §5.2 + R10/R11 (wire shape = 2 codes, code↔contextID tie, no path) · a2ui-renderer LLD §9 + LLD-C13 (`#emit` map) · NEW a2ui/protocol.ts discriminated `A2uiWireError`/`WireErrorCode`/`toWireError` · a2ui/renderer/renderer.ts · **extends ADR-0026** (FUNCTION internal emit stands; wire map is new) · relates ADR-0002 (validator parity untouched) |
| [0032](./0032-ui-content-scale-tier-system.md) | The `ui-sm…content-lg` two-band `[scale]` tier system — control ramp = `--ui-scale` multiplier ladder (user-confirmed), compact-box ramp = `--ui-compact-*` re-table (built now, user override); replaces the 3-step placeholder; resolves the scale/density vocab overlap | accepted | references/geometry-sizing-spec.md §3 + §5.2 (tier + compact ramp realized — closes #24) · shared/src/tokens/dimensions.css (`[scale]` 3→6 + NEW `--ui-compact-*`) + dimensions.test.ts · button.md/column.md prose · **extends ADR-0007** (numeric `--ui-scale` mechanism kept; vocabulary widens) · relates ADR-0025 |
| [0033](./0033-sublinear-font-glyph-decoupling.md) | Sublinear font/glyph decoupling — under `[scale]`, height stays linear but glyphs re-derive from height via §1.1 power laws (`font = base × pow(scale, 0.45)`, `icon = pow(scale, 0.58)`; gap/caret follow font); display `--ui-type-*` stays linear (fork); fixes content-lg font 24.5→18 | accepted | references/geometry-sizing-spec.md §1.1/§3 (sublinear law realized under `[scale]`) · shared/src/tokens/dimensions.css (`--ui-font-*` sublinear) + dimensions.test.ts · components/controls/button/button.css (`--ui-button-icon` sublinear) · **supersedes ADR-0007 font/glyph leg** (`*`-mechanism stands; **whole control ramp later superseded by ADR-0038** — Kim's no-multiplier lookup) · relates ADR-0032/0025 |
| [0034](./0034-a2ui-server-initiated-function-invocation.md) | A2UI server-initiated function invocation — NEW `callFunction` inbound RPC + `functionResponse` outbound; activates ADR-0031's reserved `INVALID_FUNCTION_CALL` arm; `callableFrom` enum gate (clientOnly/remoteOnly/clientOrRemote, default clientOnly); binding-eval surface untouched (distinct) | accepted *(amended: cross-catalog `callableFrom` collision = most-restrictive-wins, `clientOnly` hard floor — corrects a build defect)* | a2ui-runtime SPEC §5.1/§5.2 + NEW SPEC-R14 · a2ui-catalog SPEC-R5 (`callableFrom`) · a2ui-renderer LLD NEW LLD-C14 + §9 (`INVALID_FUNCTION_CALL` activates) · NEW a2ui/renderer/call-function.ts · a2ui/protocol.ts (callFunction/functionResponse) · a2ui/catalog/{catalog.ts,default/catalog.json} · a2ui/renderer/{dispatch.ts,renderer.ts} · **activates+extends ADR-0031** (reserved arm; corrects boolean→callableFrom enum) · relates ADR-0026 (shares the registry) |
| [0035](./0035-control-font-s1-set-explicit-table.md) | Control fonts/icons = the exact §1 SET integers via an explicit per-`[scale]`-tier table (replaces ADR-0033's `pow` approximation that yielded decimals); snap = nearest row of the Kim-ratified 8-row ramp (16→64; re-grounding changed no consumed value), one tie (h=42) round-down; icon §1-table too (4a shared `--ui-icon-*`); default 13·14·16 byte-identical; content-lg/lg = 20 exact | accepted | references/geometry-sizing-spec.md §1/§3 · shared/src/tokens/dimensions.css (`--ui-font` explicit table) + dimensions.test.ts · components/controls/button/button.css or shared `--ui-icon-*` · **supersedes ADR-0033 font/icon `pow` mechanism** (sublinear outcome stands; **font/icon values + height later re-tabled by ADR-0038** — Kim's no-multiplier lookup) · relates ADR-0032 + #25 `--ui-compact-*` re-table precedent |
| [0037](./0037-control-height-s1-set-table.md) | Control **heights** = the explicit §1 SET-height table — snap `--ui-height` to the ramp (nearest row, h=42 round-down — the SAME rule as ADR-0035, so height+font+icon come from ONE row); replaces the linear `base × var(--ui-scale)`; default 24·28·36 byte-identical; content-lg/lg = 64 exact (was 63); **font/icon UNCHANGED** (python-proven 0 moves); derived pads use the set height (cleaner integers); `content-sm ≡ content-md` fully collapse (flagged); `--ui-type-*` stays linear | **superseded by ADR-0038** (proposed, unbuilt) | shared/src/tokens/dimensions.css (`--ui-height` explicit table) + dimensions.test.ts · references/geometry-sizing-spec.md §1/§3 · references/geometry.md · **supersedes the height-linear leg of ADR-0007 / ADR-0033 / ADR-0035** (markers flipped) · relates ADR-0032 (the collapse is a ladder question) + ADR-0025 |
| [0036](./0036-single-line-control-line-height-standard.md) | Single-line **Control**-class text = `line-height: 1` (the centering law's vertical companion) via a fleet `:root` token `--ui-control-line-height: 1`; consumed on the control host `:scope` (inherits to label/editor); **EXCLUDES** the Display class (`ui-text` keeps `--ui-type-*-leading`); centering preserved (tightens the line box, not the frame) | accepted | **NEW** shared/src/tokens/dimensions.css (`--ui-control-line-height: 1`) + dimensions.test.ts · references/geometry.md (centering-law companion + Control row) · geometry-sizing-spec.md §2 · dimensional-standard.md (Control `@scope` recipe) · button.css + text-field.css (`:scope` consumer) · **relates ADR-0009** (analogous shared token-standard) · ADR-0025 (Display class excluded) |
| [0038](./0038-control-sizing-size-scale-row-lookup.md) | Control sizing = **Kim's authoritative (scale × size) → §1-row lookup** (no multipliers — "let's not use multipliers"; Kim provided the table); each cell is ONE §1 row → height/font/icon mutually consistent; default 24·28·36 / 13·14·16 / 16·18·20 byte-identical; the `content-*` band = the `ui-*` band shifted up one §1 row, so `content-sm≡ui-md` + `content-md≡ui-lg` (deliberate overlap → 4 distinct registers, Kim ratified — "we proceed"); **6 font + 6 icon non-default cells MOVE** vs ADR-0035 (same-row rule — content-sm/md glyphs smaller, ui-lg larger); `--ui-scale` LEAVES the control path, retained for `--ui-type-*` display only | accepted | shared/src/tokens/dimensions.css (3 explicit per-`[scale]` tables; `--ui-scale` display-only) + dimensions.test.ts · references/geometry-sizing-spec.md §1/§3 + geometry.md · button.css/text-field.css · **supersedes ADR-0007 control leg + ADR-0032 multipliers (controls); folds ADR-0037; re-tables ADR-0035 font/icon values** · relates ADR-0025/0033 (display stays `× --ui-scale`) · folds ADR-0036 + text-field-icon gap into one wave |
| [0039](./0039-layout-align-justify-dialect.md) | The layout family standardizes on the **box-alignment** `align`/`justify` dialect (`start`/`end`, not `flex-start`/`flex-end`) — normalize `ui-column`+`ui-list` off the flexbox keywords; `ui-row` is the reference (unchanged); `ui-grid` gap-only (unaffected). Only `start`↔`flex-start`/`end`↔`flex-end` change (center/stretch/baseline/space-* dialect-identical). Rationale: logical/writing-mode-relative + grid-`align`-compatible. The `start`/`flex-start` divergence is currently **unreachable** (wrap is boolean, direction is tag-fixed ADR-0016) → the normalization is a render no-op (forward-insurance for a future reverse mode) | accepted | controls/column/column.css + controls/list/list.css (+ browser tests) · relates **ADR-0030** (default `stretch` VALUE — untouched; this owns the DIALECT) + ADR-0016 (direction-is-the-tag) · #102 G9 sweep item 3 |
| [0040](./0040-foundation-barrel-budget-7kb.md) | Re-base the reactive+dom **foundation barrel** consumer budget 6 kB → 7 kB (6144 → 7168 B gz) — legitimate **ADR-0023** public-API growth (`mount` + the directive-authoring trio + `repeat`/`watch`, what the a2ui renderer needs); barrel measures **6180 B gz** (36 B over the old floor, ~1 kB headroom under 7 kB); tree-shake proof green (no leak). Per-control marginal (≤1.5–2 kB) + the shrink-only ratchet (above the new floor) + the 8 kB family barrel UNCHANGED. **Records a recommendation:** wire `npm run size` into the standard gate so a budget regression FAILS, not silent | accepted | .claude/docs/plan.md §10 (≤6→≤7 kB) · scripts/measure-size.mjs (`6*KB`→`7*KB`, exec) · relates ADR-0023 (the growth) + ADR-0003 (the size gate) · #102 unblocks #46 Wave-B marginals |
| [0041](./0041-widget-box-geometry-subsystem.md) | The **widget-box** geometry sub-system (Indicator/Range classes) — Kim's **8-value widget ramp** `12·14·16·18·20·22·24·28` (the widget analog of the §1 control ramp), sized via the **same explicit per-`[scale]` lookup as ADR-0038** (no multiplier; `--ui-compact-{sm,md,lg}` re-tabled). Default `ui-md` 14·16·18 (= §5.2, byte-identical); proposed table all-distinct tiers (denser ramp → no overlap). The **2px inset law** for thumbed widgets (switch/range): `thumb = box − 4px`, density-invariant, a `--ui-widget-inset: 2px` fleet constant. Reconciles §5.2 (drops 26/32) + the **stale `--ui-ind` name** (box=`--ui-compact`, glyph=`--ui-icon` ADR-0035). **Kim gates the widget table** (ADR-0038 precedent) | accepted | references/geometry-sizing-spec.md §5.2 + geometry.md · shared/src/tokens/dimensions.css (`--ui-compact-*` table + `--ui-widget-inset`) + dimensions.test.ts · **revises ADR-0032 §5.2 compact ramp** · relates ADR-0038 (the control lookup it mirrors) + ADR-0035 (glyphs) · #49 control-suite B |
| [0042](./0042-face-widget-value-control-bases.md) | FACE **widget value-control bases** — establishes the **`controls/_base/`** shared control-base sub-layer (extends a dom base + wires traits; sibling of `_surface/`) + **`UIIndicatorElement`** (base props `{checked, value, size}`, checked-state machine, pressActivation toggle, `--ui-compact`/2px geometry — checkbox/switch/radio; **`indeterminate` is checkbox-specific**, **no `:state(ready)` gate** — G6-review refinement) + **`UIRangeElement`** (props `{value, min, max, step, size}`) + the separable **`value-drag` controller** (ARIA slider, keyboard step, pointer→value — slider/slider-multi). `size` is a typed base prop (not per-leaf). Detail in the committed LLDs | accepted | NEW controls/_base/{indicator,range}-element.ts + traits/value-drag.ts (+ tests/barrel) · goals §G6/§G6.5 · .claude/docs/llds/{indicator,range}-element.lld.md · **extends ADR-0013** (UIFormElement) + ADR-0041 (geometry) · #49 Wave 0 |
| [0043](./0043-overlay-selection-primitives.md) | **Overlay + selection primitives** — the **Overlay controller** (`traits/overlay.ts`: native **Popover API** surface + light-dismiss + a **zero-dep JS positioning baseline** + anchor-positioning as `@supports` PE; two-way `open` ADR-0019; modal `<dialog>` stays the focus-trap boundary ADR-0017) + the **`roving-focus` trait** (extracted from `tabs.ts:130-168`; tabs migrates to consume it) + **`selection-commit`** + **`UIListboxElement`** (listbox value; menu = roving+action, non-value). Compose into select/combo-box/menu/tooltip/popover. Detail in the committed LLDs | accepted | NEW traits/{overlay,roving-focus,selection-commit}.ts + controls/_base/listbox-element.ts (+ tests/barrels) · REFACTOR tabs.ts (consume roving) · goals §G7 · .claude/docs/llds/{overlay-controller,listbox-roving}.lld.md · relates ADR-0042/0017/0019 · #49 Wave 0 |
| [0044](./0044-contenteditable-password-masking.md) | Mask `ui-text-field type=password` with **`-webkit-text-security: disc`** on the contenteditable editor (native `<input type=password>` is unavailable — ADR-0014's no-native-input model); the trailing **reveal toggle** flips it to `none`. De-facto-standard, CSS-only; rejected the JS mask-layer + a native password input. Caveats logged: the value is in `textContent` (client-side, like any web password field); AT reads the discs + the reveal is the accessible show/hide | accepted | controls/text-field/text-field.css + text-field.md · **relates ADR-0014** (contenteditable) + ADR-0012 (reveal = trailing slot affordance) · #49 Wave 3 |
| [0045](./0045-overlay-dismissal-semantics.md) | **Overlay dismissal semantics** — the reconciliation the Wave-4 cross-engine smokes forced (jsdom-green controls failed 18 assertions). `auto` popovers: the **PLATFORM owns Escape + outside-click** (controls add no Escape handler — a `this.open=false` there is a suppressed programmatic close; jsdom dismiss-probes use `simulateLightDismiss`). `restoreFocus()` targets the **anchor** (not `document.activeElement` — WebKit doesn't focus buttons on click). `close()` is **resilient to `isOpen`-flag desync** (guards on `:popover-open`, `try`-guarded `hidePopover`). `selectionCommit` **`preventDefault`s the committing Enter** (else it re-activates the restored `<button>` trigger → re-open). Plus a **0.25rem anchor↔panel gap**, gap-aware flip collision | accepted | traits/overlay.ts + traits/selection-commit.ts + controls/combo-box/combo-box.ts + the 5 overlay smokes · **relates ADR-0043** (hardens the primitives) + ADR-0019 (two-way open) · #49 Wave 4 |
| [0046](./0046-container-box-model.md) | **The container box-model** — one shared spacing layer (NEW `_surface/container-box.css`) beside container.css's PAINT; a surface opts in with **`[data-box]`** (a `flow-root` BFC; direct children get a **0.25rem inset margin**, collapsing to a uniform 4px; `margin:0` = full-bleed; `[padded]` = margin 0 + inset pad). The **sticky-region pattern**: `header`/`footer` full-bleed + `position:sticky` + `background:inherit`; `hr` divider; `content` the scroll region. The **region padding system**: header/content/footer **inline 12 · block 4 · gap 8** (rem → density-INVARIANT), a **nested content stepping IN one inset per level (12→8→4)** via explicit descendant levels (a self-referencing custom property is a CSS cycle — verified 0px). Content gap is an **adjacent-sibling margin** not flex (flex breaks `[scrollable]`). Rolled onto the overlay panels, ui-card (nested-radius re-based off content padding), ui-modal | accepted | NEW _surface/container-box.css (+probe) · component-styles.css · controls/{select,menu,combo-box,card,modal}/* + card geometry/browser tests · **relates ADR-0015/0016/0018** (re-bases the nested-radius chain) + ADR-0041 · Kim's container inset directive |
| [0047](./0047-numeric-codec-expansion.md) | **Numeric-codec expansion** — the Wave-5A `ui-text-field` numeric family completion: `TYPE_CONFIG` v2 splits the single trailing role into `{leading, suffix, affordance, codec, validation}` (so `unit`/`percent` carry a **suffix + steppers**); NEW types **`unit`**/**`percent`**; **multi-currency** (`currency` prop → per-code fraction digits + `narrowSymbol`, replacing the USD hardcode); **generalized steppers** (`step`/`min`/`max` props, `#step` clamp, ArrowUp/Down parity); **range validity** (`rangeUnderflow`/`rangeOverflow`; `stepMismatch` NOT enforced); **percent canonical = the typed number** (not Intl ÷100); `unitCodecOptions` a reserved seam (numeric core), unit label via `Intl` `style:unit` | accepted (2026-07-01) | controls/text-field/{ts,css,md} · traits/value-codec.ts (numeric core + currency/unit factories + label helpers) · decomp Wave-5A · **extends ADR-0044** + relates ADR-0012 · #49 Wave 5A |
| [0048](./0048-date-time-picker-architecture.md) | **Date/time picker architecture** — the Wave-5B pickers: NEW **`ui-calendar`** (`extends UIFormElement`, canonical ISO `YYYY-MM-DD`) that is BOTH standalone AND the `type=date` popup body; a **bespoke 2D grid handler** (NOT a 2D roving fork — `roving-focus` is 1D linear; reuses the Enter-`preventDefault` + `aria-selected` semantics), panel `[data-box]`; `type=date` **lazily `import()`s the calendar** (code-split — protects the tree-shake proof) opening it through the Wave-4 `overlay`; `dateCodecOptions`/`timeCodecOptions` (`date`/`time` IN; `datetime-local`/`month`/time-list/range **STRETCH/future**) | accepted (2026-07-01) | NEW controls/calendar/{ts,css,md} · traits/value-codec.ts (date/time codecs) · controls/text-field/* · decomp Wave-5B · goals §G6/§G7 · **composes ADR-0043/0045/0046/0042** + **extends ADR-0044/0047** · #49 Wave 5B |
| [0049](./0049-family-barrel-budget-22kb.md) | **Family barrel budget 16 → 22 KB gz** — the self-defining `ui-*` family barrel exceeded its 16 KB gz ceiling once Wave 5 added a whole new control (`ui-calendar`) + the date/time picker paths (19889 B gz actual). Re-based to **22 KB** (~13% headroom); the per-control marginal (~2 KB, tracked in each `{name}.md`) stays the real cap, `size` stays a MANUAL gate (ADR-0040 §3). The `INEFFECTIVE_DYNAMIC_IMPORT` warning is expected/benign — calendar is dynamically imported by text-field (granular tree-shake) AND statically by the barrel (family self-define); the granular proof (`tree-shake.test.ts`) still holds | accepted (2026-07-01) | scripts/measure-size.mjs · **follows ADR-0040** (barrel-budget re-base pattern) + relates ADR-0048 (the growth's cause) · #49 Wave 5B |
| [0050](./0050-form-provider-context-registration.md) | **form-provider context via a connect-time registration event** — the fleet's FIRST context/provider primitive (G7 completion): `UIFormElement` dispatches a composed, bubbling `ui-`-prefixed protocol event at connect (detail: control + a connection-scoped teardown `AbortSignal`); the NEAREST `ui-form-provider` registers it (`stopPropagation` = nesting scope) into a reactive registry (`traits/form-registry.ts`); deregistration rides the handle's `abort` (a detached node can't dispatch) — NO MutationObserver; aggregation = computeds over member signals (props are signals → zero polling). Rejected: the community `context-request` protocol (re-evaluate on a SECOND context consumer — the extract-on-second-consumer discipline), the querySelector+MutationObserver scan, a global registry | accepted *(user-ratified 2026-07-01 — "1b + 2B, go")* | goals §G7 (form-provider DoD) · plan §3/§5 · dom/form.ts (the dispatch) · NEW traits/form-registry.ts · NEW controls/form-provider/* · decomp g7-field-form-provider s1/s5/s9 · relates ADR-0013/0029/0042/0043 |
| [0051](./0051-uiformelement-field-labelling-seam.md) | **The `UIFormElement` field-labelling seam** (fork 2 = option B, user-ratified "1b + 2B, go") — `ui-field` hands its id-seeded label/description/error ELEMENTS to the slotted control via `setFieldLabelling` (a signal-backed method, the `setCustomValidity` shape); a base effect forwards through the overridable `applyFieldLabelling` hook — default = internals ARIA element reflection GUARDED on `internals.role` (indicator/range controls wire free; jsdom lacks reflection → the AX read-back is the browser gate's), part-role controls override to id-reference their part (text-field → editor). Option A prop-sync = the name-only BRIDGE (only-if-consumer-unset); option C (reaching into `[data-part]`) stays rejected. Stitching: under association the control's internal message node YIELDS (empty+hidden+dropped from aria-describedby) — the field's error node is the ONE announced error; `formResetCallback` dispatches protocol `ui-form-reset` — RESERVED, zero consumers (the field re-suppresses via the tracker's signal write under the gen-3 REACTIVE error rendering; activation = the first external consumer, the ADR-0031 reserved-arm precedent). Cl.5 (coordinator-ruled): the UPGRADE-path catch-up — upgrade follows define order not tree order, so `UIFormElement` gains public `announceFormConnect()` (re-dispatch iff the CONNECTION SIGNAL is live — the bulk-insert cascade guard, an s9-caught real bug) + one-shot `querySelectorAll` catch-up scans in provider/field at connect (idempotence-guarded; NOT a MutationObserver — ADR-0050's mechanism stands) | accepted *(2026-07-01 — orchestrator-ratified on the green s14 gate: promotion review PROMOTE, check + jsdom 2014 + browser 544/544 + size 21599/22528; fork user-ratified "1b + 2B, go")* | goals §G7 (ui-field DoD) · dom/form.ts (the seam + ui-form-reset) · controls/text-field/{ts,md} (the forwarding wire) · NEW controls/field/* · decomp g7-field-form-provider s1/s2/s8/s11 · relates ADR-0050 (protocol namespace) /0013/0014/0029 |
| [0052](./0052-container-z-depth-scope.md) | **A container is its own z-depth scope** — `isolation: isolate` joins the `:where([data-box])` rule (the ADR-0046 opt-in now grants layout BFC + PAINT SCOPE in one attribute): every descendant z-index (the sticky brackets' `z-index:1`, select/calendar panel chrome) resolves INSIDE the box, a sibling container can never be painted over, no global z ladder can exist. `isolation` is the one purpose-built property (no containing block, no clip side effects). Overlays unaffected (native top layer, ADR-0043/0045). Internal z stays small+local (the probe rejects ≥20). Layout primitives (row/column/list/grid) deliberately NOT isolated — transparent structure, not surfaces. Cross-engine browser proof with a NEGATIVE control (same markup minus `[data-box]` ⇒ the z-index:9999 DOES win — the probe bites) | accepted *(2026-07-01 — orchestrator-ratified on the green s14 wave gate [browser 544/544 incl. the 6 z-scope proofs]; Kim's /btw directive was the decision; the carrier choice drew no objection)* | controls/_surface/container-box.css + container-box.test.ts · NEW container-box.browser.test.ts · **extends ADR-0046** + relates ADR-0043/0045 · Kim's z-scope directive 2026-07-02 |
| [0053](./0053-a2ui-form-family-catalog-rows.md) | **A2UI form-family catalog rows** — six new default-catalog rows exposing G7: `Field`→ui-field (`label`/`description`, single-`child`) · `FormProvider`→ui-form-provider (**zero properties** — mirrors the attribute-less control; the ADR-0054 gate mark rides its factory) · `Checkbox`/`Switch` (bindable **`checked`**, bespoke `label`→textContent) · `Select`→ui-select (renames the planned `ChoicePicker`; `value`/`select` two-way; `open` deliberately absent — one `value` mark per component, a one-way `open` would desync on light-dismiss) · `Option` (a sanctioned `div[role=option]` primitive) + the TextField reach widening (`type` 12-enum · `currency` · `unit` · `step` · `min` · `max`). Pins the **bindable-naming law**: a bindable prop is named by the CONTROL's own prop (the `Tabs.selected`/`Modal.open` precedent) — the `value:{prop,event}` seam names BOTH sides, and `ui-checkbox` has both `value` ('on') and `checked` (boolean), so Basic's `value: boolean` is a live trap | accepted *(2026-07-02 — forks F2/F4 Kim-confirmed; orchestrator-ratified on the green wave gate: check + jsdom 2036 + browser 544/544 + size 21649/22528)* | a2ui-catalog SPEC §5.2 (activate/add/rename rows) · catalog/default/{catalog.json,factories.ts} · relates ADR-0019/0050/0051/0047/0048/0054 · NEXT.md item 4 intake, decomp `a2ui-form-catalog-examples` |
| [0054](./0054-a2ui-submit-gated-action.md) | **The submit-gated action** — the action shape gains an optional, CLIENT-consumed `submit: true` key (**extends ADR-0011**; the wire message is byte-identical) + `WidgetFactory` gains a generic `submitGate: true` mark (the registry derives the gate SELECTOR from marked factories' tags — no `ui-form-provider` literal in the renderer; two-tier-extensible). `#wireAction`: a flagged click resolves `closest(gateSelector)` → `submit()` false ⇒ NO action (the provider already ran first-invalid `reportValidity`); true ⇒ emit as today; no gate ⇒ emit normally. The typed aggregate rides the DATA MODEL (two-way binds + `sendDataModel`), NOT `context` (`FormValue` admits File/FormData — not JSON-wire-safe; `collectContext` is unshipped). Gating is opt-in per Button (a Cancel must not validate) | accepted *(2026-07-02 — fork F1 Kim-confirmed; orchestrator-ratified on the green wave gate; zero-drift + gate probes + the live wire-log refusal demo)* | a2ui-catalog SPEC §5.1 (WidgetFactory + action-shape note) · catalog/{types,registry}.ts · renderer.ts `#wireAction` (+ zero-drift probes) · **extends ADR-0011** · relates ADR-0019/0050/0053 |
| [0055](./0055-a2ui-example-seeds-package-home.md) | **Example payloads → a package-owned seed shelf** — the four `/site` pages' inline `A2uiServerMessage[]` consts move to **NEW `src/examples/`** as typed `ExampleSeed` records (`name`/`description`/`promptText`/pins pre-aligned field-for-field with `CorpusRecord` — the seeds ARE the corpus store's future AUTHORED admission candidates; `corpus/**.jsonl` stays admission's single-writer data, no rival) + a **`"./examples"` subpath export** (never the root barrel — no payload bytes in a consumer bundle) + the **standing gate** `examples.test.ts` (inside the vitest include by construction: shared-validator verdict + a real-host jsdom smoke per seed — the flagged runtime-only validation gap closes at check time). Canvas's 2 `callFunction` RPC literals stay page-local (protocol probes, one EXPECTS rejection; validate.ts doesn't know the envelope — the `MESSAGE_KINDS` parity gap recorded + scheduled as its own slice). The form seed re-slices into a fine-grained stream so form + streaming pages share ONE canonical payload | accepted *(2026-07-02 — forks F1/F2 Kim-confirmed; orchestrator-ratified on the green wave gate; the standing gate + shape pins live; shown ≡ fed ≡ gated across all five pages)* | packages/agent-ui/a2ui/package.json (subpath) · NEW src/examples/* · the 4 site pages (imports) · relates corpus-store LLD + corpus SPEC-R1/R2/R5/R9 + ADR-0002 · decomp `a2ui-streaming-examples` |
| [0056](./0056-region-less-card-humane-default.md) | **The region-less card humane default** — Kim's forked diagnosis (all 8 example Cards composed BARE children; ADR-0046's box-model never engaged — `ui-card` holds zero padding by law, spacing rides regions; wild Basic-conditioned agents will emit bare Cards as their DEFAULT). Decision (recommended d+b): **component-side CSS fallback**, NOT a factory rewrite — `card.css` gains a `:not(:has(> region…))` leg applying region-equivalent padding/rhythm to a region-less card (streaming-safe: a late-arriving region flips it off via `:has()` re-evaluation; payload tree ≡ DOM tree, no synthetic elements; plain-markup consumers benefit too) + **pedagogy as the contract** (ADR-0055 seeds model `Card > CardContent` from birth · catalog SPEC §5.2 Card composition note · a patterns-page "composing containers" block). Sticky/scroll still REQUIRE real regions. Rejected: factory always-wrap (double-wrap/tree divergence), factory wrap-when-no-region (streaming detection degenerates to a `childrenTarget` seam through list.ts), teach-only (renders the expected wild input worst). Modal boundary flagged as follow-up | accepted *(2026-07-02 — F4 ruled (d)+(b), Kim-confirmed; orchestrator-ratified on the green wave gate; fallback + cross-engine probes + all three pedagogy legs shipped; nested radius verified unchanged)* | controls/card/card.css (+ browser probes, **component-builder**) · card.md · a2ui-catalog SPEC §5.2 Card row · **extends ADR-0046** · relates ADR-0053/0055/0018 · decomp `a2ui-streaming-examples` v2 |
| [0057](./0057-intent-non-color-signifier-rule.md) | **Intent never travels by color alone** — the fleet non-color-signifier rule (item 5, 2026-07-02 color-verify audit): the intent anchors are deliberately L-matched, so intent is HUE-ONLY at the token level and collapses under CVD (danger↔success ΔE_OK ≈ 0.013–0.015 under deuteranopia); the ramps are NOT the defect — the guarantee is component-layer LAW: wherever an intent role styles a state/variant, a **visible non-color signifier co-carries the meaning** (text naming the state · glyph/shape · position · pattern; ARIA required alongside, never sufficient — WCAG SC 1.4.1). Checkable contract: a color-values-only diff between two states of a surface FAILS. Shipped fleet conforms (text-field message text ADR-0014 cl.4 · field error text · calendar ring-vs-fill · indicator glyph/dot/thumb — zero control fixes); enforcement = a RECOMMENDED rubric C8 line on ratification, no new machinery; forward triggers: button `family`, badge/alert/toast, first `--md-sys-color-warning`/`--md-sys-color-info` consumer | accepted *(2026-07-03 — Kim-confirmed; orchestrator-ratified on the green wave gate; the C8 line landed at ratification)* | references/component-authoring-best-practices.md (Do + Don't — edited same change) · rubrics/component.md C8 (recommended, on ratification) · relates ADR-0014/0048/0008 |
| [0058](./0058-defer-intent-fill-selected-roles.md) | **Defer the dark intent-fill AA text roles** — item 3, 2026-07-02 color-verify audit: white on the intent FILL dark leg (`--md-sys-color-{f}`→`{f}-450`) measures **3.13–3.41:1** (below the 4.5 AA TEXT bar); only `--md-sys-color-primary-selected` (ADR-0048) is AA-safe today, and **no filled intent control exists** (button is single-family; `--md-sys-color-warning`/`-info` have zero consumers) — a real but *unreachable* gap. Decision: **defer, don't mint** four unconsumed roles (matches the fleet's on-demand `-selected` discipline + the ADR-0031/0051 reserved-arm precedent); record the gap + the pre-computed remedy `--md-sys-color-{f}-selected: light-dark({f}-550, {f}-600)` (verified ≥4.5 both legs: danger 4.92/5.89 · warning 4.82/5.81 · success 4.52/5.45 · info 4.58/5.57) + the **activation trigger** = the first filled intent control. GLYPH-on-fill (3:1 bar) unaffected; zero code change this pass | accepted *(2026-07-03 — Kim-confirmed; orchestrator-ratified on the green wave gate; written deferral, reserved-arm precedent)* | references/tokens.md (`-selected` note — reserved siblings + trigger) · **extends ADR-0048** · relates ADR-0057/0008 |
| [0059](./0059-neutral-track-solid-role.md) | **The solid `--md-sys-color-neutral-track` role for state-bearing widget tracks** — item 4, 2026-07-02 color-verify audit: the switch off-track + slider rail bound `--md-sys-color-neutral-outline-variant` (a translucent decorative outline) composite to **1.51:1 light / 1.73:1 dark** — an SC 1.4.11 fail; and the OFF/empty-state thumb (1.22–1.84:1) does NOT carry identification either, so a solid track is *forced* (audit option B disproven by measurement). Decision: **mint a dedicated solid pair** `--md-sys-color-neutral-track: light-dark(neutral-600, neutral-400)` (idle, clears 3:1 every plane both schemes — worst 3.80/4.41) + `--md-sys-color-neutral-track-hover: light-dark(neutral-700, neutral-300)` (monotonic + distinct both schemes, 5.56–7.81); repoint switch/slider. Slider VALUE rides the high-contrast THUMB (clears fill 4.79/3.67 + rail 4.69/3.74); fill↔rail luminance stays low by design. Disabled tracks 1.4.11-exempt (untouched); WHCM in-sheet (untouched). **Extended by ADR-0094** (the thumb-vs-page-surface third contrast dimension — the `--ui-slider-thumb-ring` layer; this record's Decision + every floor stand, re-pinned by 0094's regression guard) | accepted *(2026-07-03 — Kim-confirmed; orchestrator-ratified on the green wave gate; solid track forced by measurement)* | shared tokens.css (roles) · references/tokens.md (`-track` role) · controls/switch/switch.css · controls/slider/slider.css · relates ADR-0041/0042/0008/0009/0057 · extended by ADR-0094 |
| [0060](./0060-corpus-store-phase1-admission-seams.md) | **Corpus store phase 1** — admission ships with the tier-2 judge as an INJECTED seam (`admit(candidate, {judge?})`; absent judge ⇒ stage skipped, `qualityScore` absent is the honest marker — the harness wave's corpus-quality rubric activates it) + the eval facet **fail-closes** (`E_LEAK`) until the LLD-C8 contamination mechanism exists — SPEC-R4/N3 hold vacuously, no gold can land unprotected. Activation triggers named (reserved-arm discipline, ADR-0031/0051/0058 precedent) | accepted | corpus SPEC-R8 phasing note + SPEC-R4/N3 reading · corpus LLD-C5 §6 + C8/C12 deferrals · relates ADR-0055 · NEXT item 1 intake, decomp `a2ui-corpus-store` |
| [0061](./0061-corpus-shared-healer-contract.md) | **The ONE shared healer's contract** — text-first `heal(input: string \| A2uiOutput, pin?) → {ok, messages, changed, repairs} \| {ok:false}` serving BOTH corpus admission and the future streaming codec per-line (the streaming v0.2 reconciliation's single-healer ruling made concrete); the repair list is **CLOSED and form-only** (fence-strip · trailing commas · single-object→array · version-fill-from-pin) — nothing semantic is ever healed (no laundering; tier-1 rejects); healed admission marks `status:"repaired"`, repairs travel in the AdmitResult (schema stays closed) | accepted | corpus LLD-C7 §7 + C5 §6 · corpus SPEC-R8 healing clause + §5.1 status reading · streaming LLD-C1 §2 binding · relates ADR-0055 · decomp `a2ui-corpus-store` |
| [0062](./0062-corpus-packaging-pure-core-subpath-data-home.md) | **Corpus packaging** — `src/corpus/*` = platform-neutral PURE core (zero deps AND zero Node builtins; `createStore(shards)` over provided text; `crypto.subtle` hashing ⇒ async canonicalize) exposed via a **NEW `"./corpus"` subpath export** (never the root barrel — zero corpus bytes in a renderer consumer, the ADR-0055 precedent); `tools/corpus/*` = the Node shell (fs-store + import-seeds via type-stripping, the ONLY writer of the data dir); JSONL data home = in-package `corpus/` (package root — unimportable by construction, the exports map is the bundle boundary) | accepted | corpus LLD-C1 §1/§2/§12 · a2ui package.json exports (build-time, gated on ratification) · **extends the ADR-0055 subpath pattern** · corpus SPEC-N5 · decomp `a2ui-corpus-store` |
| [0063](./0063-corpus-record-upstream-dataset-schema-alignment.md) | **Corpus record aligns to the VERIFIED upstream `dataset_schema.json`** (google/A2UI@main, host-fetched 2026-07-03 — the SPEC §7 THEN-branch fired): `description` unconditionally required (REVERSES the SPEC-v0.3 eval/target carve-out the s1 build had encoded); `target` optional with the upstream fallback explicit (effective target = `target ?? description`, a CONSUMER rule); **`E_NO_TARGET` retired** (unreachable by construction — NOT a reserved arm, no activation trigger can exist); R1-AC1 interop = PROJECTION onto the 7 upstream fields (drop `meta` AND `a2uiOutput` — upstream `additionalProperties:false` falsified "strip meta only" for every exemplar); the upstream dataset ARRAY-form fact recorded for LLD-C8/C12. Near-zero cost window: eval facet fail-closed (ADR-0060), s6 unbuilt | accepted | corpus SPEC v0.4 (R1/R2/§5.1/§5.3/R16/§7) · corpus LLD v0.3 (§0/§3/§6/§8/§11/§13) · record.ts + record.test.ts (s1 follow-up, gated on ratification) · decomp v3 n3/n8 · relates ADR-0060 |
| [0064](./0064-corpus-record-single-surface-v1.md) | **A v1 corpus record is SINGLE-SURFACE** — the s6 builder's trace: `validateA2ui` judges PER SURFACE but `canonical.ts`'s `foldStream` upserts into ONE global id map (canonical.ts:93-99), so a tier-1-green MULTI-surface record would silently chimera (both surfaces legally declare `root`; last-write-wins) into the hash/dedup identity. Ruling: an exemplar's `a2uiOutput` addresses EXACTLY one surface (surfaceless `callFunction` excluded from the count, not banned; the exactly-one bound also closes the callFunction-only vacuous-tier-1 hole); rejection site = `validateRecord` E_SCHEMA (a record-SHAPE rule — the standing stored-shard gate then enforces it too, which admission-stage placement wouldn't); the global fold becomes CORRECT BY PRECONDITION; widening trigger = the first real multi-surface exemplar need (ADR then: surface-scoped folding, s2-seat). Corpus-only — wire/renderer multi-surface stays legal, validator parity untouched | accepted | corpus SPEC v0.5 R2 clause · corpus LLD v0.4 §3/§4/§8 · record.ts + tests (own slice, s6 seat — the 0063 follow-up had already landed) · decomp v4 n3 · relates ADR-0063 |
| [0065](./0065-icon-adapter-swappable-pack-architecture.md) | **The icon adapter — a swappable-pack registry in a NEW zero-dep leaf `@agent-ui/icons`** (mirrors the ADR-0062 pure-core + subpath shape; sibling of `@agent-ui/shared`, edge `components → icons`). No icon system exists today — every glyph is an ad-hoc imperative literal (`select.ts:282` `'▾'` · `calendar.ts:318/332` `'‹'`/`'›'` · `text-field.ts` `'✕'`/`'👁'`/`'📅'`/`'▲'`/`'▼'`). Core: an `IconName` `as const` union + an `IconPack` (inert `{id,viewBox,icons}`) + an `IconRegistry` (`registerPack`/`setActivePack`/`overrideIcon`, PUBLIC — the a2ui two-tier `Registry` precedent, a consumer swaps packs zero-edit) + `resolveIcon(name)→SVGElement` (`createElementNS`+`innerHTML`, unknown→non-throwing `<svg data-icon-missing>`). **Geometry reconciliation (NO law change):** the SVG is stamped `fill=currentColor`+`width/height=100%`, so it fills an `[data-role=icon]` cell at icon size and — via the caret cell's EXISTING `padding:(icon−glyph)/2` — insets to font-rhythm in a `[data-role=caret]` cell FOR FREE (feeds ADR-0006/0012/0035 cells, never re-cuts them). Two surfaces over one core: imperative `setIcon(el,name)` (the migration one-liner) + declarative `ui-icon` (consumer; reacts to `name`/`label` — live pack-swap reactivity deferred, apps register their pack at startup). Trip-wire `layering.test.ts:57` widens to admit `@agent-ui/icons` as a 2nd lower-tier sibling. Migrations are a SEPARATE later wave | accepted | icon-adapter.lld.md LLD-C1/C2/C3/C5 · layering.test.ts:57 (allowlist, gated) · NEW `packages/agent-ui/icons/` (gated) · relates ADR-0062/0055/0006/0012/0035 · decomp `icon-adapter` |
| [0066](./0066-phosphor-default-pack-buildtime-vendoring.md) | **Phosphor is the default pack — a curated 9-icon subset vendored at BUILD TIME into committed inert TS (zero runtime dep)** (extends ADR-0065). Weight = `regular`. Subset = exactly the audit's need (`caret-down/-up/-left/-right · x · eye · eye-slash · calendar-blank · check`, the last the forward companion for an optional future checkbox migration) — adding more is a one-line curation-map edit. Mechanism: `scripts/vendor-phosphor.mjs` reads `regular` SVGs from **`@phosphor-icons/core` (a devDependency, never shipped)**, strips fixed `width/height/fill/stroke`, extracts the viewBox-256 inner body, emits **committed** `src/phosphor/icons.gen.ts` (`Record<IconName,string>`), so `npm ci` builds with zero Phosphor in the runtime graph. `@agent-ui/icons/phosphor` self-registers + activates on import (the fleet self-define idiom); the root barrel never pulls it (ADR-0055/0062 subpath rule — zero Phosphor bytes for a consumer who doesn't import it). A 2nd real pack is a non-goal — swappability proven by the `overrideIcon`/`registerPack` tests, not a 2nd vendor | accepted | icon-adapter.lld.md LLD-C4/C6 · `packages/agent-ui/icons/package.json` (devDep + `"./phosphor"` export, gated) · extends ADR-0065 · relates ADR-0062/0040 · decomp `icon-adapter` |
| [0067](./0067-harness-right-sizing-realized-estate.md) | **The expert harness right-sizes onto the realized estate** — TWO skills (`a2ui-compose` merges the v0.1 patterns+composition areas; `a2ui-corpus-curate` = a THIN pointer over the realized pipeline; `a2ui-jsonl-mcp` trigger-deferred to the streaming producer wave) + ONE maker/critic agent pair (`a2ui-composer` graded by the payload rubric · `a2ui-reviewer` grades payload/catalog-row/corpus-record; the v0.1 catalog-author is `a2ui-builder` territory, the corpus-curator is `import-seeds`+`admit()`; the builder description gains the mutual routing fence); rubrics live at **`.claude/docs/rubrics/`** (the v0.1 `specs/rubrics/` path never existed); **NO harness gate code** (SPEC-R4 realized by the shared validator + admission E_* + the standing shard gate; only `scripts/harness_wiring_check.py`, a MANUAL gate per the size-script precedent with a named promotion trigger); the SPEC-R6 loop is **procedural** this wave (composer charter + `validate-payload.ts` CLI, maxRounds 3, HOST-orchestrated rounds — the critic's verdict returns to the composer, which never self-grades) — the programmatic driver is the live-agent wave's (streaming LLD-C2 re-point APPLIED); NO eval facet (corpus LLD-C8 keeps its trigger) | accepted | harness SPEC v0.2 + harness LLD v0.2.1 (edited in-change) · `a2ui-streaming-pipeline.lld.md` §1/§3 (edited in-change) · `a2ui-builder.md` description incl. the stale rubrics-pending line (h8, gated) · relates ADR-0060 · decomp `a2ui-expert-harness` v2 |
| [0068](./0068-corpus-quality-judge-verdict-adapter.md) | **The corpus-quality judge = a deterministic VERDICT-ADAPTER over critic-authored verdicts** (activates ADR-0060's injected seam — its named trigger IS this wave): the `a2ui-reviewer` critic grades records against `.claude/docs/rubrics/a2ui-corpus.md` (aggregation OWNED by the rubric: `qualityScore` = MIN over `[gate]` dims 1–5, `passed` = ≥4) and emits ONE verdicts file; `src/corpus/judge.ts` (pure, joins the `"./corpus"` barrel) = `parseVerdictsFile` + `createVerdictJudge` whose `score()` **THROWS on an unjudged candidate** (fail-closed; `import-seeds --verdicts` maps it to report+halt — the θ_dup precedent); back-scoring via `tools/corpus/rescore.ts` rides repair-loop semantics (at/above bar → `qualityScore`; below bar → `status:"quarantined"`, ONE-WAY under rescore; all-or-nothing serialize, halt on unknown-name/conflicting re-verdict, idempotent) — and **QUARANTINE IS MADE SURVIVABLE end-to-end**: the standing gate `corpus-data.test.ts:45`'s never-quarantined assertion AMENDED in the same change (its "invariant ii" cite was facet-only; quarantined lines legal, tier-1/hash legs skipped for them), dedup warming enumerates quarantined records (`store.all({includeQuarantined})`), routine imports HALT on a quarantined-name collision, `--replace <name>` = the sanctioned judged exit (status recomputed by admission) — the 11 phase-1 records clear the ADR-0060 absent-marker (E_QUALITY proven by a temporary-shelf-seed plant/revert); corpus LLD-C12 SPLITS (judge half now, Inspect-AI scoring/lift deferred with LLD-C8 + the upstream `eval/a2ui_eval/{scorers,dataset}.py` fetches) | accepted | harness SPEC §5.3 + harness LLD §7 + corpus LLD §0/§1/§11 v0.5.1 (edited in-change) · `corpus-data.test.ts` + `store.ts` + `src/corpus/judge.ts` + `rescore.ts` + `--verdicts`/`--replace` (h11/h12, gated) · relates ADR-0060/0063 |
| [0069](./0069-a2ui-live-agent-demo-shape.md) | **The live-agent demo is a LAYERED shape isolated behind an `AgentTransport` seam** (Fork A, the headline): the page consumes ONLY `AgentTransport` (`turn(input)→AsyncIterable<string>` of A2UI JSONL), so backbone↔proxy↔client-direct swap with NO page edit; a deterministic RECORDED TRANSCRIPT backbone (reuses a shelf seed as turn-1's payload → validity already gate-covered by `examples.test.ts`) is the DEFAULT + the ONLY thing CI exercises + what `build` ships [Fork F]; the live overlay is a dev-server Vite middleware PROXY holding the key from `process.env`, calling the API with **plain `fetch` (no LLM SDK, no dep)**, streaming VALIDATED A2UI JSONL back — retrieval + the loop run proxy-side (Node, `fs-store` loads the shard) [Fork C]; the `.env`'s `VITE_` variant provisions a client-direct arm (CORS host-verified viable-but-**dangerous** → stays DEFERRED/dev-only). The **`VITE_` build-key-safety** invariant closes the inlining footgun: every overlay is dev-only-dynamic-imported (`import.meta.env.DEV`) so `vite build` tree-shakes it + its `VITE_` refs out — proven by a build-key-safety gate (source-grep + manual `dist/` grep). Zero package deps, secret-free CI | accepted | streaming LLD-C2 `produce()` (realized this wave) · NEXT item 3 · new SPEC/LLD/decomp `a2ui-live-agent` · relates ADR-0055/0062/0067/0073 |
| [0070](./0070-a2ui-live-runtime-loop-scope.md) | **The live runtime loop = the SPEC-R6 contract MINUS the critic round** (Fork B): retrieve → generate → shared `heal`+`validateA2ui` → self-correct on the validator's failures → `maxRounds 3` → halt-and-report; the deterministic gate is the WHOLE runtime verifier (a web demo has no Task tool / seat to run the `a2ui-payload` critic — the rubric stays AUTHORING-time: the backbone payloads were graded ≥4 pre-commit, the retrieved corpus is already tier-2 judged); **VALIDATE-THEN-STREAM** — a fully validated payload precedes any paint (PRD-G4), then its lines stream progressively (root-early), so the browser transport is IDENTICAL for recorded + live; the driver takes an injected **`AgentProvider`** (ADR-0073's `stream` seam) so the loop is stub-tested with no model | accepted | `a2ui-live-agent.spec.md` · streaming LLD-C2 · relates ADR-0067/0073 · harness LLD §6 |
| [0071](./0071-a2ui-derived-drift-gated-system-prompt.md) | **The machine system prompt is DERIVED from `catalog.json` + the `a2ui-compose` grammar and drift-gated** (Fork D): `buildSystemPrompt(catalog, exemplars)` composes the grammar (from the `a2ui-compose` references, DRY) + the catalog INVENTORY derived at run time from `catalog.json` (the sole authority — never hand-listed) + the few-shot block from `retrieve()` over the JUDGED shard; a standing test asserts the derived component/prop inventory EQUALS `Object.keys(catalog.components)`/each row's props (a planted row absent from the prompt fails) — derive-don't-duplicate + a coverage gate (the `examples.test.ts` precedent), so the model can never be told of a component the catalog lacks (PRD-G6). Tools-scoped, zero package deps | accepted | `a2ui-live-agent.spec.md`/`.lld.md` · relates ADR-0055/0060/0063 · `a2ui-compose` skill |
| [0072](./0072-a2ui-live-multi-turn-session-model.md) | **The multi-turn session: a client message becomes the next turn's user input; a stateless proxy; surface continuity via the host** (Fork E): a `Session` is the standard Messages-API turn array (user=intent or framed-client-message, assistant=emitted A2UI stream); a pure REDUCER frames each `onClientMessage` arm distinctly — `action`(+`context`/`dataModel`) · `functionResponse`(the awaited value) · `error`(fed back for CROSS-turn recovery, distinct from the intra-turn loop); turn N+1 patches the SAME `surfaceId` or opens a new one (both already host-handled); the PROXY IS STATELESS (the browser holds the turn history) + a demo max-turns cap guards runaway; A2A continuity is host-VERIFIED (`contextId`/`taskId`/`TASK_STATE_INPUT_REQUIRED`) and CITED as the conformance target — full id-conformance kept in the producer layer so conforming never touches the renderer | accepted | `a2ui-live-agent.spec.md`/`.lld.md` §4 · relates ADR-0069/0031/0011/0034 |
| [0073](./0073-a2ui-live-model-provider-seam.md) | **The config-driven multi-provider seam** (Fork G, Kim-directed via `/intent-extract`): each provider is a module exposing `AgentProvider.stream({model,system,messages,signal})→AsyncIterable<string>` (text fragments → A2UI JSONL); a committed `providers.json` registry enumerates ALL THREE (Anthropic/OpenAI/Gemini) + their models NOW — the SoT for BOTH the in-chat provider→model switcher AND the proxy allowlist (env-var NAMES + public endpoints/model-ids only, NO secrets); **Anthropic implemented now** (plain fetch, host-verified SSE flow, no LLM SDK), **OpenAI/Gemini the immediate NEXT adapter slices** (config rows present now — NOT indefinite YAGNI); the switcher (site/) renders from the config + persists to `localStorage`; the PROXY validates `{provider,model}` against the allowlist + routes to `process.env[envKey]` (the trust boundary); the `VITE_` build-key-safety generalizes to N providers unchanged. REVERSES the earlier Claude-only-YAGNI draft (a consumer now exists) | accepted | `a2ui-live-agent.spec.md` R11/R12 · `.lld.md` LLD-C10/C11/C12 §5 · decomp l16-l19 · relates ADR-0069/0070 |
| [0074](./0074-md-sys-color-token-namespace.md) | **The color-token namespace becomes `--md-sys-color-*`** — an extension of Google Material Design 3 is the official color foundation (Kim-directed; scope via `/intent-extract`). A **color-only, value-preserving** rename of `--c-*` → `--md-sys-color-*` (**1910 occurrences / 83 code + 36 doc files, conserved 1:1, zero survivors**; the `oklch` values are byte-identical — a proven **zero visual change**) + ONE alpha-padding normalization (`-500-50 → -500-050`, tokens.css only, 24 occ = 8 palettes × (def + 2 scrim-weakest refs); the `-050-50`/`-950-50` wash primitives DELIBERATELY unpadded). `--ui-*` dimension/type/geometry tokens are OUT of scope, unchanged. Historical ADRs/decomps swept to the new spelling (decisions unchanged — only the name; THIS ADR records the when/why). Retires `--c-*`; a standing zero-survivor grep is the regression guard. Gates: check(+site) · jsdom 2342 · browser 564 · size byte-identical | accepted | Cross-cutting — no owning-doc ID · renames roles from ADR-0008/0009/0015/0057/0058/0059 (each stands) · CLAUDE.md naming line · `references/tokens.md` · a2ui-catalog SPEC-R5/LLD-C8 |
| [0075](./0075-ui-column-canvas-root-stretch-no-center.md) | **`ui-column` A2UI-canvas-root refinements** (Kim-directed): a column-LOCAL **`stretch`** boolean prop → `width: stretch` (fill-available cascade; NOT folded into shared `flexProps`, so row/grid/list are untouched) so a root column FILLS the artboard instead of shrink-collapsing under the canvas's `align-items:center` (a column carries `container-type:inline-size` ⇒ ~0 intrinsic inline size); AND **`align="center"` is PROHIBITED** on `ui-column` — the `align` enum narrows to `[stretch, start, end, baseline]` (stretch-first = default + invalid-snap target), the `[align='center']` CSS repoint is removed, and `Column.align` drops `center` in the catalog (Row keeps it). Centering a column shrink-wraps its children, defeating the ADR-0030 fill default. Effective in the DOM only WITH ADR-0076 (the factory's property-set path bypasses the component's own coercion) | accepted | Extends ADR-0016/0030 (neither reversed) · `column.{ts,css,md}` + `catalog.json` `Column.align` + `site/pages/a2ui-live.{ts,css}` · column jsdom/browser/descriptor/css tests · **depends on ADR-0076** |
| [0076](./0076-renderer-honors-catalog-declared-enums.md) | **The renderer honors catalog-declared `enum`s at widget resolution**: the widget resolver (`widget.ts`, LLD-C7) consults each prop's declared enum and SKIPS applying a LITERAL value the enum does not list — on both the static-literal and the bound-value `applyProp` paths. Root cause of a lingering `align="center"`: the wire validator checks a prop's type/shape but NOT enum MEMBERSHIP, AND the catalog factory applies props via the property SETTER, which (traced in `dom/props.ts:243`) stores verbatim — only the ATTRIBUTE path runs the enum codec — so an agent value the enum forbids reflected straight to the DOM. Makes ADR-0075's `Column.align` narrowing (and every catalog enum) LOAD-BEARING; pure cleanup (such values were already CSS-inert), SELECTIVE (`justify="center"` still applies), no self-correct churn, corpus untouched. Validator-level enforcement + self-correct deferred (broader blast radius) | accepted | Realizes the enforcement half of ADR-0075 · relates ADR-0016/0030 · `src/renderer/widget.ts` + `widget.test.ts` (2 new tests: stub drop + real-default-catalog e2e) |
| [0077](./0077-docs-site-genui-playground-preview-catalog-adr-index.md) | **The docs-site Generative-UI playground: `<component-preview>` + A2UI Catalog + ADR index**: a site-local PLAIN custom element (light DOM, NOT a `ui-*` control → no fleet obligation) previews EITHER a `ui-*` component (metadata ← `{name}.md`) OR an A2UI catalog item (← `catalog.json`) as a live-knobs playground; the specimen is AUTHORITATIVE for its own state (READ-BACK before rebuild, so direct canvas interaction survives knob edits). Feeds the **A2UI Catalog** page (every `defaultCatalog` component, live) + the **ADR index** page (build-time glob `exhaustive:true` for the hidden `.claude` dir; newest-first; full-text search + `<details>` expand; `statusShort` a `StatusKey` union via an ALL-CAPS SUPERSEDED override, so 0037→superseded but 0007/0033 stay accepted). Tightens `catalog.json` `Button.variant` → `enum:[solid,soft,ghost]` (realizes ADR-0076 for Button, chip-parity + closes an agent-emittable invalid value). Two independent reviews caught a LIVE mis-badged status + a canvas↔knob desync, both fixed with regression tests. Gates: check(+site) · jsdom 2407 · browser 582 (component-preview 14, incl. canvas→knob). **Am1 (2026-07-05, accepted):** component mode gains the a2ui-counterpart model — the `NO_SLOT_TEXT`(12)/`SLOT_TEXT_OK`(13) partition (the blind `textContent=` write destroyed self-built control DOM; pinned by the bite-tested partition gate `component-preview-slot-text.test.ts`) + `COMPONENT_SAMPLE_CHILDREN` (tooltip/menu/popover anchor triggers — bare construction failed) + `COMPONENT_INITIAL` (ui-icon `name:'check'` demo seed); MAINTENANCE RULE: every new control classifies into exactly one set or the build fails; runtime children-count heuristic REJECTED (ui-text's self-healing stamp misclassifies) | accepted *(Am1 accepted)* | Realizes ADR-0076 for Button · reuses ADR-0069 canvas + ADR-0055 seed shape · consumes ADR-0016 catalog via ADR-0023 mount · decomp [`site-preview-catalog-adr.decomp.md`](../decompositions/site-preview-catalog-adr.decomp.md) |
| [0078](./0078-ui-text-three-axis-variant-size-as.md) | **`ui-text` three-axis redesign** (Kim-ratified model via intent-extract): the one `variant` prop conflated a SEMANTIC marker with a VISUAL treatment (“h4 is a semantic marker, not a variant”) — split into **`variant`** (visual type ROLE: the 5 M3 roles `display/headline/title/body/label` + editorial extras `kicker/overline/quote/lead`, default `body`) × **`size`** (`sm/md/lg`, default `md`) × **`as`** (`none/h1…h6/p/span/blockquote`, default `none` — STAMPS a real semantic element around the light-DOM children via a scope-owned adoption effect + a self-healing childList observer; the internals `role=heading` path is DELETED). NEW fleet type family **`--md-sys-typescale-{role}-{large,medium,small}-{size,weight,line-height,tracking}`** (M3-verbatim 15-row core + 12 extension rows; `-size` × `--ui-scale` on the `*` ramp, constants on `:root`, unitless line-height, em tracking) RETIRES `--ui-type-*`. A2UI wire vocab UNCHANGED — `textFactory` fans `h1…h5/caption/body` out to (as, variant, size) triples (visual-parity table; catalog.json/corpus/derived-prompt zero churn). Migration = hard break, no alias, zero-survivor grep. Ratified by Kim (2026-07-04): both knobs → fully-M3 14px (element default 14px · factory `body`→md 14px). | accepted | **Supersedes ADR-0025 cl.1's prop schema + cl.3/3a + cl.4 (leaf class/content model/catalog type STAND; amends cl.2/5)** · amends ADR-0074 (md-sys namespace gains typescale, as it anticipated — `## Amendment` recorded) · follows ADR-0007 ramp law · relates ADR-0006/0022/0057/0071/0076 |
| [0079](./0079-component-gallery-kernel-dogfood-architecture.md) | **`<component-gallery>` — the G8 kernel-dogfooding gallery**: a site-local element (plain tag, no fleet obligation — ADR-0077 precedent) whose class extends the PUBLIC `UIElement`, composing one `<component-preview mode="component">` per fleet member (zero specimen-rendering duplication); members DERIVED from the `ALL_DESCRIPTORS` glob; the reactive loop rides the public seam end to end — `mount(watch(() => visible, ms => repeat(ms, key, m => node(card(m)))), grid, this)` with a ~15-line site-local `node()` element directive on the ADR-0023 trio (a child hole stringifies raw Elements); ONE `<theme-provider>` subtree per Kim's theming reframe — "tone" DROPPED: `scheme` (light/dark → `color-scheme`) + `scale` + `density` live, plus the RESERVED wired `theme` package seam (`theme="<name>"`; one `default` package in G8, package SWAPPING deferred next-tier/F2b); order FIXED alphabetical (reorder of directive-owned content is out of `moveBefore`'s contract — the #69 tail is the named trigger); nav joins UNGROUPED (site-toc gate) | accepted | goals.md §G8 DoD-1 (incl. its "tone"→scheme wording) · new `site/lib/component-gallery.*` + `site/lib/theme-provider.ts` + `site/pages/gallery.ts` + NAV (gated on ratification) · ADR-0077 backlink (on ratification) · LLD [`component-gallery.lld.md`](../llds/component-gallery.lld.md) · extends ADR-0077 · consumes ADR-0023 · relates ADR-0022 · decomp `g8-gallery-release-readiness` |
| [0080](./0080-per-control-exports-marginal-size-gate.md) | **Per-control `exports` + the leave-one-out marginal size gate** (realizes the ADR-0049 Amendment 1 booking): explicit `"./controls/{name}"` entries (incl. `radio-group`; compounds map to their family module — the honest tree-shake truth; targets = TS source today, emit-ready shape for the deferred §12 library-emit flip) + a three-way drift gate (exports ↔ folders ↔ family barrel, negative-control-proven); the marginal = **leave-one-out through the public entries** (`gz(ALL) − gz(ALL∖{c})` — attributes shared infra to NO single control), default cap 2048 B gz with a pinned cited-override table (measure actuals FIRST, then pin; text-field/calendar expected overrides); solo absolutes informational; `size` stays MANUAL per Kim's ADR-0040 §3 ruling | accepted | components package.json exports + `scripts/measure-size.mjs` + `barrels.test.ts` (gated on ratification) · plan §10 budgets · goals.md §G8 DoD-3 · realizes ADR-0049 Am1 · follows ADR-0040 · decomp `g8-gallery-release-readiness` |
| [0081](./0081-family-coherence-standing-gate.md) | **The family coherence gate — "audit clean" defined as a STANDING mechanical gate + a judged remainder**: new `controls/family-coherence.test.ts` asserts cross-family invariants over the live fleet (descriptor events ⊆ the plan §9 six; `size` enums ≡ sm/md/lg; `:where(ui-{name})` declares only `--ui-{name}-*` + consumes only own∪allowlisted tokens; descriptor presence/naming; `extends` ∈ the base ladder; barrel + component-styles registration; ADR-0019 two-way `open` pairs) — EACH invariant with a synthetic-violation negative control; the non-mechanizable remainder (keyboard-contract parity, internals-ARIA idiom, `formUserInvalid` legs, density/scale uniformity) = a read-only per-release `component-reviewer` audit; **clean = gate green AND zero MAJOR judged findings**, budgets/tree-shake evidenced by cited runs. Future legitimate deviations = ADR + gate edit in the same change (deliberate friction). **Am1 (2026-07-05, accepted):** the A2b INVERSE-`[size]` invariant (a `{name}.css` `[size` selector requires a declared `size` attribute; `stripCssComments` kills comment-only false positives) + the count reconciled — THREE groups (A/B/C), NINE invariants (A1·A2·A2b·A3·A4·B·C1·C2·C3); the gate header's stale "Four invariant groups" comment is the builder's code-tail fix. **Am2 (2026-07-05, accepted; Kim option A):** the `click` carve-out — in-vocabulary iff `extends: UIElement` AND `events` ≡ exactly `[click]` (pure activation; `pressActivation` → platform `host.click()`; rubric C1 precedent); button's A1 `it.fails` placeholder flips to a normal pass; 2 new negative controls (UIElement click+change mixed · UIFormElement click-only); plan §9 repaired in the same change | accepted *(Am1/Am2 accepted)* | new `controls/family-coherence.test.ts` (gated on ratification) · goals.md §G8 DoD-2 · references/component-authoring-best-practices.md · plan §9 (Am2 carve-out) · mechanizes ADR-0003/0019/0042 + plan §9 · decomp `g8-gallery-release-readiness` |
| [0082](./0082-app-shell-per-instance-isolation.md) | **The app-shell's per-instance style isolation** (M1, agent-app-surfaces): `ui-app-shell` isolates **per instance** via an `isolated` boolean → `attachShadow` at CONNECT (not the class-level `static shadow` seam, which is all-or-nothing) → fleet + shell CSS injected INTO the shadow as `<style>@import foundation-styles.css; @import component-styles.css</style>` (foundation ramp INCLUDED so the universal `*` derived ramp re-matches shadow-tree elements) → region children RELOCATED into the shadow (not slotted, so host CSS can't reach them) → **plus a `:host`-shaped grid variant injected into the shadow** (F1b) so relocated in-shadow regions keep their `grid-area` + `@container` reflow (a document `@scope` sheet can't reach them). `replaceSync(barrelText)` REJECTED (constructable sheets ignore `@import` → empty sheet). Re-pointed cross-engine risk: `@scope`-in-shadow (Safari 17.4) + `*`-ramp re-derivation across the boundary — NOT `adoptedStyleSheets` support; SPEC-R6 browser legs gate it, escalate-to-Kim-on-fail (light-only ships). `isolated` connect-time only (shadow can't detach); M1 = static composition. Supersedes plan §5's `static shadow` model FOR the app-shell's per-instance opt-in | accepted | `agent-app-surfaces.prd.md` PRD-D3/Fork-3 · `plan.md` §5, §12 · SPEC-R6 (`spec/agent-app-shell.spec.md`) · LLD-C4/C5 (`lld/agent-app-shell.lld.md`) — all edited in this change · relates ADR-0003 (`@scope` barrels) + ADR-0007/0038 (the `*` ramp) · decomp `m1-app-shell` |
| [0083](./0083-app-shell-region-role-decouple.md) | **Decouple the app-shell region's column from its ARIA landmark** (M1-polish): `ui-app-shell-region`'s `region` keeps driving the grid column + the DEFAULT landmark, but a new OPTIONAL `landmark` prop (reflected literal-union over the ARIA landmark set — banner/navigation/main/complementary/contentinfo/region/form/search) overrides the ARIA role independently via `internals` (`internals.role = landmark || REGION_ROLE[region]` — `||` not `??`, since the unset value is `''`; out-of-set coerces to default, never throws). Fixes the a2ui-live coupling — chat composer wants the LEFT column (`region="navigation"`) but the `complementary` landmark. Additive/back-compat; refines (does NOT reverse) the ratified generic region model — and only the generic `[region]` element makes the override possible (named sub-elements couldn't) | accepted | SPEC-R3 (+§4) (`spec/agent-app-shell.spec.md`) · LLD-C3 (`lld/agent-app-shell.lld.md`) — NORMATIVE (ratified 2026-07-06) · refines the generic region model (LLD §4/§7.1) · relates ADR-0082 · decomp `m1-app-shell` |
| [0084](./0084-app-shell-narrow-reflow-collapse.md) | **App-shell narrow-reflow: per-region `collapse` (hide/stack)** (M1-polish): the `@container (< 40rem)` reflow becomes a per-region opt-in — `collapse:'hide'/'stack'` default `hide` (today's `display:none`, back-compat); `collapse="stack"` keeps the region VISIBLE + stacked in the single-column flow so a primary-input side region stays reachable narrow (fixes a2ui-live's composer-vanish). `toggle` reserved (future stateful affordance, not built). Wide layout unchanged; the rule lands in BOTH grid copies (base `@scope` + isolated `:host` mirror — ADR-0082 keep-in-sync); new cross-engine browser leg + biting NC | accepted | SPEC-R5 (`spec/agent-app-shell.spec.md`) · LLD-C4/§7.5/C5 (`lld/agent-app-shell.lld.md`) — NORMATIVE (ratified 2026-07-06) · resolves LLD §7.5 reserved item · relates ADR-0082 (keep-in-sync) · decomp `m1-app-shell` |
| [0085](./0085-select-combo-box-labelling-seam.md) | **The `ui-select` / `ui-combo-box` labelling seam** (accepted 2026-07-06, Kim; ADR-0079 gallery dogfood): a `label` prop (bare-usage name source) on both, plus each overriding ADR-0051's `applyFieldLabelling` for the fielded path — the text-field dual structure. **The button-vs-editor ARIA asymmetry is the substance:** `ui-select`'s trigger is a `<button>` with **no distinct value AX property**, so a plain `aria-label` would ERASE the value from the accessible name — instead the trigger takes `aria-labelledby = "<label-source-id> <value-span-id>"` (CONCATENATION → "Scheme light", value live-recomputed on pick; fielded path MERGES the field label, never clobbers the value span). `ui-combo-box`'s editor is `role=combobox` **with** a distinct value, so it mirrors text-field exactly (`label` → editor `aria-label`). combo-box is IN-scope (its bare editor has NO accessible name today) — this closes the ADR-0051 "select/combo-box labelling wires" follow-up; only select is gallery-blocking. Downstream: the gallery's 4 axis selects gain `label="Scheme\|Scale\|Density\|Theme"` + the honest gap comment is removed | accepted | `controls/select/{select.md,select.ts}` + `controls/combo-box/{combo-box.md,combo-box.ts}` (the `labelSource` clause + `attributes[]`/`parts[]` rows + the wire — the BUILDER lands these with the code) · extends ADR-0051 (reciprocal `Extended by` back-link on 0051) · relates ADR-0014/0043 · surfaced by ADR-0079 · closes the ADR-0051 select/combo-box follow-up |
| [0086](./0086-radio-group-segmented-variant.md) | **`ui-radio-group[variant=segmented]`: the joined-button single-select presentation, both orientations, one animated highlight** (docs-preview de-doubling): reflected `variant:'default'\|'segmented'` + NEW `orientation:'horizontal'\|'vertical'` props on the GROUP restyle its `ui-radio` children as adjacent joined buttons in a **grid of equal 1fr cells** (no dot, one rounded `--ui-radius-base` track with collapsed inter-segment borders), with **ONE shared moving indicator** (the group's `::before`, sized `100%/count`, translated to the selected cell via `transform` off `.ts`-written `--ui-radio-group-index`/`-count`, animated over `--ui-motion-fast` + reduced-motion gate) FILLED with `--md-sys-color-primary-selected`/`-on-primary` — via a group-authored descendant compound selector (the `radio-group.css` token/`@scope` blocks; the `:32` comment is the forced-colors WHCM hook) — **not a new component**: 100% of single-select exclusivity / roving / selection-follows-focus / value / validity reused. Segments size off the **Control-height** ramp (Pattern class — `--ui-height/font-md`, label inset `h/2`, `line-height 1`); v1 = md register, `[scale]`-free (ADR-0038), per-`[size]` deferred. ADR-0057 non-color signifier = **fill-presence** (+ forced-colors `Highlight`/`HighlightText` inversion, transform survives); focus ring reused (ADR-0009). **The arrow-axis fork is DISSOLVED:** the roving axis follows `orientation` (horizontal → Left/Right, vertical → Up/Down — always APG-correct) and the drifted `radio-group.md` keyboard table (falsely claims Left/Right under vertical roving) is corrected per-orientation. Flags a fleet gap: `--ui-motion-fast` doctrine doesn't yet name `transform` (token-builder). **Superseded by: ADR-0095** — the T3 ruling (2026-07-07, the tag itself is the requirement) fired the reopen trigger ADR-0092 named for exactly this outcome; ADR-0095 has since shipped and been reviewed GO | superseded | `controls/radio/{radio-group.css,radio-group.ts,radio-group.md}` (NEW variant block + grid + `::before` indicator + `variant`/`orientation` props + resolved roving orientation + index/count state seam + descriptor rows/keyboard drift-fix — the BUILDER lands these with the code) · decomp `segmented` · relates ADR-0048/0058 (the `-selected` fill role) · ADR-0057 (non-color rule) · ADR-0009 (focus ring) · ADR-0036 (line-height 1) · ADR-0041 (dot ramp, unused here) · ADR-0038 (scale re-tabling) · ADR-0081 (attributes trip-wire) · consumes `geometry.md` Pattern class + `dimensions.css` motion constants + the `--value-pct` state-custom-prop seam |
| [0087](./0087-a2ui-whole-fleet-catalog-scope-policy.md) | **A2UI default catalog covers the whole shipped `ui-*` fleet (gate-encoded exclusion allowlist)** (proposed — robust-App-usage intake, Kim's "keep EVERYTHING in the catalog"): flips the scope policy from *"track the family"* to **every shipped control has a catalog type OR sits on an explicit, gate-encoded exclusion allowlist with a recorded reason**. Closes the live SPEC-N2 violation (icon/menu/popover/tooltip shipped-but-uncatalogued-and-undispositioned), **closes the ADR-0053 §5.2 deferred list** (RadioGroup/Slider/Calendar/ComboBox), and **supersedes ADR-0016's ui-list/ui-grid *non-catalog* exclusion contingent on Fork A** (Row/Column decision untouched). Replaces `index.test.ts`'s hand-frozen 19-name key-list with a **fleet-derived** gate (descriptor-glob → PascalCase minus the allowlist — the `site-coverage.test.ts` doctrine) that BITES on a future uncatalogued control. Default position **INCLUDE**; four per-control shape questions surfaced as **ratification forks** (A list/grid · B radio-group+radio adjacency · C slider vs slider-multi dual-value seam · D overlay slot/item models) — not decided here. Seed-and-drain sequencing: the gate lands FIRST with the allowlist seeded full, each wave drains its types | proposed | `a2ui-catalog.spec.md` SPEC-N2 / SPEC-R3 AC1 / §5.2 · `catalog/default/index.test.ts` (frozen list → fleet-derived gate — the BUILDER lands per wave) · supersedes ADR-0016 (list/grid non-catalog, fork-contingent) · closes ADR-0053 §5.2 deferred note · relates ADR-0019/0043/0048/0086 · decomp `a2ui-whole-fleet-catalog` |
| [0088](./0088-a2ui-live-conversational-channel.md) | **The live-agent conversational channel: a natural-language `note` beside (never inside) the A2UI stream, a grounded per-turn decision-trace, and `wantResponse`-routed click→turn** (proposed — two-way-conversation intake): closes the "no NL channel anywhere in the turn model" gap (`Turn.content` = A2UI JSONL only; the chat's "Agent:" line is a client-side `summarize()` of message KINDS, not model prose). THREE coupled parts on ONE new wire mechanism: (1) a reserved **meta-line** emitted FIRST on the existing `AsyncIterable<string>` stream (`{"a2uiMeta":{"note":…}}`, no `version` ⇒ provably not an `A2uiServerMessage` ⇒ filtered before `host.ingest`, never validated, never in the corpus — `AgentTransport.turn` type UNCHANGED, wire purity by construction); (2) a light browser-held per-turn **`TurnTrace`** (exemplar ids · rounds · healed · failure codes · model) carried back on the meta-line + re-injected as a digest so a "why X vs Y" **normal intent turn** is grounded, not confabulated; (3) **`wantResponse`-routed** click→turn — `action.wantResponse===false` ⇒ silent apply, absent/`true` ⇒ today's full turn (OPT-OUT default = back-compat: the seed `canvas-button.ts:27` sets no `wantResponse`, so an opt-in default would break the shipped demo). Extends ADR-0070/0072/0011; **1 fork for Kim** (routing default: back-compat opt-out [recommended] vs RPC-aligned opt-in, costs a re-seed) **+ 2 build-time re-verify points** (trace-richness sufficiency · wire-shape upgrade timing). OUT: trace-browsing UI · quality rubric · new LLM capability | proposed | on ratification+build: `a2ui-live-agent.spec.md` SPEC-R8/R5/N4/§5 · `a2ui-live-agent.lld.md` §4/§5/C3/C5/C9 · extends ADR-0070/0072/0011 · relates ADR-0071 (prompt grammar half) |
| [0089](./0089-a2ui-live-clarify-and-catalog-boundary-negotiation.md) | **Clarify-before-acting + catalog-boundary negotiation: teaching the live agent to ASK (a note-only turn) instead of guess, and to be honest about the wall the catalog is** (proposed — the "next level of conversational UX" intake after 0088): adds the agent's missing THIRD move (ask, not just guess-or-halt). TWO capabilities, ONE mechanism (a note-only turn on ADR-0088's channel — NO new wire surface): (1) **clarify-before-acting** — a note-only qualifying question when a turn is under-determined ("make it better"/"add more stuff"), while an actionable turn ("build me a form") still just builds; (2) **catalog-boundary awareness** — the model already knows its wall (the derived inventory, `system-prompt.ts:71-72`), so at the boundary it NAMES the limit + asks permission to **approximate WITHIN the catalog** (Grid/Row/Text ≈ a table) + discloses the approximation. Both are GRAMMAR-half prompt behaviors (ADR-0071; drift gate untouched). "Improvise beyond the catalog" = approximate-within-catalog ONLY: rendering an uncatalogued type is a hard NO — mechanically self-defeating (`conformance.ts:18-19` ⇒ `ProduceHalt`, never streams) AND a security decision, not UX. Extends ADR-0088/0071; **hard dependency on ADR-0088** (needs the note channel — the grammar currently forbids prose, `system-prompt.ts:15`). **1 fork for Kim** (demo the live-only behaviors in the recorded backbone [re-seed transcript] vs live-only [recommended]) + 2 build-time re-verify (vagueness threshold wording · whether a `noteKind` chat style is needed). OUT: runtime allowlist widening · runtime catalog extension · multi-turn dialogs · new LLM capability | proposed | on ratification+build: `a2ui-live-agent.spec.md` SPEC-R6/R8 · `a2ui-live-agent.lld.md` LLD-C4/C9 · `a2ui-runtime`/ADR-0071 GRAMMAR block · extends ADR-0088/0071 |
| [0090](./0090-a2ui-gen-ui-mode-axis.md) | **The Gen-UI mode axis: a per-turn `mode` that SCALES the ADR-0089 ASK grammar (directive `specific` ↔ exploratory `blue-sky`), a mode-invariant honesty floor, and Structural named as the already-shipped recorded transport** (proposed — config/mode-axis intake after 0089): 0089 shipped the clarify/negotiate ASK grammar UNCONDITIONAL (`buildSystemPrompt(catalog, exemplars)` — no mode; the GRAMMAR is one `const` emitted every turn), but Kim's THREE use cases pull it apart. FOUR coupled clauses, ONE decision: (1) a per-turn **`GenUiMode`** threaded proxy-body → `ProduceOptions.mode` → `buildSystemPrompt(…, mode)` that SCALES the clarify/negotiate block — `specific` dials it DOWN (directive mapping, decline-and-redirect at the wall), `blue-sky` UP (lower clarify threshold, several rounds, elaborate approximations, reasoning-in-note), **default = today's grammar byte-for-byte** (zero regression, the ADR-0088 opt-out discipline); (2) the **honesty floor is mode-INVARIANT** — never invent a type/prop, never fake (the SPEC-R9 allowlist) holds in EVERY mode, lifted into a shared spine; (3) **Structural is NOT on the axis** — it is the SHIPPED `createRecordedTransport` rendering pre-generated, commit-time-validated JSONL with no live model (`recorded-transport.ts:25-36`), a doc/example clarification, ZERO new mechanism; (4) the config SURFACE = `ProduceOptions.mode` (core, the `model` path) + a dev-only switcher selector (LLD-C12 precedent) + a construction-site deployment default — **NOT the catalog** (the component authority stays pure). Extends ADR-0089/0088/0071 (default preserves 0089 ⇒ extension, not supersession — the log's own test); **hard dependency on ADR-0089/0088**. **1 fork for Kim** (Structural = a NAMED 3-value enum member [uniform, Kim's mental model] vs documented separately [recommended — a transport choice is orthogonal to a grammar disposition]) + build-time re-verify (mode names/wording · demo selector exposure) + deferred `noteKind` reasoning-trace refinement. OUT: allowlist widening in any mode · multi-turn planning · new wire surface · per-mode quality rubric | proposed | on ratification+build: `a2ui-live-agent.spec.md` SPEC-R6/R8/R12/R2 · `a2ui-live-agent.lld.md` LLD-C2/C3/C4/C6/C7/C12 · `a2ui-runtime`/ADR-0071 GRAMMAR block · extends ADR-0089/0088/0071 |
| [0091](./0091-a2ui-gen-ui-mini-skill-registry.md) | **The Gen-UI mini-skill registry: a modular, on-demand-selected idiom-instruction layer composed into the prompt as one bounded block, degrading to empty on no match, promotable to the corpus** (proposed — Kim's "manage mini-skills without context bloat" intake; ADR-0090's deferred-corpus follow-up arriving). Grounds on the PROVEN constraint that a live model mid-generation has NO Skill-tool path (`produce.ts:177-182` — only `system` + retrieved exemplars), so a "mini-skill" = a prompt-injectable idiom-instruction unit SELECTED + ASSEMBLED per turn, never a Skill invocation. FIVE coupled clauses, ONE decision: (1) a **mini-skill** = a named, self-contained idiom-instruction module (`{id, triggers, body}`, ADR-0090's `need → parts → catalog mapping → wall` shape) — instruction-shaped modules DON'T fit the exemplar `CorpusRecord` (`a2uiOutput` required, `record.ts:119-121`), which is WHY they need their own home; (2) selection = a static committed TS **registry** + a cheap intent-match REUSING `retrieve.ts`'s tokenizer/cosine (NOT the schema/admission pipeline), degrading to `[]` on zero vocabulary overlap (mirrors `retrieve.ts:99`/`fewShot` `''` `system-prompt.ts:93`), run ONCE per turn beside `retrieve()` at `produce.ts:152` (the loop fixes `system` after `:153` — NOT per round); (3) composition = ONE new `buildSystemPrompt` segment, a `fewShot` twin, under a **per-module ~200-token budget × per-turn cap 3** ⇒ the prompt grows by at most `cap × budget` REGARDLESS of registry size (the anti-bloat guarantee; grounded in the measured ~964-token `GRAMMAR`); (4) hand-curation + the `doc-reviewer` seat at v1 scale (~10-15 idioms — the catalog's natural idiom count, converging with 0090's own estimate), no admission pipeline; one gate: embedded worked JSONL passes `validateA2ui`; (5) **upgrade trigger** — promote the registry into corpus records (a new `Facet`, via `retrieve()`/`admit.ts`) WHEN the registry exceeds ~15-20 modules OR a module needs per-turn quality-gating, behind the unchanged `selectMiniSkills` interface. **No genuine values-level fork** (stated as settled; registry-vs-corpus is the §5 upgrade axis, not a fork) + build-time re-verify (budget/cap/floor · demo matched-idiom readout · seed registry contents). OUT: allowlist widening · the corpus schema extension itself · runtime selection-quality gate · new LLM capability | proposed | on ratification+build: `a2ui-live-agent.spec.md` SPEC-R6/R7 · `a2ui-live-agent.lld.md` LLD-C3/C4/C8 + NEW `SPEC-R#`/`LLD-C#` · extends ADR-0090 (reciprocal back-link on accept) · builds on the shipped `retrieve()`/`fewShot` seam (ADR-0071, corpus LLD-C9) |
| [0092](./0092-segmented-control-pattern-identity.md) | **"Segmented control" becomes a first-class NAMED PATTERN; the tag stays `ui-radio-group[variant=segmented]` — ADR-0086 STANDS** (proposed — the design-only answer to Kim's 2026-07-07 "ui-segmented-control: should be its own component" intake, revisiting his own 2026-07-06 ratification). The steelman re-verifies: 0086's behavioral-identity claim holds against the shipped code (exclusivity/roving/commit/value/reset all live once in `radio-group.ts`; the segmented delta = one CSS section + 2 props + the 2-line `#writeIndexCount` seam). What's NEW since ratification is NAMING evidence only — the fleet has no findable artifact named "segmented control" (the gallery derives 1:1 from descriptor tags, `component-gallery.ts:44-60`; the sole doc home is a `variant` enum row), and the owner himself read the shipped knobs as a control the fleet doesn't name. FOUR clauses: (1) `radio-group.md` prose repositioning (title/summary + a *Segmented control* section w/ specimens — the name becomes searchable via the existing toc/nav gates); (2) ONE spelling, no alias, no wire change (`catalog.json` `RadioGroup.variant` stands; teaching the live model the name = corpus/prompt content, not schema); (3) the PROMOTION path is fully COSTED + RECORDED in §Alternatives-1 (subclass cheap; CSS re-key + private-seam `--ui-radio-group-*` rename + `ui-radio`-children vs `ui-segment` cascade + descriptor/gallery/exports/ADR-0080-marginal/browser-suite/a2ui-type/migration surface = ~1.5–2 day multi-seat wave, hard cutover per ADR-0074/0078, buying the tag name only); (4) named REOPEN TRIGGERS — T1 semantics outside the radio model (multi-select/icon-only segments, form-machinery leak biting), T2 measured discoverability failure, T3 Kim rules the tag IS the requirement. **1 fork for Kim** (ratify this pattern-name path [~0.5 day docs slice] vs T3 → discard this ADR and author the SUPERSESSION from the recorded §Alt-1 design [~1.5–2 days]). **T3 FIRED 2026-07-07 — Kim ruled the tag itself is the requirement.** This ADR is discarded unratified per its own §Consequences; superseded by ADR-0095, built from the §Alternatives-1 design this ADR recorded for exactly this outcome | superseded | on ratification+build: `controls/radio/radio-group.md` prose — MOOT, must not be built · ADR-0086 `Superseded by ADR-0095` back-link (pending 0095's ratification) · extends ADR-0086 (as authored; superseded before that link took effect) · relates ADR-0077/0079/0080/0074/0078 |
| [0095](./0095-ui-segmented-control-standalone-component.md) | **`ui-segmented-control` becomes a standalone first-class component (supersedes the ADR-0086 variant architecture)** (proposed — authored from ADR-0092 §Alternatives-1, per that record's own instruction for a fired T3 trigger: Kim ruled 2026-07-07 that the tag itself is the requirement). Builds the fully-costed promotion design ADR-0092 recorded: NEW `controls/segmented-control/*` (class + segment + CSS + descriptors), `ui-radio-group` variant retirement (`orientation` + its per-orientation keyboard table STAND — unrelated to the retired `variant`), `catalog.json` `RadioGroup.variant` retired + NEW `SegmentedControl` type + factory/conformance/prompt-drift tests, `component-preview.ts` knob rewire, the two segmented browser suites re-keyed, exports + the ADR-0080 marginal pin, `dimensions.css:76` doctrine re-point. Hard cutover, no alias — per ADR-0074/0078 precedent. ~1.5–2 day multi-seat wave (component-builder + a2ui-builder + site), gated on this ADR's ratification. Docs-only this pass (no source touched): ADR-0092 disposed to `superseded`, ADR-0086's reciprocal back-link added (Status stays `accepted` until the ratifier flips it), this index row lands. Decomp coverage-clean: `segmented-control-supersession.decomp.json` | proposed | on ratification: flip ADR-0086 `Status → superseded`. On ratification+build: NEW `controls/segmented-control/*` · `controls/radio/{radio-group.ts,radio-group.css,radio-group.md}` (variant retirement) · `catalog.json`/`factories.ts` (NEW `SegmentedControl`, retire `RadioGroup.variant`) · `site/lib/component-preview.ts` · both segmented browser suites · package exports + ADR-0080 marginal · `dimensions.css:76` · supersedes ADR-0086 · relates ADR-0092 (the §Alt-1 source design) |
| [0093](./0093-calendar-range-selection.md) | **Date-range selection: a `mode="range"` FLAG on `ui-calendar` — swap-complete picks, single grid** (proposed — Kim's "from → to calendar, days inside selection filled" intake; realizes ADR-0048's reserved range wave; REWRITTEN IN PLACE 2026-07-07 after Kim resolved all 3 forks — lawful: never-ratified `proposed`, the append-only rule attaches at acceptance). Kim's F1 ruling (mode flag, a product call) overrules the draft's standalone `ui-calendar-range`+`_base`-extraction design — preserved under §Alternatives so the record shows why the design changed. SEVEN clauses: (1) `mode` enum (`single`|`range`, default `single`, reflect) + `valueStart`/`valueEnd` (`value-start`/`value-end`, the `valueLo`/`valueHi` shape) under the ONE-LIVE-VALUE-SURFACE rule — off-mode value props are INERT (held+reflected, contribute nothing) and NEVER written by the control (mode switch = reconfiguration, not migration; attribute-order-safe; the `ui-slider-multi` inert-inherited-`value` precedent, named normative); (2) range shape: FormData 2 entries under ONE `name` when complete, `null` when half-open/inverted · inverted reachable ONLY programmatically and NEVER auto-swapped (props stay author-faithful) · `select` per pick, `change` only on completion (the range-mode two-way `value` bind = inert-but-harmless); (3) single-grid two-pick, second pick commits `[min,max]` — earlier pick SWAP-COMPLETES (Kim F2; replaces the draft's MD3 restart), preview shows the NORMALIZED band both directions so commit never differs from preview, same-day = 1-day range, Escape stays the overlay's (ADR-0045), aria-live announces completion with both dates in order (swap audible); (4) `data-range-start/-end/-in-range` (single mode stamps NONE) — endpoints reuse selected-fill circles, interior = square wash via the realized `--ui-calendar-range-fill/-ink` (aliases `primary-surface`/`-on-surface`, AA both schemes); WHCM band = Highlight/HighlightText, today ring stays ButtonText-inset (4 distinct states); (5) in-range predicate = lexicographic ISO compare, ZERO Date construction (the proven `isOutOfRange` mechanism — swap+preview normalize via string compare too); (6) NO extraction — machinery stays `#`-private in `calendar.ts` (one consumer; ADR-0048's `roving-grid` trigger still unfired), range = guarded mode branches, `mode="single"` runs the existing paths unchanged; (7) additive at the default (no-mode markup byte-identical, existing suites UNMODIFIED = the gate; no barrel/exports change — the calendar ADR-0080 marginal re-bases; single-date consumers now carry the range code, an accepted cost); A2UI = a `Calendar` ROW EDIT follow-up under ADR-0087 (optional `mode` + one-way pair binds), not a new row. **Forks RESOLVED by Kim 2026-07-07** (F1 mode flag · F2 swap · F3 single pane). MEDIUM: ~5 gated slices, slice 1 = serial in-place surgery on a shipped control = the risk center. Decomp coverage-clean (v2, replan diff in `_meta`): `calendar-range.decomp-v2.json` | proposed | on ratification+build: `controls/calendar/calendar.ts` (mode flag + range machinery IN PLACE, no new files) · `calendar.css` (line-71 reservation realized + band/WHCM) · `calendar.md` (mode/pair rows, per-mode liveness) · site calendar page · ADR-0080 calendar marginal re-base · extends ADR-0048 (reciprocal on accept) · relates ADR-0042/0045/0057/0080/0087 |
| [0096](./0096-column-row-reflow-prop-gate.md) | **The `reflow` prop gate: catalog-reachable container-reflow control for `ui-row`/`ui-column`; the column default flips to `locked`** (proposed — Kim's reproduced live Gen-UI card-game layout break): ADR-0016 cl.4's unconditional switcher (`column.css:130` wide ≥30rem → row · `row.css:124` narrow <24rem → column) assumed the escape hatch is "plain CSS on the element" — false for the catalog's #1 consumer class: a live model composes validated PROP-ONLY node trees (`catalog.json`→`factories.ts`→`renderer.ts`) and has NO CSS verb, so an agent-emitted `Column` on any ≥480px canvas silently and unpreventably rendered as a row (8/11 shipped corpus exemplars affected). FIVE clauses: (1) NEW reflected enum `reflow:'auto'\|'locked'` on `ui-row`+`ui-column` (element-local, NOT `flexProps` — ADR-0075 precedent; list/grid have no switcher, untouched); (2) per-tag defaults — row stays `auto` (the narrow→stack leg is protective, fires only cramped), **column flips to `locked`** (its wide→row leg fires in the COMMON case and contradicts cl.2's own direction-is-the-tag identity); enum ordered default-first per tag (values[0] = default + snap, ADR-0030 per-consumer-default doctrine); (3) mechanism = one guarded selector per sheet (`:scope[reflow='auto']` opt-in on column — unreflected default can never match, ADR-0005; `:scope:not([reflow='locked'])` on row); (4) catalog rows + `accessorFactory` 1:1 pass-through + ADR-0071 derived-inventory advertisement (drift-gated) + ADR-0076 enum honoring; (5) idiom teaching OPTIONAL because the default is safe (one recommended ADR-0091 mini-skill for pin/spread; corpus records UNTOUCHED — the new default renders them as authored). Breaking-default blast radius VERIFIED: sole site `ui-column` construction (`column-doc.ts:20`) is repaired-not-broken (it renders flipped today); `layout-permutations` live demo = ui-row, unchanged (prose lines 97/104 corrected). Rejected: opt-in-lock-only (probabilistic fix for a deterministic defect), both-locked (kills row's load-bearing graceful degradation), host-side patch (fixes one embedder, not the contract), `direction` prop (the mode grammar cl.2 rejected), boolean prop (can't express the per-tag default asymmetry) | proposed | on ratification+build: `controls/{column,row}/{*.ts,*.css,*.md}` · `catalog.json` Row/Column rows + a2ui-catalog SPEC §5.2 · `site/pages/layout-permutations.ts` prose · named test re-keys. This change: **extends ADR-0016** (reciprocal `Extended by` landed) · relates ADR-0030/0075/0076/0071/0091/0005 · decomp [`column-row-reflow-lock.decomp.json`](../decompositions/column-row-reflow-lock.decomp.json) |
| [0097](./0097-a2ui-feed-embedded-asks.md) | **Feed-embedded interactive A2UI asks: the ADR-0089 ASK becomes a small inline surface in the chat message — an `ask` routing field on the ADR-0088 meta envelope, a per-message frozen-history lifecycle, and a gate-encoded feed sub-catalog** (proposed — Kim's feed-embedded-asks intake after 0088/0089/0090): the ASK is prose the user answers by typing, though closed-choice/typed-value answers already have shipped controls AND a shipped per-surface answer path (`sendDataModel` ⇒ `action.dataModel`, `action.ts:107` — verified, no wire extension needed). FIVE coupled clauses: (1) **wire** — the meta envelope gains additive `ask:{surfaceId}` (versionless discriminator + unknown-field-drop preserved, `AgentTransport.turn` byte-identical); the ask's UI is ORDINARY validated A2UI on the SAME stream targeting a fresh surfaceId; `produce()` peels/re-composes + an ask-integrity check (declared-but-absent or colliding surface ⇒ drop `ask`, note stands); `RecordedTurn.ask` parity, transcript NOT seeded; (2) **lifecycle** — one page-level `createRenderer()` host per ask mounted in the message bubble (renderer `mount()` is host-global — zero package change), `pending → frozen(answered\|bypassed)` via bubble `inert`+`data-state`, freeze-on-turn-DISPATCH (⇒ ≤1 pending ask ever; silent `wantResponse:false` applies freeze nothing), frozen-target lines dropped, Reset disposes all; (3) **feed sub-catalog** — `FEED_SURFACE_TYPES` (23) ⊕ `FEED_EXCLUDED` (11, reasons recorded: Modal/Tabs/overlays/List/Grid/Switch) = a gate-encoded TOTAL PARTITION of the catalog's 34 types (the ADR-0087 lesson: a new catalog type turns CI red until dispositioned; composite closure + negative control), enforced at prompt-build (derived list), producer (post-validator `FEED_SCOPE` self-correct via the `TurnTrace.failureCodes: string[]` seam — no `ErrorCode` change), and page (fail-closed drop-to-note); SPEC-R9 untouched — a stricter POLICY view, never a second catalog; (4) **prompt** — mechanics in the invariant spine (fresh `ask-<n>` id, `sendDataModel:true`, ONE commit Button, ask-only payload, the note-standalone rule) + five archetypes (single-choice recommended-first · multi-select · typed-value · negotiation option cards · confirm/cancel) in the MODE-SCALED block; NOT mini-skills (selection matches USER-intent vocabulary; asks trigger on the AGENT's situation); default byte-identity to the literal `GRAMMAR` held by construction, SPEC-R6 AC3's historical wording re-based; (5) **mode + degrade** — `specific` redirects-as-choice, `blue-sky` prefers structured whenever enumerable; EVERY failure (naive consumer, broken declaration, out-of-scope payload) lands on the prose note — no protocol break. **No new Kim fork** (the partition table is ratification-reviewable; membership edits are one-line gate-visible); the ONE adjacent open fork stays ADR-0089's backbone-seed. Verified: Calendar's catalog row has NO `mode` — date-RANGE asks wait on ADR-0093's own row-edit follow-up. Decomp coverage-clean (PLAN): [`feed-embedded-asks.decomp.json`](../decompositions/feed-embedded-asks.decomp.json) | accepted | on ratification+build: `a2ui-live-agent.spec.md` NEW SPEC-R14/R15 + SPEC-R5/R6/R8/§2/§5 · `a2ui-live-agent.lld.md` NEW LLD-C14 + LLD-C2/C3/C4/C9/§4/§6 · extends ADR-0088/0089/0090 (reciprocal back-links on accept) · relates ADR-0087 (partition-gate pattern) · ADR-0011/0054 (action shape/submit gate, consumed unchanged) · ADR-0093 (Calendar range row edit) |
| [0094](./0094-slider-thumb-page-surface-ring.md) | **The slider thumb ring: the thumb-vs-page-surface third SC 1.4.11 contrast dimension** (proposed — re-filed from ADR-0059's `## Amendment` per the doc-reviewer's three-way-test ruling [no booked branch; decision-shaped: own alternatives + new mechanism + new forward contract = Extension]; the Kim-filed "thumb blends in" fix, shipped + reviewed GO 2026-07-07 — the build is unchanged by this re-filing). ADR-0059 verified the slider thumb against TWO backdrops (fill 4.79/3.67 · rail 4.69/3.74) — but the thumb is TALLER than the 3px rail, so its dominant border is the PAGE SURFACE, never measured (the switch has no such dimension: its track fills the control height). Measured (the `tokens.test.ts` OKLCH→WCAG path): the dark-mode thumb (`neutral-surface-brightest` = neutral-800) vs the dark surface ladder = **1.09–1.46:1** — an SC 1.4.11 fail. Decision: the fill stays UNTOUCHED (all ADR-0059 floors re-pinned by a regression guard); a SECOND, independent layer — slider-scoped `--ui-slider-thumb-ring: var(--md-sys-color-neutral-on-surface)` (fleet page-ink: AA vs every plane, scheme-adaptive) as a **2px `::after` border**, `border-box`, so the outer diameter keeps the ADR-0041 cl.3 `box − 4px` law (the ring eats fill, never grows the box); `[disabled]` mutes to `-on-surface-variant` (0059's exempt stance). The flat `primary-on-primary` swap REJECTED on measurement (**2.76:1** vs the dark rail — the very stop 0059 brightened; negative-control pinned; no single flat fill clears both floors — luminance intervals ≤~0.077 vs ≥~0.21 are DISJOINT, forcing a second layer, not a repoint). Forward contract: the thumb's SC 1.4.11 matrix is THREE-dimensional (fill · rail · page-surface ladder, both schemes) — any repoint of thumb/ring/rail/surface-ladder must clear all three; the slider browser legs (both engines) are the standing gate. Costs: a two-layer paint (the old two-backdrop matrix no longer judges either token alone) + the ring eats interior fill (sm: 10px thumb → 6px disc) | proposed | controls/slider/slider.css (`--ui-slider-thumb-ring` + the 2px `::after` border + header note) · slider.browser.test.ts (ring legs + negative control) · controls/slider/slider.md (ring property + two-layer Anatomy, this change) · extends ADR-0059 (reciprocal `Extended by` back-link landed, this change) · relates ADR-0041/0009 |
| [0098](./0098-validator-enum-membership-enforcement.md) | **Validator-level catalog enum-membership enforcement: a non-member literal fails `validateA2ui` and enters the self-correct loop** (proposed — the fleet-wide gap confirmed twice by independent reviewers 2026-07-07: `matchesSchemaType` (`conformance.ts:55-82`) reads only the JSON-Schema `type`, never `enum` — so all **36 enum props across 16 of the catalog's 34 components** accept ANY string at validation; the bad value only degrades at render via ADR-0076's resolver drop + the `enumType` `values[0]` snap). **Extends ADR-0076** — realizes exactly the promotion its Alternatives reserved ("once the corpus is known-clean"), whose precondition is now MEASURED true: a programmatic sweep of the corpus shard (11 records) + 11 example seeds + the recorded transcript (2 turns) = 226 component nodes / 123 enum literals → **0 violations**; the 14 validator-path test suites audited (22 enum-literal fixtures, all members) — enforcement rejects NOTHING that ships today. Stakes raised by `Calendar.mode` (ADR-0093): `mode` forks the form value contract (`value` vs `valueStart`/`valueEnd`), so a silently-snapped `"weekly"` renders a form surface the model never composed, zero feedback. Decision: ONE clause in `matchesSchemaType` — a schema carrying `enum` rejects any value not **strictly equal** (`===`, case-sensitive, no coercion; JSON Schema §6.1.2, mirroring `enumType.from`'s exact `includes`); rides the EXISTING `{code:'CATALOG', path:'<id>.<prop>'}` shape (no `ErrorCode`/`Failure`/`TurnTrace` change, ADR-0031 untouched); static literals only — `{path}`/`{call}` bindings stay ADR-0076's render-gate charter (measured: 0/36 shipped enum props are `bindable`, so the shipped enum surface is 100% statically gated); one implementation, three callers (N3): live loop self-corrects same-turn, corpus admission keeps the shard clean BY CONSTRUCTION, renderer withholds instead of silently snapping. Honest costs: severity promotion (one bad prop now withholds the WHOLE payload until corrected — a self-correct round, or `ProduceHalt` at the bound, where the user previously saw a slightly-wrong surface — accepted: fail-loud > silently-forked form contract) · feedback is code+path only and the ADR-0071 derived inventory lists prop NAMES not members (`system-prompt.ts:217`) — named follow-up: enum members into the derived inventory + drift gate, deliberately NOT this decision. Rejected: render-only status quo · a distinct `ENUM` code · heal-side auto-snap (ADR-0061: heal = form repair) · `Failure.detail` members list · a full JSON-Schema dep (SPEC-N4). No decomp — a one-clause validator addition + tests + two doc clauses | proposed | on ratification+build: `a2ui-catalog.spec.md` SPEC-R7 (enum membership named in "typed-correct") · `a2ui-catalog.lld.md` LLD-C6 §6 sketch + failure-table row · `src/catalog/conformance.ts` + `conformance.test.ts` · `produce-loop.test.ts` (one self-correct leg) · `widget.test.ts` comment repair (its non-member fixtures now assert the SECOND line of defense) · extends ADR-0076 (reciprocal `Extended by` back-link at ACCEPT time) · relates ADR-0093/0031/0061/0071 |
| [0099](./0099-updatedatamodel-slash-root-alias.md) | **`updateDataModel` `path:"/"` is the protocol's root alias — whole-model replace at every apply-site** (proposed — the reproduced live-agent "empty hand" defect, host-verified upstream 2026-07-07: BOTH v0.9 + v1.0 §updateDataModel define "If `path` is omitted (or is `/`), the entire data model … is replaced", and the omitted-path default is literally `/` — our RFC-strict reading (`renderer.ts:273` whole-model = `undefined \|\| ""` only, so `"/"` falls to `setPointer` → nests everything under a spurious `{"":…}` key, every binding silently `undefined` per SPEC-R4 AC2) makes US the non-conformant party; pinned by the `renderer.test.ts:963-1030` pair currently asserting the WRONG behavior). Decision: alias `"/"` alongside `undefined`/`""` at each apply-site's existing whole-model branch — renderer host + the corpus's two fold mirrors (`canonical.ts:104`, `admit.ts:286`, parity by construction) — NEVER inside `setPointer`/`setAtPointer` (protocol-layer message semantics, not pointer arithmetic; RFC-6901 stays pure, deeper `""` keys like `"/a/"` keep resolving); validator unchanged (`"/"` already passes `isValidPointer`, ADR-0002 cl.3 stands). Scope = `updateDataModel` ONLY — binding `{path:"/"}` is unverified upstream, booked as the open question. Honest costs: a top-level `""`-named key becomes unaddressable (upstream's trade; sweep = zero in-repo reliance) · the regression pair flips + its "NOT a renderer defect" narration rewrites · the just-added GRAMMAR "never path:'/'" line (`system-prompt.ts:124-126`) must be corrected (keep omit-path as the idiom, `/` = legal root alias). Rejected: alias inside setPointer · keep-strict + prompt fence (covers only our own producer) · fail-loud POINTER error (still non-conformant, breaches ADR-0002) · generalizing to binding paths (unverified). No decomp — a one-token condition at three sites + test/prompt/doc repairs | proposed | runtime `SPEC-R5`+AC2 · renderer LLD §5 (alias + layering note) · corpus-store LLD §canonicalize step 1 — edited in-change; on ratification+build: `renderer.ts:273` · `corpus/canonical.ts:104` · `corpus/admit.ts:286` · `tools/agent/system-prompt.ts:124-126` GRAMMAR · `renderer.test.ts:948-1030` expectation flip · relates ADR-0002 |
| [0100](./0100-query-container-boundary-establishment.md) | **Query-container establishment moves off the layout primitives to externally-sized boundaries; card `min-inline-size: 0` retained** (accepted — the live visual-audit ruling, computed-style proofs Chromium+WebKit identical): `container.css:127-129` (ADR-0016 cl.4) made all four layout primitives query containers, but `container-type: inline-size` = inline-axis SIZE CONTAINMENT — intrinsic sizes compute as if EMPTY, so a primitive in any content-sized position collapses (Row-in-Row = **0px**, the document-row-toolbar seed; `ui-column[align=start]` children too — ADR-0030's stretch default merely MASKS it) and, NON-LOCALLY, a contained primitive contributes 0 intrinsic size through every content-sized ancestor (the pattern-dashboard-tiles cards crush to ~38px = chrome-only, via their INNER contained column). No selector can fix it in place — safety is a whole-ancestor-chain property (measured: position-conditional containment leaves tiles at 36px). THREE clauses: (1) DELETE the blanket rule — a layout primitive is never a size query container by default; the 0096-gated `@container` rules stand unchanged and resolve against ancestor boundaries; grid auto-fit is track sizing, unaffected; (2) THE LAW — `container-type: inline-size` only on externally-sized boxes (definite/stretched/tracked, never content-derived); at build the A2UI mount surfaces gain it (`.canvas-surface`, `.seed-surface`, patterns + `host.mount()` sweep — NONE establishes one today, grep-verified, so ADR-0016 A1's canvas claim was aspirational) and the embedding contract gains the normative line (no boundary ⇒ rules never match ⇒ tag identity renders — graceful); (3) audited "Defect B" RE-ATTRIBUTED — card `min-inline-size: 0` retained: measured NOT binding in the crush (min-content floor renders the identical 36px under containment; identical 176-180px in wrap rows post-fix) and PROTECTIVE in grids (floor blows a 200px track to 484px); the Container class keeps the shrink permission (geometry.md's floor law stays control-class). Honest cost: reflow granularity coarsens to the nearest boundary (a row in a ~15rem grid cell no longer stacks — measured; named catalog-reachable "externally sized" opt-in prop as the future extension, NOT decided). Rejected: position-conditional containment (non-local, measured insufficient) · `flex:1 1 auto` counter-rule (fill ≠ shrink-wrap; 300/300 split defeats justify=between, measured) · reflow-prop gating (category error — a container serves its CHILDREN's queries) · `contain-intrinsic-size` placeholders · card min-content floor (the dispatch's lean — falsified on 3 measurements) · grid-item-only establishment (keeps the landmine). Decomp coverage-clean (PLAN): [`layout-containment-ruling.decomp.json`](../decompositions/layout-containment-ruling.decomp.json) | accepted | BUILT: `container.css` rule deletion + banners · `{row,column,list,grid}.{css,md}` establishment citations · `column.browser.test.ts` re-key · `canvas-surface.css`/`a2ui-gallery.css`/`a2ui-patterns.css` + the full `host.mount()` sweep (`a2ui-{list,stream,form,canvas}.css` also gained establishment; `.ask-surface` verified content-derived, deliberately left un-established) · `a2ui-catalog.spec.md` §5.2 note · `card.css:122` comment + `card.md` sizing note · the named whole-shape browser legs w/ negative controls (`_surface/container.{test,browser.test}.ts`), both engines green. This change: **partially supersedes ADR-0016** cl.4 establishment sub-clause (reciprocal line landed) · extends ADR-0096 · relates ADR-0030/0084 |
| [0101](./0101-overlay-transitions-always-announce.md) | **Overlay open-state transitions always announce — `toggle` fires on every actual show/hide (platform fidelity), ending light-dismiss-only suppression** (proposed — root-caused from Kim's gallery ticket #28 "ui-select and ui-menu: when a selection is made, the menu should close"; fork diagnosis re-verified against source). The shipped contract fires `toggle`/`close` ONLY on platform light-dismiss (`overlay.ts:248-261`; `close()` swallows the platform's own programmatic echo-toggle, `:283-285` — in-repo proof the native Popover API fires ToggleEvents on programmatic show/hide too, i.e. the suppression DEVIATES from the platform it wraps) — so a commit-close (`menu.ts:249-252` · `select.ts:320-329`) is silent and every event-driven mirror of `open` breaks: the A2UI open-binds (Menu/Popover/Tooltip `value:{prop:'open',event:'toggle'}`; `input.ts:86-98` writes back only on the declared event; `widget.ts:163-168` re-applies the model's stale `open:true` on its next wake → the panel visibly REOPENS; until then the model lies) + the gallery read-back (`component-preview.ts:877`). ADR-0045's ratifying premise "the renderer set them, so it already knows" is FALSE for a commit-close (the COMPONENT set it). Decision: the TRAIT announces every ACTUAL transition (open/close/light-dismiss through the same points; `close` alongside every hide; prop settled BEFORE the emit — settles 0045's deferred listener-order race); no-transition ⇒ no event (the `close()` early-return = the loop-breaker; `Object.is` cutoffs terminate both directions — exactly one announce per transition, idempotent not cycling); tooltip's manual `userClose` emits retired (one announcer); NO new `open` event or payload; `ui-modal` verified conformant (`modal.ts:68-77`), out of scope. Sweep: NO shipped consumer distinguishes light-dismiss from programmatic. Honest costs: provenance no longer observable from `toggle`/`close` alone · model-driven transitions also announce (native-faithful; bind write-back idempotent) · suppression pins invert (`overlay.test.ts:272` · `menu.test.ts:300/364/375/569` · `popover.test.ts:275` + browser legs) + a NEW loop negative control owed · five descriptor `.md` event rows + `overlay-controller.lld.md` re-derived (incl. `tooltip.md:40`'s stale ordering claim) · tooltip emit timing shifts one tick. Rejected: per-control emits (tooltip's drift proves they rot) · native-echo relay (jsdom-blind, ordering lost) · catalog-side bind removal (dodges — kills agent-driven open) · renderer MutationObserver · add-`open`-event-now. Decomp coverage-clean (PLAN) | accepted | on ratification+build: `traits/overlay.ts` · `controls/tooltip/tooltip.ts` · `{menu,select,popover,tooltip,combo-box}.md` event rows · `overlay-controller.lld.md` · the inverted test pins. Docs-only this pass: this record + the index row + ADR-0045's reciprocal partial-supersession link (its Status stays `accepted` — cl.2/3/4 + platform-owned light-dismiss STAND; the ADR-0025/0078 precedent). **Supersedes ADR-0045 in part** (the event-discipline leg) · relates ADR-0019 (the two-way machinery this makes hearable) · ADR-0043 (its "Two-way `open` via the `toggle` event" clause becomes true on EVERY transition) · ADR-0053 (its desync rationale dissolves; the one-value-mark constraint stands) |
| [0102](./0102-css-less-consumer-contract-law.md) | **The CSS-less-consumer contract law: no rendered-correctness concern (layout/spacing/surface/width/overflow) may live only in "the page author's CSS"; the three-lane decision rule** (proposed — generalizes ADR-0096's ratified forcing argument fleet-wide after the 2026-07-08 gallery family, tickets #29-#33: five independent defects, one contract form failing one consumer class). Lanes: **A** component-owned safe default (required when the catalog grammar can't express the fix by composition, or the concern is the component's own identity) · **B** catalog-reachable intent prop over a safe default (ADR-0071 advertising, ADR-0076/0098 validation; intent, never repair) · **C** taught idiom (ADR-0091) — only for graceful no-uptake failures, never destructive ones. Chooser: expressible-by-composition? → destructive-vs-graceful? → per-instance intent? Page CSS survives as override freedom, never the sole correctness mechanism; every application measures who relies on the old default + repairs lying docs in-change. Applications = ADR-0103/0104/0105/0106. Flagged not repaired: #27 stamp gap (needs-live-diagnosis → post-ADR-0100 re-audit) · the catalog Text row cannot express emphasis/bold (Lane B intake). Decomp coverage-clean (PLAN): [`css-less-consumer-family.decomp.json`](../decompositions/css-less-consumer-family.decomp.json) | accepted | on ratification: `a2ui-catalog.spec.md` §5 normative sentence · this record owns the law; generalizes ADR-0096 · extends ADR-0091 · relates ADR-0071/0076/0098/0015/0030 |
| [0103](./0103-radio-group-owns-layout-form-provider-teaches-wrap.md) | **The spacing pair (#31+#33): `ui-radio-group` owns its interior layout — flex axis from `[orientation]` + `--ui-radio-group-gap: var(--ui-space-sm)`; `ui-form-provider` stays layout-free, the Column-gap wrap is the taught idiom** (proposed — ADR-0102 lanes A and C at the fork's two poles, decided structurally: radio-group's discovery is DIRECT CHILDREN ONLY (`radio-group.ts:194`) so the wrap idiom is grammatically expressible but functionally destructive → Lane A; its shipped `orientation` prop finally gets a visual referent (today it moves only the roving axis while `ui-radio` inline-flex mashes in both orientations), adopting byte-for-byte the layout the site already hand-authors (`component-preview.css:108-117`, demo/doc inline styles). FormProvider = pure coordination (ADR-0050), descendants event-discovered, the wrap is idiomatic + committed evidence (`68d2a8d`, pattern-settings-form, booking-reservation) and the no-wrap failure is graceful → Lane C, residual uptake risk stated + accepted with the ADR-0096 severity split; escalation-to-Lane-A re-open condition named. No new catalog prop (a RadioGroup `gap` enum = foreseen Lane B extension). Measured blast radius: zero site reliance on the zero-gap default (both usages inline-override). Rejected: teach-the-wrap for radio-group (breaks discovery) · gap-prop-first (unsafe default fixes nothing) · flex defaults on form-provider (fights arbitrary composition) · renderer auto-wrap (breaks payload↔DOM faithfulness) | accepted | on ratification+build: `radio-group.css` (reserved token block + @scope layout rules) · `radio-group.md:94-95,160-161` (the layout-neutral paragraphs — also factually wrong today) · `component-preview.css:108-117` deletion · browser-leg re-keys + negative control · `a2ui-catalog.spec.md` §5.2 RadioGroup row · `form-provider.{css,md}` clarifying cite (contract unchanged) · ADR-0091 form-rhythm mini-skill · applies ADR-0102 |
| [0104](./0104-tabs-transparent-surface-default.md) | **`ui-tabs` drops its self-seeded surface plane — transparent by default (ADR-0015 cl.1 restored); a plane becomes an asked-for intent via the already-shipped `elevation`/`brightness` axes** (proposed — ticket #29: `tabs.css:20` seeds `neutral-surface`, so a bare Tabs inside the pattern-wizard's `Card elevation='1'` paints a wrong second box the model cannot un-ask (no "none" axis value, no CSS verb — ADR-0102). Identity test: card/modal ARE surfaces (anatomy delineates nesting) and keep their defaults; tabs' identity is strip+indicator+divider — the plane was bolted-on convenience. One-line deletion; intent lane already catalog-reachable (Tabs row axes since ADR-0087), so `elevation="0"` reproduces the old plane exactly for both consumer classes. Measured reliance: `tabs-css.test.ts:45` (inverts to negative control) · tabs-doc/demo specimens on the canvas-surface grid (repaired with explicit `elevation="0"`, dogfooding the intent lane) · `a2ui-live.ts:131` chrome tabs = verify-at-build. Rejected: composition-aware ancestor-sniffing CSS (open set, inverts the local-seam surface model, misses host backgrounds) · a surface-none opt-out prop (fixes nothing by default, second vocabulary) · seed-side brightness matching (repairs the corpus, not the contract) | accepted | on ratification+build: `tabs.css:16-20` + banners · `tabs.md` · `tabs-css.test.ts:45` re-key · `tabs-doc.ts`/`tabs-demo.ts` `elevation="0"` · `container.css:29` comment trio → duo · `a2ui-catalog.spec.md` §5.2 Tabs note · applies ADR-0102, amends the tabs-specific seeding of the ADR-0015 era (0015's clauses stand) |
| [0105](./0105-calendar-fluid-tracks-two-layer-cells.md) | **`ui-calendar` fills its given width: `repeat(7, minmax(var(--ui-calendar-cell-size), 1fr))` tracks + a two-layer cell — the band layer (the gridcell button, stretching, carries the square in-range wash + hit target) and the point layer (a fixed cell-size circle centered via pseudo-element, carrying selected/endpoint fill, hover, today ring, focus ring), endpoints adding a logical-inset half-track wash toward the band interior** (proposed — ticket #30: `calendar.css:204` fixed tracks leave a stretched panel half-empty (booking-reservation, Column `stretch` default); the fork falsified naive widening — stretched buttons = ellipse endpoints, fixed buttons in wide tracks = band holes. CSS-only, zero `calendar.ts` change; shrink-wrapped rendering pixel-identical (minmax min = the ADR-0048 ramp, which survives as floor + point-layer diameter); every ADR-0093 cl.4 invariant preserved by construction and named (circles unconstructible-to-ellipse, square wash, self-delimiting band, four WHCM states, ADR-0057 shape signifier); RTL-safe (logical insets, no directional gradient); hit target grows = a11y win. Rejected: naive 1fr · cosmetic centering (panel still wastes the width) · `max-inline-size: max-content` cap as default (vetoes the consumer's stretch intent; kept as page escape) · wider gaps (band holes) · inner-span DOM anatomy (42 nodes + heal churn vs two pseudos) · a fill/fluid prop (a repair knob, ADR-0102 Lane B bar) | accepted | on ratification+build: `calendar.css:204,235-296,116` · `calendar.md` layer table · `calendar.browser.test.ts` floor/wide/RTL/WHCM legs + naive-stretch negative control · screenshot re-baselines · `a2ui-catalog.spec.md` §5.2 Calendar note · applies ADR-0102, extends ADR-0048 + ADR-0093 |
| [0106](./0106-text-truncate-css-only.md) | **`ui-text` gains `truncate` (boolean, reflected, default false) — single-line ellipsis on the host AND the ADR-0078 stamp; a unconditional `title` mirror (CSS-only clipping — Kim ruled out the measured/ResizeObserver variant) (minted only while actually clipped, ResizeObserver + the existing childList observer, never overwriting an author title); rich reveal = the taught `Tooltip > [Text truncate, …]` first-child-anchor idiom, never auto-minted** (proposed — ticket #32's remaining intake after the `ui-button` nowrap fix `442802d`; Kim's `overflow-ellipsis` sketch adopted in substance, renamed to the single-word intent vocabulary with a future line-clamp extension reserved). No wrapper anatomy needed: ui-text's text lives in real block boxes (host or stamp) unlike button's anonymous grid text — the button label-wrapper ellipsis stays a NAMED DEFERRAL. Catalog: the Text row gains `truncate` (non-bindable presentation intent; ADR-0071 advertises; nothing for ADR-0098 to gate). A11y honest cost: keyboard-only sighted users get no `title` reveal (full text stays in the accessible name; the pairing idiom is the step-up). Rejected: auto-minting a ui-tooltip (overlay lifecycle in a Display primitive, double-reveal, breaks first-child-anchor composition) · unmeasured always-on title (lies when text fits) · an overflow enum with one designed value · a fleet-wide truncate | accepted | on ratification+build: `text.ts` (prop + measured-title effect) · `text.css` `[truncate]` host+stamp legs · `text.md` · `catalog.json` Text row + `factories.ts` · `prompt-drift.test.ts`/catalog `index.test.ts` · `a2ui-catalog.spec.md` §5.2 Text row · applies ADR-0102, extends ADR-0078 |
