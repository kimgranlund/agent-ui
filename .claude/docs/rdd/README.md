# RDD ledger — deliberately empty

Zero-RDD placeholder minted per Kim's 2026-08-24 ruling (plan 3.1): this repo maps the RDD tier
to req docs + PLAN/ROADMAP/GitHub Issues (product-brief.md §5). The directory exists only so
revalidation_checkpoint.py's three-source mandate (#655/#656) passes; no RDD records are meant
to live here.
