# Repo-alignment follow-up queue (typed, durable — committed per the playbook's closing law)

> The campaign's NOT-executed items, each with its type and routing. Chat and PR bodies are not
> tracking; this file + the ticket spine are. Owner column = who picks it up.

| # | Type | Item | Routing |
|---|---|---|---|
| Q1 | engineering | **tkt-0024 — the renderer never repaints a mounted container on a structural resend** (pre-campaign; the standing top engineering item — the lifecycle teaching's rule 2 is a visual no-op at runtime) | The ticket exists; design intake first (widget-cache invalidation, order preservation, focus survival; reconcile ADR-0053) |
| Q2 | decisions | **The component-naming master plan** — Kim's exploration (2026-07-12): the namespace/grammar inventory with exception counts → `references/naming.md` + gate gaps (descriptor↔DOM parts, custom-state vocabulary, event allowlist-at-emit) | TKT-0025 (minted with this queue) |
| Q3 | mechanical+authoring | **LLD dialect unification** — three header dialects + two `Layer:` spellings across 36 LLDs (hygiene H1 keeps reporting until cleared; then promote) | update-in-place authoring; batchable with any LLD-touching wave |
| Q4 | decisions | **The reviewer-rubric verification** — does the plugin `ui:component-reviewer` actually grade against THIS repo's `rubrics/component.md`? (GOs keep landing; unverified from docs alone) | one orchestration-reviewer dispatch |
| Q5 | mechanical | **Oversize skill splits** — a2ui-compose (154) · docs-author (165) want references/ splits | forge:skill-decompose when either is next serviced |
| Q6 | strategic/upstream | **forge D9 role registry** — register `composer` (the repo's naming law sanctions -er actor nouns; the accepted-divergence in harness_wiring_check.py retires when upstream lands) | Kim → the forge plugin |
| Q7 | strategic | **M2 conversation-surface + surface-host primitives** (agent-app-surfaces PRD-G3/G4; a2ui-chat's surface-registry is the embryo) | the PRD's own milestone queue |
