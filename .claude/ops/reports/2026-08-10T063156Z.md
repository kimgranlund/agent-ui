Status: 🟢 done

# issue-sorter sweep — 2026-08-10T063156Z

Window: 2026-08-10T05:03:40Z → 2026-08-10T06:31:56Z. Both sources (gh_issues, gh_prs) reachable
and advanced independently; no UNMEASURED sections.

## Verified this window (independent `gh` check, not trusted from dispatch brief)

5 PRs merged, 5 issues closed as their direct result:

| PR | State | Merged at | Closed issue |
|---|---|---|---|
| #674 fix(build): exclude scratch/dist from MPA glob | MERGED | 06:22:19Z | #669 (bug) |
| #675 feat(conversation): composer placeholder prop | MERGED | 06:22:27Z | #672 (enhancement, size:small) |
| #676 fix(conversation): drop redundant leading icon | MERGED | 06:22:41Z | #673 (bug) |
| #677 fix(agent-admin): Author column IS the chat card | MERGED | 06:22:47Z | #666 (task, size:small) |
| #678 feat(scripts): adr_ratify.py amendment-ratification | MERGED | 06:22:55Z | #664 (task, size:small) |

No new issues or PRs filed this window (`created:>=` search empty on issues; PR search returns
only the 5 already-merged above). No PRs closed-without-merging. No stray open PRs (0 open).

## Open issues — full current list (2, down from 7 carried)

| # | Kind | Size | Status | Note |
|---|---|---|---|---|
| #670 | bug | — | doing | Author composer-first empty state missing Model/Effort picker. **Status change this window**: all three design forks (storage / precedence / effort-scope) RULED by Kim via host AskUserQuestion round, comment posted 06:31:02Z — "Build now unblocked." Previously held as do-not-build/track-only; that gate is now clear. Label/state unchanged (still `bug`+`doing`, open) — only build-readiness moved. Flagged for the record, not acted on (classification/dedup read only). |
| #616 | task | size:small | open, upstream-gated | Unchanged. Blocked on `a2ui-project/a2ui#2150`'s YAML conformance-suite convention landing upstream + a signed Google CLA — external wait, not this repo's to hurry. No movement observed upstream this window. |

## Trust check

Every item touched this window (5 merges, both remaining open issues) authored by `kimgranlund`
— present on `friendlies.json`'s `allow_list`. Zero unknown authors, zero holds triggered this
firing. `held-items.md` unchanged (not touched, no payload). `friendlies.json` unchanged (no new
authors to seed; policy block left untouched per standing instruction — canonical record).

## Failure branches checked

- Backend: resolved (Option B, git-native/GitHub, ADR-0002/0003) — no re-guess.
- Discovery: both `gh_issues` and `gh_prs` reachable this firing, checkpoint advanced for both.
- Dedup: no new items this window — nothing to dedupe against.
- Step 7 (bootstrap roster) / Step 8 (MCP offer): both already resolved in `friendlies.json`'s
  policy block (`roster_confirmed: true`, `github_mcp_offer: "declined"`) — not re-evaluated,
  gate correctly skipped as not-applicable-this-firing.

## Done condition

Every item discovered this firing classified/trust-checked (5 merges, 0 new filings) and no
holds needed (all `kimgranlund`); both reachable sources' checkpoint entries advanced
independently; report carries every touched state file's full content as target-pathed payload
(watch-checkpoint.json only — friendlies.json and held-items.md untouched this firing, no payload
needed for either).
