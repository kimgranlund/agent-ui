Signal: Impact detector · ADR-0170 ratified Decision (agent-admin capability entry-kind roster gains its
first SINGLE-select kind — catalog; selection truth lives outside the entries store, derived at render
time; library pack maps live from the registry) · .claude/docs/adr/0170-catalog-library-kind-single-select.md
(Decision section) · 2026-08-04
Signal: Impact detector · ADR-0171 ratified Decision (button label-alignment law — CENTER only for
bare/double-adorned structures, a SINGLE adornment START-aligns the label; amends ADR-0006/0012's
four-structure roster + ADR-0133's ellipsis anchor) · .claude/docs/adr/0171-button-label-alignment-single-adornment-start.md
(Decision section) · 2026-08-05

Plan:  agent-ui · adr-0170: new-skill (no existing axis covers agent-admin's entry-kind/capability
architecture — checked a2ui-prompt-authoring, fleet-review, doc-standards,
component-catalog, a2ui-multi-catalog; none own this domain), exact scope/name deferred to Phase 3 ·
adr-0171: extend-reference (`.claude/docs/references/anatomy.md`, the existing "four structures" doc
ADR-0006/ADR-0012 already own via `component-standards`' routing table) — a correction/sharpen
of documented law, not a new axis
Gate:  deferred — no human confirm round run this firing (see below; a human IS present this session,
so naming the batched-confirm as the next step, not attempting it blind)
Landed: nothing authored. Queued to `.claude/ops/adr-queue.json` (worktree copy, see Sandbox note):
adr-0170 (harvest), adr-0171 (harvest). Checkpoint NOT advanced (same blocking tooling gap as the prior
firing — see below).

## Sandbox note — same split as the prior 2026-08-04 firing

This session's Write/Bash-write access is pinned to this dispatch's own worktree
(`.claude/worktrees/ratify-adr-0168`, currently checked out on branch `ratify-adr-0171` — the directory
name and current branch don't match, same naming drift the prior repo-cleaner report flagged) and
refused writes to the shared main checkout's `.claude/ops/` — confirmed two ways: a bare `touch` failed
silently, and `adr_queue.py clear` against the main-checkout path was explicitly denied by the sandbox
classifier. Reads against the shared checkout worked fine (`pending` succeeded there). So:

- This firing's new state (`adr-0170`, `adr-0171` queued) lives ONLY in
  `/Users/kimba/Projects/nonoun/agent-ui/.claude/worktrees/ratify-adr-0168/.claude/ops/adr-queue.json`
  and this report sits beside it in that worktree's `.claude/ops/reports/`.
  Neither `.claude/ops/adr-checkpoint.json` nor a committed `adr-queue.json` update exists in the
  shared checkout from this firing.
- I did **not** commit/push these two files — the worktree is on `ratify-adr-0171` (an unrelated
  feature branch, not a dedicated ops branch), and committing ops state onto it would be confusing.
  A human should reconcile: merge this worktree's `.claude/ops/adr-queue.json` into the shared
  checkout's copy (or move the worktree's ops files there directly) and commit from a clean branch.

## Blocking tooling gap — confirmed still present, not re-diagnosed

Ran `adr_checkpoint.py classify .claude/docs/adr --checkpoint .claude/ops/adr-checkpoint.json` in both
the shared checkout and this worktree: both report **0 ADR(s) scanned** — the same table-dialect
parsing bug the 2026-08-04 ~08:40Z firing already root-caused (this repo's ADR shape is a markdown
`| **Status** | ... |` table under an `# ADR-NNNN` H1, not either shape `scan_dir`/`scan_source`
currently recognizes). Confirmed the fix is genuinely absent locally, not a re-diagnosis: the only
harness plugin cache installed is
`/Users/kimba/.claude/plugins/cache/nonoun-plugins/harness/3.1.0/` — no `3.1.1` directory exists
anywhere under `nonoun-plugins/harness/` (`ls` of the cache dir, 20 entries, highest is `3.1.0`). The
upstream fix (claude-plugins PR #123/#124) has not reached this machine's installed cache yet — a
plugin-update/cache-refresh issue, not a code issue I can act on from here.

Effect: same as last firing — `classify` will report "0 scanned, nothing changed" on every call until
the cache updates. I did not run `advance`: writing a checkpoint now would record a false "0 known
ADRs, clean baseline" that a future firing would trust, silently hiding every real ADR. Classification
this firing was done by direct read (`git show origin/main:<path>` where the local checkout was stale)
+ GitHub verification (`gh pr view`), same as last firing.

## Corpus delta since last checkpoint (manual, since `classify` can't see it)

The prior firing (2026-08-04 ~08:40Z) manually classified ADR-0168 and ADR-0169 as the delta at that
point and queued both as harvest candidates. Since then:

- **No new ADR directory entries besides 0170/0171** — confirmed via `git ls-tree origin/main --
  .claude/docs/adr/`, 173 files, highest numbered `0171`.
- **ADR-0168, ADR-0169 already both LANDED as skills** — not by this agent (never authored), but the
  queued candidates are now stale/resolved:
  - `integration-standards/SKILL.md` exists and states "Five ratified laws governing every
    tool/integration the live agent can call... Source of record: ADR
    `.claude/docs/adr/0168-...md` (accepted 2026-08-04)" — the ADR-0168 harvest candidate is landed.
  - `a2ui-multi-catalog/SKILL.md` exists and states "Four ratified patterns govern every 'another
    catalog' ask... The contract is the ADR itself —
    `.claude/docs/adr/0169-a2ui-basic-catalog-upstream-interop.md` (accepted 2026-08-04)" — the
    ADR-0169 harvest candidate is landed.
  - The shared checkout's `.claude/ops/adr-queue.json` still lists both rows as pending/unconfirmed
    (uncommitted, from the prior firing) — this is now stale bookkeeping, not a live gap. **I could
    not clear them** (writes to that path are denied from this sandbox, see Sandbox note). Flagging
    for a human/orchestrating session to run:
    `python3 .../adr_queue.py clear /Users/kimba/Projects/nonoun/agent-ui/.claude/ops/adr-queue.json --ids adr-0168:harvest,adr-0169:harvest`
- **No `newly_superseded` ADRs this window.** Neither ADR-0170 nor ADR-0171 uses the literal
  `Supersedes` verb — both use `Amends`/`Relates` (relationship, not supersession, per this agent's
  contract). No ADR's own `Status` field flipped to `superseded` between the two firings (checked
  every file under `.claude/docs/adr/` at `origin/main` for a `superseded` status/heading — the
  matches found — 0082/0083/0084/0086/0092/0037/0000-template — are all pre-existing historical
  supersessions from before this agent's checkpoint ever existed, not new this window).
- **Local main checkout is 15 commits behind `origin/main`** (`git status`: `behind 15`) — its on-disk
  copy of ADR-0171 still reads `Status: proposed`, which is stale; verified the true state via
  `git show origin/main:.claude/docs/adr/0171-...md` (`Status: accepted`, ratified via PR #443's
  `ratify ADR-0171` utterance, PR #444 merged 2026-08-05T04:49:04Z) and cross-checked against
  `gh pr view 443/444` (`gh pr view 440/441` for 0170, both `MERGED`).

## Candidates judged

**ADR-0170 → HARVEST candidate (queued).** Crosses the Impact bar: a ratified decision establishing
the pattern any future single-select or registry-backed capability kind in agent-admin must follow —
the entries store holds the roster, a persisted key (never the entries store) holds the selection,
every switch's checked state is a render-time derivation (never a stored radio-normalized invariant —
the second-writer defect this decision explicitly rejects), and the library pack maps live from the
registry (no hand-copied trio table to drift). Placement search (a2ui-prompt-authoring,
fleet-review, doc-standards, component-catalog, a2ui-multi-catalog) found no
existing skill owning agent-admin's entry-kind/capability architecture at all — this is the third
decision in that lineage (ADR-0132 minted the family, ADR-0164 extended `EntryListOptions`, ADR-0170
adds the single-select shape), which is exactly the kind of recurring-architecture signal the Impact
detector is for. Likely a new skill; TBD at the human confirm whether it scopes to just 0170 or the
whole 0132/0164/0170 lineage.

**ADR-0171 → HARVEST candidate (queued), narrower placement than 0168/0169/0170.** Crosses the Impact
bar (a fleet-wide law amendment every future button-adjacent design must follow), but placement search
found this is a **sharpen of existing documented law**, not a new axis:
`.claude/docs/references/anatomy.md` — owned by `component-standards`' own routing table,
already the resolved "how-to-apply" doc for ADR-0006/ADR-0012's structures — documents "the four
structures" by position/grid-template today. ADR-0171 adds exactly one more property to that same
table (per-structure label alignment). Recommended plan: extend `anatomy.md` in place (save-lessons
Phase 2 outcome #2 — covered but this sharpens/corrects it), not a new skill or new reference file.

## Files changed

- `/Users/kimba/Projects/nonoun/agent-ui/.claude/worktrees/ratify-adr-0168/.claude/ops/adr-queue.json`
  — created (worktree-local; the shared checkout's copy is untouched by this firing). 2 new candidates:
  adr-0170 (harvest), adr-0171 (harvest).
- `.claude/ops/adr-checkpoint.json` — **not created** anywhere (classify still finds 0 ADRs; advancing
  would write a misleading empty baseline — same reasoning as last firing).
- This report:
  `/Users/kimba/Projects/nonoun/agent-ui/.claude/worktrees/ratify-adr-0168/.claude/ops/reports/2026-08-05T052245Z.md`

## Tests/checks run

- `adr_checkpoint.py classify .claude/docs/adr --checkpoint .claude/ops/adr-checkpoint.json` — run in
  both the shared checkout and this worktree, both report 0 scanned (confirms the gap, not a fluke of
  one checkout).
- `adr_queue.py add ... adr-0170 harvest` / `... adr-0171 harvest` (worktree copy) — both queued
  successfully; `pending` confirms 2 candidates.
- `adr_queue.py pending` against the **shared checkout's** existing `adr-queue.json` — read succeeded
  (2 stale rows: adr-0168, adr-0169), confirming those candidates already landed as skills and the
  queue rows are the only thing left unresolved.
- `adr_queue.py clear` against the shared checkout — **denied by the sandbox classifier**, not
  attempted further (no workaround pursued, per the classifier's own guidance).
- `gh pr view 440/441/443/444` — all `MERGED`, confirming both ADR-0170 and ADR-0171 are genuinely
  ratified, not just drafted.
- `git ls-tree origin/main -- .claude/docs/adr/` — 173 entries, confirms no ADR beyond 0171 exists yet
  and no file besides 0170/0171 is new since the last firing's manual baseline.
- `git show origin/main:.claude/docs/adr/0171-...md` — used in place of the stale local working copy
  (main checkout is 15 commits behind `origin/main`).
- Grep sweep for existing coverage: `integration-standards`, `a2ui-multi-catalog` (0168/0169,
  confirmed landed) · `.claude/skills/*/SKILL.md` + `.claude/docs/references/*.md` for
  entry-kind/capability/button-anatomy axes (0170/0171 placement search).

## Evidence

- `.claude/docs/adr/0170-catalog-library-kind-single-select.md` (Decision section, cl.1–8) — read via
  the local worktree copy (already current, `Status: accepted`).
- `.claude/docs/adr/0171-button-label-alignment-single-adornment-start.md` (Context ruling table +
  Decision section) — read via `git show origin/main:...` (local main-checkout copy stale at
  `proposed`; worktree copy independently confirmed `accepted`).
- PR #440 (design, MERGED 2026-08-04T21:21:36Z), #441 (ratify, MERGED 2026-08-04T21:28:01Z) — ADR-0170.
- PR #443 (design, MERGED 2026-08-05T04:13:23Z), #444 (ratify, MERGED 2026-08-05T04:49:04Z) — ADR-0171.
- `.claude/skills/integration-standards/SKILL.md:1-20`,
  `.claude/skills/a2ui-multi-catalog/SKILL.md:1-25` — confirm 0168/0169 harvests already landed.
- `.claude/docs/references/anatomy.md:70-92` ("## 3 · The four structures") — the extend-target for
  the ADR-0171 candidate.
- `.claude/skills/component-standards/SKILL.md:35` — the routing-table row citing
  ADR-0006/ADR-0012 for `anatomy.md`, the row an ADR-0171 extension would also touch.

## Risks

- 🔴 The `adr_checkpoint.py` classify tooling gap is now confirmed across two consecutive firings and
  two separate checkouts — every scheduled firing continues to silently report "nothing changed"
  until the installed harness plugin cache updates past 3.1.0. This is a standing risk to this agent's
  whole economic contract (delta-only judgment), not something either firing can self-heal.
- 🟡 Two already-landed candidates (adr-0168, adr-0169) sit as stale unconfirmed rows in the shared
  checkout's `adr-queue.json`, uncommitted, and this firing cannot clear them (sandbox write denial).
  If untouched, a future firing reading that file would re-surface them as "pending" even though
  they're resolved.
- 🟡 This firing's own new candidates (adr-0170, adr-0171) live only in the worktree's local
  `adr-queue.json`, uncommitted, on a branch (`ratify-adr-0171`) unrelated to ops bookkeeping — same
  reconciliation risk the prior firing flagged for its own report file.
- 🟡 The shared main checkout is 15 commits behind `origin/main` — any future manual read of ADR files
  from that checkout's working tree (rather than `git show origin/main:...`) will see stale status
  fields. Worth a `sync_main.py` pass (repo-cleaner's tool) before the next ADR sweep trusts local
  files directly.
- 🟢 Both new candidates' ratified status and placement search are independently re-verified (GitHub
  `gh pr view` + direct grep of the actual skill/reference corpus), not taken on the dispatch
  briefing's say-so.

## Open questions

1. **Batched confirm**: a human is present this session (on-demand ask). Recommend one
   `AskUserQuestion` round covering all 4 outstanding candidates once reconciled into one file:
   adr-0168 (harvest, landed — likely just needs its queue row cleared, no action), adr-0169 (harvest,
   landed — same), adr-0170 (harvest, new-skill, placement TBD), adr-0171 (harvest, extend
   `anatomy.md`, TBD only on exact wording). I don't carry `AskUserQuestion` myself — naming it for
   the orchestrating/coordinating session.
2. **Ops-file reconciliation**: which copy of `adr-queue.json` is authoritative — the shared checkout's
   (2 stale rows, uncommitted) or this worktree's (2 new rows, uncommitted)? Recommend merging both
   into the shared checkout once write access is available there, then deleting the worktree's copy.
3. **Checkpoint-tool fix**: same open question as last firing — is a plugin cache refresh/update
   already in motion, or does something need to trigger it manually? I can't self-update the installed
   plugin cache from this seat.

## Recommended next action

1. Refresh/update the installed `nonoun-plugins/harness` cache past 3.1.0 so `classify` can actually
   see this repo's ADRs (the fix is already merged upstream per the dispatch briefing — this is a
   local cache-pull gap, not a code gap).
2. Reconcile `.claude/ops/adr-queue.json` between the shared checkout and this worktree; clear the two
   already-landed rows (`adr_queue.py clear <path> --ids adr-0168:harvest,adr-0169:harvest`).
3. Run one batched `AskUserQuestion` confirm over adr-0170 and adr-0171 (and the two landed-but-uncleared
   rows, if a "yes, these already landed, clear them" pass is useful to fold in).
4. On confirm: adr-0170's next command is `/make-skill` (new skill, no existing axis) or `/make-pack`
   if it lands inside a newly-minted skill's `references/`; adr-0171's next command is a direct extend
   of `.claude/docs/references/anatomy.md` per `make-pack`'s "extend-reference" shape (Phase 4) — never
   run by this agent.
5. Once ADR-0170/0171 are resolved either way, clear their queue rows and consider a
   `sync_main.py` pass on the shared checkout (15 commits behind) before the next ADR sweep trusts its
   local working tree directly.
