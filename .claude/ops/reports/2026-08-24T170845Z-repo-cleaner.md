Sweep firing 2026-08-24T17:08:45Z — repo-cleaner report for /Users/kimba/Projects/nonoun/agent-ui.

Findings differ from the prior report (`2026-08-24T031536Z-repo-cleaner.md`) — a merged PR's
remote branch existed this firing and was closed via the gated script, and issue #1609 flipped
from open-and-tracking-noise to closed-and-standing-ruled-noise — so this is a full report, not
abbreviated.

## Inventory
- `git fetch --prune`: exit 0, clean. One stale remote-tracking ref pruned
  (`origin/1611-thread-pendingcount-and`, its PR #1613 merged same window).
- Worktrees: primary (`main`) + `.claude/worktrees/build-1600` (branch
  `1600-docs-site-reorganize-sitemap`, dirty — live in-progress work backing open issue #1600,
  assigned kimgranlund, in-flight label, last comment 2026-08-23T18:03:02Z, worktree diff still
  matches that ticket's scope — active, not stale).
- `git branch -vv`: `main` and `1600-docs-site-reorganize-sitemap` locally. `main` reads
  `[origin/main: behind 1]` — one commit (`fff8e2d7`, PR #1613's merge) landed on `origin/main`
  after local `main`'s current tip; not a dirty-tree case, just an un-pulled fast-forward.
- `gh pr list --state all`/`--state open`: 0 open PRs. 11 recently merged PRs surveyed; only one
  (`#1607`, `harvest-adr-queue-2026-08-23`) still had its remote branch present —
  merged-and-verified → executed (see below). No other merged PR left a remote branch behind.
- `reap-branches.mjs --dry`: 1 row, `1600-docs-site-reorganize-sitemap` KEPT (checked out live).
  Nothing reapable.
- `reap-worktrees.mjs --dry`: `build-1600` KEEP(dirty) — correctly not reapable.
- `primary_checkout_check.py`: clean · main, exit 0 (both before and after the campaign_close
  mutation).
- `gitignore_check.py` (3.18.6): same 6 stale G1 rules flagged as every recent firing
  (`npm-debug.log*`, `*.local`, `.vscode/*`, `.idea`, `*.sw?`, `.claude/docs/other`) — but the
  tracking issue (#1609) is now CLOSED, not open: the builder found these six are a standing,
  twice-ruled KEEP-LIST (Kim, 2026-08-09, reasoning in PR #632 / ADR-0074's token-export path),
  and Kim re-ruled 2026-08-24 (live) that the recurring noise is accepted permanently — this line
  is now resolved-as-accepted, not open cruft to track further. No suppression mechanism exists in
  `gitignore_check.py` itself, so the WARN output will keep appearing every firing by design; this
  agent will stop flagging it as a risk line going forward unless the ruling changes.
- Ticket-claim convention: none ruled under this repo's own numbered ADR (this repo's ADR-0005 is
  lazy-upgrade-property-wins, unrelated) — stale-claim gate not formally applicable, but the one
  open issue was still read for staleness per the general finding classes. Open issues: only #1600
  remains (#1605/#1608 from the prior firing's report have since closed via merged PRs #1612/#1610)
  — assigned, in-flight, live dirty worktree matching its scope, not stale.
- Dirty `main`: `.claude/ops/watch-checkpoint.json` modified and
  `.claude/ops/reports/2026-08-24T170845Z-issue-sorter.md` untracked — both match this same
  firing's sibling seat (decision-watcher / issue-sorter running in parallel per the resolved
  three-seat scope), not cruft. `.claude/ops/sweep-in-flight.json` untracked — this firing's own
  dispatching-session marker (`session agent-ui-90`, `startedAt 2026-08-24T17:08:45.724Z`, matches
  the stated firing key). One additional untracked file:
  `packages/agent-ui/a2ui/corpus-genui/verdicts/2026-08-24t16-09-28-173z--claude-sonnet-5.json` — a
  generated eval-verdict artifact, not gitignored, left by a live/recent corpus-genui eval run; not
  classifiable as cruft vs. WIP without more context — flagged, not touched.
  `sync_main.py` withheld regardless, per this firing's explicit unattended-dispatch instruction
  (no interactive-only mutations).

## Executed
- `campaign_close.py 1607` — PR #1607 (`harvest-adr-queue-2026-08-23`) independently re-verified
  MERGED; remote branch `harvest-adr-queue-2026-08-23` deleted and reverified gone. All four of the
  script's own gates (C1 merged / C5 no open PR has it as head / C4 no open PR has it as base / C2
  delete-and-reverify) read `ok`. Confirmed post-run: `git branch -r` no longer lists it.

## Proposed-but-not-executed
- `main` is 1 commit behind `origin/main` (local `79ed7da3` vs. remote `fff8e2d7`, PR #1613's
  merge) with two untracked non-cruft files sitting on top (see Inventory) — recommend a human (or
  a future interactive-dispatch firing) run `sync_main.py` to fast-forward once the untracked
  eval-verdict artifact is reviewed/committed-or-discarded; withheld here solely because this is a
  scheduled/unattended firing, per charter.
- No stale-open PRs, orphaned worktrees/branches, stale ticket claims, or off-main-primary findings
  this firing.

## Risks
- 🟢 Git surface otherwise clean: primary correctly stays on `main`, one live worktree correctly
  kept, 0 open PRs, 0 reapable branches, the one stale merged-PR remote branch closed via the
  gated script and reverified.
- 🟢 No mutating script was refused or overridden this firing.
- 🟢 The 6 stale `.gitignore` G1 rules are now a resolved, standing-accepted finding (issue #1609
  closed 2026-08-24 under a re-affirmed 2026-08-09 KEEP-LIST ruling) — drops off future risk
  reporting unless the ruling changes.
- 🟡 Local `main` is 1 commit behind `origin/main` — routine, but worth a human `sync_main.py`/pull
  next interactive session rather than letting it accumulate silently across further unattended
  firings.
