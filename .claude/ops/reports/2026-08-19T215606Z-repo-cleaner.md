# repo-cleaner firing — 2026-08-19T21:56:06Z

Full report (not abbreviated) — executed set differs from the prior firing
(`.claude/ops/reports/2026-08-19T045557Z.md`, which closed 19 PR branches + 24 orphaned markers).

Naming note: this firing's standing-default path (`.claude/ops/reports/2026-08-19T215606Z.md`)
collided with the parallel issue-sorter seat's own report for the identical firing timestamp —
disambiguated to this `-repo-cleaner` suffix rather than overwrite it. Recommend the plugin adopt
a seat-suffixed report-path convention for any firing running more than one ops-family seat.

## Inventory
- `git fetch --prune`: 12 stale `origin/*` refs pruned (already-merged campaign branches).
- Worktrees: primary (`main`), `.claude/worktrees/agent-a3539f9b43d026844` (`1445-table-dogfood`,
  locked), `.claude/worktrees/agent-a773fb963b42942b1` (`0223-s3-leaves-gate-flip` — observed
  mid-firing shifting to detached HEAD at the same tip, evidence of live concurrent work in that
  worktree right now; not touched).
- `primary_checkout_check.py` → clean, both times run (main advanced live via fast-forward pulls
  from a concurrent session between the two checks; re-verified clean after).
- `branch.main.merge` config anomaly: points at `refs/heads/1411-tracker-partition` (a rename
  artifact per `git reflog show main`), not `refs/heads/main` — content is byte-identical to
  `origin/main` (0 ahead/0 behind) so no data risk, but it produced a confusing pre-fetch
  `git status` divergence read. Proposed fix below, not executed (config-only, no script gates it).
- PR cross-reference: 6 remote branches on already-MERGED PRs (`0223-s0-pilot` #1427,
  `0223-s1-entry-family` #1432, `1376-map-design` #1414, `1410-preview-typed-text` #1415,
  `1429-service-card-design` #1431, `fill-by-default-design` #1425); 0 open PRs repo-wide.
- Host-repo reap script (`npm run ops:reap-branches`, named in this repo's `package.json`) —
  dry-run: 9 REAP / 4 KEPT (the two live-worktree branch pairs), 0 fail/0 warn.
- Ticket-claim convention: none ruled in this repo (unchanged) — stale-claim gate not-applicable.
- `gitignore_check.py`: same 6 stale G1 rules as the prior firing, standing noise, exit 0.

## Executed
- `campaign_close.py` on 6 merged-and-verified PRs, all clean/exit 0: 1427/1432/1414/1415/1431/1425
  (branches: 0223-s0-pilot, 0223-s1-entry-family, 1376-map-design, 1410-preview-typed-text,
  1429-service-card-design, fill-by-default-design) — each remote branch deleted+reverified gone.
- `node scripts/reap-branches.mjs` dry-run (0 fail/0 warn) then `--execute` on its own 9 REAP rows
  — all deleted: the same 6 branches above (locally) plus 3 orphaned worktree-agent markers:
  worktree-agent-a156c42d2aa1c8db0, worktree-agent-a212d8676d7591637, worktree-agent-ad049c3670231bd08.
  Correctly kept 0223-s3-leaves-gate-flip / worktree-agent-a773fb963b42942b1 and
  1445-table-dogfood / worktree-agent-a3539f9b43d026844 — both still registered live worktrees.
- `sync_main.py`: **not run** — main is dirty (`.claude/ops/watch-checkpoint.json` modified,
  `.claude/ops/reports/2026-08-19T215606Z.md` + `.claude/ops/sweep-in-flight.json` untracked), but
  every one of those three paths is independently evidenced as a live parallel seat/session's own
  in-flight work this same firing, not cruft — quarantining it via stash would reproduce the exact
  failure mode `sync_main.py`'s own docstring names as its uncovered gap. Propose-only below.

## Proposed-but-not-executed
- `branch.main.merge` misconfig: recommend a human run `git branch --set-upstream-to=origin/main main`
  on the primary checkout — cosmetic today, prevents a future confusing divergence read.
- Dirty `main` (the three paths above): recommend the dispatching session commit all three seats'
  applied `.claude/ops/...` state together once this sweep concludes, rather than running
  `sync_main.py` against a live neighbor's own uncommitted work.
- Report-path collision with issue-sorter (see naming note above): recommend a standing
  seat-suffix convention for report filenames on any multi-seat firing.
- `.gitignore`'s 6 stale G1 rules: standing noise, not re-proposed as new.

## Risks
- 🟢 6 PR branches + 3 orphaned local branches closed/reverified gone; both live worktrees
  correctly preserved by both gated scripts.
- 🟢 Primary checkout clean on `main`, byte-identical to `origin/main`.
- 🟡 `branch.main.merge` misconfig — cosmetic, needs a human's one-line fix.
- 🟡 `sync_main.py` withheld on dirty `main` — correct given the evidence, but the underlying
  dirt needs the dispatching session to resolve once all three seats finish this sweep.
- 🟡 Report-path collision with issue-sorter this firing — worked around, convention gap named.

## Open questions
- None requiring a human answer beyond the two 🟡 items above, each already carrying a named
  recommended action.
