# decision-watcher sweep — 2026-08-07T21:34:00Z

Dispatch: chore-lead fan-out sweep, one of three parallel seats (decision-watcher, issue-sorter, repo-cleaner).

## Status: 🟡 attention (bootstrap + 3 new candidates)

### Checkpoint status: this is the first classify that ever actually ran

`.claude/ops/adr-checkpoint.json` had never existed in this repo — not in git history, not on disk. Two
prior firings (2026-08-04T083458Z, 2026-08-05T052245Z) both hit a harness-cache parsing bug (installed
cache 3.1.0 couldn't parse this repo's H1+blockquote ADR dialect — `classify` reported "0 scanned" every
time), so they hand-classified ADR-0168/0169 and ADR-0170/0171 manually and could never run `advance`.

The installed cache is now 3.1.5 and correctly parses all 176 ADRs. This firing is a genuine bootstrap:
`classify` reports all 176 ADRs as `new` purely because there's no prior baseline — not 176 real new
decisions. Judged only the genuinely-new-since-last-review delta (ADR-0172–0177, ratified 2026-08-06),
consistent with both prior firings' own scoping choice.

### Delta judged this firing (ADR-0172–0177, ratified 2026-08-06)

| ADR | Verdict | Why |
|---|---|---|
| 0172 persona-catalog composition | Already landed, no candidate | Documented in a2ui-multi-catalog SKILL.md §5 |
| 0173 descriptor inversion (props-gen) | **Harvest candidate — queued** | No skill owns descriptor-generation architecture |
| 0174 planner-stage pilot | **Harvest candidate — queued** | No skill owns the a2ui producer/meta-line envelope architecture |
| 0175 association multiselect field | **Harvest candidate — queued** | No skill states mint-vs-compose criteria for aggregate-valued FACE fields |
| 0176 identity/account flow family | Already covered, no candidate | agent-ui-composition-patterns tracks it live |
| 0177 MCP client integration | Already covered, no candidate | agent-ui-integration-standards names it explicitly |

### Already-queued candidates resolved (cleared)

`adr-0170` and `adr-0171` (queued 2026-08-05) are already landed: `agent-admin-library-kinds` skill and
`anatomy.md` §3a respectively. Cleared from the queue as resolved bookkeeping.

### Newly-superseded (bootstrap artifact, not new events)

13 IDs flagged `newly_superseded` purely from having no prior checkpoint to diff against (all historical,
pre-dating any tracked window). Checked citations for all 13 — adr-0007/0017/0032/0045 are cited and every
citation correctly scopes to the surviving portion; adr-0016/0025/0033/0037/0082/0083/0084/0086/0092 have
no citations at all. No stale-citation candidates.

### Batched confirm — deferred

Deferred to a human (ops plan item 3.1) rather than attempted blind in this unattended dispatch.

### Next steps (never run by this agent)

- adr-0173: likely `/make-pack` inside `agent-ui-component-standards`.
- adr-0174: likely `/make-skill` (new skill; scope vs. the ADR-0088/0097/0146/0159 lineage TBD at confirm).
- adr-0175: likely `/make-pack` inside `agent-ui-component-design`.

### Checkpoint + queue — landed this dispatch

`.claude/ops/adr-checkpoint.json` (176-entry hash map, first-ever persisted baseline) and
`.claude/ops/adr-queue.json` (3 pending candidates: adr-0173, adr-0174, adr-0175) both landed directly by
chore-lead in this same dispatch, closing the "seat produced a payload it couldn't write" gap for this
firing.
