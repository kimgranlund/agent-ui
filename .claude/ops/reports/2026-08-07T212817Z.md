# issue-sorter sweep — 2026-08-07T21:28:17Z

Dispatch: chore-lead fan-out sweep, one of three parallel seats (decision-watcher, issue-sorter, repo-cleaner).

## Status: 🟢 done — nothing pending, one structural drift flagged

**Discovery window:** since last checkpoint (2026-08-05T05:19:23Z) through now (2026-08-07T21:28:17Z), both sources reachable (`gh` auth valid, `kimgranlund/agent-ui`).

- **Issues:** 70 touched in-window (7 open, 63 closed).
- **PRs:** 99 touched in-window (98 merged, 1 closed unmerged — `#447`, a superseded sitemap-fix stub).

**Pending/unsorted:** none. Every discovered issue and PR is authored by `kimgranlund`, already on the `friendlies.json` allow-list. All 7 currently-open issues already carry a valid `kind` label (`bug`/`enhancement`/`task`) plus a `size:*` label where the kind calls for one (`#586`, `#581`, `#579`, `#576`, `#575`, `#574`, `#567`) — Kim self-files through the capture skills with full labels already applied, so there was nothing left un-triaged for this pass to route.

**Classified/routed this pass:** nothing new minted or resumed — no untriaged item existed to act on. No `needs-triage-approval` labeled items exist on the tracker either (checked directly).

**Dedupes found:** none (no candidate items to dedupe against).

**Unknown-filer items flagged:** none — zero non-`kimgranlund` authors appeared in the window.

**Step 7 (roster bootstrap) / Step 8 (MCP offer):** both gates already resolved on prior firings (roster confirmed 2026-08-05; `github_mcp_offer: declined` 2026-08-05, GH #438) — not-applicable-this-firing, correctly skipped, not re-evaluated.

### Flagged for a human — `friendlies.json` structural drift (resolved this firing by chore-lead)

The two prior decisions (roster-confirmed, MCP-offer-declined) were recorded as stray top-level keys in `.claude/ops/friendlies.json`, sitting outside the `kimgranlund/agent-ui.policy` block the spec requires, while the nested `policy` block still showed `roster_confirmed: false` and `github_mcp_offer: null`. Corrected payload applied directly to `.claude/ops/friendlies.json` by chore-lead in this same dispatch (facts unchanged, relocated only). The `standing_rule` field (auto-friendly-on-access vs hold-first-filing) remains genuinely undecided — carried as ops plan item 3.2.

### Checkpoint advance — applied

`.claude/ops/watch-checkpoint.json` advanced to `2026-08-07T21:28:17Z` for both `gh_issues` and `gh_prs`, applied directly by chore-lead in this dispatch.

`held-items.md` untouched — no new holds this firing, nothing to append.

**Left open for a human:**
1. The missing `standing_rule` decision (auto-friendly-on-access vs. hold-first-filing) — needed before the allow-list can safely grow. Tracked as ops plan item 3.2.
