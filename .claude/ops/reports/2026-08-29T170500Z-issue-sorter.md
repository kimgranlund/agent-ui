# issue-sorter report — 2026-08-29T17:05:00Z firing (agent-ui-marshal, session agent-ui-93)

Repo: kimgranlund/agent-ui (GitHub Issues, resolved backend). Multi-seat sweep (decision-watcher,
issue-sorter, repo-cleaner) — this is the issue-sorter shard only.

## Discovery
- gh_issues: reachable, window 2026-08-28T03:40:00Z -> 2026-08-29T17:24:30Z, checkpoint advanced.
- gh_prs: reachable, same window, checkpoint advanced.
- No UNMEASURED source this firing.

## Items
- #1699, #1701 — skipped per dispatch context (in-flight, active build dispatches; not re-triaged).
- #1702 (task) — new, filed by kimgranlund (doc-checker finding, stale seam comment in
  agent-admin-schema.ts re: SPEC-N1 v0.5 ./agent export). Author on friendlies allow_list and
  matches policy.roster_confirmed_by — no user-signal tag (filer is the operator, not foreign).
  Dedup against #1699/#1700/#1701 bodies: no match, distinct concern. Already carries the `task`
  kind label, no size label (legal, unsized task), no assignee, well-formed record (Summary/
  Acceptance/Links present). No mint needed — resumed as-is, no held item.
- #1692, #1694, #1695 (closed issues), #1693/#1696/#1697/#1698 (merged PRs) — inside the updated
  window but already resolved; no action.
- #1700 (open PR, docs: agent-model reference) — PR discovery only; out of ticket-record/
  provenance scope per this skill's own carve-out (a PR is not a ticket-record concept). No
  action.

## REQ-011 / REQ-013
Both already resolved in friendlies.json (roster_confirmed: true, github_mcp_offer: declined,
recorded 2026-08-05). Not this repo's first firing — steps 7-8 correctly skipped, not re-asked.

## State-file mutations this firing
- watch-checkpoint.json: both gh_issues/gh_prs last_successful_check advanced to
  2026-08-29T17:24:30Z (payload below).
- friendlies.json: unchanged (no new author, no roster/offer state change).
- held-items.md: unchanged (no new holds, no approvals/denials carried this firing).
- .mcp.json: not applicable (offer already recorded as declined).

## Done-when check
Every item discovered this firing classified, trust-checked, and resumed/skipped correctly per
context; both reachable sources' checkpoint entries advanced; report shipped as this fenced block
at its named path.
