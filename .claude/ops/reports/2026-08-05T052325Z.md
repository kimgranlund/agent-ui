# repo-cleaner sweep — 2026-08-05T05:23:25Z

**Sandbox note**: this session's Write tool is pinned to this dispatch's own worktree
(`.claude/worktrees/ratify-adr-0168`) and refused the shared main checkout's
`.claude/ops/reports/` path (error: "This session is now isolated ... Edit the worktree copy of
this file instead") — same friction the prior firing (2026-08-04T08:41:45Z) hit and flagged as
Open question 3, unresolved since. This report landed here instead. A human should reconcile the
two report locations by hand; a future firing reading "the most recent file in
`.claude/ops/reports/`" from the shared checkout will not see this one either.

Dispatch: interactive, ad hoc ("run an on-demand repo-hygiene sweep," briefed with a specific list
of newly-merged-PR candidates since the prior firing). Compared against the most recent file in
this worktree's own `.claude/ops/reports/` (`2026-08-04T084145Z.md`, the only file there — the
sibling report at the shared checkout's `2026-08-04T083458Z.md` is a different agent's ADR-harvest
report, not a prior repo-cleaner firing). This firing's classification set differs materially from
that report (worktree count 33→5, new merges landed mid-firing, a real fast-forward happened) —
full report, running count resets to **1**.

## Status: 🟢 done

7 merged-and-verified PRs' remote branches closed via `campaign_close.py` (all C1/C2 clean — 6
from the briefed candidate list, plus PR #449 which merged *during* this firing and was caught by
the required independent re-survey). `sync_main.py` ran on main's dirty tree (interactive
dispatch): quarantined 3 dirty items, fast-forward-pulled 17 incoming commits, reverified
HEAD==origin/main, popped the stash back after confirming the one true overlap file had converged
to identical content on both sides (no data loss, no conflict). `gitignore_check.py` ran clean
(17 warnings, unchanged from last firing). One briefed candidate (`ratify-adr-0171`, PR #444) was
deliberately NOT executed — its branch is checked out in this session's own hard-excluded
worktree. Everything else is proposed only.

## Inventory delta vs. last full report (2026-08-04T08:41:45Z)

The human executed nearly all of that report's "Recommended next action" between firings:
worktrees dropped from **33 → 5** (main + 4), local branches from **~40 → 7**. The standalone
`ratify-adr-0168` local branch (that report's Open question 1) is gone. The two flagged
uncommitted-diff worktrees (`agent-ui-wt-307`, `agent-acf34d2a73f6caf46`) are gone. The 34
`worktree-*` scratch branches are gone (one new one reappeared this cycle, see below).
`agent-a948d312447b47f15` (`intake-368-nav-rail-popover`, Open question 2) is gone as a worktree,
but the identically-named **branch** persists with no worktree — still unresolved, still
ambiguous (see below).

## Executed

**`campaign_close.py` (7 runs, each independently `gh pr view`-verified `MERGED` before touch,
reverified gone after):**

| PR | Branch | Merged | Note |
|---|---|---|---|
| 439 | skills-adr-harvest | 2026-08-04T14:08:09Z | briefed |
| 440 | design/catalog-library-pack-docs | 2026-08-04T21:21:36Z | briefed |
| 441 | ratify-adr-0170 | 2026-08-04T21:28:01Z | briefed |
| 443 | design/button-alignment-law | 2026-08-05T04:13:23Z | briefed |
| 446 | build/catalog-library-pack | 2026-08-05T04:49:10Z | briefed |
| 448 | fix-442-button-alignment | 2026-08-05T04:56:06Z | briefed |
| 449 | fix-429-e7-conformance-gate | 2026-08-05T05:17:34Z | **not briefed** — merged live during this firing (see Notes) |

Two briefed names were already fully closed before this firing started (verified, not re-run):
`design/tool-enablement-adr-0168` (#407, remote already gone) and `fix-427-calendar-date-rot`
(#435, remote already gone) — both closed by the prior firing. The stub branch
`design/catalog-library-pack` (the unmodified 87295cd pointer) no longer exists on the remote at
all — already gone, nothing to execute.

**`sync_main.py`** (interactive dispatch, gate satisfied): main was dirty (`.claude/settings.json`
modified, `site/public/adr-index.json` modified, `.claude/ops/` untracked) with 34 files incoming
from origin. Classifier flagged one genuine **OVERLAP**: `site/public/adr-index.json` (both sides
touched it — origin's incoming commits included live ADR-index regenerations through ADR-0171).
Quarantined as `stash@{0}`, pulled `--ff-only` clean, reverified `HEAD == origin/main` at
`3bbea32f`. Per the doctrine's "read both sides before choosing," diffed `HEAD` against the
stashed version of the overlap file — **byte-identical**, a converged false-alarm, not a real
conflict. Popped the stash; `git status` after showed the overlap file no longer modified (only
`.claude/settings.json` and `.claude/ops/` remain dirty, both pre-existing local-only items,
untouched). Net effect: main genuinely advanced 17 commits (was 15 behind at the last firing's
snapshot, more merged during this one, including the mid-firing #449); zero data loss.

**`gitignore_check.py`**: exit 0, warn-only, same 17 `G1` stale-rule warnings as last firing
(`logs`, `*.log`, `.idea`, `.vscode/*`, `.claude/docs/other`, etc.) — untouched, surfaced only.

## Notes — live parallel activity during this firing

PR #449 (`fix-429-e7-conformance-gate`) was **OPEN** at this firing's first inventory read and
**MERGED** minutes later at the point of re-verification — a live session finished and merged it
while this sweep was mid-flight. Its worktree (`agent-add380113c5e7f7eb`) was still present,
unlocked, when the merge was caught; by the time of the final re-survey (seconds later) that
worktree and its local branch had both vanished — the live session self-cleaned its own worktree
and local branch, independent of anything this firing did. `campaign_close.py 449` only deletes
the verified-merged PR's **remote** branch, which is exactly the same class of action already run
against the other candidates' still-checked-out worktrees (`build/catalog-library-pack`,
`fix-442-button-alignment`) with no adverse effect — proceeded on the same reasoning: the gate's
own re-verification is what makes this safe regardless of what else is running.

## Briefed candidate deliberately NOT run — `ratify-adr-0171` (PR #444)

Independently verified `MERGED` (2026-08-05T04:49:04Z), remote branch still present — meets every
`campaign_close.py` criterion. **Not run**: `ratify-adr-0171` is the exact branch currently
checked out in this firing's own hard-excluded worktree (`.claude/worktrees/ratify-adr-0168`,
locked). The dispatch's hard-exclusion covers "any deletion/reap against" that worktree; deleting
the remote branch a live, locked worktree is actively sitting on is close enough to that boundary
that this firing treated it as excluded too — same precedent as the prior firing's PR #411/#433
handling. Ambiguity is never a license to execute. Flagging for a human: if this worktree's task
is truly done and about to be torn down, `campaign_close.py 444` is a one-line follow-up; if it's
still in active use, leave it.

## Classification — worktrees (5 total)

- **Hard-excluded (this firing's own live worktree, never touched):**
  `.claude/worktrees/ratify-adr-0168` (branch `ratify-adr-0171`, locked, PR #444 merged but its
  remote branch left alone — see above).
- **Dangling (merged PR now remote-closed above; worktree/local branch itself untouched —
  propose removal):**
  - `.claude/worktrees/agent-a48e4f7f62f4175ff` (`fix-442-button-alignment`, PR #448)
  - `.claude/worktrees/agent-a5de03181dce98f69` (`build/catalog-library-pack`, PR #446)
- **Healthy — active, leave alone:**
  `.claude/worktrees/agent-a0717b89cdce067fb` (`fix-445-ma1-table-widening`, **locked**, no PR
  yet; tracks the open, unassigned GH issue #445 "MA-1 — the ui-table interactive widening build,"
  genuinely in-progress).

## Classification — local branches with no worktree (main checkout)

- **Stale, propose `git branch -d`** (remote already gone, PR merged, no worktree):
  `design/tool-enablement-adr-0168` (#407, closed by prior firing).
- **Dangling, remote now closed this firing** (worktree still holds the checkout — leave the
  local branch until its worktree is reaped): `build/catalog-library-pack`,
  `fix-442-button-alignment` — listed here and above only once each; not double-counted as a
  separate action.
- **Ambiguous — orphaned vs. still-needed (no repo-configured staleness window; propose only,
  unresolved from last firing's Open question 2):** `intake-368-nav-rail-popover` — no PR, no
  worktree, tip unchanged (`afe2bbf`) since the last firing. May be superseded by the
  already-merged `build-368-nav-rail-overlay` (PR #375, closed in the prior firing) or may still
  be a live prep doc. Recommend a human glance before deleting — carried over unresolved a second
  cycle now.
- **Orphaned scratch placeholder (no unique content, propose deletion):**
  `worktree-agent-add380113c5e7f7eb` — a leftover auto-named branch tipping on the already-merged
  `fix-429-e7-conformance-gate` commit (4b69cfb), its own worktree/branch pairing already gone (see
  Notes above). Same class as the 34 pruned last cycle; this one reappeared because its worktree
  was spawned after the last sweep.
- **Excluded (this firing's own worktree's branch, proposed-not-executed above):**
  `ratify-adr-0171`.
- **Healthy, no action:** `fix-445-ma1-table-widening` (tracks `origin/main`, no divergence yet —
  genuinely fresh, matches its worktree's healthy classification above).

## Open PRs

`gh pr list --state open` returned **zero** open PRs at the end of this firing (PR #449 was the
last one open and it merged mid-sweep). No stale-open PRs to classify this cycle.

## Claimed-ticket sweep

Not run. This workspace's own ADR-0005 is `lazy-upgrade-property-wins`
(`.claude/docs/adr/0005-lazy-upgrade-property-wins.md`) — unrelated to the ticket-claim convention
this agent's Scope section describes. That convention is not ruled in this workspace (re-confirmed
this firing, same finding as the prior one), so this firing degrades to git-surface-only hygiene;
no `gh issue list` claim-staleness pass run. (Issue #445, surfaced above only because it's the
target of an active worktree, has no assignee — not a claim in the ADR-0005 sense regardless.)

## Tests / checks run

- `git fetch origin --prune` — clean.
- `gh auth status` — authenticated, network reachable throughout.
- `git worktree list`, `git branch -vv`, `gh pr list --state all/open --json ...` — full inventory,
  read twice (before and after the live-merge event caught mid-firing).
- `git ls-remote --heads origin <branch>` per candidate — confirmed present/absent both before and
  after each `campaign_close.py` run.
- `gh pr view <number> --json state,mergedAt,headRefName` per candidate — independent
  re-verification, never trusted the dispatch briefing's claims alone.
- `python3 campaign_close.py <pr>` × 7 — all `campaign_close · clean` (C1 ok, C2 ok).
- `python3 sync_main.py` — quarantine/pull/reverify cycle, `ok` throughout; manual
  `git diff HEAD stash@{0} -- site/public/adr-index.json` to resolve the one flagged OVERLAP
  before popping (empty diff → safe, popped clean, confirmed via `git status` after).
- `python3 gitignore_check.py /Users/kimba/Projects/nonoun/agent-ui` — exit 0, warn-only, 17
  findings, unchanged from last firing.

## Risks

- 🟡 `intake-368-nav-rail-popover` is now a **second consecutive** unresolved ambiguous finding —
  worth a human decision this time rather than another carry-forward.
- 🟡 `ratify-adr-0171`/PR #444's remote branch is a genuine tension between "briefed as a
  deletable candidate" and "sits under this firing's own hard-excluded worktree" — resolved
  toward non-execution per the ambiguity rule, but it's a live-hanging item, not a closed one.
- 🟡 The report-destination split (shared checkout vs. worktree-local) is now a **second
  consecutive** firing hitting the same sandbox refusal — worth fixing so future firings can
  actually diff against each other from one place.
- 🟢 The `site/public/adr-index.json` OVERLAP resolved to byte-identical content — no judgment
  call was actually needed once diffed, but it was diffed, not assumed.
- 🟢 The mid-firing PR #449 merge (a live parallel session) was caught by re-surveying rather than
  trusting the initial snapshot — consistent with "a command's own report of success is a claim,
  not evidence," applied here to a repo *snapshot* rather than a single command.

## Open questions

1. Is `.claude/worktrees/ratify-adr-0168`'s task (branch `ratify-adr-0171`, PR #444 merged) done?
   If yes, `campaign_close.py 444` is the one remaining follow-up on this firing's briefed list.
2. `intake-368-nav-rail-popover` — confirm superseded-by-#375 or still-needed; unresolved two
   firings running now.
3. Retire the 17 stale `.gitignore` rules at your convenience — unchanged, read-only, no urgency.
4. Where should reports actually land so consecutive firings can diff against each other — the
   shared checkout's `.claude/ops/reports/` or a worktree-local path? Carried over from the last
   firing's Open question 3, still unresolved.

## Recommended next action

1. Reap the 2 confirmed-dangling worktrees (`agent-a48e4f7f62f4175ff`,
   `agent-a5de03181dce98f69`) and delete their now-remote-closed local branches
   (`fix-442-button-alignment`, `build/catalog-library-pack`) — no script gates this.
2. Delete the stale local branch `design/tool-enablement-adr-0168` (remote long gone).
3. Prune the one orphaned scratch branch `worktree-agent-add380113c5e7f7eb`.
4. Decide `intake-368-nav-rail-popover`'s fate (Open question 2).
5. Once `.claude/worktrees/ratify-adr-0168` wraps, run `campaign_close.py 444` by hand.
