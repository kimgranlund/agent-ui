# issue-sorter report — 2026-08-26T01:57:30Z (sweep-chores fan-out)

Repo: kimgranlund/agent-ui. Discovery window: 2026-08-25T17:55:43Z → 2026-08-26T01:57:30Z.

## Sources
- gh_issues: reachable, OK — checkpoint advanced.
- gh_prs: reachable, OK — checkpoint advanced.

## Discovered (20 issues, 16 PRs) — the docs/catalog audit campaign

All 20 issues and all 16 PRs in-window are authored by `kimgranlund` (trusted, on the allow-list,
matches `friendlies.json`'s `confirmed_by` — no `user-signal` tag applies). Every issue arrived
already fully classified and labeled (kind + severity/size) directly via the docs `file-bug`/
`file-task` skills as part of the same session, not minted by this seat:

- #1627, #1628, #1629 — bug, severity minor/major/minor — all CLOSED (PRs #1630 and prior merges).
- #1631, #1632 — ADR amendment tasks, size:small — CLOSED.
- #1633, #1634 — task, size:small / unsized — #1633 CLOSED (PR #1659), #1634 still OPEN, `in-flight`.
- #1637 through #1650-range (catalog-vs-component prop-diff, text-catalog variant enum,
  attachment/choosing attribute mismatches, radio-group/segmented-control dead listeners,
  color-picker prose, table-demo native-input staleness, timeline-item status cycle, super-shell
  guide, chat-shell guide, traits-doc stale line, agent-admin stale architecture) — bug/task mix,
  size:small or severity minor/major, one `wontfix` (#1638) — all CLOSED via their matching merged
  PRs (#1648–#1660).
- #1653 — "ADR-0023(c) plan-approval write-gate inconsistently enforced..." — OPEN, labeled
  `bug`+`needs-ruling`. **Observation, not acted on:** watch-tickets step 2 has a ruling-shaped
  item take `needs-ruling` INSTEAD OF a kind label; this one carries both. Not edited by this seat
  — #1653 is an active item in Kim's own ruling queue (held-items.md) and the retained `bug` label
  may be deliberate origin-context from its `docs:file-bug` filing route; flagging for a human or
  a later firing to resolve rather than guessing.
- #1618 — "ADR-0129 Amendment 2: extract shared artboard core to @agent-ui/app" — already
  `needs-ruling`+`in-flight` from a prior firing, now CLOSED via merged PR #1656 (ruling was made
  and built between firings, outside this seat). No action.
- PR #1656 (ADR-0129 Am.2 extraction) — the only non-doc/non-bugfix PR this window; MERGED,
  authored by `kimgranlund`. No ticket-record concept applies to PRs; tracked for checkpoint only.

Nothing unclassified, nothing held, nothing newly minted this firing — every record already existed
before this seat looked at it. Dedup-before-mint sweep confirms no duplicates.

## REQ-011 / REQ-013 status
- Roster: confirmed (2026-08-05), not re-asked.
- github_mcp_offer: `declined` (recorded 2026-08-05) — not-applicable this firing, skipped silently
  per contract (decision already recorded).

## State files touched
- `watch-checkpoint.json`: both entries advanced to this firing's timestamp (payload below).
- `friendlies.json`: unchanged, no payload.
- `held-items.md`: unchanged, no payload — no new hold/denial this firing; the #1653 label
  observation above is not a trust-check hold and doesn't belong on that ledger.

Done per watch-tickets' own Done-when clause: every discovered item classified/trust-checked
(already resolved before this firing for all 20, none new), every reachable source's checkpoint
advanced, this report shipped as its own fenced payload, no item silently dropped, one label
inconsistency named rather than silently absorbed or silently fixed.
