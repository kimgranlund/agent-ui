Sweep firing 2026-08-26T01:57:30Z — repo-cleaner report for /Users/kimba/Projects/nonoun/agent-ui.

Findings differ from the prior report (`2026-08-25T175543Z-repo-cleaner.md`) — a new finding
class this firing: 33 orphaned scratch **clones** (not worktrees — plain `git clone` temp
directories under `/var/folders/.../T/agent-ui-<ticket>`, invisible to `git worktree list`) left
by this session's ~20 concurrent `teamwork:build-leader` dispatches plus older residue from prior
sessions. Full report, not abbreviated.

## Inventory
- `git fetch --prune`: exit 0, clean. 10 stale `origin/*` tracking refs pruned (all
  already-merged PR branches whose remote copy GitHub already deleted:
  `1618-adr-0129-amendment-2`, `1631-adr-0021-amendment-restate`,
  `1632-adr-0025-amendment-restate`, `1633-commit-the-uncommitted-produce`,
  `1637-build-a-real-catalog`, `1642-table-demo-ts-describes`,
  `1643-timeline-item-demo-ts`, `1644-super-shell-ts-guide`, `1645-chat-shell-ts-guide`,
  `1647-traits-doc-ts-cites`) — nothing for `campaign_close.py` since the remote branch was
  already gone before this firing read it.
- Worktrees (`git worktree list`, 1 total): only the primary
  (`/Users/kimba/Projects/nonoun/agent-ui`), HEAD `c9065cfa`, branch `main`. No
  `.claude/worktrees/` entries.
- `git branch -a` / `git branch -vv`: local `main` only, tracking `origin/main` at `0 0` — exact
  match (`git log -1 origin/main` also reads `c9065cfa`).
- `git branch -r`: only `origin/main` survives — no leftover merged-PR remote branch for
  `campaign_close.py` to act on this firing.
- `gh pr list --state open`: **0 open PRs.**
- `gh pr list --state all` (most recent ~30): a large run of MERGED PRs since the prior firing
  (#1660, #1659, #1658, #1657, #1656, #1655, #1654, #1652, #1651, #1650, #1649, #1648, #1636,
  #1635, #1630, plus the earlier #1626/#1625/#1622/#1621/#1617 already accounted for) — all from
  this session's build-leader batch; none left a remote branch behind (GitHub's own
  auto-delete-on-merge, confirmed via the fetch-prune list above).
- `primary_checkout_check.py /Users/kimba/Projects/nonoun/agent-ui`: `clean · main`, exit 0 — no
  off-main-primary risk.
- `npm run ops:reap-branches -- --dry`: "no local branches beyond main" — 0 fail / 0 warn, 0 rows.
- `npm run ops:reap-worktrees -- --dry`: "no worktrees under .claude/worktrees/" — 0 fail / 0
  warn, 0 rows. (Caveat, not blocking this firing since both dry runs found nothing to apply:
  neither `CLAUDE.md` nor `README.md` names these two npm scripts directly — they're documented
  only via `package.json`'s own script names and this seat's own prior-firing precedent. Worth a
  human confirming the naming convention clean-git's Scope clause requires before a future firing
  ever reaches a non-empty `--apply` case on their say-so alone.)
- `gitignore_check.py`: same standing 6 stale G1 rules as every recent firing (`npm-debug.log*`,
  `*.local`, `.vscode/*`, `.idea`, `*.sw?`, `.claude/docs/other`) — resolved, standing-accepted
  per issue #1609 (closed, superseded by the 2026-08-09 keep-list ruling); not re-flagged.
- Ticket-claim convention: this repo's own ADR-0005 is "Lazy-property upgrade precedence:
  property-wins" — confirmed unrelated to ticket-claim (read directly,
  `.claude/docs/adr/0005-lazy-upgrade-property-wins.md`). No host-repo-numbered ADR names a
  ticket-claim convention here. Stale-claim gate degrades to git-surface-only hygiene per
  clean-git's own scope clause.
- Open issues (`gh issue list --state open`): exactly **2**.
  - #1634 ("Harden /status probe to detect an invalid API key, not just a missing one") —
    assignee `kimgranlund`, label `in-flight`, claimed by build-1634 at `2026-08-26T01:10:41Z`
    (~47 min before this firing). This matches dispatch's own note: build-1634 is the one live
    seat. Its scratch clone (`/var/folders/.../T/agent-ui-1634`) independently confirms
    in-progress work — `git status` inside it shows 4 modified, uncommitted files
    (`anthropic.ts`, two `.test.ts` files, `dev-proxy-plugin.ts`). Fresh claim, live work —
    correctly excluded from every reap proposal below.
  - #1653 ("ADR-0023(c) plan-approval write-gate inconsistently enforced...") — label `bug` +
    `needs-ruling`, **no assignee**, not a claim (no "Claimed by" comment) — Kim's own comment
    recommends downgrading it to a design question or closing it working-as-designed. This is a
    decision-watcher/ruling-shaped finding, not a git-surface finding — read as data under survey
    only, not actioned or further triaged here.
- Dirty primary: only `.claude/ops/sweep-in-flight.json` untracked, matching this firing's own
  dispatching-session marker — not cruft. `sync_main.py` not applicable regardless: `main` already
  reads `0 0` against `origin/main`.
- **New finding — orphaned scratch clones** (per dispatch's own note; not worktrees, so
  `git worktree list` never saw them): 33 plain `git clone` directories under
  `/var/folders/0b/jf4lh4jd4sd9y2q7x271c9jm0000gn/T/agent-ui-<ticket>[-suffix]`, totaling ~6.4G.
  Cross-checked every ticket number against `gh pr list`/`gh issue view`:
  - **32 are stale residue with no live claim** — either their ticket's PR is independently
    verified `MERGED` (agent-ui-1580, 1581, 1584, 1599, 1600, 1600-3j6zz76s, 1600-inpfobq0, 1604,
    1608, 1611, 1618, 1620, 1623, 1624, 1627, 1631, 1632, 1633, 1637, 1639, 1640, 1641, 1642,
    1643, 1644, 1645, 1646, 1647 — 29 dirs) or the ticket's issue is independently confirmed
    `CLOSED COMPLETED` with no code change warranted (agent-ui-1221, 1609, 1628, 1629 — 4 dirs,
    closed as "no fix warranted"/"superseded"/ruling-resolved). No gated script in this plugin
    covers removing an arbitrary scratch-clone directory (`campaign_close.py` only ever touches a
    verified-merged PR's **remote** branch; the two `npm run ops:reap-*` scripts only ever cover
    `.claude/worktrees/` entries and local branches) — this is a real gap in what's gated, not an
    oversight in this firing's read. Per dispatch's own note, several of these were already
    attempted for removal by their own build-leader session and reported back
    "sandbox denied" — they're standing residue, not fresh.
  - **1 is live** — agent-ui-1634, the in-flight seat above. Explicitly excluded from the
    proposal below.

## Executed
- None. No merged PR left a remote branch behind this firing (nothing for `campaign_close.py`),
  `main` was already fast-forward-current (nothing for `sync_main.py`, and this is a scheduled
  sweep firing regardless), and both this repo's own gated reap scripts' dry runs classified zero
  rows as reapable (nothing for `--apply`).

## Proposed-but-not-executed
- **32 orphaned scratch-clone directories, ~6.4G** — already resolved out-of-band by the
  dispatching host before this compute pass: independently verified against each ticket's merged
  PR or closed-no-fix-warranted state (matching this seat's own read exactly) and removed via a
  plain host-level `rm -rf`, keeping only the one live clone (`agent-ui-1634`). Confirmed absent
  this compute pass. A tooling gap ticket was filed separately (#1661) for a real gated
  reap-scratch-clones script so this class doesn't require manual sweep-triggered cleanup again.
- No stale-open PRs, no orphaned worktrees, no stale local branches, no stale-claim findings, no
  off-main-primary findings this firing.

## Risks
- 🟢 Primary checkout correctly stays on `main`, exactly current with `origin/main` (0 ahead/0
  behind).
- 🟢 Zero open PRs, zero stale/orphaned worktrees, zero stale local branches, zero stale ticket
  claims.
- 🟢 No mutating script was refused or overridden this firing; both reap scripts' dry runs found
  nothing to `--apply`; `campaign_close.py` had no leftover remote branch to act on (all 10
  newly-pruned remote refs were already gone via GitHub's own auto-delete-on-merge).
- 🟢 32 orphaned scratch-clone directories (~6.4G) resolved out-of-band this compute pass (see
  Proposed-but-not-executed); tooling gap ticketed as #1661.
- 🟢 One live in-progress ticket claim (#1634, build-1634, /status probe hardening) — fresh
  (~47m old), correctly excluded from every reap proposal per dispatch's own note; its scratch
  clone shows real uncommitted work, confirming it's genuinely live, not abandoned.
- 🟡 The `ops:reap-branches`/`ops:reap-worktrees` scripts are invoked by this seat on prior-firing
  precedent, but neither is actually named in this repo's own `CLAUDE.md` or `README.md` today —
  harmless this firing (both dry runs returned zero rows), but worth confirming/documenting
  before a future firing trusts their `--apply` gate on a non-empty dry-run result.
