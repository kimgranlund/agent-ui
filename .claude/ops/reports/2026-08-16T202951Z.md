# repo-cleaner sweep — agent-ui — 2026-08-16T202951Z

**Status: 🟢 done.** Classification set differs from the most recent report on disk
(`.claude/ops/reports/2026-08-12T181116Z.md`) — count resets to 1. Worktree topology collapsed from
4 (2 live agent stubs + 1 sweep worktree + main) down to just the main checkout; the two remote
branches that report closed via `campaign_close.py` (#789, #790) are gone as expected, and no new
merged-PR branch survivors have appeared since. Open-issue count grew 5 → 23 (all still unassigned,
none stale-claim). Nothing required execution this firing.

## Inventory

- **Worktrees** (`git worktree list`), 1: main checkout only — `076790d3`, branch `main`.
- **Local branches** (`git branch -vv`), 1: `main` @ `076790d3`, tracking `origin/main`, `+0 -0`.
- **Remote branches** (`git branch -r`), 1: `origin/main`. No merged-but-undeleted remote branches survive.
- **PRs**: 0 open, confirmed. Full history MERGED/CLOSED; no head branches remain on `origin`.
- **Stashes**: none.
- **Working tree**: one dirty file, `.claude/skills/doc-standards/references/status-dialects.md` — left untouched per dispatch brief; `sync_main.py` NOT run.
- **`.gitignore`** (`gitignore_check.py`): exit 0, 7 `G1` warnings, unchanged — standing Kim-ruled noise (`.claude/ops/rulings.md`), never re-proposed.
- **Open issues**: 23 total, all `"assignees":[]` — no stale-claim candidates. ADR-0005 claim-staleness gate: not applicable (this repo's ADR-0005 is `lazy-upgrade-property-wins`).
- **Host-repo reap script check**: none named in `package.json`/`README.md`/`CLAUDE.md`. Local-branch/worktree cleanup stays propose-only (moot — no orphans).

## Classification

| Item | Classification |
|---|---|
| Main checkout | healthy — at `origin/main` tip, one known dirty file left per brief |
| Local branches (1: `main`) | healthy — no orphans |
| Remote branches (1: `origin/main`) | healthy — no merged-PR survivors |
| PRs (all MERGED/CLOSED history) | healthy — nothing to close |
| Open issues (23, all unassigned) | healthy-open — no stale-claim, no stale-open flagged |
| `.gitignore` | 7 stale `G1` rules, standing Kim-ruled noise, exit 0 |
| Dirty file `status-dialects.md` | left alone per dispatch instruction |

## Executed — evidence

- `campaign_close.py` — not invoked (no surviving merged-PR remote branch).
- `sync_main.py` — not run, per explicit dispatch instruction.
- No host-repo gated reap script exists.

## Proposed-but-not-executed

- `.gitignore` 7 `G1` warnings — standing carried noise, not re-proposed.
- Nothing else.

## Risks

- 🟢 Zero merged-PR branch survivors; no orphaned local branches/worktrees; `.gitignore` stable; dirty-file scope held.
- 🟡 Worktree topology changed 4 → 1 via other sessions — point-in-time read.

## Open questions

None.
