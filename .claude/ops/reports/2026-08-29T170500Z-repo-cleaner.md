Sweep firing 2026-08-29T17:05:00Z — repo-cleaner report for /Users/kimba/Projects/nonoun/agent-ui.

Full report, not abbreviated — genuinely new/changed findings versus the prior report
(`2026-08-28T034000Z-repo-cleaner.md`): (1) PR #1700 opened (docs/agent-model-reference, planner
seat, live); (2) three new open issues — #1699, #1701 (both claimed, in-flight, backing this
firing's two live scratch clones), #1702 (unclaimed); (3) one live scratch clone
(`agent-ui-1701`) now discoverable by `reap-scratch-clones.mjs`'s own scan and correctly
classified `KEEP(dirty)`.

## Inventory
- `git fetch --prune`: exit 0. Pruned 1 stale `origin/*` ref
  (`origin/1692-adr-0057-0058-0059`) — that PR's remote branch was already deleted by GitHub's
  own merge-time cleanup; local tracking ref was stale until this fetch. Survey below is against
  fresh refs.
- Worktrees (`git worktree list`, 3 total, matches dispatch's stated expectation):
  - primary `/Users/kimba/Projects/nonoun/agent-ui`, HEAD `9d2075df`, branch `main`, exact parity
    with `origin/main`.
  - `.claude/worktrees/agent-ui-planner`, HEAD `9900bdd2`, branch `docs/agent-model-reference` —
    PR #1700 OPEN, active seat per dispatch context. NOT proposed for reap.
  - `.claude/worktrees/agent-ui-reviewer`, HEAD `ac6b80f2`, branch
    `reviewer-worktree-2026-08-26` — standing fleet-seat infrastructure, confirmed `KEEP(fleet-seat)`
    again this firing. NOT proposed for reap.
- `git branch -vv`: `main` (tracking `origin/main`, exact parity), `docs/agent-model-reference`
  (checked out in the planner worktree), `reviewer-worktree-2026-08-26` (checked out in the
  reviewer worktree). No other local branches, no drifted/orphaned local branch.
- `git branch -r`: `origin/main`, `origin/docs/agent-model-reference` (backs open PR #1700) —
  nothing else. Zero leftover merged-PR remote branches; nothing for `campaign_close.py` this
  firing.
- `gh pr list --state all` (recent window): 1 open — #1700 (docs/agent-model-reference, opened
  2026-08-29T16:31:54Z, ~34 min old at firing time) — fresh, not stale-open, matches dispatch's
  live-planner-seat context. Newest merged: #1698 (docs(goals) G4/G5, 2026-08-28T19:09:45Z),
  remote branch already gone (GitHub's own merge-time cleanup) — nothing for `campaign_close.py`.
- `primary_checkout_check.py /Users/kimba/Projects/nonoun/agent-ui`: `clean`, exit 0 — no
  off-main-primary risk. HEAD (`9d2075df`) matches `origin/main` exactly.
- `npm run ops:reap-branches -- --dry`: 2 rows — `docs/agent-model-reference` and
  `reviewer-worktree-2026-08-26`, both `KEPT` (checked out in their respective worktrees) — 0
  fail / 0 warn. Nothing to `--apply`.
- `npm run ops:reap-worktrees -- --dry`: 2 rows — `agent-ui-planner`/`docs/agent-model-reference`
  and `agent-ui-reviewer`/`reviewer-worktree-2026-08-26`, both `KEEP(fleet-seat)` per
  `.claude/ops/fleet.json` `live_state.joined` — 0 fail / 2 warn (informational, not a conflict).
  Matches dispatch's stated expectation exactly.
- `npm run ops:reap-scratch-clones -- --dry`: 1 row —
  `/private/tmp/.../e812fe31-.../scratchpad/agent-ui-1701` classified `KEEP(dirty)` (`git status
  --porcelain` non-empty) — 0 fail / 1 warn. Matches dispatch's "do not touch" instruction for
  build-1701 (#1701). The second live clone dispatch named (#1699, build-agent-record-rename) was
  NOT found by this script's own documented scan roots (`/tmp`, `$TMPDIR`, the
  `$CLAUDE_SCRATCHPAD`-shaped walk, the sibling agent-ui session's own scratchpad — all checked by
  hand: no `agent-ui-1699`-shaped directory anywhere reachable). Nothing for this script to
  classify or touch for that clone either way — flagged as a survey gap, not a finding requiring
  action.
- `gitignore_check.py /Users/kimba/Projects/nonoun/agent-ui`: same standing 6 stale G1 rules as
  every recent firing (`npm-debug.log*`, `*.local`, `.vscode/*`, `.idea`, `*.sw?`,
  `.claude/docs/other`) — standing-accepted, not re-flagged.
- Ticket-claim convention: unchanged — no host-repo-numbered ADR names a ticket-claim convention
  in this repo (checked; ADR-0042 here is `UIIndicatorElement`, unrelated). Stale-claim gate
  degrades to git-surface-only hygiene; the read below is informational only.
- Open issues (`gh issue list --state open`): 3.
  - #1699 ("Rename PersonaRecord/PersonaRoster*..."), claimed via explicit "Claim:" comment
    (`build-agent-record-rename`, branch `refactor/1699-agent-record-rename`,
    2026-08-29T16:29:08Z, ~36 min old) — fresh, not stale, backs the dispatch-named scratch clone.
  - #1701 ("devtools: accept both Task and Agent..."), claimed via explicit "Claim:" comment
    (`build-1701`, branch `devtools/1701-task-agent-tool-name`, 2026-08-29T16:42:30Z, ~23 min
    old) — fresh, not stale, backs the confirmed `agent-ui-1701` scratch clone above.
  - #1702 ("Stale seam comment: agent-admin-schema.ts..."), opened 2026-08-29T16:42:35Z (~23 min
    old), unassigned, 0 comments — unclaimed, not a stale-claim (nothing to be stale yet). A
    triage/intake decision, out of this seat's scope (routes to `issue-sorter` if action is
    wanted, not proposed here).
- Dirty primary (`git status --porcelain`): empty — clean. `sync_main.py` not applicable
  regardless: `main` is at exact parity with `origin/main` and this is a scheduled sweep firing
  (never appropriate for `sync_main.py`).

## Executed
- Nothing. No merged PR left a remote branch behind (GitHub's own PR-merge branch deletion
  already handled everything since the last firing — nothing for `campaign_close.py`), `main` is
  at exact parity with `origin/main` and this is a scheduled firing regardless (nothing for
  `sync_main.py`), `reap-branches --dry` found 0 reapable rows, `reap-worktrees --dry` found 0
  `REAP` rows, `reap-scratch-clones --dry` found 0 `REAP` rows (1 `KEEP(dirty)` row only).

## Proposed-but-not-executed
- No stale-open PRs, no orphaned worktrees, no stale local branches, no off-main-primary findings
  this firing.
- No stale-claim findings: #1699 and #1701 are freshly claimed (both under 40 minutes old at
  firing time) with explicit claim comments; #1702 is open but unclaimed — nothing to propose
  reclaiming for any of the three. Filing/triaging or acting on #1702 itself is out of this
  seat's scope (`issue-sorter`'s territory, not acted on here).

## Risks
- Primary checkout correctly stays on `main`, exact parity with `origin/main` (`9d2075df`).
- One open PR (#1700), fresh, backing the live planner seat — nothing for `campaign_close.py`.
- Both fleet-seat worktrees (`agent-ui-planner`, `agent-ui-reviewer`) still correctly
  `KEEP(fleet-seat)`, not `REAP` — re-confirmed independently this firing rather than assumed
  from dispatch context or the prior report.
- The one discoverable live scratch clone (`agent-ui-1701`, #1701) correctly `KEEP(dirty)`,
  not reaped.
- The second live scratch clone dispatch named (#1699) sits outside `reap-scratch-clones.mjs`'s
  own documented scan roots — not a hygiene defect (nothing reapable is being missed; there's
  nothing there to reap either way), but worth a human's awareness that this script's silence for
  a specific clone isn't proof of absence.
- Standing G1 gitignore warnings (6 rules) unchanged, not re-flagged.

Consecutive-unchanged-firing count: reset to 0 (this firing) — PR #1700, issues #1699/#1701/#1702,
and the newly-discoverable `agent-ui-1701` scratch clone all changed relative to
`2026-08-28T034000Z-repo-cleaner.md`.
