# repo-cleaner sweep — agent-ui — 2026-08-09T14:55:12Z

**Status: 🟢 done.** This firing's classification set differs from the most recent prior report
(`.claude/ops/reports/2026-08-09T131234Z.md`) — count resets to 1 (genuinely new/changed findings).
Changes since that report: PR #636 merged (design/633-agent-authoring-intake, squash of `5883a55`
+ `417f9d9`); its remote branch already gone at merge time (`campaign_close.py 636` clean no-op);
its LOCAL worktree/branch (`agent-a76f2b0def232c02f`) now confirmed fully merged (zero unique diff
vs. main) and reapable — a new finding, since a prior dispatch had flagged this worktree as
"known-live." `.gitignore` warnings dropped from 17 to 8 (PR #632's trim landed). Issue #634 filed
new since last report.

## Prior-claim correction (verify-before-trusting)

The dispatching brief carried a strong prior: "the `agent-a76f2b0def232c02f` worktree and the
`design/633-agent-authoring-intake` branch — both belong to the LIVE, currently-open PR #636...
awaiting Kim's review." Verified live rather than taken on faith:

- `gh pr view 636`: `state: MERGED`, `mergedAt: 2026-08-09T14:50:17Z` — merged during this sweep's
  own window, not open.
- Remote branch `design/633-agent-authoring-intake`: already absent (`git fetch --prune` showed
  `[deleted] -> origin/design/633-agent-authoring-intake`; `campaign_close.py 636`'s C2 independently
  confirmed absent before this run).
- Local worktree `.claude/worktrees/agent-a76f2b0def232c02f` (branch
  `worktree-agent-a76f2b0def232c02f`, note: DIFFERENT name than the PR's own head ref
  `design/633-agent-authoring-intake` — a separately-created local worktree tracking the same
  design work, not a checkout of the PR branch itself): `git status` inside it is clean, and
  `git diff main worktree-agent-a76f2b0def232c02f` shows ZERO unique content — every file
  difference is main having moved AHEAD (later merges #625/#629/#632/#635 postdate this branch's
  fork point), none behind. Both of the branch's own commits (`5883a55`, `417f9d9`, the second a
  "review minors" follow-up predating the PR's merge by ~5 minutes) are content-identical to what
  landed in main's squash commit `70c4b3a`.

**Conclusion: this worktree is now merged-and-stale, not live.** The dispatch's prior was accurate
at the time it was written but went stale during this sweep's own concurrent-activity window — the
caution about mid-merge concurrent seats in the dispatch brief was well-founded; this is exactly
that case, caught by verifying rather than trusting.

## Inventory

- **Worktrees**: two — the main checkout (`70c4b3a`, on `main`, clean, tracking `origin/main`
  exactly) and `.claude/worktrees/agent-a76f2b0def232c02f` (`417f9d9`, clean, fully merged per
  above).
- **Local branches**: `main` (healthy) and `worktree-agent-a76f2b0def232c02f` (merged-stale, see
  above).
- **Remote branches** (post-`fetch --prune`): `origin/main` only. Pre-prune, three stale local
  refs were pruned this firing: `origin/chore/visual-baselines-gitignore`,
  `origin/design/633-agent-authoring-intake`, `origin/task/628-typography-ink` — all three
  correspond to PRs (#632, #636, #635) that merged with correctly-working remote-side deletes; the
  local refs were simply unpruned in this checkout until now. Per the #613 ruling
  (`.claude/ops/rulings.md`), none of these are real survivors of the #613 failure class.
- **PRs** (`gh pr list --state all`, 200 total): 199 MERGED, 1 CLOSED-unmerged (#447, old,
  no branch remnant), **0 OPEN**.
- **Open issues** (`gh issue list --state open`): 3 — #633, #634, #616, all `assignees: []`.
  #633 carries `doing` (active multi-slice family, just advanced by #636's merge, not stale).
  #634 is fresh (filed/updated today). #616 unchanged from every prior sweep. No ADR-0005
  claim-staleness check applies to this repo (its ADR-0005 is unrelated — lazy-upgrade-property-
  wins — no assignee-based claim convention in use).
- **`.gitignore`**: 8 pre-existing `G1` stale-rule warnings (down from 17 in the prior report —
  PR #632's verify-then-trim landed since), exit 0, no `G2` FAIL.

## Classification

| Item | Classification |
|---|---|
| Worktree/branch `agent-a76f2b0def232c02f` / `worktree-agent-a76f2b0def232c02f` | **merged-and-stale** (corrects a stale "live" prior) — propose reap, no gated script covers local worktree/branch removal in this repo |
| Main checkout | healthy |
| Remote branches | healthy — `origin/main` only post-prune, three local-ref-staleness non-survivors (see #613 ruling) |
| PR #636 | merged-and-verified — `campaign_close.py 636` clean (C1 ok, C2 ok/no-op) |
| Open PRs | none |
| PR #447 (old CLOSED) | historical, no remnant, no action |
| Issue #633 | healthy-open, active — not a stale claim (no assignee, PR just landed) |
| Issue #634 | healthy-open, fresh |
| Issue #616 | healthy-open, unchanged |
| `.gitignore` | 8 stale `G1` rules, unchanged proposal carried forward, exit 0 |

## Executed — evidence

**`campaign_close.py 636`**:
```
campaign_close · clean
  ok   C1  PR merged
  ok   C2  design/633-agent-authoring-intake already absent from remote before this run
EXIT:0
```
Nothing else executed: no other merged PR had a surviving remote branch; `main` is clean so
`sync_main.py` was not needed; no host-repo reap script exists (`package.json`/`CLAUDE.md`/
`README.md` name none) so the merged-stale worktree/branch stays propose-only despite being safe
to remove.

## `gitignore_check.py` — read-only, exit 0

```
gitignore_check · warn · /Users/kimba/Projects/nonoun/agent-ui
  WARN  G1  `*.log` matches nothing in the tree -> stale, retire it
  WARN  G1  `npm-debug.log*` matches nothing in the tree -> stale, retire it
  WARN  G1  `*.local` matches nothing in the tree -> stale, retire it
  WARN  G1  `.vscode/*` matches nothing in the tree -> stale, retire it
  WARN  G1  `.idea` matches nothing in the tree -> stale, retire it
  WARN  G1  `*.sw?` matches nothing in the tree -> stale, retire it
  WARN  G1  `.claude/docs/other` matches nothing in the tree -> stale, retire it
  WARN  G1  `.vitest-attachments/` matches nothing in the tree -> stale, retire it
EXIT:0
```
Down from 17 warnings in the prior report (PR #632's verify-then-trim already retired 9; one new
warning, `.vitest-attachments/`, appeared — also stale, matches nothing in the tree).

## Claimed-ticket sweep (ADR-0005 gate)

Not applicable, reconfirmed: this repo's ADR-0005 is `0005-lazy-upgrade-property-wins.md`,
unrelated to ticket-claim staleness. TICKET tier is a frozen archive (ADR-0145); work items route
to GitHub Issues with no assignee-based claim convention (#633/#634/#616 all `"assignees":[]`).
Degrades to git-surface-only hygiene per this agent's own scope rule.

## Proposed-but-not-executed

- **Reap the merged-stale worktree**: `git worktree remove .claude/worktrees/agent-a76f2b0def232c02f`
  then `git branch -d worktree-agent-a76f2b0def232c02f` — safe (zero unique content vs. main,
  clean working tree, fully landed via PR #636's squash merge), but no host-repo gated reap script
  covers local worktree/branch removal in this repo, so this stays a plan for a human/gated tool.
- Retire the 8 stale `G1` `.gitignore` lines flagged above (carried forward,
  `.claude/ops/plan.md` entry 4.2, reduced scope since PR #632).
- Nothing else — no other worktree, local branch, or PR requires cleanup; no stale claim exists.

## Risks

- 🟢 Zero orphaned/live-contested worktrees after correction. Zero open PRs. `main` clean.
  `.gitignore` warn-only, shrinking trend (17→8).
- 🟡 The dispatch-time "known-live" prior for `agent-a76f2b0def232c02f` went stale within this
  sweep's own window — confirms the dispatch brief's own concurrent-activity caution was
  warranted; every prior carried into a sweep needs live re-verification, not inheritance.
- 🟢 #613 (worktree-held-branch merge rule) stayed CLOSED — no new survivor this firing, three
  apparent pre-prune stragglers all traced to local-ref staleness, not real remote survivors.
