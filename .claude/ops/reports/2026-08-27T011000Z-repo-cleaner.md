Sweep firing 2026-08-27T01:10:00Z — repo-cleaner report for /Users/kimba/Projects/nonoun/agent-ui.

Findings differ from the prior report (`2026-08-26T220500Z-repo-cleaner.md`) — full report, not
abbreviated. Every git-surface finding (worktrees, branches, remote branches, PRs, the
reap-worktrees conflict, gitignore warnings) is byte-for-byte unchanged from that firing. The one
genuinely new item: two new open, unclaimed issues now exist (#1680, #1681) versus zero open
issues last firing — that alone resets the unchanged-count to 0 / this-is-firing-1, per step 5's
"a genuinely new or changed finding always gets the full report" rule. Context for this firing
(per dispatch): package v0.1.0 shipped and tagged since the last firing (expected, not drift); the
incidental `reap-scratch-clones.mjs` selftest fix (commit `9a62617f`) is already on `main`'s HEAD.

## Inventory
- `git fetch --prune`: exit 0, no output — nothing to prune; `origin/*` tracking refs already
  matched ground truth (the 5 stale refs a prior firing pruned stay pruned). Survey is against
  fresh refs, not stale ones.
- Worktrees (`git worktree list`, 2 total, matches dispatch's stated expectation):
  - primary `/Users/kimba/Projects/nonoun/agent-ui`, HEAD `9a62617f`, branch `main`.
  - `/Users/kimba/Projects/nonoun/agent-ui/.claude/worktrees/agent-ui-reviewer`, HEAD `ac6b80f2`,
    branch `reviewer-worktree-2026-08-26` — the standing reviewer seat's home. NOT proposed for
    reap, per explicit dispatch context and issue #1680 (see below).
- `git branch -vv`: `main` (tracking `origin/main`, exact parity, `9a62617f`) and
  `reviewer-worktree-2026-08-26` (checked out in the reviewer worktree). No other local branches.
- `git branch -r`: only `origin/HEAD -> origin/main` and `origin/main` — zero leftover merged-PR
  remote branches. Nothing for `campaign_close.py` to act on this firing.
- `gh pr list --state open`: **0 open PRs.**
- `gh pr list --state all`: newest merged is still #1679 (17:52Z 2026-08-26) — no new PR merged
  since the prior firing (22:05Z 2026-08-26). No new remote-branch cleanup surface.
- `primary_checkout_check.py /Users/kimba/Projects/nonoun/agent-ui`: `clean · /Users/kimba/...`,
  exit 0 — no off-main-primary risk.
- `npm run ops:reap-branches -- --dry` (host-repo-named gated script, CLAUDE.md): 1 row —
  `reviewer-worktree-2026-08-26` classified `KEPT` ("checked out in .../agent-ui-reviewer") — 0
  fail / 0 warn. Nothing to `--apply`.
- `npm run ops:reap-worktrees -- --dry`: 1 row — `.claude/worktrees/agent-ui-reviewer` /
  `reviewer-worktree-2026-08-26` classified **`REAP`** ("git cherry origin/main clean, 0 commits
  equivalent/none") — same mechanical/intent conflict as every prior firing since fleet-bootstrap.
  **Not executed.** This gap is now filed as **agent-ui#1680** (per dispatch context) — not
  re-flagged as a new finding here, held exactly as instructed.
- `npm run ops:reap-scratch-clones -- --dry`: "(no scratch-clone directories found)" — 0 fail / 0
  warn. Nothing to `--execute`.
- `gitignore_check.py /Users/kimba/Projects/nonoun/agent-ui`: same standing 6 stale G1 rules as
  every recent firing (`npm-debug.log*`, `*.local`, `.vscode/*`, `.idea`, `*.sw?`,
  `.claude/docs/other`) — standing-accepted, not re-flagged.
- Ticket-claim convention: unchanged — no host-repo-numbered ADR names a ticket-claim convention
  here. Stale-claim gate degrades to git-surface-only hygiene.
- Open issues (`gh issue list --state open`): **2** — matches dispatch's stated expectation.
  - #1680 "reap-worktrees.mjs can't tell a standing fleet-seat worktree from a finished campaign
    worktree" — unassigned, `size:small task`, updated 2026-08-26T22:50:55Z. This is the reap-
    worktrees classification gap named above, already filed — no re-flag, no reclaim action (it
    was never claimed).
  - #1681 "ADR-0140 rename sweep: 24 pre-2026-07-18 ADRs still name stale --ui-* foundation
    tokens" — unassigned, `size:big task`, updated 2026-08-26T23:03:12Z. Unclaimed, freshly filed
    the same day — not a stale-claim finding (no claim exists to go stale). No action for this
    seat; work-item triage on either issue routes to `issue-sorter`, not here.
- Dirty primary (`git status --porcelain`): only `?? .claude/ops/sweep-in-flight.json` — this
  firing's own dispatching-session marker, dirty-but-intentional, per dispatch context.
  `sync_main.py` not applicable regardless: `main` is at exact parity with `origin/main` and this
  is a scheduled sweep firing (never appropriate for `sync_main.py` even with unexplained dirt).

## Executed
- Nothing this firing. No merged PR left a remote branch behind (nothing for `campaign_close.py`),
  `main` is at exact parity with `origin/main` and this is a scheduled firing regardless (nothing
  for `sync_main.py`), `reap-branches --dry` found 0 reapable rows, `reap-scratch-clones --dry`
  found zero scratch-clone directories, and `reap-worktrees --dry`'s 1 `REAP` row is the known
  #1680 conflict — held, not applied.

## Proposed-but-not-executed
- No stale-open PRs, no orphaned worktrees, no stale local branches, no off-main-primary findings
  this firing.
- No stale-claim findings: both open issues (#1680, #1681) are unassigned — nothing claimed to go
  stale.
- The reviewer worktree/branch reap conflict: still held, no action proposed either way — tracked
  by #1680, not re-proposed as a fresh finding here.

## Risks
- 🟢 Primary checkout correctly stays on `main`, exact parity with `origin/main`.
- 🟢 Zero open PRs; zero leftover merged-PR remote branches — nothing for `campaign_close.py`.
- 🟡 `reap-worktrees.mjs`'s own dry-run gate still reads the standing reviewer worktree as `REAP`.
  Not executed, not re-proposed — tracked by agent-ui#1680, unclaimed as of this firing.
- 🟢 v0.1.0 package release (all 8 packages, tag pushed) and the `reap-scratch-clones.mjs`
  selftest fix (`9a62617f`) are both accounted for as expected state, not drift.
- 🟢 Two new open issues (#1680, #1681) exist since the prior firing, both unassigned/unclaimed —
  informational only; no stale-claim risk, no action for this seat.
- 🟢 Standing G1 gitignore warnings (6 rules) unchanged, not re-flagged.
- 🟢 `.claude/ops/sweep-in-flight.json` untracked-but-intentional (this firing's own marker) — not
  drift.

Consecutive-unchanged-firing count: reset to 0 (this firing) — the two new open issues are a
genuinely new finding relative to `2026-08-26T220500Z-repo-cleaner.md`, which read zero open
issues.
