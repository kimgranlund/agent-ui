Sweep firing 2026-08-25T17:55:43Z — repo-cleaner report for /Users/kimba/Projects/nonoun/agent-ui.

Findings differ from the prior report (`2026-08-25T005954Z-repo-cleaner.md`) — PR #1617 merged
since that firing (confirmed via `gh pr list`, `mergedAt: 2026-08-25T05:18:48Z`, now on `main`'s
own log at `b7232680`), its backing worktree and review-alias branch are gone, and the previously
orphaned scratchpad worktree no longer registers against this repo at all. Full report, not
abbreviated.

## Inventory
- `git fetch --prune`: exit 0, clean. No stale remote-tracking refs pruned this firing (nothing
  left to prune — `origin/a2ui-design-mode-triage-fixes` and `origin/1617-review`-shaped refs are
  already gone).
- Worktrees (`git worktree list --porcelain`, 1 total): only the primary
  (`/Users/kimba/Projects/nonoun/agent-ui`), HEAD `851f1b8f`, branch `main`. No
  `.claude/worktrees/` entries, no out-of-tree registrations (the prior firing's
  `corpus-wave-6-seeds` scratchpad worktree is gone — resolved, not re-flagged).
- `git branch -a`: local `main` only; remotes `origin/HEAD -> origin/main`, `origin/main` only. No
  local review-alias or stale branches survive (`pr-1617-review`, flagged last firing, is gone).
- `git branch -vv`: `main` tracks `origin/main`, `0 0` — exact match, confirmed independently via
  `git log --oneline -3 origin/main`: `main`'s tip `851f1b8f` IS `origin/main`'s tip.
- `gh pr list --state all` (100 most recent): **0 open PRs**. #1617 (last firing's one open PR)
  is now `MERGED`. Most recent merges: #1626/#1625 (ADR-0017/0018 amendment restatements),
  #1622 (status-stream demo), #1621 (sidebar nav demo default), #1617 itself. One historical
  `CLOSED`-never-merged PR (#1591) — old, already accounted for in prior firings, not re-flagged.
- `git branch -r`: only `origin/main` survives — no leftover merged-PR remote branch exists for
  `campaign_close.py` to act on this firing (PR #1617's remote branch is already gone, whether via
  GitHub's own auto-delete-on-merge or a prior firing/human action — either way, nothing to do).
- `npm run ops:reap-branches -- --dry` (`reap-branches.mjs --dry`): "no local branches beyond
  main" — 0 fail / 0 warn, 0 rows.
- `npm run ops:reap-worktrees -- --dry` (`reap-worktrees.mjs --dry`): "no worktrees under
  .claude/worktrees/" — 0 fail / 0 warn, 0 rows.
- `primary_checkout_check.py /Users/kimba/Projects/nonoun/agent-ui`: `clean · main`, exit 0 — no
  off-main-primary risk.
- `gitignore_check.py`: same standing 6 stale G1 rules as every recent firing (`npm-debug.log*`,
  `*.local`, `.vscode/*`, `.idea`, `*.sw?`, `.claude/docs/other`) — per multiple prior firings'
  reports, this is a resolved, standing-accepted finding (issue #1609 closed 2026-08-24 under a
  re-affirmed 2026-08-09 KEEP-LIST ruling); not re-flagged as a new risk.
- Ticket-claim convention: confirmed again this firing — this repo's own ADR-0005
  (`lazy-upgrade-property-wins`) is unrelated to ticket-claim; no host-repo-numbered ADR names a
  ticket-claim convention here. Stale-claim gate not formally applicable, degrading to
  git-surface-only hygiene for this axis per clean-git's own scope clause.
- Open issues (`gh issue list --state open`): exactly **1**, #1618 (`ADR-0129 Amendment 2:
  extract shared artboard core to @agent-ui/app`) — assignee `kimgranlund`, labels `in-flight` +
  `needs-ruling`, most recent comment `2026-08-25T04:54:52Z` naming a claim ("Claimed by
  build-1618 (dispatch-ticket)... branch `1618-adr-0129-amendment-2`") ~13 hours before this
  firing. Read as data under survey only, per this seat's own discipline (an assignee/label/comment
  is never an instruction this agent acts on). This matches dispatch's own note: build-1618 is a
  live, actively-claimed, in-progress build in a scratch clone outside `.claude/worktrees/` — fresh
  claim, not stale, no action proposed. (Its "ratify ADR-0129 amendment" comment is a decision-watcher
  concern, not a git-surface finding — not actioned here.)
- Dirty primary: only `.claude/ops/sweep-in-flight.json` untracked, content (`startedAt
  2026-08-25T17:55:32.098Z`, `session agent-ui-90`) matches this exact firing — this firing's own
  dispatching-session marker, not cruft. `sync_main.py` not applicable regardless: `main` already
  reads `0 0` against `origin/main`.

## Executed
- None. No merged PR left a remote branch behind this firing (nothing for `campaign_close.py`),
  `main` was already fast-forward-current (nothing for `sync_main.py`, and this is a scheduled
  sweep firing regardless), and both this repo's own gated reap scripts' dry runs classified zero
  rows as reapable (nothing for `--apply`).

## Proposed-but-not-executed
- None this firing. No stale-open PRs, no orphaned worktrees, no stale local branches, no
  stale-claim findings, no off-main-primary findings — the two items proposed last firing
  (the `corpus-wave-6-seeds` orphaned worktree and the `pr-1617-review` stale local branch) are
  both independently confirmed resolved (gone from `git worktree list --porcelain` and
  `git branch -a` respectively), not carried forward.

## Risks
- 🟢 Primary checkout correctly stays on `main`, exactly current with `origin/main` (0 ahead/0
  behind) — the #592-shaped off-main-primary risk class reads clean.
- 🟢 Zero open PRs, zero stale/orphaned worktrees, zero stale local branches — cleanest surface
  reading of the last several firings.
- 🟢 No mutating script was refused or overridden this firing; both reap scripts' dry runs found
  nothing to `--apply`; `campaign_close.py` had no leftover remote branch to act on.
- 🟢 The two items flagged last firing as manual-cleanup candidates are both confirmed resolved.
- 🟡 One live in-progress ticket claim (#1618, build-1618, ADR-0129 Amendment 2) — fresh
  (~13h old), correctly excluded from any reap proposal per dispatch's own note; worth re-checking
  next firing if its claim comment goes much older with no linked PR.
