# repo-cleaner sweep — agent-ui — 2026-08-10T221726Z

**Status: 🟢 done.** Classification set differs from the most recent report on disk
(`.claude/ops/reports/2026-08-10T120602Z.md`) — count resets to 1. Since that firing: the S7
header-rethink campaign advanced (main now at `bc0d16fd`, PRs #690/#693/#697/#699 merged), a new
worktree (`gh-691-authoring-hydration`) and a new local-WIP branch (`feat/agent-admin-header-s7d`)
appeared, issue `#680` (active in the prior report) is now closed, and 8 new issues opened
(#686/#688/#691/#694/#695/#696/#698/#700). A new finding not previously seen: a worktree's git
lock names a dead PID.

## Inventory

- **Worktrees**: 3.
  - Main checkout — `bc0d16fd`, branch `main`, clean, `branch.ab +0 -0` vs `origin/main`.
  - `.claude/worktrees/agent-a3ffdf1acb5c9a5bb` — branch `feat/agent-admin-header-s7d` @
    `89eab8db`, clean working tree, 1 commit ahead of `main`, **not pushed to any remote** (no
    `origin/feat/agent-admin-header-s7d`), no PR open for it. Not locked (no lock line in
    `--porcelain`).
  - `.claude/worktrees/gh-691-authoring-hydration` — branch `worktree-gh-691-authoring-hydration` @
    `dc9b0846`, tracks `origin/worktree-gh-691-authoring-hydration` exactly (`+0 -0`), clean.
    Locked per git's own porcelain output: `locked claude session gh-691-authoring-hydration (pid
    99449 start Mon Aug 10 15:50:18 2026)`. **Independently verified: `ps -p 99449` returns no
    process — the PID is dead.** The lock file's claim of an active session is not evidence per
    this pack's core invariant; re-read, and it doesn't hold. This is a stale lock, not a live
    session — but the branch/PR/issue behind it (below) are all genuinely active on other
    evidence, so this is a metadata-hygiene finding, not a reason to treat the branch as
    reapable.
- **Local branches** (`git branch -vv`), 5:
  - `feat/agent-admin-header-s7d` (`89eab8db`, worktree-held) — see above.
  - `main` (`bc0d16fd`, `[origin/main]`) — healthy.
  - `worktree-agent-a02368e41e2b2641c` (`d5daffe1`) — **unchanged for the 3rd consecutive
    firing**: no worktree, zero unique commits vs `main` (`d5daffe1` confirmed an ancestor).
    Ordinarily reapable, but pid `30537` is still alive (running continuously since Aug 3) with
    its cwd still pointed at the main checkout (unchanged from the prior firing) — same
    unresolved ambiguity about whether that live session holds a stake in this branch name.
    Propose only, same as last two firings.
  - `worktree-agent-a3ffdf1acb5c9a5bb` (`bc0d16fd`) — exact match to `main`'s tip, zero unique
    commits. This is the paired stub branch git creates alongside the worktree directory of the
    same name (same pattern as the now-orphaned `worktree-agent-a02368e41e2b2641c` before its
    worktree vanished) — harmless, healthy, not diverged.
  - `worktree-gh-691-authoring-hydration` (`dc9b0846`, `[origin/worktree-gh-691-authoring-hydration]`)
    — healthy, backs open draft PR #692.
- **Remote branches** (post `git fetch origin --prune`): `origin/main` and
  `origin/worktree-gh-691-authoring-hydration` only. Three tracking refs pruned this firing —
  `origin/chore/regen-sitemap-adr0180` (#690), `origin/design/688-declarative-conversation-composition`
  (#689), `origin/fix/680-picker-ghost-grid-gaps` (closes #680) — all cleanly deleted remote-side
  on merge. Per the `#613` ruling (`.claude/ops/rulings.md`), local-ref staleness after a clean
  merge is not a survivor.
- **PRs** (`gh pr list --state all --limit 1000`): 405 total — 398 MERGED (9 new since the last
  report: #690/#693/#697/#699 confirmed by title, plus others not individually re-verified beyond
  the count), 6 historical CLOSED-unmerged (`#447`, `#154`, `#23`, `#18`, `#17`, `#16` — identical
  set to every prior sweep, unchanged), 1 OPEN — `#692` (draft, `worktree-gh-691-authoring-hydration`,
  linked to issue #691). Zero merged PRs have a surviving remote branch — `campaign_close.py` had
  no valid target this firing.
- **Open issues** (`gh issue list --state open`): 9.
  - `#700` task/size:small, "Flatten agent-admin's shell stack" — updated 2026-08-10T22:09:05Z,
    fresh, healthy-open.
  - `#698` task/size:small, "ui-toggle: WebKit flake" — updated 21:09:22Z, fresh, healthy-open.
  - `#696` bug, "Builder can never replace built-in content" — updated 18:18:28Z, fresh,
    healthy-open.
  - `#695` enhancement/size:big, "Chat-driven UI reaction" — updated 17:22:08Z, fresh,
    healthy-open.
  - `#694` task/size:small, "switch.css cascade-specificity bug" — updated 17:05:45Z, fresh,
    healthy-open.
  - `#691` bug/`doing`, "Builder interview chat does not hydrate agent's config" — updated
    18:19:58Z, 7 comments, linked to open draft PR #692 (`worktree-gh-691-authoring-hydration`) —
    active, healthy.
  - `#688` enhancement/size:big/`doing`, "ui-conversation: declarative composition" — updated
    16:47:43Z; its ADR (#689, "proposed, awaiting ratification") already merged today, but the
    issue itself stays open — consistent with ratification being a separate, human-owned step.
    Healthy-open, active.
  - `#686` task/size:big/`doing`, "agent-admin: header rethink — unified selector/toggle/actions
    bar" — updated 22:12:18Z, **5 minutes before this sweep started**. This is the parent issue
    for the S7-a→d sequence (#693/#697/#699 already merged, S7-d = the unpushed
    `feat/agent-admin-header-s7d` WIP found above). Actively worked, healthy-open.
  - `#616` task/size:small, "Upstream sibling conformance suite" — unchanged since
    2026-08-09T14:27:18Z, external upstream wait (`a2ui-project/a2ui#2150`). Healthy-open, not
    stale.
  - `#680` (open/active in the prior report) is now **CLOSED** (2026-08-10T17:30:22Z), via the
    merged-and-pruned `fix/680-picker-ghost-grid-gaps` branch — resolved.
  - ADR-0005 claim-staleness gate: not applicable, reconfirmed (same finding as every prior
    sweep) — this repo's own ADR-0005 is `lazy-upgrade-property-wins`, unrelated to ticket-claim
    staleness. All 9 open issues carry `"assignees":[]`; `doing` is a plain label, not an
    assignee-based claim convention (ADR-0145: work items route to GitHub Issues, not ADR-0005's
    claim operation). Degrades to git-surface-only hygiene per this agent's own scope rule.
- **`.gitignore`** (`gitignore_check.py`, plugin version 3.1.16): exit 0, 7 `G1` warnings,
  unchanged from every prior report. Standing Kim-ruled noise (`.claude/ops/rulings.md`), never
  re-proposed.
- **Host-repo reap script check**: `package.json`'s `scripts` block and `README.md`/`CLAUDE.md`
  grepped for `reap`/`ops:reap`/`branch-clean`/`ADR-0005` — no gated branch-reap script named by
  this repo. Local-branch/worktree cleanup stays propose-only per this agent's own scope rule.

## Classification

| Item | Classification |
|---|---|
| Main checkout | healthy — clean, at `origin/main` tip |
| Worktree `agent-a3ffdf1acb5c9a5bb` / `feat/agent-admin-header-s7d` | healthy-in-progress — unpushed WIP, linked to actively-worked issue #686, sequential to merged S7-a/b/c |
| Worktree `gh-691-authoring-hydration` / `worktree-gh-691-authoring-hydration` | healthy-active (open draft PR #692, active issue #691) **but** its lock names dead pid 99449 — stale-lock finding, propose-only |
| Local branch `main` | healthy |
| Local branch `worktree-agent-a02368e41e2b2641c` | orphaned, zero unique commits — ambiguous re: live pid 30537 → propose only (unchanged, 3rd firing) |
| Local branch `worktree-agent-a3ffdf1acb5c9a5bb` | healthy — exact match to main, zero unique commits |
| Local branch `worktree-gh-691-authoring-hydration` | healthy — backs open PR #692 |
| Remote branches | healthy — `origin/main` + 1 open-PR branch only; 3 clean post-merge refs pruned |
| PRs | 398 MERGED clean (0 surviving branches); 6 historical CLOSED unchanged; 1 OPEN (#692) healthy-active |
| Issue #680 | now closed — resolved since last report |
| Issues #686/#688/#691/#694/#695/#696/#698/#700 | all healthy-open, all fresh (updated within hours), none stale |
| Issue #616 | healthy-open, unchanged, external upstream wait |
| `.gitignore` | 7 stale `G1` rules, standing Kim-ruled noise, exit 0 |

## Executed — evidence

Nothing executed this firing:
- `campaign_close.py` — no valid target. Read the script directly (plugin 3.1.16): it gates on
  `gh pr view <pr> --json state` reading `MERGED` (C1), then deletes+reverifies the PR's remote
  head branch via a 4-observation/~3.5s poll (C2, the ten-branch 2026-07-16/17 incident's fix),
  then optionally checks a named gate root (C3, warn-only). Post-`prune`, `git branch -r` shows
  only `origin/main` and the one open-PR branch — no merged PR has a surviving remote branch to
  target.
- `sync_main.py` — not run. Read the script directly: it classifies dirty-vs-incoming files (S1),
  quarantines under a named stash (S2), pulls `--ff-only` (S3), and reverifies HEAD==origin/main
  by SHA (S4) — but carries no refusal gate on whether running is appropriate. Moot here regardless
  of dispatch posture: `main`'s working tree is clean (`git status --porcelain=v2 --branch` shows
  no dirty entries), so there is nothing to quarantine.
- No host-repo gated reap script exists (checked `package.json`, `README.md`, `CLAUDE.md`) — local
  branch/worktree cleanup stays propose-only.

## `gitignore_check.py` — read-only, exit 0

```
gitignore_check · warn · /Users/kimba/Projects/nonoun/agent-ui
  WARN  G1  `*.log` matches nothing in the tree -> stale, retire it
  WARN  G1  `npm-debug.log*` matches nothing in the tree -> stale, retire it
  WARN  G1  `*.local` matches nothing in the tree -> stale, retire it
  WARN  G1  `.vscode/*` matches nothing in the tree -> stale, retire it
  WARN  G1  `.idea` matches nothing in the tree -> stale, retire it
  WARN  G1  `*.sw?` matches nothing in the tree -> stale, retire it
  WARN  G1  `.claude/docs/other` matches nothing in the tree -> stale, retire it
EXIT:0
```
Kim-ruled standing noise (`.claude/ops/rulings.md` — "gitignore KEEP-LIST fence is permanent");
never proposed for trimming.

## Claimed-ticket sweep (ADR-0005 gate)

Not applicable, reconfirmed: this repo's own `ADR-0005` is `lazy-upgrade-property-wins`, unrelated
to ticket-claim staleness. Work items route to GitHub Issues (ADR-0145) with a plain `doing` label,
not an assignee-based claim convention (all 9 open issues carry `"assignees":[]`). Degrades to
git-surface-only hygiene per this agent's own scope rule.

## Proposed-but-not-executed

- Delete orphaned local branch `worktree-agent-a02368e41e2b2641c` (zero unique commits, worktree
  already gone, unchanged for 3 consecutive firings) — held back pending confirmation that pid
  30537's still-live session (cwd = main checkout) has no remaining stake in that branch name.
- Investigate the stale lock on the `gh-691-authoring-hydration` worktree: its lock file names pid
  99449, which is confirmed dead. Either that session crashed without cleanup (worth a
  `git worktree unlock` + resumption check) or it's harmless because the branch/PR/issue chain
  behind it is independently healthy on other evidence — no gated script covers worktree
  lock/unlock, so this is a human call, not an execution.
- `.gitignore` 7 `G1` warnings — standing carried noise, explicitly not re-proposed.

## Risks

- 🟡 New this firing: worktree `gh-691-authoring-hydration`'s lock metadata claims a live session
  (pid 99449) that is independently confirmed dead. The branch, its open draft PR #692, and its
  linked issue #691 are all otherwise healthy and active — so no branch-safety risk today, but the
  stale lock itself is worth a direct check (a crashed session, or a session that exited without
  releasing the lock).
- 🟡 Carryover, unchanged: pid 30537 (alive since Aug 3, cwd = main checkout) still creates
  ambiguity around `worktree-agent-a02368e41e2b2641c`'s reapability — 3rd consecutive firing with
  no resolution either way.
- 🟢 Zero open-PR-branch survivors on remote; zero `#613`-class stragglers; `.gitignore` stable at
  7 warnings; issue #680 resolved cleanly since the last sweep.

## Open questions

- Is pid 30537 a live Kim session that should be asked directly whether
  `worktree-agent-a02368e41e2b2641c` is still needed before deletion?
- Is the `gh-691-authoring-hydration` worktree's stale lock (dead pid 99449) from a crashed session
  that should be resumed, or is it safe to `git worktree unlock` given the branch/PR/issue are all
  independently active?
