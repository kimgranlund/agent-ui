Status: 🟢 done

# issue-sorter sweep — 2026-08-22T182833Z

Window: 2026-08-21T11:20:00Z → 2026-08-22T18:28:33Z. Both sources (gh_issues, gh_prs) reachable
and advanced independently; no UNMEASURED sections.

## Discovered this window

Issues (search excludes `backlog`/`roadmap`): #1584, #1583, #1581, #1580 (OPEN), #1578/#1576/#1575
(already CLOSED, touched only by the merges below).

PRs (all MERGED): #1582 (ui-column-chart HTML plot layer, closes #1580 booked repairs), #1579
(ADR-0230 doc), #1577 (chart-family.spec.md repairs, closed #1575).

## Trust check

Every item this window authored by `kimgranlund` — present on `friendlies.json`'s `allow_list`
and matching `policy.roster_confirmed_by`. Zero unknown authors, zero holds triggered.
`held-items.md` unchanged (not touched, no payload). `friendlies.json` unchanged (no new authors;
roster + MCP-offer policy already resolved, left untouched).

## Record-shape completion (mint/resume carve-out — direct `gh` calls, not filesystem writes)

Two already-filed, trusted, TICKET-shaped items were missing a required contract label:

| # | Title | Before | Action |
|---|---|---|---|
| #1584 | [Feature] GenUI B3 — judged pack-idiom eval | `size:big` only | added `feature` |
| #1581 | ui-line-chart RTL category-label clamp off-box | `bug` only | added `severity:minor` |

#1583 and #1580 already carried complete `task`+`size` labels — no action. #1578/#1576/#1575
already fully labeled and closed — no action. Confirmed both edits landed via the returned issue
URLs (not narrated-but-absent).

## Steps 7/8

Not-applicable-this-firing: `roster_confirmed: true` (2026-08-05), `github_mcp_offer: "declined"`
(2026-08-05) — both already resolved in `friendlies.json`'s policy block, not re-evaluated.

## Failure branches checked

- Backend: resolved (Option B, git-native/GitHub, ADR-0002/0003) — no re-guess.
- Discovery: both `gh_issues` and `gh_prs` reachable this firing, checkpoint advanced for both.
- Dedup: no new items found by author outside `friendlies.json` — nothing held.
- Step 8 gate: not applicable (decision already recorded) — silently skipped per the skill's own
  named non-failure branch.

## Done condition

Every item discovered this firing classified/trust-checked (7 issues touched, 3 PRs merged, 0 new
filings, 0 holds needed); both reachable sources' checkpoint entries advanced independently;
report carries every touched state file's full content as target-pathed payload
(`watch-checkpoint.json` only — `friendlies.json` and `held-items.md` untouched this firing, no
payload needed for either).
