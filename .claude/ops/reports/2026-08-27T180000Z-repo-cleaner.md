Sweep firing 2026-08-27T18:00:00Z — repo-cleaner report for /Users/kimba/Projects/nonoun/agent-ui.

Full report, not abbreviated — two genuinely new/changed findings versus the prior report
(`2026-08-27T011000Z-repo-cleaner.md`): (1) the reap-worktrees classification gap tracked by
agent-ui#1680 now reads fixed, and (2) three scratch-clone directories existed this firing where
the dispatch expected zero residue, and were executed-reaped per the gated script's own output.

## Inventory
- `git fetch --prune`: exit 0. Pruned 3 stale `origin/*` refs
  (`origin/1680-reap-worktrees-mjs-can`, `origin/1681-adr-0140-rename-sweep`,
  `origin/1682-adr-0046-orphaned`) — their PRs (#1684, #1685, #1683) merged and GitHub deleted the
  remote branches; local tracking refs were stale until this fetch. Survey below is against fresh
  refs.
- Worktrees (`git worktree list`, 2 total, matches dispatch's stated expectation):
  - primary `/Users/kimba/Projects/nonoun/agent-ui`, HEAD `ad1d457d`, branch `main`.
  - `/Users/kimba/Projects/nonoun/agent-ui/.claude/worktrees/agent-ui-reviewer`, HEAD `ac6b80f2`,
    branch `reviewer-worktree-2026-08-26` — standing fleet-seat infrastructure, per dispatch
    context. NOT proposed for reap.
- `git branch -vv`: `main` (tracking `origin/main`, exact parity, `ad1d457d`) and
  `reviewer-worktree-2026-08-26` (checked out in the reviewer worktree). No other local branches.
- `git branch -r`: only `origin/HEAD -> origin/main` and `origin/main` — zero leftover merged-PR
  remote branches. Nothing for `campaign_close.py` to act on this firing.
- `gh pr list --state open`: **0 open PRs** — matches dispatch's stated expectation.
- `gh pr list --state all`: newest merged is #1685 (05:58:57Z 2026-08-27, "ADR-0140 rename sweep");
  #1684 and #1683 also merged same window (05:40:23Z, 05:31:25Z) — the three build-ticket
  dispatches named in dispatch context. All three remote branches already gone (see fetch-prune
  above); nothing new for `campaign_close.py`.
- `primary_checkout_check.py /Users/kimba/Projects/nonoun/agent-ui`: `clean`, exit 0 — no
  off-main-primary risk. `HEAD` (`ad1d457d`) matches `origin/main` exactly.
- `npm run ops:reap-branches -- --dry`: 1 row — `reviewer-worktree-2026-08-26` classified `KEPT`
  ("checked out in .../agent-ui-reviewer") — 0 fail / 0 warn. Nothing to `--apply`.
- `npm run ops:reap-worktrees -- --dry`: 1 row — `.claude/worktrees/agent-ui-reviewer` /
  `reviewer-worktree-2026-08-26` classified **`KEEP(fleet-seat)`** ("worktree backs a live fleet
  seat (agent_name \"agent-ui-reviewer\" in .claude/ops/fleet.json live_state.joined) — held open
  by design regardless of commits-ahead-of-main count") — 0 fail / 1 warn (informational, not a
  conflict). **This is the #1680 fix working as intended** — every prior firing back to
  fleet-bootstrap read this same worktree as `REAP`, a mechanical/intent conflict. PR #1684
  (merged, ticket #1680) closed that gap; this firing's dry-run independently confirms the fix
  landed correctly. No action needed — reported as the positive result it is.
- `npm run ops:reap-scratch-clones -- --dry` (FIRST pass, before execution): **3 REAP rows** —
  `/private/var/.../agent-ui-1680` (branch `1680-reap-worktrees-mjs-can`, PR #1684 MERGED at tip),
  `/private/var/.../agent-ui-1681` (branch `1681-adr-0140-rename-sweep`, PR #1685 MERGED at tip),
  `/private/var/.../agent-ui-1682` (branch `1682-adr-0046-orphaned`, PR #1683 MERGED at tip). This
  contradicts dispatch context's expectation that these three build-ticket dispatches "should have
  left no scratch-clone residue (they self-clean on their own Phase 5)" — flagged as a finding, not
  silently absorbed (see Risks). Executed per procedure step 3 (see Executed below).
- `npm run ops:reap-scratch-clones -- --dry` (SECOND pass, after execution): "(no scratch-clone
  directories found)" — 0 fail / 0 warn. Confirms the execute landed.
- `gitignore_check.py /Users/kimba/Projects/nonoun/agent-ui`: same standing 6 stale G1 rules as
  every recent firing (`npm-debug.log*`, `*.local`, `.vscode/*`, `.idea`, `*.sw?`,
  `.claude/docs/other`) — standing-accepted, not re-flagged.
- Ticket-claim convention: unchanged — no host-repo-numbered ADR names a ticket-claim convention
  here. Stale-claim gate degrades to git-surface-only hygiene.
- Open issues (`gh issue list --state open`): **1** — matches dispatch's stated expectation.
  - #1686 "doc-standards has no evals/ directory — /check-routing has nothing to verify against" —
    unassigned, `task`, updated 2026-08-27T13:44:14Z. No assignee, no claim to go stale. No action
    for this seat; work-item triage routes to `issue-sorter`, not here.
- Dirty primary (`git status --porcelain`): only `?? .claude/ops/sweep-in-flight.json` — this
  firing's own dispatching-session marker, per dispatch context, not residue. `sync_main.py` not
  applicable regardless: `main` is at exact parity with `origin/main` and this is a scheduled sweep
  firing (never appropriate for `sync_main.py` even with unexplained dirt, which this isn't).

## Executed
- `npm run ops:reap-scratch-clones -- --execute`: removed all 3 REAP-classified directories
  (`agent-ui-1680`, `agent-ui-1681`, `agent-ui-1682`), each independently re-verified against its
  PR's MERGED state by the script itself before removal. Re-run `--dry` confirms zero remaining.
  This is the host-repo-named gated reap script's own dry-then-apply posture (procedure step 3) —
  execute-only-what's-gated, not an agent judgment call.
- Nothing else executed. No merged PR left a remote branch behind (nothing for `campaign_close.py`
  — GitHub's own PR-merge branch deletion already handled #1684/#1685/#1683), `main` is at exact
  parity with `origin/main` and this is a scheduled firing regardless (nothing for `sync_main.py`),
  `reap-branches --dry` found 0 reapable rows, `reap-worktrees --dry` found 0 `REAP` rows (the
  known conflict is now resolved).

## Proposed-but-not-executed
- No stale-open PRs, no orphaned worktrees, no stale local branches, no off-main-primary findings
  this firing.
- No stale-claim findings: the one open issue (#1686) is unassigned — nothing claimed to go stale.

## Risks
- 🟢 Primary checkout correctly stays on `main`, exact parity with `origin/main` (`ad1d457d`).
- 🟢 Zero open PRs; zero leftover merged-PR remote branches — nothing for `campaign_close.py`.
- 🟢 `reap-worktrees.mjs`'s classification gap (agent-ui#1680) reads FIXED this firing — the
  reviewer worktree now classifies `KEEP(fleet-seat)`, not `REAP`. Independently confirmed, not
  taken on faith from the ticket being merged.
- 🟢 Three scratch-clone directories existed at this firing's start
  (`agent-ui-1680`/`agent-ui-1681`/`agent-ui-1682`) — the dispatching session's expectation that
  build-leader dispatches self-clean their own scratch clone was overstated; there is no such
  guarantee in `dispatch-ticket`'s own contract, and `reap-scratch-clones.mjs` exists specifically
  as the standing catch-all for exactly this (its own creation story: 32 orphaned scratch clones
  found before the script existed). Working as designed, not a regression — all three correctly
  REAP-classified (PR independently verified MERGED) and removed via the gated script.
- 🟢 One open issue (#1686), unassigned/unclaimed — informational only; no stale-claim risk.
- 🟢 Standing G1 gitignore warnings (6 rules) unchanged, not re-flagged.
- 🟢 `.claude/ops/sweep-in-flight.json` untracked-but-intentional (this firing's own marker) — not
  drift.

Consecutive-unchanged-firing count: reset to 0 (this firing) — both the #1680 fix landing and the
scratch-clone reap are genuinely new/changed findings relative to `2026-08-27T011000Z-repo-cleaner.md`.
