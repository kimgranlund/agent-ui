Sweep firing 2026-08-26T16:28:22Z — repo-cleaner report for /Users/kimba/Projects/nonoun/agent-ui.

Findings differ from the prior report (`2026-08-26T015730Z-repo-cleaner.md`) — two new-shaped
findings this firing: (1) a fresh open PR (#1671) that didn't exist last firing, and (2) the
newly-merged `reap-scratch-clones.mjs` script (PR #1667/#1662) was actually invoked and its own
gate called for `--execute`, replacing last firing's manual out-of-band scratch-clone cleanup.
Full report, not abbreviated.

## Inventory
- `git fetch --prune`: exit 0, clean. 4 stale `origin/*` tracking refs pruned (all
  already-merged PR branches whose remote copy GitHub already deleted via auto-delete-on-merge):
  `1634-harden-status-probe-to` (PR #1665), `1661-no-gated-script-reaps` (PR #1662),
  `1664-tabbed-code-view` (PR #1668), `1666-reap-scratch-clones-mjs` (PR #1667) — nothing for
  `campaign_close.py` since each remote branch was already gone before this firing read it.
- Worktrees (`git worktree list`, 1 total): only the primary
  (`/Users/kimba/Projects/nonoun/agent-ui`), HEAD `93eae1ab`, branch `main`. No
  `.claude/worktrees/` entries.
- `git branch -vv` / `git status --short --branch`: local `main` only, tracking `origin/main` at
  exact parity (no ahead/behind annotation).
- `git branch -r`: `origin/main` and `origin/1670-styling-overhaul-across-the` (the one live open
  PR's branch, below) — no leftover merged-PR remote branch for `campaign_close.py` to act on.
- `gh pr list --state open`: **1 open PR** — #1671 "feat(command-modal): add footer
  keyboard-hint bar (↑↓ Navigate · ↵ Select · ⎋ Close)", branch
  `1670-styling-overhaul-across-the`, opened 2026-08-26T16:12:43Z (~16 minutes before this
  firing), `Closes #1670`. Fresh — classified healthy/active-open, not stale-open. No action.
- `gh pr list --state all` (most recent run): a batch of MERGED PRs since the prior firing —
  #1668 (tabbed code view, closes #1664), #1667 (reap-scratch-clones origin-comparison fix,
  closes #1666), #1665 (status-probe hardening, closes #1634), #1662 (the new gated
  reap-scratch-clones.mjs itself, closes #1661) — none left a remote branch behind (confirmed via
  the fetch-prune list above).
- `primary_checkout_check.py /Users/kimba/Projects/nonoun/agent-ui`: `clean · main`, exit 0 — no
  off-main-primary risk.
- `npm run ops:reap-branches -- --dry`: "no local branches beyond main" — 0 fail / 0 warn, 0 rows.
- `npm run ops:reap-worktrees -- --dry`: "no worktrees under .claude/worktrees/" — 0 fail / 0
  warn, 0 rows.
- `npm run ops:reap-scratch-clones -- --dry`: 7 rows total — 6 classified `REAP` (each ticket's
  PR independently verified `MERGED` by the script itself: agent-ui-1634/PR#1665,
  agent-ui-1661/PR#1662, agent-ui-1664/PR#1668, agent-ui-1666/PR#1667, plus two clones sitting
  under this session's own scratchpad path — agent-ui-1605-fresh/PR#1612 and
  agent-ui-1620/PR#1622, both older residue) and 1 classified `KEEP(open-ticket)`
  (agent-ui-1670 — issue #1670 still OPEN, PR #1671 still OPEN at read time) — 0 fail / 1 warn
  (the warn is the KEEP row surfacing as non-blocking commentary, not a failure).
- `gitignore_check.py`: same standing 6 stale G1 rules as every recent firing (`npm-debug.log*`,
  `*.local`, `.vscode/*`, `.idea`, `*.sw?`, `.claude/docs/other`) — resolved, standing-accepted
  per issue #1609 (closed, superseded by the 2026-08-09 keep-list ruling); not re-flagged.
- Ticket-claim convention: reconfirmed this repo's own ADR-0005 is "Lazy-property upgrade
  precedence: property-wins" — unrelated to ticket-claim. No host-repo-numbered ADR names a
  ticket-claim convention here. Stale-claim gate degrades to git-surface-only hygiene per
  clean-git's own scope clause (unchanged from prior firing).
- Open issues (`gh issue list --state open`): exactly **1** — #1670 ("ui-command-modal: styling
  overhaul across the compound CSS stack"), assignee `kimgranlund`, labels `size:big, feature`,
  opened 2026-08-26T16:13:09Z (~15 min before this firing), linked open PR #1671. Fresh, live
  work — correctly excluded from the scratch-clone reap above (the script's own `KEEP(open-ticket)`
  classification independently agrees). Last firing's live claim (#1634) is now closed/merged
  (PR #1665) and absent from this open-issue read.
- Dirty primary: only `.claude/ops/sweep-in-flight.json` untracked, matching this firing's own
  dispatching-session marker — not cruft. `sync_main.py` not applicable regardless: `main` already
  reads exact parity against `origin/main`, and this is a scheduled sweep firing regardless.

## Executed
- **`npm run ops:reap-scratch-clones -- --execute`** — the script's own dry-run gate classified 6
  of 7 scanned scratch-clone directories as `REAP` (each independently re-verified against its
  ticket's `MERGED` PR); executed directly on exactly those 6, per the same execute-what's-gated
  posture as `campaign_close.py`'s merged-and-verified case:
  - `/private/var/folders/.../T/agent-ui-1634` (PR #1665 MERGED) — removed
  - `/private/var/folders/.../T/agent-ui-1661` (PR #1662 MERGED) — removed
  - `/private/var/folders/.../T/agent-ui-1664` (PR #1668 MERGED) — removed
  - `/private/var/folders/.../T/agent-ui-1666` (PR #1667 MERGED) — removed
  - `.../scratchpad/agent-ui-1605-fresh` (PR #1612 MERGED) — removed
  - `.../scratchpad/agent-ui-1620` (PR #1622 MERGED) — removed
  - `agent-ui-1670` was NOT touched — the script's own gate classified it `KEEP(open-ticket)`
    (issue #1670 open, PR #1671 open) and refused to include it in the REAP set.
- No merged PR left a remote branch behind this firing (nothing for `campaign_close.py`), `main`
  was already at exact parity (nothing for `sync_main.py`), and both `ops:reap-branches`/
  `ops:reap-worktrees` dry runs classified zero rows as reapable (nothing to `--apply`).

## Proposed-but-not-executed
- No stale-open PRs, no orphaned worktrees, no stale local branches, no stale-claim findings, no
  off-main-primary findings this firing.
- Nothing further on scratch clones — the one surviving clone (agent-ui-1670) is correctly live
  per both this seat's own read and the reap script's own gate; no manual action recommended.

## Risks
- 🟢 Primary checkout correctly stays on `main`, exactly current with `origin/main`.
- 🟢 One open PR (#1671), fresh (~16 min old at read time), correctly excluded from any
  stale-open classification.
- 🟢 The newly-merged `reap-scratch-clones.mjs` gated script worked exactly as designed this
  firing: 6 stale clones executed via its own `--execute` gate, the 1 genuinely-live clone
  correctly held back by the script's own `KEEP(open-ticket)` logic — no manual out-of-band
  cleanup needed this time (contrast with the prior firing, which required a host-level `rm -rf`
  before this script existed).
- 🟢 One live in-progress ticket claim (#1670, styling-overhaul, ~15m old) — correctly excluded
  from every reap proposal; its linked PR #1671 is open and unmerged.
- 🟢 No mutating script was refused or overridden this firing; both `reap-branches`/
  `reap-worktrees` dry runs found nothing to `--apply`; `campaign_close.py` had no leftover
  remote branch to act on (all 4 newly-pruned remote refs were already gone via GitHub's own
  auto-delete-on-merge).
- 🟢 Zero stale local branches, zero stale ticket claims, zero off-main-primary findings.
