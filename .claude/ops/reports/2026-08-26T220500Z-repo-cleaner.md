Sweep firing 2026-08-26T22:05:00Z — repo-cleaner report for /Users/kimba/Projects/nonoun/agent-ui.

Findings differ from the prior report (`2026-08-26T162822Z-repo-cleaner.md`) — full report, not
abbreviated. Changed shape this firing: (1) a second worktree + local branch now exist
(`.claude/worktrees/agent-ui-reviewer` / `reviewer-worktree-2026-08-26`), created by this
session's own `/teamwork:fleet-bootstrap` immediately prior — standing fleet infrastructure, not a
stale campaign worktree, per explicit dispatch context; (2) PR #1671 and issue #1670 (both open
last firing) are now merged/closed, dropping open-PR and open-issue counts to zero; (3) 8 new
merged PRs landed since the prior firing (#1671, #1676, #1677, #1678, #1679 plus scratch-clone
residue already reaped by GitHub's auto-delete); (4) a mechanical/intent conflict surfaced on the
new worktree (below) that did not exist in any prior firing.

## Inventory
- `git fetch --prune`: exit 0, clean. 5 stale `origin/*` tracking refs pruned, all already-merged
  PR branches whose remote copy GitHub already deleted via auto-delete-on-merge:
  `1670-styling-overhaul-across-the` (PR #1671), `1672-adr-0030-restatement-amendment` (PR #1679),
  `1673-adr-0032-restatement-amendment` (PR #1676), `1674-adr-0033-restatement-amendment`
  (PR #1678), `1675-adr-0035-restatement-amendment` (PR #1677) — nothing for `campaign_close.py`
  since each remote branch was already gone before this firing read it.
- Worktrees (`git worktree list`, 2 total — new normal per this session's fleet-bootstrap, not a
  finding):
  - primary `/Users/kimba/Projects/nonoun/agent-ui`, HEAD `ac6b80f2`, branch `main`.
  - `/Users/kimba/Projects/nonoun/agent-ui/.claude/worktrees/agent-ui-reviewer`, HEAD `ac6b80f2`
    (same as main), branch `reviewer-worktree-2026-08-26` — the standing reviewer seat's home per
    this session's `/teamwork:fleet-bootstrap`; holding indefinitely by design. Not proposed for
    reap despite the mechanical read below.
- `git branch -vv`: `main` (tracking `origin/main`, exact parity) and
  `reviewer-worktree-2026-08-26` (checked out in the reviewer worktree, HEAD matches `main`). No
  other local branches.
- `git branch -r`: only `origin/HEAD -> origin/main` and `origin/main` — zero leftover merged-PR
  remote branches, nothing for `campaign_close.py` to act on this firing.
- `gh pr list --state open`: **0 open PRs.** (#1671, open last firing, is now merged.)
- `gh pr list --state all`: newest merged since the prior firing — #1679, #1678, #1677, #1676
  (four ADR-restatement docs PRs) and #1671 (command-modal footer keyboard-hint, closes #1670) —
  all confirmed gone from `origin/*` by the fetch-prune above.
- `primary_checkout_check.py /Users/kimba/Projects/nonoun/agent-ui`: `clean · main`, exit 0 — no
  off-main-primary risk.
- `npm run ops:reap-branches -- --dry`: 1 row — `reviewer-worktree-2026-08-26` classified `KEPT`
  ("checked out in .../agent-ui-reviewer") — 0 fail / 0 warn. Script's own gate correctly declines
  to touch a checked-out branch; nothing to `--apply`.
- `npm run ops:reap-worktrees -- --dry`: 1 row — `.claude/worktrees/agent-ui-reviewer` /
  `reviewer-worktree-2026-08-26` classified **`REAP`** ("git cherry origin/main clean, 0 commits
  equivalent/none") — 0 fail / 0 warn. **Not executed.** This is a mechanical/intent conflict, not
  a clean gate-pass: the script's cherry-diff has no way to know this worktree is a deliberately
  content-identical standing seat (walled by file-on-disk, not by commit divergence) rather than a
  finished campaign branch. Per explicit dispatch context for this firing, I am treating this as
  ambiguous-by-design and NOT running `--apply` on this row, and NOT proposing manual removal
  either — holding it exactly as instructed. Flagging the conflict itself as a finding: if a future
  firing runs this same dry-run without the fleet-bootstrap context in hand, it will read the
  identical `REAP` row and may execute it, since nothing in the script or the repo's own state
  distinguishes "finished, mergeable-clean campaign worktree" from "intentionally parity-holding
  standing seat." No script change proposed here (out of scope for this seat) — naming the gap for
  whoever owns `reap-worktrees.mjs`'s classification rule.
- `npm run ops:reap-scratch-clones -- --dry`: "(no scratch-clone directories found)" — 0 fail / 0
  warn. Nothing to `--execute`.
- `gitignore_check.py /Users/kimba/Projects/nonoun/agent-ui`: same standing 6 stale G1 rules as
  every recent firing (`npm-debug.log*`, `*.local`, `.vscode/*`, `.idea`, `*.sw?`,
  `.claude/docs/other`) — standing-accepted, not re-flagged.
- Ticket-claim convention: unchanged from prior firings — no host-repo-numbered ADR names a
  ticket-claim convention here (this repo's own ADR-0005 is the unrelated
  lazy-property-upgrade-precedence ruling). Stale-claim gate degrades to git-surface-only hygiene.
- Open issues (`gh issue list --state open`): **0.** (#1670, open last firing, is now closed.)
  Nothing to check for staleness this firing.
- Dirty primary (`git status --porcelain`):
  - `M .claude/ops/fleet-roster.md`
  - `M .claude/ops/fleet.json`
  - `?? .claude/ops/sweep-in-flight.json`
  All three are this session's own `/teamwork:fleet-bootstrap` (fleet.json/fleet-roster.md) and
  this firing's own dispatching-session marker (sweep-in-flight.json) — dirty-but-intentional,
  same posture as prior firings' treatment of `sweep-in-flight.json` alone. `sync_main.py` not
  applicable regardless: `main` already reads exact parity against `origin/main`, and this is a
  scheduled sweep firing (never appropriate for `sync_main.py` even if the dirt were unexplained).

## Executed
- Nothing this firing. No merged PR left a remote branch behind (nothing for `campaign_close.py`),
  `main` was already at exact parity and this is a scheduled firing regardless (nothing for
  `sync_main.py`), `reap-branches --dry` found 0 reapable rows, and `reap-scratch-clones --dry`
  found zero scratch-clone directories at all. `reap-worktrees --dry` found 1 row it calls `REAP`,
  but per the conflict above I did not run `--apply` on it — see Inventory.

## Proposed-but-not-executed
- No stale-open PRs, no orphaned worktrees, no stale local branches (beyond the flagged conflict
  below), no stale-claim findings (zero open issues to check), no off-main-primary findings this
  firing.
- The reviewer worktree/branch reap conflict above: no action proposed either way — held per
  explicit dispatch context that this is standing fleet infrastructure, not cruft. Naming it as a
  standing item for whoever tracks `reap-worktrees.mjs`'s own classification logic, since the
  script itself cannot currently distinguish this case from genuine cruft.

## Risks
- 🟢 Primary checkout correctly stays on `main`, exact parity with `origin/main`.
- 🟢 Zero open PRs, zero open issues — clean slate this firing.
- 🟢 Zero leftover merged-PR remote branches; `campaign_close.py` had nothing to act on.
- 🟡 `reap-worktrees.mjs`'s own dry-run gate reads the new standing reviewer worktree as `REAP`
  (0 commits ahead of `main`). Not executed and not proposed for removal — held per this firing's
  explicit dispatch context that it is intentional standing infrastructure — but the script's own
  classification cannot tell the two cases apart today, so a future firing without that context
  in hand could read the same row and act on it. Worth a human decision on whether the script
  needs an exclusion rule (e.g. an explicit fleet-marker file) rather than relying on dispatch
  context every firing.
- 🟢 `.claude/ops/fleet.json` / `fleet-roster.md` dirty-but-intentional (this session's own
  fleet-bootstrap); `.claude/ops/sweep-in-flight.json` untracked-but-intentional (this firing's own
  marker) — neither is drift.
- 🟢 Zero stale local branches beyond the flagged worktree conflict, zero stale ticket claims,
  zero off-main-primary findings, standing G1 gitignore warnings unchanged and not re-flagged.
