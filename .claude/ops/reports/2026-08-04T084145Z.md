# repo-cleaner sweep — 2026-08-04T08:41:45Z

Dispatch: interactive, ad hoc ("run your standard repo-hygiene sweep"). Firing #1 for this
report shape at this path. **Sandbox note**: this session's Write tool is pinned to this
dispatch's own worktree (`.claude/worktrees/ratify-adr-0168`) and refused the shared main
checkout's `.claude/ops/reports/` path — so this report landed here instead, not alongside the
concurrent report already sitting in the shared checkout at
`/Users/kimba/Projects/nonoun/agent-ui/.claude/ops/reports/2026-08-04T083458Z.md` (a different
agent's ADR-harvest report, not a prior repo-cleaner firing — no comparable classification set to
diff against regardless). A human should reconcile these two report locations by hand; a future
firing reading "the most recent file in `.claude/ops/reports/`" from the shared checkout will not
see this one. Full report, running-count = 1.

## Status: 🟢 done

27 merged-and-verified PRs' remote branches closed via `campaign_close.py` (all C1/C2 clean).
`sync_main.py` ran on main's dirty tree (interactive dispatch, verified 100% foreign-only, popped
back safe). `gitignore_check.py` ran clean (warnings only). Two branches/PRs that met the
execute criteria were deliberately NOT executed — hard-excluded per this firing's explicit
constraint (see below). Everything else is proposed only; nothing else was mutated.

## Summary

**Inventory scope**: 33 worktrees (32 + main), ~40 local branches (12 real-named + 34
`worktree-agent-*`/`worktree-*` scratch placeholders, some overlapping), 249 PRs surveyed (`gh pr
list --state all --limit 500`), `gh`/network healthy throughout. ADR-0005 in this workspace is
`lazy-upgrade-property-wins` (unrelated to ticket-claim conventions) — the ADR-0005 claim-ticket
convention described in this agent's own Scope section is not ruled here, so this firing degrades
to git-surface-only hygiene; no claimed-Issue staleness sweep run.

**Executed (27 remote-branch deletes, all independently re-verified via `gh pr view` reading
`MERGED` before touch, and reverified gone after):**

| PR | Branch | Merged |
|---|---|---|
| 435 | fix-427-calendar-date-rot | 2026-08-04T08:24:42Z |
| 407 | design/tool-enablement-adr-0168 | 2026-08-04T04:33:31Z |
| 401 | fix-397-cardcontent-gap | 2026-07-31T06:23:28Z |
| 399 | fix-380-adr-0166-coordinated-repair | 2026-07-31T05:52:12Z |
| 398 | fix-307-fpath-live-validation | 2026-07-31T05:52:09Z |
| 393 | fix-392-ratify-repairs-checklist | 2026-07-31T04:53:47Z |
| 373 | fix-369-370-371-visual-batch | 2026-07-30T11:13:00Z |
| 379 | build-371-bar-seam | 2026-07-30T13:34:34Z |
| 375 | build-368-nav-rail-overlay | 2026-07-30T12:13:59Z |
| 386 | fix-batch-followups-2 | 2026-07-30T21:26:41Z |
| 384 | prd-ma-data-workbench | 2026-07-30T21:26:37Z |
| 382 | fix-381-overlay-border | 2026-07-30T14:57:13Z |
| 367 | fix-361-adr-rev | 2026-07-30T07:11:48Z |
| 374 | intake-371-bar-seam | 2026-07-30T10:59:47Z |
| 391 | fix-390-adr-0068-pointer | 2026-07-30T21:52:23Z |
| 420 | fix-412-modality-neutral-presets | 2026-08-04T05:49:07Z |
| 388 | fix-385-import-seeds-header | 2026-07-30T21:42:48Z |
| 396 | fix-394-parser-followups | 2026-07-31T05:52:06Z |
| 400 | fix-394-scope-inheritance | 2026-07-31T06:18:03Z |
| 389 | adr-0167-ratification-repairs | 2026-07-30T21:42:55Z |
| 387 | adr-377-traits-subpath | 2026-07-30T21:26:45Z |
| 434 | docs-roadmap-ma-prd-correction | 2026-08-04T08:24:30Z |
| 432 | docs-roadmap-mb-done | 2026-08-04T08:06:03Z |
| 395 | fix-354-rebase-components-cap | 2026-07-31T05:52:02Z |
| 405 | fix-403-calendar-month-rollover | 2026-08-03T21:20:37Z |
| 410 | fix-406-persona-export-import | 2026-08-04T04:24:58Z |
| 430 | fix-413-a2ui-basic-catalog | 2026-08-04T07:59:56Z |

Only 4 of these (435, 407, 411, 433) were named in the dispatch briefing; the other 23 were found
by the required independent full-repo survey and met the identical execute criteria (PR
independently reads `MERGED` via `gh pr view`, remote branch still present) — the contract's
"done when" bar is every merged-and-verified PR's remote branch, not only the briefed ones.

**Hard-excluded — met the execute criteria but NOT run, per this firing's explicit constraint:**

- **PR #433 / `feat/tool-enablement`** — independently verified `MERGED`
  (2026-08-04T08:13:06Z). This is the branch **actually checked out in this sweep's own live
  worktree** (`.claude/worktrees/ratify-adr-0168` — the directory name and the checked-out branch
  name do NOT match; the briefing's assumption that the worktree ran on a branch literally named
  `ratify-adr-0168` was wrong, confirmed by direct read). Never touched.
- **PR #411 / `ratify-adr-0168`** — independently verified `MERGED` (2026-08-04T07:25:00Z). A
  separately-existing local branch (not checked out anywhere, sitting at `c4dfe7e`, exactly
  matching `origin/ratify-adr-0168`) — genuinely a different git ref than the branch checked out
  in the live worktree. Its remote branch technically meets every criterion campaign_close.py
  gates on. I did NOT run it: the dispatch's hard-exclusion text names "the ratify-adr-0168
  branch" verbatim, and given the name collision with the live worktree's directory, treating
  this as ambiguous-therefore-excluded is the safer read of an explicit, repeated constraint.
  Flagging both refs by exact identity so a human can execute confidently: the live worktree's
  checkout is `feat/tool-enablement` (already covered above), and the standalone local
  `ratify-adr-0168` branch is the one still needing `campaign_close.py 411` if you want it closed.

**Interactive-dispatch action run:** `sync_main.py` on main (dirty: modified
`.claude/settings.json` + untracked `.claude/ops/`, in the shared main checkout, not this
worktree). Classifier read 0 incoming files (main was already at `origin/main`) and both dirty
items as 100% foreign-only (no overlap) — quarantined as `stash@{0}`, reverified
HEAD==origin/main, then popped the stash back immediately since the script's own classification
proved zero risk (nothing incoming to conflict with). Net effect: none — main's dirt is exactly
as it was, just independently verified safe.

**`gitignore_check.py`**: exit 0, warn-only. 17 `G1` stale-rule warnings (unused Vite-template
boilerplate: `logs`, `*.log`, `.idea`, `.vscode/*`, `.claude/docs/other`, etc.) — none touched,
surfaced only.

## Classification — worktrees (33 total)

**Dangling (merged PR now closed above; worktree/local branch itself untouched by
campaign_close — propose removal):** `agent-ui-fix-397`, `agent-ui-worktrees/fix-380`,
`agent-ui-wt-307` (⚠ carries untracked `.scratch-307/` — review before removing),
`agent-ui-wt-392`, `wt-394`, `wt-394b`, `scratchpad/fix-385` (tmp), `.claude/worktrees/{agent-a2cebc52e2fe5e5ba, agent-a34e68934c0bd9cfb, agent-a48bb08afb2296ad8, agent-a504004360489d110,
agent-a613e31ac1f886f2b, agent-a614391c9b8dc287f, agent-a7801bfdc0ca4e401, agent-a84449e43d91c2961,
agent-a8b704f4399e1d92a, agent-acb81354e720bf22c, agent-acf34d2a73f6caf46}`.

- `agent-acf34d2a73f6caf46` (design/tool-enablement-adr-0168, PR #407 now closed) carries an
  **uncommitted modification** to `.claude/docs/adr/0168-...md` — review this diff before
  removing; it may be a stray edit worth keeping.

**Dangling — today's ADR-0168 build-campaign stage worktrees, no own PR (content already
absorbed into `main` via PR #433/`feat/tool-enablement`, confirmed by ancestor-check; S6's
commit landed under a rebased SHA — same message, `972f071`→`26a81fc`, confirmed by log grep):**
`agent-a6d0046732f32341b` (S2), `agent-a8fc6e01eaf79b260` (S3), `agent-ac15c5cd55b736258` (S4),
`agent-a9e24b21949840d59` (S5), `agent-addab14ec997738b2` (S6), `agent-ac081e7edc0535392` (S7),
`agent-ac6a390a858784131` (S1), `build-tool-enablement` (S-DOCS, branch `build/tool-enablement`).

**Orphaned (no PR, detached HEAD or duplicate — propose removal):** `agent-a7cf8a03eaeb6ef4c` and
`agent-af380b3185b770db6`/`agent-af6421e7b30673a6f` (detached, matching already-merged commits),
`agent-a9b009ae879fe7ec4` (`review-382`, a duplicate checkout of the already-closed
`fix-381-overlay-border`, PR #382).

**Ambiguous — orphaned vs. still-needed (no repo-configured staleness window; propose only, not
executed):** `agent-a948d312447b47f15` (`intake-368-nav-rail-popover`) — unmerged, no PR; may be
superseded by the already-merged `build-368-nav-rail-overlay` (PR #375) or may still be a live
prep doc. Recommend a human glance before deleting.

**Healthy — active, leave alone:** `.claude/worktrees/agent-a023f9b44bd16b2b3`
(`docs-436-ma-spec`, **locked**, unmerged, no PR — genuinely in-progress M-A SPEC work per the
current roadmap).

**Hard-excluded from any action — this sweep's own live worktree:**
`.claude/worktrees/ratify-adr-0168` (checked out on `feat/tool-enablement`, **locked**). Noted as
a merged-branch-shaped candidate at most; never touched, never removed, per this firing's
explicit constraint.

## Classification — branches with no worktree (local-only, main checkout)

8 already-merged, now remote-closed above (`adr-0167-ratification-repairs`,
`adr-377-traits-subpath`, `docs-roadmap-ma-prd-correction`, `docs-roadmap-mb-done`,
`fix-354-rebase-components-cap`, `fix-403-calendar-month-rollover`,
`fix-406-persona-export-import`, `fix-413-a2ui-basic-catalog`) — local branch itself is now stale
(remote gone, PR merged); propose `git branch -d <name>`, not executed (no script gates local
branch deletion).

**34 orphaned scratch placeholder branches** (`worktree-agent-*` / `worktree-*`, e.g.
`worktree-agent-a023f9b44bd16b2b3`, `worktree-ratify-adr-0168`, `worktree-fix-416-adr-index-row`,
`worktree-roadmap-421-bespoke-catalogs`, full list via `git branch --list 'worktree-*'`) — none
checked out anywhere, no remote tracking, no PR; each is a leftover auto-named placeholder from
the worktree-spawning mechanism, tips landing on ordinary already-in-`main` merge commits (no
unique content). `worktree-ratify-adr-0168` is a **distinct object** from both this sweep's live
worktree (`feat/tool-enablement`) and the standalone dangling `ratify-adr-0168` local branch (PR
#411) — named explicitly here to avoid conflation; it is NOT hard-excluded (no unique content, no
PR), but proposed only, same as its 33 siblings, since local-branch deletion is never gated.

## Tests/checks run

- `git fetch origin --prune` — clean.
- `gh auth status` — authenticated, network reachable throughout.
- `git worktree list`, `git branch -vv`, `gh pr list --state all --limit 500 --json ...` — full
  inventory.
- `git merge-base --is-ancestor <head> origin/main` per worktree — used only as a secondary
  signal; several already-merged PRs (squash-merged) correctly read `NO`, so classification relied
  on `gh pr view`'s own `state`/`mergedAt`, never the ancestor check alone.
- `python3 campaign_close.py <pr>` × 27 — all `campaign_close · clean` (C1 ok, C2 ok).
- `python3 sync_main.py` — quarantine/reverify/pop cycle, all `ok`.
- `python3 gitignore_check.py /Users/kimba/Projects/nonoun/agent-ui` — exit 0, warn-only.
- `git ls-remote --heads origin <branch>` — used to confirm remote existed before closing, and
  independently re-confirmed absent after, for the two named hard-exclusion candidates.

## Evidence

- Full PR dump: `/tmp/all_prs.json` (249 PRs, `gh pr list --state all --limit 500`).
- `git ls-remote --heads origin design/tool-enablement-adr-0168 ratify-adr-0168 feat/tool-enablement fix-427-calendar-date-rot` (pre-close) confirmed all four still present on remote before any
  action — the briefing's four candidates were real, just incompletely scoped against the rest of
  the repo.
- S6 rebase-landed confirmation: `git log origin/main --oneline --grep="S6 —"` shows both
  `972f071` (worktree tip) and `26a81fc` (main's landed commit), identical message, different SHA.
- `.claude/docs/adr/0005-lazy-upgrade-property-wins.md` — confirms this workspace's ADR-0005 is
  unrelated to ticket-claim conventions (checked before skipping the claimed-Issue sweep).

## Risks

- 🟡 `agent-ui-wt-307` and `agent-acf34d2a73f6caf46` carry uncommitted content (a scratch dir and
  a modified ADR file respectively) inside worktrees whose PRs are now closed — flagged above,
  not touched; a human should glance at both diffs before reaping either worktree.
- 🟡 The `ratify-adr-0168` PR #411 exclusion is a judgment call under genuine name-collision
  ambiguity, not a hard fact — the dispatch text could reasonably be read either way. Erred
  toward non-execution per "ambiguity is never a license to execute."
- 🟡 This report landed in the worktree-local `.claude/ops/reports/`, not the shared checkout's —
  see the sandbox note at top. A future firing's "most recent file" diff will miss this one unless
  reconciled by hand.
- 🟢 Everything else in this firing carries independent `gh pr view`/`git ls-remote`
  re-verification before and after action; no claim was taken on a command's own say-so alone.

## Open questions

1. Should `campaign_close.py 411` (branch `ratify-adr-0168`, the standalone local branch, NOT the
   live worktree's checkout) be run manually now that its distinct identity from the live
   worktree's branch is confirmed? Left to a human — this agent errs toward not touching anything
   whose name overlaps the hard-exclusion text.
2. `agent-a948d312447b47f15` (`intake-368-nav-rail-popover`) — confirm superseded-by-#375 or
   still-needed before removing.
3. Where should this agent's reports actually land — the shared checkout's `.claude/ops/reports/`
   (where the concurrent ADR-harvest report is) or a worktree-local path (where the Write sandbox
   forced this one)? Worth a fix so future firings can actually diff against each other.

## Recommended next action

1. Reap the ~19 confirmed-dangling/orphaned worktrees listed above (after glancing at the two
   flagged uncommitted diffs) and delete their now-remote-closed local branches — no script gates
   this; it's a manual `git worktree remove` + `git branch -d` per entry.
2. Prune the 34 orphaned `worktree-agent-*`/`worktree-*` scratch branches:
   `git branch --list 'worktree-*' | xargs git branch -d` (after confirming none are checked out —
   already verified none are).
3. Decide on PR #411/`ratify-adr-0168`'s standalone local branch (Open question 1).
4. Retire the 17 stale `.gitignore` rules `gitignore_check.py` flagged, at your convenience —
   read-only finding, no urgency.
