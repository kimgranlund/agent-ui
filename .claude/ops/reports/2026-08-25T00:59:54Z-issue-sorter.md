# issue-sorter sweep report — 2026-08-25T00:59:54Z

Repo: kimgranlund/agent-ui. Scope: sweep-wide (decision-watcher, issue-sorter, repo-cleaner all
firing in parallel) — seat-suffixed report path per the #774 collision rule.

## Discovery
Window: 2026-08-24T17:08:45Z → 2026-08-25T00:59:54Z. Both `gh_issues` and `gh_pr` sources reached
(gh auth OK) — neither UNMEASURED.

## Items
- #1620, #1619, #1614, #1600 (issues) — all CLOSED via their own merged PRs (#1622, #1621, #1615,
  #1616 respectively) before this firing ran. Authors trusted (kimgranlund, on friendlies
  allow-list). No action.
- #1618 (issue, OPEN) — ruling-shaped (asks for an explicit `ratify ADR-0129 amendment` comment
  before any build dispatches; watch-tickets step 2, ruled 2026-08-17). Was minted with a `task`
  label; relabeled to `needs-ruling` (label created once, didn't exist in repo) via direct `gh
  issue edit` calls. Now correctly excluded from chore-planner's buildable-work lane — referenced
  by id there, not restated as prose.
- #1622, #1621, #1616, #1615, #1607 (PRs) — MERGED, closing the issues above. No action.
- #1617 (PR, OPEN) — author trusted, not linked to any open issue. No ticket-record action; PRs
  carry no kind-label concept in this repo's convention.

## Trust
Every discovered item's author is `kimgranlund`, on `friendlies.json`'s allow-list and matching
`policy.confirmed_by` — no provenance tag, no hold.

## Held items
None this firing. `held-items.md` unchanged — no payload.

## friendlies.json
Unchanged this firing. Roster already confirmed (2026-08-05); `github_mcp_offer` already recorded
`declined` (2026-08-05) — step 7 (roster bootstrap) and step 8 (MCP offer) both not-applicable
(already-decided, not re-offered/re-asked).

## Checkpoint
Both sources reachable and advanced (payload below).

## Done-when check
Every item discovered this firing classified, trust-checked, and resolved (already-merged) or
correctly labeled (#1618). Both source checkpoints advanced. No item silently dropped, no
unreachable source, no silent auto-create of an unknown author's item.
