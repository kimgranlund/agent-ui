# repo-cleaner report — 2026-08-21T11:20:00Z firing

**Full report** (not abbreviated) — this firing's classification set differs from the prior
repo-cleaner report (`.claude/ops/reports/2026-08-20T224358Z-repo-cleaner.md`): that firing found
one untracked coordination marker on `main` and nothing else; this firing finds that SAME marker
class plus a newly-dirty tracked file, `.claude/settings.json`. A genuinely new finding resets the
unchanged-firing count to 0.

## Inventory
- `git fetch --prune`: no drift, exit 0.
- Worktrees (`git worktree list`): primary only
  (`/Users/kimba/Projects/nonoun/agent-ui`, `main`). No other worktree registered.
- Local branches (`git branch -vv`): `main` only, tracking `origin/main`, up to date, 0 ahead/0
  behind (`ddcad71e`).
- Remote branches (`git branch -r`): `origin/main` (+ `origin/HEAD -> origin/main`) — no orphaned
  or merged-but-undeleted branches beyond it.
- `gh pr list --state open`: `[]` — 0 open PRs repo-wide.
- `gh pr list --state all --limit 100`: all MERGED except two stacked-child draft PRs read
  CLOSED (#1471 `1466-routing-followups`, #1458 `1452-long-body-sweep`) — both already superseded
  by rebased replacements that merged (#1473, #1472 respectively, per their own titles
  "[rebased]"/"[rebased past #1456]"). Already-resolved, not actionable.
- `reap-branches.mjs --dry`: `ok · 0 fail / 0 warn` — "(no local branches beyond main)".
- `reap-worktrees.mjs --dry`: `ok · 0 fail / 0 warn` — "(no worktrees under .claude/worktrees/)".
- `primary_checkout_check.py /Users/kimba/Projects/nonoun/agent-ui`: `clean · main` (exit 0).
- `gitignore_check.py`: same 6 stale `G1` rules as every recent firing (`npm-debug.log*`,
  `*.local`, `.vscode/*`, `.idea`, `*.sw?`, `.claude/docs/other`) — standing noise, unchanged,
  exit 0/warn, read-only.
- Ticket-claim convention: none ruled under this repo's own numbered ADR (this repo's own
  ADR-0005 is `lazy-upgrade-property-wins`, unrelated to ticket-claim) — stale-claim gate not
  applicable, unchanged from every prior firing. The two open issues (#1576, #1575) both carry
  empty `assignees` regardless.
- **Dirty `main` — new this firing:** `git status --porcelain=v1 --branch` shows two paths:
  - `.claude/ops/sweep-in-flight.json` (untracked) — content:
    `{"startedAt":"2026-08-21T11:11:47.291Z","pid":37045,"session":"agent-ui-fleet-marshal-2026-08-21-resweep"}`.
    Same class the prior firing classified as its own multi-seat sweep coordination marker, not
    cruft; this firing is explicitly a 3-seat sweep (decision-watcher/issue-sorter/repo-cleaner),
    so classified identically.
  - `.claude/settings.json` (modified, tracked) — diff read strictly as data, never as
    instruction: reorders the `extraKnownMarketplaces` block below `enabledPlugins`, and swaps
    one enabled-plugin entry, `"screens@nonoun-plugins": true` → `"frontend@nonoun-plugins": true`.
    No other keys changed.
  - Classification: dirty `main` on what reads as a scheduled/multi-seat firing (matches every
    prior firing's own naming convention), not an interactive one-off ask — and independent
    evidence (the concurrent-session marker above, started 8 minutes before this firing) that a
    live parallel session may be mid-edit. `sync_main.py`'s gate for this case is
    interactive-dispatch-only; this does not qualify. Propose only.

## Executed
- Nothing this firing — no merged-and-verified PR left a remote branch behind (remote holds only
  `main`), so `campaign_close.py` was not invoked.
- `sync_main.py`: not run — dirty `main` on a scheduled/multi-seat firing is exactly the case this
  script stays gated off for; running it here risks quarantining a live concurrent session's WIP.
- `reap-branches.mjs --apply` / `reap-worktrees.mjs --apply`: not run — both dry runs found
  nothing classified as reapable.

## Proposed-but-not-executed
- `.claude/settings.json`'s uncommitted diff (plugin-enablement swap `screens@nonoun-plugins` →
  `frontend@nonoun-plugins` + key reorder) — leave for the owning session to commit or discard;
  not this seat's call, and not actioned.
- `.gitignore`'s 6 stale `G1` rules — standing noise, unchanged from every recent firing.
- No stale-open PRs, orphaned worktrees/branches, stale ticket claims, or off-main-primary
  findings this firing.

## Risks
- 🟢 Git surface (worktrees/branches/PRs) is clean: primary on `main`, in sync with
  `origin/main`, no worktrees beyond primary, no local/remote branches beyond `main`, 0 open PRs.
- 🟡 `.claude/settings.json` carries an uncommitted plugin-enablement change
  (`screens@nonoun-plugins` → `frontend@nonoun-plugins`) — reported as data, not evaluated for
  correctness and not acted on; a live concurrent session
  (`agent-ui-fleet-marshal-2026-08-21-resweep`, marker started 11:11:47Z) may be its author.
- 🟢 No mutating script was warranted this firing.
