Sweep firing 2026-08-24T03:15:36Z — repo-cleaner report for /Users/kimba/Projects/nonoun/agent-ui.

Findings differ from the prior report (`2026-08-23T200011Z-repo-cleaner.md`) — the prior firing's
one open item, local branch `pr-1590`, has resolved (no longer exists) — so this is a full report,
not abbreviated.

## Inventory
- `git fetch --prune`: exit 0, clean.
- Worktrees: primary (`main`, clean) + `.claude/worktrees/build-1600` (branch
  `1600-docs-site-reorganize-sitemap`, dirty — live in-progress work backing open issue #1600,
  assigned kimgranlund, updated today).
- `git branch -vv`: only `main` and `1600-docs-site-reorganize-sitemap` locally — `pr-1590` from
  the prior firing's report is gone entirely.
- `gh pr list --state all`/`--state open`: 0 open PRs; no merged PR left a remote branch behind —
  `campaign_close.py` had nothing to act on.
- `reap-branches.mjs --dry`: 1 row, `1600-docs-site-reorganize-sitemap` KEPT (checked out live).
  Nothing reapable.
- `reap-worktrees.mjs --dry`: `build-1600` KEEP(dirty) — correctly not reapable.
- `primary_checkout_check.py`: clean · main, exit 0.
- `gitignore_check.py` (3.18.4): same 6 stale G1 rules as every recent firing — standing noise,
  unchanged, read-only; now tracked by open issue #1609 (filed since last firing, issue-sorter's
  territory).
- Ticket-claim convention: none ruled under this repo's own numbered ADR (ADR-0005 here is
  lazy-upgrade-property-wins, unrelated) — stale-claim gate not applicable. Open issues: #1600
  (assigned, in-flight, live worktree, updated today — not stale), #1605/#1608 (unassigned, no
  claim to evaluate).
- Dirty `main`: one untracked file, `.claude/ops/sweep-in-flight.json` (session agent-ui-90,
  startedAt 2026-08-24T03:15:31.047Z), matching this firing's own dispatching session — sweep
  marker, not cruft. `sync_main.py` withheld regardless per this firing's unattended-dispatch
  instruction.

## Executed
- Nothing this firing — no merged-PR remote branch, no host-gated-reapable branch/worktree, no
  interactive dispatch to warrant `sync_main.py`.

## Proposed-but-not-executed
- Nothing new — no stale-open PRs, orphaned worktrees/branches, stale ticket claims, or
  off-main-primary findings. Prior firing's `pr-1590` item has resolved and drops off.

## Risks
- 🟢 Git surface clean: primary on main in sync with origin/main, one live worktree correctly
  kept, 0 open PRs, 0 reapable branches.
- 🟢 No mutating script was refused or overridden this firing.
- 🟡 The 6 stale .gitignore G1 rules are now tracked by open issue #1609 rather than being pure
  recurring noise — confirm it gets picked up so this line finally clears.
