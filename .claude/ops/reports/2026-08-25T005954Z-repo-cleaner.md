Sweep firing 2026-08-25T00:59:54Z — repo-cleaner report for /Users/kimba/Projects/nonoun/agent-ui.

Findings differ from the prior report (`2026-08-24T170845Z-repo-cleaner.md`) — the prior firing's
live worktree (`build-1600`) closed out exactly as its own last report anticipated (merged via
PR #1616), a new open PR (#1617) appeared with its own live backing worktree, and a previously
unseen worktree (`corpus-wave-6-seeds`) surfaced registered against this repo but outside the
standard `.claude/worktrees/` location — so this is a full report, not abbreviated.

## Inventory
- `git fetch --prune`: exit 0, clean. Three stale remote-tracking refs pruned
  (`origin/1614-adr-0015-amend-cl`, `origin/1620-status-stream-demo-detail-header`,
  `origin/worktree-1619-sidebar-nav-demo-default` — all three PRs merged in the interim: #1615,
  #1622, #1621).
- Worktrees (`git worktree list`, 3 total):
  - primary (`main`) — clean, `main`...`origin/main` reads `0 0` (exact match, no divergence).
  - `.claude/worktrees/frontier-swiper-fixes` (branch `a2ui-design-mode-triage-fixes`) — clean,
    backs **open PR #1617** (`fix(a2ui): box-sizing overflow, SplitPane ratio units, action-row
    truncation, swiper scroll-snap`), tracking reads `behind 2` (5 more commits landed on the PR's
    remote branch after this worktree's local tip; local HEAD confirmed an ancestor of
    `origin/a2ui-design-mode-triage-fixes` — routine, not a conflict). Active, healthy.
  - a worktree at
    `/private/tmp/claude-501/-Users-kimba-Projects-nonoun-agent-ui/8d773af0-e8a8-4835-acb4-26acf2aac600/scratchpad/corpus-wave`
    (branch `corpus-wave-6-seeds`) — **registered against this repo's own `.git/worktrees/`**
    (confirmed via `git worktree list --porcelain`'s `gitdir:` line and `git branch -vv`), so this
    is a real repo-surface finding, not one of the dispatch-noted sandbox-denied scratch clones
    (those aren't `git worktree list` entries at all). 5 commits, none cherry-equivalent to
    anything on `main` (`git cherry main corpus-wave-6-seeds` — all `+`, genuinely unmerged
    corpus-eval work: seed admit/reject judgments), no matching branch on `origin`
    (`git ls-remote origin corpus-wave-6-seeds` — empty), **no PR of any kind**. Orphaned by
    classification, and outside `reap-worktrees.mjs`'s own scan scope entirely (that script only
    ever considers paths physically under `<repo-root>/.claude/worktrees/`, by its own source
    comment — this path isn't one, so its dry run doesn't even list it).
- `git branch -vv` / `git branch -r`: local `main`, `a2ui-design-mode-triage-fixes` (worktree
  above), `corpus-wave-6-seeds` (worktree above), and **`pr-1617-review`** — not checked out
  anywhere, created via `fetch origin pull/1617/head:pr-1617-review` (per its own reflog) as a
  review-alias copy of the same open PR #1617. Verified fully absorbed into that PR's real branch
  (`git rev-list --left-right --count origin/a2ui-design-mode-triage-fixes...pr-1617-review` → `5
  0`: zero commits unique to `pr-1617-review`). `git branch -r` shows only `origin/main` and
  `origin/a2ui-design-mode-triage-fixes` — no stale merged-PR remote branch left behind anywhere.
- `gh pr list --state all` (30 most recent): exactly **1 open PR** (#1617, above) and 1 closed
  (never merged: #1591). All other recent PRs merged, and none of their remote branches survive
  (confirmed against the `git branch -r` list above) — nothing for `campaign_close.py` to act on
  this firing.
- `reap-branches.mjs --dry`: `a2ui-design-mode-triage-fixes` KEPT (checked out),
  `corpus-wave-6-seeds` KEPT (checked out), `pr-1617-review` **PROPOSED** — read the script's own
  source before acting: PROPOSED is this script's "not provably disposed of — do NOT delete"
  tier (same refusal class as KEPT for `--execute` purposes; only a `REAP` row deletes). 0 REAP
  rows this firing → no `--execute` run.
- `reap-worktrees.mjs --dry`: 1 row, `frontier-swiper-fixes` → `KEEP(live-branch)` (PR #1617 OPEN,
  9 pending commits vs. `origin/main`). 0 REAP rows → no `--execute` run.
- `primary_checkout_check.py`: `clean · main`, exit 0.
- `gitignore_check.py`: same standing 6 stale G1 rules as every recent firing (`npm-debug.log*`,
  `*.local`, `.vscode/*`, `.idea`, `*.sw?`, `.claude/docs/other`) — per the prior firing's report,
  this is a resolved, standing-accepted finding (issue #1609 closed 2026-08-24 under a
  re-affirmed 2026-08-09 KEEP-LIST ruling); not re-flagged as a risk.
- Ticket-claim convention: no host-repo-numbered ADR ruling for ticket-claim exists in this repo
  (this repo's own ADR-0005 is unrelated) — stale-claim gate not formally applicable. One open
  issue, #1618 (`ADR-0129 Amendment 2: extract shared artboard core to @agent-ui/app`) — unassigned,
  `needs-ruling` label, no comments: not a claim at all (no assignee), so no stale-claim finding.
- `state-checkpoint.json`: present but out of scope for this seat in this repo — this plugin's own
  `check-state` skill is the existing claim on that path here, per the ruled ownership clause; not
  read or advanced by this firing.
- Dirty primary: only `.claude/ops/sweep-in-flight.json` untracked, content matches this exact
  firing (`startedAt 2026-08-25T00:59:49.589Z`, `session agent-ui-90`) — this firing's own
  dispatching-session marker, not cruft. `sync_main.py` not applicable regardless: `main` already
  reads `0 0` against `origin/main`.

## Executed
- None. No merged PR left a remote branch behind (nothing for `campaign_close.py`), `main` was
  already fast-forward-current (nothing for `sync_main.py`, and this is a scheduled/unattended
  firing regardless), and both host-repo reap scripts' own dry runs classified zero rows as
  reapable (nothing for `--execute`).

## Proposed-but-not-executed
- **Orphaned worktree** `corpus-wave-6-seeds` at the scratchpad path above: no PR, no remote
  branch, 5 genuinely-unmerged commits, and physically outside `.claude/worktrees/` so no gated
  script even sees it. Recommend a human either open a PR for this corpus-eval work or, if
  superseded/abandoned, run `git worktree remove <path>` by hand (followed by `git worktree
  prune` to clear the `.git/worktrees/corpus-wave` admin record) — not executed here since no
  script gates it and its liveness (same-day commits, may be a concurrent session's active WIP)
  is ambiguous.
- **Stale local branch** `pr-1617-review`: `reap-branches.mjs`'s own gate declined to reap it
  (PROPOSED, not REAP) so it is not touched — but by independent read it carries zero commits not
  already on PR #1617's real remote branch (verified above). Recommend a human delete it by hand
  (`git branch -D pr-1617-review`) once PR #1617's review activity is done; not executed since the
  gated script itself did not classify it as reapable.
- No stale-open PRs, no stale ticket claims, no off-main-primary findings this firing.

## Risks
- 🟢 Primary checkout correctly stays on `main`, exactly current with `origin/main` (0 ahead/0
  behind) — the #592-shaped off-main-primary risk class reads clean.
- 🟢 One open PR (#1617), correctly backed by a clean, live worktree — no stale-open risk.
- 🟢 No mutating script was refused or overridden this firing; both reap scripts' dry runs found
  nothing to `--execute`.
- 🟡 An orphaned, no-PR worktree (`corpus-wave-6-seeds`) is sitting registered against this repo
  from a non-standard path (a session scratchpad, not `.claude/worktrees/`) — worth a human look
  before that scratchpad is ever cleaned out from under it, which would leave a dangling
  `.git/worktrees/` admin record behind.
- 🟡 One stale-but-harmless local branch (`pr-1617-review`) duplicates open PR #1617's own content
  with zero unique commits — low-priority manual cleanup, not gated.
