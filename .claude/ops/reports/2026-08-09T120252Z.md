# repo-cleaner sweep — agent-ui — 2026-08-09T12:02:52Z

**Status: 🟢 done.** This firing's classification set differs from the most recent prior report
(`.claude/ops/reports/2026-08-08T222423Z.md`): that sweep found 1 open PR, 2 worktrees, 3 local
branches, with `s5-615` explicitly live in-flight. This sweep finds 0 open PRs, 1 worktree, 1
local branch, everything merged and clean. Count resets to 1.

Two `campaign_close.py` runs executed (both clean, both found the branch already gone — no
mutation performed). `sync_main.py` evaluated, not run (working tree fully clean, nothing to
quarantine). No host-repo reap script exists for this repo. `gitignore_check.py` read-only,
exit 0, same 17 pre-existing `G1` warnings as every prior sweep. Zero orphaned
worktrees/branches/PRs. No ADR-0005 claim convention in use here (reconfirmed).

## Inventory

- **Worktrees** (`git worktree list`): one only — `/Users/kimba/Projects/nonoun/agent-ui` at
  `ca00faa`, on `main`, tracking `origin/main`, working tree clean. (Prior sweep's second
  worktree, `.claude/worktrees/s5-615`, is gone — its branch `feat/615-identity-s5-account`
  shipped as PR #622, closed issue #615.)
- **Local branches** (`git branch -vv`): `main` only, up to date with `origin/main`. No stray
  local branches.
- **Remote branches**: before `git fetch --prune`, local tracking refs still listed
  `origin/chore/adr-harvest-0173-0174-0175` and `origin/feat/614-identity-s4-onboarding` — both
  already deleted on GitHub's side; `fetch --prune` removed the stale local refs. After prune:
  `origin/main` only.
- **PRs** (`gh pr list --state all`): #618 through #623 all `MERGED` since the prior sweep
  (identity S3–S5 family #620/#621/#622/#623 plus supporting fixes #618/#619). `gh pr list
  --state open` → zero open PRs.
- **Open issues**: #613 (open — the `campaign_close` skip-history tracker, evidence-first ruling
  recorded 2026-08-08, no assignee) and #616 (open task, no assignee, no linked PR). #614 and
  #615 both closed (`closedAt` 2026-08-08T22:39:54Z / 23:02:08Z), consistent with their linked
  merges (#621/#622).

## Classification

| Item | Classification |
|---|---|
| PR #623 branch (`chore/adr-harvest-0173-0174-0175`) | merged-and-verified → executed `campaign_close.py 623` |
| PR #621 branch (`feat/614-identity-s4-onboarding`) | merged-and-verified → executed `campaign_close.py 621` |
| Worktree `s5-615` / branch `feat/615-identity-s5-account` | already reaped (worktree gone, PR #622 merged, its branch confirmed absent via the same `fetch --prune`) — no action needed this firing |
| Local branch `main` | healthy — sole checkout, clean, in sync |
| Issue #613 | healthy-open (a real tracker, standing protocol active per Kim's ruling; not a stale claim — no assignee, not ADR-0005-gated) |
| Issue #616 | healthy-open, unassigned, no claim to evaluate |
| Issues #614/#615 | correctly closed alongside their merged PRs — no action |
| `.gitignore` | 17 pre-existing `G1` stale-rule warnings, unchanged, exit 0 |

## Executed — evidence

```
$ python3 campaign_close.py 623
campaign_close · clean
  ok   C1  PR merged
  ok   C2  chore/adr-harvest-0173-0174-0175 already absent from remote before this run

$ python3 campaign_close.py 621
campaign_close · clean
  ok   C1  PR merged
  ok   C2  feat/614-identity-s4-onboarding already absent from remote before this run
```

Both branches read as "already absent" because they'd already been deleted on GitHub's side by
the time the script ran — confirmed by `git fetch --prune origin`, which pruned exactly those
two stale local remote-tracking refs and nothing else. `delete_branch_on_merge` is `false` at
the repo level (`gh api repos/kimgranlund/agent-ui --jq .delete_branch_on_merge`), so the
deletion came from the `--delete-branch` flag at merge time, not a repo-wide auto-delete
setting. This is a small positive data point for **#613**'s standing evidence-first protocol:
the branches behind #621 and #623 both deleted cleanly at ship time, extending the run of clean
ships noted in #613's 2026-08-08 comment (#618–#621, now also #623) with no new survivor since
#622. Not posted to #613 — proposing it as evidence for whoever next updates that tracker, per
the propose-only boundary on comment-posting (see `.claude/ops/plan.md` entry 3.1).

## `sync_main.py` — evaluated, NOT run

`git status --porcelain` on `main` returned empty (fully clean) both before and after the
campaign_close runs and the prune. `HEAD` already matches `origin/main` by SHA (`ca00faa`). No
dirty tree exists to quarantine, so `sync_main.py`'s quarantine-and-ff-pull contract has nothing
to act on — running it would be a no-op. (Note for the record: this dispatch is interactive, so
the scheduled-firing gate would not have blocked it had there been dirty state — moot here.)

## `gitignore_check.py` — read-only, exit 0

Same 17 `G1` stale-rule warnings as every prior sweep (`logs`, `*.log`, `npm-debug.log*`,
`yarn-debug.log*`, `yarn-error.log*`, `pnpm-debug.log*`, `lerna-debug.log*`, `dist-ssr`,
`*.local`, `.vscode/*`, `.idea`, `*.suo`, `*.ntvs*`, `*.njsproj`, `*.sln`, `*.sw?`,
`.claude/docs/other`), no `G2` FAIL. Proposed (never self-edited): retire these 17 lines
(`.claude/ops/plan.md` entry 4.2).

## Host-repo reap script

None found. Checked `package.json` `scripts` (no `reap`/`ops:`-style entry) and this repo's own
`CLAUDE.md`/`README.md` (no mention). Local-branch/worktree cleanup stays propose-only per the
standing rule — moot this firing, nothing local needs cleaning.

## Claimed-ticket sweep (ADR-0005 gate)

Not applicable, reconfirmed: this repo's own ADR-0005 is `0005-lazy-upgrade-property-wins.md`,
unrelated to ticket-claim staleness. The TICKET tier is a frozen archive (ADR-0145); work items
route to GitHub Issues with no assignee-based claim convention (#613/#616 both `"assignees":[]`).
Degrades to git-surface-only hygiene per this agent's own scope rule.

## Proposed-but-not-executed

- Retire the 17 stale `G1` `.gitignore` lines flagged above.
- Optionally attach this sweep's `campaign_close.py` evidence (both branches already-clean at
  ship time) as a comment on #613, extending its clean-ship run — a human/dispatching-session
  call, not posted by this agent.
- Nothing else — no worktree, local branch, or PR requires cleanup beyond what was already
  executed above.

## Risks

- 🟢 Zero orphaned worktrees/branches/PRs. Two merged PRs' remote branches independently
  reverified already-gone. No dirty `main` to quarantine. No stale ticket claims. `.gitignore`
  clean (warn-only, unchanged).
- 🟡 #613's underlying root cause (why the branch-checked-out-in-linked-worktree variable
  sometimes correlates with a survivor) is still not conclusively resolved — this firing adds
  two more clean data points but no new test of the worktree-checkout variable itself, since no
  merge happened from inside a linked worktree this time.
