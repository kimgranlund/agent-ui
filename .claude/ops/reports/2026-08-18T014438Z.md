# repo-cleaner sweep — agent-ui — 2026-08-18T014438Z

**Status: 🟡 attention.** Classification set differs materially from the most recent report on
disk (`.claude/ops/reports/2026-08-17T140720Z.md`, a squeaky-clean 1-worktree/1-branch/0-open-PR
baseline) — count resets to 1. This firing found the primary checkout off `main` (with live
uncommitted work), plus 6 merged PRs whose remote branches hadn't been deleted.

## Inventory

- **`git fetch --prune`**: pruned 2 stale remote-tracking refs — `origin/1182-genui-lean`,
  `origin/fix/1178-segment-nowrap` — before any other read.
- **Worktrees** (`git worktree list`), 1: `/Users/kimba/Projects/nonoun/agent-ui` @ `c77b45bc`
  `[planner-adr-wave]` — no other worktrees.
- **Local branches** (`git branch -vv`), 2: `main` @ `c77b45bc` tracking `origin/main` (in sync,
  0 ahead/0 behind) and `planner-adr-wave` @ `c77b45bc` (same tip, no tracking branch) — the
  latter is the checked-out branch on the sole worktree.
- **Primary checkout** (`primary_checkout_check.py /Users/kimba/Projects/nonoun/agent-ui`): FAIL
  — `primary checkout is not on 'main' -> branch 'planner-adr-wave' (0 ahead / 0 behind
  origin/main)`. `git status --porcelain=v1 --branch` on that checkout shows it is also dirty: a
  99-line uncommitted edit to `.claude/docs/adr/0198-ask-flow-completion-flowend-meta-signal.md`
  and an untracked `.claude/ops/sweep-in-flight.json` — evidence of a live in-progress session,
  not cruft. Classified off-main-primary; per the #592 rule this is reported only, never switched
  or quarantined. `sync_main.py` does not apply either — the dirt sits on `planner-adr-wave`, not
  `main`, so its gate condition ("local dirt on `main`") is never met.
- **Remote branches** (`git branch -r`), before cleanup: `origin/HEAD -> origin/main`,
  `origin/main`, plus 6 branches with no open PR pointing at them —
  `origin/1168-flowend-hint`, `origin/1171-hotel-booking-flow`, `origin/1172-library-row-context`,
  `origin/1174-confirm-receipt`, `origin/1184-toast-outcomes`, `origin/1185-description-list`.
  Cross-referenced against `gh pr list --state all`: each maps 1:1 to a PR that reads `MERGED`
  (#1176, #1179, #1177, #1181, #1186, #1187 respectively) with no remaining open PR on either
  side. After execution (below), `git branch -r` reads `origin/HEAD -> origin/main`, `origin/main`
  only.
- **PRs** (`gh pr list --state open`): 0 open. `gh pr list --state all --limit 200` shows the full
  recent history as `MERGED`/`CLOSED` only (most recent 6: #1187, #1186, #1183, #1181, #1180,
  #1179), nothing open or dangling.
- **Host-repo reap script** (`npm run ops:reap-branches`, `scripts/reap-branches.mjs`, named in
  this repo's own `package.json`): ran dry (default posture) — `planner-adr-wave  c77b45bc  KEPT
  checked out in /Users/kimba/Projects/nonoun/agent-ui`, `reap-branches · ok · 0 fail / 0 warn`,
  exit 0. Correctly refuses to reap a checked-out branch; nothing else present to classify.
- **`.gitignore`** (`gitignore_check.py`): exit 0, 6 `G1` warnings — `npm-debug.log*`, `*.local`,
  `.vscode/*`, `.idea`, `*.sw?`, `.claude/docs/other` — one fewer than the prior firing's 7
  (`*.log` no longer flagged; a real log file now exists in the tree matching that rule).
  Remaining 6 are the same standing Kim-ruled noise as every prior firing, not re-proposed.
- **Ticket-claim convention**: none ruled in this repo — `.claude/docs/adr/0005-*` is
  `lazy-upgrade-property-wins`, unrelated to ticket claims; no other numbered ADR names one.
  Stale-claim gate stays not-applicable — git-surface-only hygiene, unchanged from every prior
  firing. (Open-issue backlog itself, e.g. #1215–#1188, is ordinary feature/task intake, out of
  this seat's remit.)
- **Stashes**: none observed.

## Classification

| Item | Classification |
|---|---|
| Primary checkout on `planner-adr-wave` (dirty, live WIP) | off-main-primary — propose only |
| 6 merged PRs' remote branches (#1176/#1177/#1179/#1181/#1186/#1187) | merged-and-verified — executed |
| `origin/main` (post-cleanup) | healthy — sole remaining remote branch |
| Local branch `main` | healthy — in sync with `origin/main` |
| PRs (0 open) | healthy — nothing else to close |
| `ops:reap-branches` dry-run | healthy — correctly kept the checked-out branch, 0 apply |
| `.gitignore` | 6 stale `G1` rules, standing Kim-ruled noise, exit 0 |

## Executed — evidence

- `campaign_close.py 1187` → `clean`: C1 PR merged, C5/C4 no open PR references the branch, C2
  `1185-description-list` deleted and reverified gone. Exit 0.
- `campaign_close.py 1186` → `clean`, same four checks, `1184-toast-outcomes` deleted+reverified.
  Exit 0.
- `campaign_close.py 1181` → `clean`, `1174-confirm-receipt` deleted+reverified. Exit 0.
- `campaign_close.py 1179` → `clean`, `1171-hotel-booking-flow` deleted+reverified. Exit 0.
- `campaign_close.py 1177` → `clean`, `1172-library-row-context` deleted+reverified. Exit 0.
- `campaign_close.py 1176` → `clean`, `1168-flowend-hint` deleted+reverified. Exit 0.
- `npm run ops:reap-branches` (dry-run, the gate itself) — kept `planner-adr-wave` (checked out),
  0 fail/0 warn. Nothing for its own gate to classify as reapable beyond that, so `--apply` was
  not run.
- `sync_main.py` — not run; the only dirty tree found is on `planner-adr-wave`, not `main`, so the
  gate's own condition is never met.

## Proposed-but-not-executed

- **Off-main-primary**: the shared primary checkout at `/Users/kimba/Projects/nonoun/agent-ui` is
  on `planner-adr-wave` (0 ahead/0 behind `origin/main`) with an uncommitted 99-line edit to
  `.claude/docs/adr/0198-ask-flow-completion-flowend-meta-signal.md` and an untracked
  `.claude/ops/sweep-in-flight.json` — evidence this is a live session's in-progress work, not
  abandoned cruft. Recommended action for a human: once that session's own work is committed,
  pushed, or otherwise resolved, run `git checkout main` on the primary — never executed here,
  since switching a live peer's branch out from under it is exactly the #592 mistake this finding
  exists to catch.

## Risks

- 🟡 The primary checkout sitting off `main` mid-session is the exact shape of the #592 incident
  (fleet-rules) — not yet a repeat, since nothing here switched it, but it should not linger past
  whatever session currently owns `planner-adr-wave`.
- 🟢 The 6 merged-PR remote branches are now fully closed and reverified gone — no orphaned remote
  state remains.
- 🟢 `ops:reap-branches` correctly refused to reap the checked-out branch — its own gate held.

## Open questions

- None outstanding beyond the off-main-primary finding above, which is a human action, not an
  open question about this seat's own procedure.
