Sweep firing 2026-08-30T00:20:00Z — repo-cleaner report for /Users/kimba/Projects/nonoun/agent-ui.

Full report — findings changed versus `2026-08-29T170500Z-repo-cleaner.md`: (1) five PRs merged
since 17:05Z (#1705, #1706, #1707, #1708, #1712), all re-verified MERGED, all five remote branches
already deleted by GitHub's merge-time cleanup (nothing for `campaign_close.py`); (2) the planner
worktree is at `main`'s tip (detached HEAD `0d49969d`); (3) #1699 and #1702 resolved via #1708 and
#1707; (4) three new open issues #1709, #1710, #1711, unclaimed; (5) three scratch-clone dirs read
`REAP` under `reap-scratch-clones.mjs` dry-run, plus `scratchpad/1699-clone` found by hand (the
#1710 scan-gap; script does not see it).

## Inventory
- `git fetch --prune`: exit 0; pruned `origin/refactor/1699-agent-record-rename`.
- `gh auth status`: ok, `kimgranlund`, repo/workflow scopes.
- Worktrees (3): primary `main` at `0d49969d`; `agent-ui-planner` detached at `0d49969d` (fleet
  seat, kept); `agent-ui-reviewer` on `reviewer-worktree-2026-08-26` at `ac6b80f2` (fleet seat, kept).
- Local branches: `main`, `reviewer-worktree-2026-08-26`. Remote: `origin/main` only.
- `gh pr list --state open`: empty.
- `primary_checkout_check.py`: clean, exit 0. `git status --porcelain`: empty.
- `ops:reap-worktrees` dry: 2 KEEP rows (planner `KEEP(live-branch)`, reviewer `KEEP(fleet-seat)`).
- `ops:reap-branches` dry: 1 KEPT row (reviewer branch). Nothing reapable.
- `ops:reap-scratch-clones` dry: REAP `/private/var/folders/0b/jf4lh4jd4sd9y2q7x271c9jm0000gn/T/agent-ui-1702`
  (PR #1707), `.../T/agent-ui-1703` (PR #1705), `.../T/agent-ui-1704` (PR #1706); KEEP(dirty)
  `.../scratchpad/agent-ui-1701` (active build-1701). By hand: `.../scratchpad/1699-clone` clean at
  `5dc68112`, PR #1708 MERGED at that tip, REAP-by-hand (script blind, #1710).
- `gitignore_check.py`: same standing 6 G1 rules, not re-flagged.
- Open issues: #1701 claimed (build-1701, ~7h40m, no ruled staleness window, informational);
  #1709, #1710, #1711 unclaimed, fresh.

## Executed
- Nothing. Dry-run only per dispatch scope.

## Proposed-but-not-executed
- Reap four scratch clones: the three `T/agent-ui-170{2,3,4}` via
  `npm run ops:reap-scratch-clones -- --execute`; `scratchpad/1699-clone` by direct removal until
  #1710 lands.

## Risks
- Primary on `main` at parity. Fleet-seat worktrees correctly kept. `agent-ui-1701` clone correctly
  kept. #1710 gap corroborated, fix filed not shipped. G1 warnings unchanged.

Consecutive-unchanged-firing count: reset to 0.
