# issue-sorter sweep — kimgranlund/agent-ui — 2026-08-05T05:19:23Z

On-demand intake/triage sweep. Prior checkpoint: 2026-08-04T08:30:01Z (both `gh_issues`/`gh_prs`).
This firing's window: 2026-08-04T08:30:01Z → 2026-08-05T05:19:23Z.

**Write-sandbox note**: this session's cwd is the `ratify-adr-0168` worktree; the shared checkout
at `/Users/kimba/Projects/nonoun/agent-ui/.claude/ops/` refused the Write tool ("session is now
isolated... edit the worktree copy instead"), same split the prior 2026-08-04 firing hit (see its
plan.md item 2.1). State below was written to the worktree copy
(`.claude/worktrees/ratify-adr-0168/.claude/ops/`) only — **not yet committed anywhere**, and the
two locations are now out of sync (main checkout still reads the stale 2026-08-04T08:30:01Z
checkpoint). Rescuing/committing this onto main is a host-side follow-up, same shape as last time.

## 1. Discovery — both sources reachable, checkpoint advanced

`gh auth status` OK (kimgranlund, repo scope). `gh issue list --search "updated:>=..."` and
`gh pr list --search "updated:>=..."` both returned cleanly — no UNMEASURED source this firing.
`watch-checkpoint.json` (worktree copy) advanced both `gh_issues` and `gh_prs` to
`2026-08-05T05:19:23Z`.

## 2. Classify + trust-check — nothing to mint

Every issue/PR touched in the window (7 issues, 30 PRs — full list below) is authored by
`kimgranlund`, who is on `friendlies.json`'s `allow_list`. All are **already** properly filed
records (Kim's own `gh issue create`/`gh pr create` activity, not raw unsorted filings) — nothing
here is an unsorted item requiring a fresh mint or dedup-and-resume. `held-items.md`: no new holds
this firing (no unknown-author filings observed) — file left as-is, still accurate.

Issues touched: #445 (open, `doing`), #442 (closed), #438 (open), #436 (closed), #429 (closed),
#413 (closed), #406 (closed).
PRs touched: #449, #448, #447 (closed unmerged — regenerate-adr-index fixup superseded/rolled into
446's branch, not a defect), #446, #444, #443, #441, #440, #439, #437, #435, #434, #432, #430,
#420, #411, #410, #407, #405, plus a long tail #367–#401 (all pre-existing merges whose
`updatedAt` moved because of an unrelated bulk timestamp touch — no new content there).

## 3. Carried checks from the prior plan (`plan.md` items 3.2/3.3/4.1)

- **4.1 (stale `doing` labels on #413/#406)** — RESOLVED. Both issues checked directly
  (`gh issue view --json labels`): neither carries `doing` anymore (labels are `enhancement` +
  `size:big` only). No action needed, not re-flagging.
- **3.3 (MCP-deferral tracking record)** — SATISFIED by #438, filed 2026-08-04T13:41:53Z:
  "MCP client integration for the tool-enablement layer (deferred by ADR-0168)" — carries Summary,
  Trigger condition, and an Acceptance clause tied to a future ADR being ratified/rejected, plus
  Links to ADR-0168/SPEC v0.9/GH #49/PR #433. This is the shape plan.md item 3.3 asked for; treat
  it as closed, no further filing needed.
- **3.2 (friendlies.json policy fields)** — STILL OPEN, unchanged. `policy.roster_confirmed:
  false`, `policy.github_mcp_offer: null`. No dispatch has carried Kim's ruling on either since the
  prior firing. Not re-flagging as new — surfacing per the spec's "surfaced-but-pending" state
  (REQ-013 step 8) so it isn't silently dropped from the report trail. Backend is confirmed Option
  B/GitHub (git-native, ADR-0002/0003), repo is public, so step 8's gate is still met and still
  pending a carrying dispatch.

## 4. New hygiene finding this sweep — one more stale `doing` label

- **#436** ("[Feature] M-A SPEC — the SaaS Data Workbench spec") is `CLOSED`/`COMPLETED` but still
  carries the `doing` label — same defect class as the now-fixed #413/#406. Recommend the same
  fix: `gh issue edit 436 --remove-label doing`.
- Also found, while checking: six OLDER closed issues carrying a stale `doing` label that predate
  this firing's window and were never flagged by the prior sweep (#170, #144, #69, #68, #63, #52).
  Out of scope of "since last checkpoint" but real current mis-triage state — worth a batched
  `gh issue edit <n> --remove-label doing` sweep across all seven (#436 + these six) rather than
  fixing #436 alone and leaving the rest. This agent does not execute closes/label-removals itself
  beyond its own ticket-record contract — routing to the chore-planner queue as one hygiene item.

## 5. Full-repo scan for anything else unsorted/mis-triaged

- **No open item lacks a kind label.** Every currently-OPEN issue (#445, #438, #421) carries
  `enhancement` + a size label; #445 additionally carries `doing` (correctly — it's the active
  MA-1 build).
- **No item carries `needs-triage-approval`** — confirms nothing is currently stuck mid-hold.
- Several CLOSED issues have no `bug`/`enhancement` label at all (#383, #380, #377 — titled
  `[Task]...`, #321/#283 — `documentation` only, #276/#273/#197/#13 — no labels). This matches this
  repo's own stated convention (CLAUDE.md: `kind` is the `bug`/`enhancement` label; a `task` kind
  has no equivalent default GitHub label and legitimately ships unlabeled per `file-task`'s own
  unsized-is-legal default) — NOT flagging these as mis-triaged; they're closed, historical, and
  consistent with the documented scheme. #13 is a leftover `TEST issue-type probe (will delete)`
  artifact (closed 2026-07-18) — a repo-hygiene cleanup candidate, not a triage defect; noting only,
  not acting.
- No unattended/ambiguous-shape item required a `find-intent` round or a scheduled-firing generic-
  task fallback — nothing new to classify this firing at all.

## 6. Step 7 (REQ-011 bootstrap) / Step 8 (REQ-013 MCP offer)

Not re-run — `friendlies.json` already carries a seeded roster from the 2026-08-04 first firing;
this is not a first firing. Step 8's gate (Option B backend, no `github_mcp_offer` decision
recorded) is still met, and the offer is still surfaced-but-pending per §3 above — same named state
as last firing, not re-asked as new, not silently dropped.

## Done-state for this firing

- Every item discovered this window is classified and trust-checked; none required minting,
  resuming, or holding (all self-filed by the allow-listed author, already carrying complete
  records).
- Both sources reachable; both checkpoint entries advanced (worktree copy — see write-sandbox note).
- State changes written but **NOT yet committed** (host follow-up, same as the prior firing's
  plan.md item 2.1 pattern) — `watch-checkpoint.json` is the only file that changed this firing;
  `friendlies.json` and `held-items.md` are unmodified.
- Report exists at this path. Step 8's outcome for this firing: **offered-and-still-pending** (not
  re-asked, re-surfaced per §3/§6).

**NOT done**: the checkpoint update sits uncommitted in the worktree, split from the main
checkout's stale copy — a host-side commit/rescue is required before the next firing to avoid
re-diverging the two locations further.
