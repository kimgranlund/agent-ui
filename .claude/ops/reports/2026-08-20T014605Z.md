# repo-cleaner firing — 2026-08-20T01:46:05Z

Full report (not abbreviated) — executed set differs materially from the prior firing
(`.claude/ops/reports/2026-08-19T215606Z-repo-cleaner.md`, which closed 6 PR branches + 3
orphaned markers): this firing closed 33 merged-PR remote branches (the largest backlog this
seat has measured) and ran `reap-worktrees.mjs --execute` for the first time alongside
`reap-branches.mjs --execute`.

## Inventory
- `git fetch --prune`: 10 stale `origin/*` refs pruned before survey.
- Worktrees (start of firing): primary (`main`), `1466-routing-followups` (locked),
  `agent-a391225d4e8890e21`/`1489-summary-action-card` (locked — advanced mid-firing
  `13febfb4`→`5fab8aab`, live concurrent work, not touched), plus four unlocked worktrees each
  already at a MERGED PR's tip: `agent-a3f9d0396dc5a7ae7`/`1475-footer-actions-row`,
  `agent-a4643297c6eb62fc7`/`1480-review-card-seed`, `agent-ab66b0cb430458ad4`/`1478-playing-card-design`,
  `agent-ae9dc38971e5b908e`/`1479-features-list-card`.
- `primary_checkout_check.py` → clean, both times run.
- `branch.main.merge`: correctly `refs/heads/main` — the prior firing's flagged misconfig is
  already resolved, no longer a finding.
- PR cross-reference: 0 open PRs repo-wide. 33 remote branches sat on already-MERGED PRs
  (`delete_branch_on_merge` is `false` on this repo, confirmed via `gh api`): #1420, #1442,
  #1449, #1464, #1444, #1446, #1463, #1350, #1400, #1390, #1401, #1358, #1388, #1357, #1362,
  #1361, #1367, #1384, #1408, #1409, #1413, #1439, #1461, #1481, #1482, #1490, #1484, #1488,
  #1360, #1404, #1385, #1381, #1405.
- Host-repo reap scripts (both named in `package.json`; `reap-worktrees.mjs` additionally named
  and ordered in `seat-map` §Dispatch laws — worktree reap first, branch reap second): dry-run
  `reap-worktrees.mjs` → 4 REAP (merged-PR tips) / 2 KEEP(locked); dry-run `reap-branches.mjs`
  (re-run after worktree reap) → 4 REAP local branches (merged) + 4 REAP orphaned worktree
  markers / 2 KEPT (still-live worktrees).
- Ticket-claim convention: none ruled in this repo (unchanged) — stale-claim gate not-applicable.
- `gitignore_check.py`: same 6 stale G1 rules as the prior firing (`npm-debug.log*`, `*.local`,
  `.vscode/*`, `.idea`, `*.sw?`, `.claude/docs/other`) — standing noise, exit 0.
- Dirty `main`: two untracked paths — `.claude/ops/sweep-in-flight.json`
  (`{"startedAt":"2026-08-20T01:40:26.903Z","pid":5719,"session":"agent-ui-86-8e38eb01"}`) and
  `.claude/overhaul-run-2026-08-20.md` — both independently evidenced as a live parallel
  `/overhaul-execute` session's own in-progress marker/record, not cruft.

## Executed
- `campaign_close.py` on all 33 merged-and-verified PRs listed above — every one clean/exit 0
  (C1/C4/C5 ok, C2 branch deleted+reverified gone).
- `node scripts/reap-worktrees.mjs --execute` — removed the 4 REAP-classified worktrees (all
  merged-PR tips); correctly kept the 2 locked ones.
- `node scripts/reap-branches.mjs --execute` (run second, per `seat-map`'s own ordering) —
  deleted the 4 now-unworktreed local branches (`1475-footer-actions-row`,
  `1478-playing-card-design`, `1479-features-list-card`, `1480-review-card-seed`) plus 4
  orphaned worktree-agent markers (`worktree-agent-a3f9d0396dc5a7ae7`,
  `worktree-agent-a4643297c6eb62fc7`, `worktree-agent-ab66b0cb430458ad4`,
  `worktree-agent-ae9dc38971e5b908e`); correctly kept `1466-routing-followups` and
  `1489-summary-action-card` (still-live locked worktrees).
- Re-verified after execution: `git branch -r` now shows only `origin/main`; `git worktree list`
  down to primary + the 2 locked worktrees; `primary_checkout_check.py` re-run clean.
- `sync_main.py`: **not run** — the two untracked paths on `main` are independently evidenced as
  a live parallel session's own in-flight work (a running `/overhaul-execute` process, pid 5719,
  still active), not cruft; quarantining via stash would reproduce the exact failure mode
  `sync_main.py`'s own docstring names as its uncovered gap.

## Proposed-but-not-executed
- Dirty `main` (the two paths above): recommend the dispatching session leave them alone until
  the live overhaul-run session concludes and commits its own record; do not stash.
- `.gitignore`'s 6 stale G1 rules: standing noise, not re-proposed as new (unchanged from prior
  firing).
- `1466-routing-followups` local branch: its `origin` tracking ref reads `gone` (remote branch
  already deleted — this branch's own PR #1471 was CLOSED-not-merged, superseded by the merged
  `1466-routing-rebased`/PR #1473) but it sits in a currently-locked worktree, so both gated
  scripts correctly declined to touch it; propose a human check whether that worktree/lane is
  still live before any future reap.

## Risks
- 🟢 33 merged PRs' remote branches closed+reverified gone; 4 worktrees + 8 local-branch/marker
  rows reaped via the two gated scripts; both live worktrees correctly preserved throughout.
- 🟢 Primary checkout clean on `main`, byte-identical to `origin/main`; `branch.main.merge`
  misconfig from the prior firing is already resolved.
- 🟡 Dirty `main` (2 untracked paths) withheld from `sync_main.py` — correct given the
  live-session evidence, but needs the dispatching session to leave it alone rather than sweep it.
- 🟡 `1466-routing-followups`'s stale-but-locked branch — named above, no action taken, needs a
  human check on that worktree's liveness before any future firing reaps it.

## Open questions
- None requiring a human answer beyond the two 🟡 items above, each already carrying a named
  recommended action.
