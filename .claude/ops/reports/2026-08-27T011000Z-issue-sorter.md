# issue-sorter report — kimgranlund/agent-ui — 2026-08-27T01:10:00Z

Firing: /sweep-chores fan-out (decision-watcher, repo-cleaner, issue-sorter all in scope).
Checkpoint window: 2026-08-26T22:05:00Z → 2026-08-27T01:10:00Z.

## Discovery

- gh_issues: REACHABLE. `gh issue list --search "updated:>=2026-08-26T22:05:00Z -label:backlog -label:roadmap" --state all` → 2 items: #1680, #1681.
- gh_prs: REACHABLE. `gh pr list --search "updated:>=2026-08-26T22:05:00Z" --state all` → 0 items.

## Items processed

### #1680 — reap-worktrees.mjs can't tell a standing fleet-seat worktree from a finished campaign worktree
- Author: kimgranlund (allow-listed, matches policy.roster_confirmed_by — no user-signal tag).
- Classification: task (defect-adjacent gap in tooling, not itself a code bug in shipped product surface — file-task's own call, confirmed independently).
- Labels present: `task`, `size:small` — verified correct via direct gh read, not assumed from dispatch context.
- Record shape: Summary/Acceptance/Links/Scope-Open/empty Findings all present and conformant.
- Trust-check: T1 (allow-listed, self-authored by the roster-confirmed operator).
- Action: RESUME (no-op) — already correctly minted and labeled by file-task's own contract at filing time. No duplicate found on dedup sweep.

### #1681 — ADR-0140 rename sweep: 24 pre-2026-07-18 ADRs still name stale --ui-* foundation tokens
- Author: kimgranlund (allow-listed, matches policy.roster_confirmed_by — no user-signal tag).
- Classification: task (corpus-wide doc-repair sweep, not a bug/feature/ruling-shaped item).
- Labels present: `task`, `size:big` — verified correct via direct gh read, not assumed from dispatch context.
- Record shape: Summary/Acceptance/Links/Scope-Open/empty Findings all present and conformant.
- Trust-check: T1 (allow-listed, self-authored by the roster-confirmed operator).
- Action: RESUME (no-op) — already correctly minted and labeled by file-task's own contract at filing time. No duplicate found on dedup sweep.

## PRs

None discovered this window.

## Held items

None held this firing — no unknown-author items discovered. `held-items.md` unchanged; no payload emitted for it.

## REQ-011 / REQ-013

Both already resolved on `friendlies.json`'s `policy` block (roster_confirmed: true since 2026-08-05;
github_mcp_offer: declined since 2026-08-05) — not-applicable this firing, not re-evaluated. No
`.mcp.json` payload. `friendlies.json` otherwise unchanged; no payload emitted for it.

## Non-ticket note

A v0.1.0 package release shipped inside this checkpoint window per dispatch context — no
ticket-record concept applies (ADR-0145 tickets are GitHub Issues/PRs only); no action taken.

## Checkpoint

Both gh_issues and gh_prs sources reached successfully this firing — both entries advance to the
firing timestamp. See `.claude/ops/watch-checkpoint.json` payload below.

Done-when check: every item discovered this firing (2/2: #1680, #1681) is classified,
trust-checked, and resumed (already correctly minted); both reachable sources' checkpoint entries
advanced; no item silently dropped; no unknown-author item auto-created or self-approved.
