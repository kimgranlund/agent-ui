<!-- target-path: .claude/ops/reports/2026-08-10T050520Z.md -->
# repo-cleaner sweep — agent-ui — 2026-08-10T05:05:20Z

**Status: 🟢 done.** This firing's classification set differs from the most recent prior report on
disk (`.claude/ops/reports/2026-08-09T145512Z.md`) — count resets to 1 (genuinely new/changed
findings). Note: the intervening sixth-sweep report (`2026-08-09T202038Z.md`) never landed as a
file (known carried debt, `.claude/ops/plan.md` hygiene item 4.2 — not re-discovered here, not
re-flagged as new). Changes since the last file actually on disk: ~35 more PRs merged (up through
`#671`), the previously-flagged merged-stale worktree/branch (`agent-a76f2b0def232c02f` /
`worktree-agent-a76f2b0def232c02f`) is now fully gone (reaped by a human or another process since
that report — its own proposal is resolved), the open-issue set turned over completely (3 → 7,
different numbers), and `.gitignore` warnings dropped 8 → 7.

## Live-session context (from dispatch, verified not stale)

Main (`13033f9f`) carries uncommitted changes to
`packages/agent-ui/app/src/controls/agent-admin/{agent-admin-authoring.test.ts,agent-admin.css,agent-admin.ts}`
and `packages/agent-ui/app/src/controls/conversation/{conversation.md,conversation.ts}`. This
matches `task-666b`'s live work on GH `#666`/`#670`/`#673` exactly — `#666` was reopened today for
a pixel-truth follow-up (Kim: "the center pane should be a CHAT, just like 'Test chat'") whose
target files are precisely these. Confirmed live, not orphaned, not touched. Per dispatch
instruction, not proposed for `sync_main.py` even though this is an interactive dispatch — the
tree isn't cruft, it's a named session's in-progress edit.

One dispatch-brief expectation did NOT hold: the brief anticipated multiple live worktrees for the
parallel `#666`/`#670`/`#673` work. `git worktree list` shows exactly one worktree (the main
checkout itself) — that parallel work is evidently all landing as direct edits in the shared main
checkout rather than separate worktrees. Recorded as an observation, not a finding requiring action.

## Inventory

- **Worktrees**: one — the main checkout (`13033f9f`, on `main`, tracking `origin/main` exactly,
  uncommitted live-session diff as above).
- **Local branches**: `main` only, healthy, up to date with `origin/main`.
- **Remote branches** (post-`git fetch --prune`): `origin/main` only. Ten stale local
  remote-tracking refs were pruned this firing: `origin/chore/660-retirement-repairs`,
  `origin/design/651-s1-lld`, `origin/design/651-three-pane-intake`,
  `origin/feat/651-s1-three-pane-shell`, `origin/feat/656-settings-grouping`,
  `origin/feat/658-live-fill-proof`, `origin/feat/662-triple-dock`, `origin/fix/665-triple-polish`,
  `origin/fix/665b-drop-settings-kicker`, `origin/fix/666-composer-first-author` — every one
  corresponds to a PR (`#661`/`#654`/`#652`/`#655`/`#657`/`#659`/`#663`/`#667`/`#668`/`#671`) that
  merged with a correctly-working remote-side delete; per the `#613` ruling
  (`.claude/ops/rulings.md`) these are local-ref staleness, not real `#613` survivors — no
  survivor found this firing.
- **PRs** (`gh pr list --state all` + `gh api search/issues` cross-check): 389 total, 388 MERGED, 1
  CLOSED-unmerged (`#447`, old, no branch remnant), **0 OPEN**. Most recent merge `#671`
  (`13033f9f`, matches main's own HEAD exactly).
- **Open issues** (`gh issue list --state open`): 7 —
  - `#673` bug/`doing`, filed+root-caused today (2026-08-10), no linked PR yet — active, matches
    live session context.
  - `#672` enhancement/`size:small`, no `doing`, fresh, unclaimed backlog.
  - `#670` bug/`doing`, filed+root-caused today, follow-up of `#666`, no linked PR yet — active,
    matches live session context.
  - `#669` bug/`doing`, filed+root-caused today, no linked PR yet — active, matches live session
    context.
  - `#666` task/`doing`, `size:small` — PR `#671` already merged for its first fix, then REOPENED
    today (pixel-truth: the unarmed Author column needs the "same card treatment as Test chat")
    — directly matches main's uncommitted diff, live.
  - `#664` task/`size:small`, no `doing`, fresh (updated today), unclaimed backlog.
  - `#616` task/`size:small`, no `doing`, unchanged from every prior sweep — external upstream wait
    (`a2ui-project/a2ui#2150`).
  - No ADR-0005 claim-staleness gate applies to this repo (its `ADR-0005` is
    `0005-lazy-upgrade-property-wins.md`, unrelated to ticket-claim staleness — reconfirmed same as
    every prior sweep). All `assignees: []`; `doing` here is a plain label, not an assignee-based
    claim convention, so this degrades to git-surface-only hygiene per this agent's own scope rule.
    None of the `doing`-labeled issues are stale by any reasonable window regardless — all four
    were touched within hours of this firing.
- **`.gitignore`** (`gitignore_check.py`): exit 0, 7 `G1` warnings (down from 8 in the last report
  on disk — `.vitest-attachments/` no longer flagged), no `G2` FAIL.

## Classification

| Item | Classification |
|---|---|
| Main checkout | healthy — live-editing-session (task-666b, `#666`/`#670`/`#673`), correctly left untouched |
| Local branch `main` | healthy |
| Remote branches | healthy — `origin/main` only post-prune, 10 local-ref-staleness non-survivors (see `#613` ruling), zero real survivors |
| PRs | 0 open; 388 MERGED all already clean (no surviving remote branch); 1 historical CLOSED (`#447`), no action |
| Issue `#673` | healthy-open, active, matches live session — not stale |
| Issue `#670` | healthy-open, active, matches live session — not stale |
| Issue `#669` | healthy-open, active — not stale |
| Issue `#666` | healthy-open, active, reopened same day, matches live session's uncommitted diff — not stale |
| Issue `#672` | healthy-open, fresh unclaimed backlog |
| Issue `#664` | healthy-open, fresh unclaimed backlog |
| Issue `#616` | healthy-open, unchanged, external upstream wait |
| `.gitignore` | 7 stale `G1` rules, unchanged proposal carried forward, exit 0 |

## Executed — evidence

Nothing executed this firing:
- No merged PR has a surviving remote branch (`campaign_close.py` had no valid target — the prune
  step above already shows every merged-PR branch cleanly gone).
- `sync_main.py` not run — main's dirty state is a named live peer session's in-progress work per
  the dispatch brief, not quarantine-eligible cruft, regardless of this being an interactive
  dispatch.
- No host-repo gated reap script exists — checked `package.json`, `CLAUDE.md`, `README.md` for a
  named branch-reap command; none found. Moot this firing anyway (no dangling worktree/branch to
  reap).

## `gitignore_check.py` — read-only, exit 0

```
gitignore_check · warn · /Users/kimba/Projects/nonoun/agent-ui
  WARN  G1  `*.log` matches nothing in the tree -> stale, retire it
  WARN  G1  `npm-debug.log*` matches nothing in the tree -> stale, retire it
  WARN  G1  `*.local` matches nothing in the tree -> stale, retire it
  WARN  G1  `.vscode/*` matches nothing in the tree -> stale, retire it
  WARN  G1  `.idea` matches nothing in the tree -> stale, retire it
  WARN  G1  `*.sw?` matches nothing in the tree -> stale, retire it
  WARN  G1  `.claude/docs/other` matches nothing in the tree -> stale, retire it
EXIT:0
```
Down from 8 in the last report on disk; `.vitest-attachments/` dropped off the warning list.

## Claimed-ticket sweep (ADR-0005 gate)

Not applicable, reconfirmed: this repo's `ADR-0005` is `lazy-upgrade-property-wins`, unrelated to
ticket-claim staleness. Work items route to GitHub Issues (ADR-0145) with a plain `doing` label,
not an assignee-based claim convention (all 7 open issues carry `"assignees":[]`). Degrades to
git-surface-only hygiene per this agent's own scope rule.

## Proposed-but-not-executed

- Retire the 7 stale `G1` `.gitignore` lines flagged above (carried forward, Kim-ruled standing
  noise per `.claude/ops/plan.md`, no new proposal needed).
- Nothing else — no worktree, local branch, or PR requires cleanup; no stale claim exists; main's
  live dirty state is correctly left alone.

## Risks

- 🟢 Zero orphaned/contested worktrees. Zero open PRs. Zero `#613`-class survivors. `.gitignore`
  warn-only, shrinking trend (17→8→7 across the last three landed reports).
- 🟢 Main's live uncommitted work (task-666b) correctly identified via the dispatch brief and left
  untouched — no false-positive dirty-main quarantine.
- 🟡 Known carried debt, not rediscovered here: `.claude/ops/plan.md` hygiene items 4.2 (the
  missing `2026-08-09T202038Z.md` report artifact) and 4.3 (cross-repo harness-agent contract
  encoding in `nonoun-plugins`) — both still open, both out of this seat's scope.
