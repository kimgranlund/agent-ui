Sweep firing 2026-08-27T23:20:00Z — repo-cleaner report for /Users/kimba/Projects/nonoun/agent-ui.

Full report, not abbreviated — genuinely new/changed findings versus the prior report
(`2026-08-27T180000Z-repo-cleaner.md`): (1) PR #1688 merged since last firing, leaving one new
stale `origin/*` tracking ref pruned this firing; (2) its own scratch clone (`agent-ui-1687`) was
this firing's only REAP row and was executed-reaped, distinct from the prior firing's three
different clones; (3) ticket #1686 is now open/claimed (freshly, ~20 min old) where the prior
firing showed it as unassigned.

## Inventory
- `git fetch --prune`: exit 0. Pruned 1 stale `origin/*` ref (`origin/1687-adr-0040-adr-0049`) —
  its PR (#1688) merged and GitHub deleted the remote branch; local tracking ref was stale until
  this fetch. Survey below is against fresh refs.
- Worktrees (`git worktree list`, 2 total, matches dispatch's stated expectation):
  - primary `/Users/kimba/Projects/nonoun/agent-ui`, HEAD `720b907d`, branch `main`.
  - `/Users/kimba/Projects/nonoun/agent-ui/.claude/worktrees/agent-ui-reviewer`, HEAD `ac6b80f2`,
    branch `reviewer-worktree-2026-08-26` — standing fleet-seat infrastructure per dispatch
    context, confirmed `KEEP(fleet-seat)` again this firing. NOT proposed for reap.
- `git branch -vv`: `main` (tracking `origin/main`, exact parity, `720b907d`) and
  `reviewer-worktree-2026-08-26` (checked out in the reviewer worktree). No other local branches.
- `git branch -r`: only `origin/HEAD -> origin/main` and `origin/main` — zero leftover merged-PR
  remote branches. Nothing for `campaign_close.py` to act on this firing.
- `gh pr list --state open`: **0 open PRs** — matches dispatch's stated expectation.
- `gh pr list --state all` (recent window): newest merged is #1688 (23:11:34Z 2026-08-27,
  "restate ADR-0040/ADR-0049 family-barrel budget at its live figure"), branch
  `1687-adr-0040-adr-0049` — remote branch already gone (see fetch-prune above); nothing new for
  `campaign_close.py`.
- `primary_checkout_check.py /Users/kimba/Projects/nonoun/agent-ui`: `clean`, exit 0 — no
  off-main-primary risk. `HEAD` (`720b907d`) matches `origin/main` exactly.
- `npm run ops:reap-branches -- --dry`: 1 row — `reviewer-worktree-2026-08-26` classified `KEPT`
  ("checked out in .../agent-ui-reviewer") — 0 fail / 0 warn. Nothing to `--apply`.
- `npm run ops:reap-worktrees -- --dry`: 1 row — `.claude/worktrees/agent-ui-reviewer` /
  `reviewer-worktree-2026-08-26` classified `KEEP(fleet-seat)` (live fleet seat, per
  `.claude/ops/fleet.json` `live_state.joined`) — 0 fail / 1 warn (informational, not a conflict).
  Unchanged from prior firings; already-confirmed KEEP, not re-flagged as a finding.
- `npm run ops:reap-scratch-clones -- --dry` (FIRST pass, before execution): **2 rows** —
  `/private/var/.../agent-ui-1686` classified `KEEP(dirty)` (`git status --porcelain` non-empty —
  ticket #1686 is live, currently-building work per dispatch context; correctly left alone, not
  treated as orphaned residue) and `/private/var/.../agent-ui-1687` classified `REAP` (branch
  `1687-adr-0040-adr-0049`, "PR #1688 MERGED at this tip"). Independently re-verified via
  `gh pr view 1688`: `state: MERGED`, `mergedAt: 2026-08-27T23:11:34Z`, `headRefName:
  1687-adr-0040-adr-0049` — matches the script's own tip claim exactly. Executed per procedure
  step 3 (see Executed below).
- `npm run ops:reap-scratch-clones -- --dry` (SECOND pass, after execution): 1 row —
  `agent-ui-1686` still `KEEP(dirty)`, unchanged. `agent-ui-1687` no longer listed. Confirms the
  execute landed and #1686's live build was left untouched.
- `gitignore_check.py /Users/kimba/Projects/nonoun/agent-ui`: same standing 6 stale G1 rules as
  every recent firing (`npm-debug.log*`, `*.local`, `.vscode/*`, `.idea`, `*.sw?`,
  `.claude/docs/other`) — standing-accepted, not re-flagged.
- Ticket-claim convention: unchanged — no host-repo-numbered ADR names a ticket-claim convention
  here. Stale-claim gate degrades to git-surface-only hygiene; the read below is informational.
- Open issues (`gh issue list --state open`): **1** — matches dispatch's stated expectation.
  - #1686 "doc-standards has no evals/ directory — /check-routing has nothing to verify against" —
    assigned to kimgranlund, labels `task`/`in-flight`, claimed via comment "Claimed by build-1686
    (dispatch-ticket) at 2026-08-27T23:00:13Z on branch `1686-doc-standards-has-no`", `updatedAt`
    2026-08-27T23:00:23Z — ~20 minutes old at this firing's timestamp. No linked open PR yet
    (build in progress, per dispatch context, confirmed by the scratch clone's own `KEEP(dirty)`
    read above). Fresh claim, not stale by any reasonable window — no reclaim proposed.
- Dirty primary (`git status --porcelain`): only `?? .claude/ops/sweep-in-flight.json` — this
  firing's own dispatching-session marker, per dispatch context, not residue. `sync_main.py` not
  applicable regardless: `main` is at exact parity with `origin/main` and this is a scheduled sweep
  firing (never appropriate for `sync_main.py` even with unexplained dirt, which this isn't).

## Executed
- `npm run ops:reap-scratch-clones -- --execute`: removed the 1 REAP-classified directory
  (`agent-ui-1687`), independently re-verified against its PR's (#1688) MERGED state via a direct
  `gh pr view` call (not taken on the script's own claim alone) before executing. Re-run `--dry`
  confirms it is gone and `agent-ui-1686` (ticket #1686's live build) was correctly left alone.
- Nothing else executed. No merged PR left a remote branch behind (nothing for `campaign_close.py`
  — GitHub's own PR-merge branch deletion already handled #1688), `main` is at exact parity with
  `origin/main` and this is a scheduled firing regardless (nothing for `sync_main.py`),
  `reap-branches --dry` found 0 reapable rows, `reap-worktrees --dry` found 0 `REAP` rows.

## Proposed-but-not-executed
- No stale-open PRs, no orphaned worktrees, no stale local branches, no off-main-primary findings
  this firing.
- No stale-claim findings: the one open issue (#1686) is freshly claimed (~20 minutes old) with an
  active, dirty scratch clone backing it — informational only, no reclaim proposed.

## Risks
- 🟢 Primary checkout correctly stays on `main`, exact parity with `origin/main` (`720b907d`).
- 🟢 Zero open PRs; zero leftover merged-PR remote branches — nothing for `campaign_close.py`.
- 🟢 Reviewer worktree still correctly `KEEP(fleet-seat)`, not `REAP` — the #1680 fix continues to
  hold, re-confirmed independently this firing rather than assumed from the prior report.
- 🟢 `reap-scratch-clones.mjs` correctly distinguished #1686's live, dirty, in-progress clone
  (`KEEP(dirty)`) from #1687's merged-and-idle one (`REAP`) — the gate worked exactly as the
  dispatch context predicted; the one execute this firing is independently re-verified, not taken
  on the script's own say-so alone.
- 🟢 One open issue (#1686), freshly claimed and actively building — no staleness risk at this
  age.
- 🟢 Standing G1 gitignore warnings (6 rules) unchanged, not re-flagged.
- 🟢 `.claude/ops/sweep-in-flight.json` untracked-but-intentional (this firing's own marker) — not
  drift.

Consecutive-unchanged-firing count: reset to 0 (this firing) — the new merged PR, the different
scratch-clone REAP target, and #1686's claim state all changed relative to
`2026-08-27T180000Z-repo-cleaner.md`.
