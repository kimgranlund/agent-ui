# repo-cleaner sweep — agent-ui — 2026-08-17T042857Z

**Status: 🟢 done.** Classification set differs from the most recent report on disk
(`.claude/ops/reports/2026-08-16T202951Z.md`) — count resets to 1. Worktree topology grew from
1 (main only) to 6: main + 5 **locked** worktrees under `.claude/worktrees/agent-*` belonging to a
LIVE peer session (AGENT-UI-2) per this firing's dispatch brief — reported only, never touched.
Two squash-merged `#956`-arc local branches and five zero-PR "worktree-agent-*" bookkeeping local
branches surfaced as orphans with no gated deletion path (no host-repo reap script exists in this
repo). One stale local remote-tracking ref (`origin/docs/subpath-coverage-gaps`) verified via
`campaign_close.py 1040` — PR #1040 reads MERGED, its remote branch was already absent before this
run (deleted by an earlier campaign_close firing or manually), so the script exited clean with
nothing further to delete; only the local remote-tracking ref itself is stale, and pruning that ref
is not one of this agent's gated executes, so it's proposed, not run. `main` carries only the known
pre-existing `.claude/settings.json` edit named in the dispatch brief — not dirty beyond that, so
per explicit dispatch instruction `sync_main.py` was **not** run this firing. 0 open PRs. 8 open
issues: 5 (#1042–#1046) assigned to Kim and exactly matching the 5 live peer worktrees' branch
names/topics (`1042-composition-hub` … `1046-persistence-guide`) — healthy, not stale; 3
(#1047–#1049) unassigned backlog. This repo's own ADR-0005 is `lazy-upgrade-property-wins`, not the
ticket-claim ADR, so the stale-claim gate is not applicable here (git-surface-only hygiene, per
`clean-git`'s own degrade clause).

## Inventory

- **Worktrees** (`git worktree list`), 6: main checkout (`521ce770`) + 5 **locked** peer worktrees
  under `.claude/worktrees/agent-*` on branches `1042-composition-hub`, `1043-motion-guide`,
  `1044-accessibility-guide`, `1045-event-vocabulary`, `1046-persistence-guide`, all at `521ce770`
  — belong to LIVE peer session AGENT-UI-2 per dispatch brief; report only, untouched.
- **Local branches** (`git branch -vv`), 13:
  - `main` @ `521ce770`, tracking `origin/main`, `+0 -0` — the known `.claude/settings.json` edit
    only (see Working tree below).
  - `1042-composition-hub` / `1043-motion-guide` / `1044-accessibility-guide` /
    `1045-event-vocabulary` / `1046-persistence-guide` — the 5 peer-worktree branches, all
    checked out by the locked worktrees above. Report only.
  - `956-agent-ui-data` @ `b1dc7cdd`, upstream `[origin/956-agent-ui-data: gone]` — matches PR
    #1022 (`feat(data): @agent-ui/data — DataSource seam, gateway client, stream bridge`),
    confirmed **MERGED**. Remote branch already deleted. NOT in `git branch --merged main`
    (squash-merged, so ancestry check doesn't see it as merged even though its PR is) — a plain
    `git branch -d` would refuse; `-D` is the correct force-delete once the PR-merged fact is
    independently confirmed, which it is here.
  - `956-data-n5-finish` @ `5d3d9ca7`, upstream `[origin/956-data-n5-minors: gone]` — head commit
    message matches PR #1033 (`fix(data): #956 n5 — the seven code-checker minors …`), confirmed
    **MERGED**. Remote branch already deleted. Also absent from `git branch --merged main`
    (squash-merged) — same `-D` note applies.
  - `worktree-agent-a1617e1db6fb96d3e`, `worktree-agent-a1fb2767242640421`,
    `worktree-agent-a5ccd7ce12129625c`, `worktree-agent-a739f3fc68fb50f34`,
    `worktree-agent-ae10cb27246964846` — all sitting exactly at `521ce770` (== current `main` tip),
    not checked out by any current worktree, and `gh pr list --search "head:<branch>"` returns
    `[]` for every one of the five — zero PR trace, apparent leftover bookkeeping refs from an
    earlier worktree-naming convention. Orphaned, no gated deletion path.
- **Remote branches** (`git branch -r` / `git ls-remote --heads origin`), effectively 1 live +
  1 stale local ref: `origin/main` (healthy); `origin/docs/subpath-coverage-gaps` present as a
  **local** remote-tracking ref but absent from `git ls-remote --heads origin` — `git remote prune
  origin --dry-run` confirms `[would prune] origin/docs/subpath-coverage-gaps`. Its PR (#1040) is
  the current `main` tip commit itself, confirmed MERGED via `campaign_close.py 1040` (see
  Executed below) — the real remote branch is already gone; only the local tracking ref is stale.
- **PRs**: 0 open (`gh pr list --state open` → `[]`). Full history for the branches above reads
  MERGED (#1022, #1029, #1033, #1040, #996, #967 — the whole `#956`/data + docs-subpath arcs).
- **Stashes**: none observed.
- **Working tree**: one dirty file, `.claude/settings.json` (2 insertions, 1 deletion) — the known
  pre-existing local edit named in the dispatch brief. Left untouched; `sync_main.py` NOT run
  (main not dirty beyond this named file, and this is an interactive dispatch that still names the
  exception explicitly).
- **`.gitignore`** (`gitignore_check.py /Users/kimba/Projects/nonoun/agent-ui`): exit 0, 7 `G1`
  warnings (`*.log`, `npm-debug.log*`, `*.local`, `.vscode/*`, `.idea`, `*.sw?`,
  `.claude/docs/other`) — identical set to the prior report, standing Kim-ruled noise
  (`.claude/ops/rulings.md`), not re-proposed.
- **Open issues** (`gh issue list --state open`), 8 total: #1042–#1046 assigned to Kim
  (`kimgranlund`), titles/numbers exactly matching the 5 live peer worktree branches, all with
  `updatedAt` within the last few hours — healthy, actively worked, not stale. #1047–#1049
  unassigned backlog, no claim to assess. ADR-0005 claim-staleness gate: not applicable — this
  repo's own ADR-0005 is `lazy-upgrade-property-wins`, not the ticket-claim ADR (git-surface-only
  hygiene per `clean-git`'s degrade clause).
- **Host-repo reap script check**: none named in `package.json` `scripts` block, `README.md`, or
  `CLAUDE.md` (grepped for `reap`/`ops:reap`/`reap-branches` — no match). Local-branch/worktree
  cleanup stays propose-only for every orphan found this firing.

## Classification

| Item | Classification |
|---|---|
| Main checkout | healthy — at `origin/main` tip, one known dirty file left per brief |
| 5 locked peer worktrees (`1042`–`1046`) + their branches | healthy, **do not touch** — live AGENT-UI-2 session, report only |
| Local branch `956-agent-ui-data` | orphaned — PR #1022 MERGED, remote already gone, squash-merged (not in `--merged main`), no gated local-delete path → propose |
| Local branch `956-data-n5-finish` | orphaned — PR #1033 MERGED, remote already gone, squash-merged, no gated local-delete path → propose |
| 5 `worktree-agent-*` bookkeeping branches | orphaned — zero PR trace, at main tip, not worktree-attached, no gated local-delete path → propose |
| Remote-tracking ref `origin/docs/subpath-coverage-gaps` | stale — PR #1040 verified MERGED via `campaign_close.py` (ran clean, nothing to delete remotely); only the local ref is stale → propose `git remote prune origin` (not one of this agent's gated executes) |
| PRs (0 open; full history MERGED) | healthy — nothing to close |
| Open issues #1042–#1046 (assigned, active) | healthy — matches live peer worktrees, not stale |
| Open issues #1047–#1049 (unassigned) | healthy-open — no claim, nothing to flag |
| `.gitignore` | 7 stale `G1` rules, standing Kim-ruled noise, exit 0 |
| Dirty file `.claude/settings.json` | left alone per dispatch instruction |

## Executed — evidence

- `campaign_close.py 1040` — ran to independently verify PR #1040's MERGED state before treating
  `origin/docs/subpath-coverage-gaps` as anything but live. Output:
  ```
  campaign_close · clean
    ok   C1  PR merged
    ok   C4  no open PR uses docs/subpath-coverage-gaps as its base
    ok   C2  docs/subpath-coverage-gaps already absent from remote before this run
  ```
  exit 0. Nothing to delete remotely — the real branch was already gone; no further mutation
  performed by the script or by this agent.
- `sync_main.py` — not run. `main`'s only dirty file is the named-exception
  `.claude/settings.json`; per dispatch brief, quarantine only fires if main is dirty beyond that
  file, which it is not.
- No host-repo gated reap script exists → no local-branch/worktree reap executed.
- No other gated execute case applied this firing.

## Proposed-but-not-executed

- **`git branch -D 956-agent-ui-data`** — PR #1022 independently confirmed MERGED, remote branch
  already deleted; local branch is squash-merged (won't `-d` cleanly) — safe to force-delete once
  a human confirms no unpushed work sits on it beyond the squashed PR content.
- **`git branch -D 956-data-n5-finish`** — same shape: PR #1033 confirmed MERGED, remote already
  deleted, squash-merged locally.
- **`git branch -D worktree-agent-a1617e1db6fb96d3e worktree-agent-a1fb2767242640421 worktree-agent-a5ccd7ce12129625c worktree-agent-a739f3fc68fb50f34 worktree-agent-ae10cb27246964846`**
  — five zero-PR bookkeeping branches, all at the current `main` tip, not attached to any live
  worktree. No gated reap path in this repo; propose only.
- **`git remote prune origin`** — clears the one stale local remote-tracking ref
  (`origin/docs/subpath-coverage-gaps`) confirmed already deleted on GitHub via `campaign_close.py`
  above. Not itself one of this agent's three (or conditional fourth) gated executes, so proposed
  rather than run.
- `.gitignore` 7 `G1` warnings — standing carried noise, not re-proposed.

## Risks

- 🟢 `campaign_close.py` confirms #1040's real remote branch already gone — no silent-delete-failure
  risk on that one.
- 🟡 5 locked peer worktrees + their branches are live AGENT-UI-2 state — correctly excluded from
  every proposal above; re-verify liveness before any human acts on the propose-only list, in case
  the peer session has since closed out.
- 🟡 Two `#956` local branches and five `worktree-agent-*` branches are orphaned with **no** gated
  script covering local-branch deletion in this repo — they will keep reappearing in every future
  sweep's inventory until a human runs the proposed `git branch -D` commands (or this repo grows
  its own reap script per `clean-git`'s conditional fourth case).

## Open questions

- Should this repo grow a gated local-branch/worktree reap script (mirroring gen-ui-kit's
  `npm run ops:reap-branches`, issue #138's realization) so orphans like the two `#956` branches
  and the five `worktree-agent-*` branches stop landing as propose-only every firing? Out of scope
  for this seat to build — naming it as a candidate improvement only.
