{
  "candidates": [
    {
      "claim_id": "adr-0072",
      "evidence": "Clauses 1-4 confirmed live verbatim (session.ts headed 'ADR-0072', frameClientMessage handles action/functionResponse/error exactly as decided; the proxy is stateless per dev-proxy-plugin.ts). Clause 5 is FALSIFIED: the described cross-turn 'demo-level max-turns cap' and its named state machine (idle -> generating(turn N) -> streaming -> awaiting-interaction -> ...) do not exist anywhere in the implementation -- grepped session.ts, produce.ts, agent-transport.ts, a2ui-live.ts, a2ui-agent.ts for maxTurns/turnCount/turns.length-cap/the named state labels: zero hits. The only caps that DO exist are per-generation (ADR-0070's maxRounds intra-turn self-correct loop, GH #49's tool-loop round cap) -- a different, narrower guard than the cross-turn session ceiling clause 5 names. (Table dialect, no owner field -> unassigned, unattended fallback-fan-out firing, no human present.)",
      "kind": "falsified",
      "owner": "unassigned",
      "queued_at": "2026-08-30T00:20:00Z"
    }
  ]
}
