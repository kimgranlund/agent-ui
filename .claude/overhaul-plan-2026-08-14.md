# Overhaul plan — agent-ui `.claude/` estate — 2026-08-14

Target: `agent-ui/.claude/` (28 skills · 6 agents · 4 hooks; 34 governed artifacts).
Generated by `authorkit:overhaul-planning` inside an `overhaul-execute` run. Plan-only —
nothing below is executed without a Gate-A approval.

Doc home note: `docs:make-doc` is installed, but this plan is written at the dated fallback
path rather than as a repo LLD — the repo's LLD tier (`.claude/docs/lld/`, `LLD-C#` spine)
maps the a2ui product system, and a `.claude/`-estate campaign artifact does not belong on
that map. Named as a deliberate deviation, not silently.

## Phase 0 — Measurements

| Instrument | Available? | Finding summary |
|---|---|---|
| naming-audit | yes | 34 artifacts · errors=0 · exemptions=34 (seeded 2026-08-14, every member grandfathered verbatim; classes: 27× terminal-not-in-lexicon, 10× a2ui-brand-token-in-name, 6× agent-missing-`-agent`, 1× skill-object unresolved) |
| bloat-audit | yes | 31/34 flagged · long-body ×17 (6.7k–21.6k chars) · dense-description ×20 (701–1712) · phase-heavy ×13 · 1 duplicate pair (`agents/a2ui-builder.md` ↔ `skills/a2ui-build/SKILL.md`, Jaccard 0.5) |
| check-routing (harness) | partial — NOT RUN | installed, but it runs a plugin's trigger-eval suites; this estate is not a plugin and only 6/28 skills carry `evals/`. Stolen/leaked evidence UNMEASURED; description-trim rows are therefore Wave 3, not Wave 2 |
| plan-plugin-split surface_map (harness) | NOT RUN | plugin-oriented dependency closure; substituted with a mechanical cross-reference sweep (repo config + agent bodies + hooks + CLAUDE.md + the session-memory ledger), counts cited per member below |

## Phase 1 — Per-member kill-switch

Species is uniform (all `skills/*` = skill, model-routed; all `agents/*` = agent, delegated)
and every member is correctly homed (job evidence: each serves this repo's own build/review
loop; no absence-gap and no cross-repo tenant). Those two columns are stated once here and
only exceptions appear in the table. Blast radius = cross-reference count from the sweep
above (`repo` = .claude config + CLAUDE.md; `mem` = session-memory files; substring overlap
slightly inflates a2ui-build/-builder and compose/-composer pairs).

| Member | Blast radius | Merge/Split? | Knowledge tier | Verdict |
|---|---|---|---|---|
| a2ui-build | repo 14 · mem 5 | NO — its dup pair (agents/a2ui-builder) is the deliberate thin-agent+preloaded-skill pattern; fix is dedup, not merge | PROCEDURE — keep-inline | NO MOVE (name grandfathered: a2ui is the protocol family's own name) |
| a2ui-compose | repo 24 · mem 3 | NO — no Phase-0 evidence | PROCEDURE — keep-inline (10.9k body: trim finding, not a tier move; has references/+scripts already) | NO MOVE (same grandfather) |
| a2ui-corpus-curate | repo 8 · mem 2 | NO | PROCEDURE — keep-inline (11.0k: trim finding) | NO MOVE |
| a2ui-multi-catalog | repo 4 · mem 1 | NO | KNOWLEDGE — move-to-references (long-body 9278; no references/ yet) | MOVE (Wave 2: extract references/) |
| a2ui-prompt-author | repo 1 · mem 1 | NO | PROCEDURE — keep-inline (5.5k, under threshold) | NO MOVE |
| agent-admin-library-kinds | repo 3 · mem 1 | NO | KNOWLEDGE — move-to-references (long-body 12760; no references/) | MOVE (Wave 2) |
| agent-ui-a2ui-meta-line-facts | repo 0 · mem 0 | NO evidence for merge; zero inbound references is a retire QUESTION, not a verdict | KNOWLEDGE — contested: retire vs move-to-references (long-body 13847 AND 0 consumers found) | CONTESTED (Wave 3) |
| agent-ui-a2ui-review-standards | repo 1 · mem 0 | NO | KNOWLEDGE — move-to-references (long-body 8014) | MOVE (Wave 2) |
| agent-ui-catalog | repo 12 · mem 1 | NO | KNOWLEDGE — keep-inline (2.6k, clean) | NO MOVE |
| agent-ui-component-create | repo 11 | NO | PROCEDURE — keep-inline (7.6k: trim finding) | NO MOVE |
| agent-ui-component-design | repo 12 · mem 1 | NO | PROCEDURE — keep-inline (9.7k: trim finding; has references/) | NO MOVE |
| agent-ui-component-packaging | repo 7 | NO | KNOWLEDGE — move-to-references (long-body 6730, marginal) | MOVE (Wave 2, lowest priority) |
| agent-ui-component-patterns | repo 6 | NO | KNOWLEDGE — move-to-references (long-body 9413; no references/) | MOVE (Wave 2) |
| agent-ui-component-standards | repo 12 | NO | KNOWLEDGE — keep-inline (4.6k, clean; references/ already sized right) | NO MOVE |
| agent-ui-component-testing | repo 10 | NO | KNOWLEDGE — move-to-references (long-body 11273; no references/) | MOVE (Wave 2) |
| agent-ui-compose-app | repo 4 | NO | PROCEDURE — keep-inline (4.8k; phase-heavy ×8 = trim finding) | NO MOVE |
| agent-ui-compose-layout | repo 4 | NO | PROCEDURE — keep-inline (4.0k) | NO MOVE |
| agent-ui-compose-ui | repo 5 | NO | PROCEDURE — keep-inline (3.3k) | NO MOVE |
| agent-ui-composition-patterns | repo 4 | NO | KNOWLEDGE — move-to-references (long-body 14838; no references/) | MOVE (Wave 2) |
| agent-ui-doc-standards | repo 10 · mem 5 | NO | KNOWLEDGE — move-to-references (long-body 16302; no references/; CLAUDE.md names it canonical home — body must keep the routing surface) | MOVE (Wave 2) |
| agent-ui-example-standards | repo 1 | NO | KNOWLEDGE — keep-inline (5.1k, under threshold) | NO MOVE |
| agent-ui-integration-standards | repo 3 · mem 1 | NO | KNOWLEDGE — move-to-references (long-body 12175; no references/) | MOVE (Wave 2) |
| agent-ui-lateral-review | repo 0 · mem 1 | NO | PROCEDURE — keep-inline, but the single largest body (21642) is the estate's #1 trim target | NO MOVE (trim seed in Wave 2) |
| agent-ui-package-release | repo 0 | NO | PROCEDURE — keep-inline (8.8k: trim finding) | NO MOVE |
| agent-ui-repo-hygiene | repo 0 · mem 1 | NO | PROCEDURE — keep-inline (4.3k) | NO MOVE |
| agent-ui-seat-map | repo 1 · mem 2 | NO | KNOWLEDGE — keep-inline (3.4k, clean) | NO MOVE |
| docs-author | repo 7 · mem 3 | NO | PROCEDURE — keep-inline (12.7k: trim finding; has references/ — push overflow there) | NO MOVE (trim seed in Wave 2) |
| project-docs | repo 0 | NO | KNOWLEDGE — keep-inline (4.1k) | NO MOVE |
| agents/a2ui-builder | repo 15 · mem 5 | NO — dup pair with a2ui-build is by-design pairing; dedup the agent body | n/a (agent) | NO MOVE (dedup+desc-trim seed, Wave 2) |
| agents/a2ui-composer | repo 4 · mem 1 | NO | n/a | NO MOVE (desc 975 → Wave 3 trim question) |
| agents/a2ui-reviewer | repo 8 · mem 4 | NO | n/a | NO MOVE (desc 1494 → Wave 3) |
| agents/component-builder | repo 8 · mem 5 | NO | n/a | NO MOVE (desc 892 → Wave 3) |
| agents/example-builder | repo 2 · mem 1 | NO | n/a | NO MOVE (desc 1712, estate max → Wave 3) |
| agents/orchestrator | repo 1 · mem 18 | NO | n/a | NO MOVE (mem 18 — the most memory-load-bearing name in the estate; any rename is prohibitively expensive) |

**Renames: killed across the board.** Every naming-grammar violation is grandfathered
(ADR-0011 D8 ratchet) rather than moved, on cited evidence: (a) the `agent-ui-`/`a2ui-`
prefixes are the estate's deliberate repo-scoping idiom — 28 siblings share it, so a
conforming rename of any subset breaks family consistency without clearing the estate;
(b) the six agent names are seat names wired into the Agent-tool registry, orchestrator
dispatch briefs, CLAUDE.md, and up to 18 memory files each; (c) the #197 precedent — a
kill-switch phase that returns "no move" is a normal, healthy outcome. The exemptions array
stays at 34 and shrinks only if a future campaign renames with the full blast radius paid.

## Phase 2 — Waved ticket seeds (not yet minted)

### Wave 0 — merge/split nominations
(none — the only duplicate pair is the by-design agent/skill pairing; no nomination clears
plan-skill-merge/-split's evidence bar)

### Wave 1 — mechanically-clean moves
(none — all renames killed at Phase 1)

### Wave 2 — knowledge-tier extractions + trim (semantic; critic pass required)
- [ ] W2-1 `agent-ui-doc-standards` → extract references/ (16302 → body ≤6000; routing surface + status-law summary stay inline) (Blocked-by: none)
- [ ] W2-2 `agent-ui-composition-patterns` → extract references/ (14838) (Blocked-by: none)
- [ ] W2-3 `agent-admin-library-kinds` → extract references/ (12760) (Blocked-by: none)
- [ ] W2-4 `agent-ui-integration-standards` → extract references/ (12175) (Blocked-by: none)
- [ ] W2-5 `agent-ui-component-testing` → extract references/ (11273) (Blocked-by: none)
- [ ] W2-6 `agent-ui-component-patterns` → extract references/ (9413) (Blocked-by: none)
- [ ] W2-7 `a2ui-multi-catalog` → extract references/ (9278) (Blocked-by: none)
- [ ] W2-8 `agent-ui-a2ui-review-standards` → extract references/ (8014) (Blocked-by: none)
- [ ] W2-9 `agent-ui-component-packaging` → extract references/ (6730, marginal — lowest priority) (Blocked-by: none)
- [ ] W2-10 `agent-ui-lateral-review` + `docs-author` + `a2ui-compose` + `a2ui-corpus-curate` → PROCEDURE trim pass (21642/12714/10889/11021: cut retellings, keep dated gotchas + laws; overflow to existing references/ where present) (Blocked-by: none)
- [ ] W2-11 `agents/a2ui-builder.md` → dedup vs `a2ui-build/SKILL.md` (the Jaccard-0.5 pair): agent body points at the preloaded skill instead of restating its charter (Blocked-by: none)

### Wave 3 — contested (open design questions)
- [ ] W3-1 `agent-ui-a2ui-meta-line-facts` — retire vs slim: 13847-char body, ZERO inbound references found in repo config or memory. Is anything consuming it at run time? (Blocked-by: none)
- [ ] W3-2 Agent description trim sweep (6 agents, 892–1712 chars vs 700 cap) — routing-risk contested: descriptions ARE the routing surface and eval coverage is 6/28, so no trim ships without a routing proof; harness `check-routing` doesn't cover a non-plugin estate today (Blocked-by: none)
- [ ] W3-3 Should the estate ever adopt the `-agent` suffix / grammar-conforming names? Deliberately parked: re-open only with a campaign that pays the full blast radius (registry, briefs, memory, docs) in one wave (Blocked-by: none)

### Grandfathered (ADR-0011 D8 ratchet)
All 34 members' names — exemptions retained verbatim; reasons per the Phase-1 kill note.

## Phase 3 — Execution contract (per ticket, once approved)

claim → worktree → `git mv` (history preserved) → supersession note or `renames.json` entry
→ gates + critics → PR → human merge → verified close. Serial through shared ledgers
(`naming.manifest.json`, `renames.json`) — two tickets never touch the same ledger entry in
parallel.

## Phase 4 — Closeout (per wave, once tickets land)

- [ ] `/check-routing` clean for every touched plugin (this estate: UNMEASURED — not a plugin; state it)
- [ ] `fix-old-names` sweep run against consumer repos
- [ ] Records amended with dated supersession notes (never rewritten)

## The five respect invariants

1. Evidence can veto the plan.
2. History preserved, never replaced.
3. Consumers degrade gracefully.
4. The old design's intent is read before it is judged.
5. Nothing semantic rides hidden in a move.

## Next step

This plan and its ticket seeds are generated only — nothing above has been executed. A human
reviews the seed list and approves each row; approved rows are minted through their owning
intake skill (`file-feature`/`file-task`), never auto-created here.
