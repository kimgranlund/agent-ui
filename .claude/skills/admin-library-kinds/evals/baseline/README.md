# Baseline probes — NOT CAPTURED (honest gap, 2026-08-05)

Two fresh-context probes were dispatched without the skill (PROBE 1: "all the places to touch
for a new capability kind"; PROBE 2: "single-select kind — where does selection state live and
how do toggles behave") and a matching with-skill pair. Both probe agents went idle without
returning transcripts inside the forge's one-round budget (host-directed close). No output is
recorded here — absent evidence is recorded as absent, never paraphrased from memory.

Re-run recipe (any fresh session): ask the two probes above against this repo with
`.claude/skills/` unread (baseline) and again after reading `../SKILL.md` (with-skill); grade
against intent.md's four behavioral assertions. See ../behavior-check.md for the gate state.
