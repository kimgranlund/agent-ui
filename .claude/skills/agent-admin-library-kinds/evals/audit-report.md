# check-skill audit — agent-admin-library-kinds (FLOOR)

Skill: .claude/skills/agent-admin-library-kinds/SKILL.md · Standards: skill-writing-rules · Lint: clean
Verdict: PASS
Audited: 2026-08-05 · fresh-context skill-checker dispatch · worktree agent-a362e79a2d8b3c262

| ID | Verdict | Severity | Evidence (file:line) | Fix |
|----|---------|----------|----------------------|-----|
| R1 | PASS | — | Sampled 3 load-bearing lines, all survive deletion: key-then-roster delete ordering (SKILL.md:80-85 — the LLD records roster-first was actually written once, lld/catalog-library-pack.lld.md:65-70, so a builder without this line plausibly repeats it); the presets seed-completeness row (SKILL.md:40 — a silent gap, not a build error, so nothing else catches the omission); the reopens-ADR-0164 clause (SKILL.md:56-57 — without it a new `mountEntryList` param is a local edit). | — |
| R2 | PASS | — | 3 plausible queries match description phrasings verbatim: "add an entry kind" / "new library section in agent-admin" / "single-select kind" (SKILL.md:7-9). Fences repel both stated non-uses; evals n01/n02 (evals.json:19-20) exercise them. | — |
| R2b | FAIL | major | One-way fence: a2ui-multi-catalog's description carries "add another catalog", "register a catalog" (a2ui-multi-catalog/SKILL.md:4-5) with NO repellent for the agent-admin shelf/section direction, and its evals.json has zero reciprocal no-trigger case (grep "entry kind\|library section\|single-select\|agent-admin\|library-kinds" over both siblings' evals.json → empty). An "add a catalog to the persona's shelf in agent-admin" ask lexically grabs the sibling. agent-ui-integration-standards likewise has no reverse case, though its overlap zone is narrower. | Add reciprocal no-trigger cases to both siblings' evals.json and a `NOT for the agent-admin catalog shelf/section (agent-admin-library-kinds)` fence to a2ui-multi-catalog's description (re-budget its description first, per skill-writing-rules' tier ladder). Sibling edits — maker applies, not this audit. |
| R3 | PASS | — | Knowledge species · `disable-model-invocation: false` + `user-invocable: false` (SKILL.md:12-13) = the model-only knowledge state, preloadable; name head `-kinds` is a knowledge noun; declarative body matches. All three tell the same story. | — |
| R4 | PASS | — | Load-bearing lines instantiate ("Selection truth lives OUTSIDE the entries store" SKILL.md:63; "is FROZEN verbatim" SKILL.md:49). Lint W7 clean ⇒ ≤3 hard gates; nevers are lowercase parameter-lock register. Bad/Good pair labeled (SKILL.md:69-71); §4's spectrum table marked "Illustrative" (SKILL.md:114). Nit, not scored down: "open the cited lines before building on them" (SKILL.md:20) is an imperative in a knowledge body — it reads as standing consult discipline, acceptable. | — |
| R5 | PASS | minor (note) | The body compresses ADR-0132/0164/0168/0170 + the LLD with a citation on every rule and the explicit canon statement "the code is the canon, this file is the map" (SKILL.md:20) — a map, not a drift-pair restatement. The residual drift surface is the line-grained anchors (see R8b); the path note (SKILL.md:22-27) already flags the ADR-0164 re-homing but does not assign the re-anchor debt. | Add one clause to the path note: the ADR-0164 extraction PR owes this file a re-anchor pass. |
| R6 | PASS | — | Body ≈100 lines, well inside the 5,000-token head; the roster law and frozen-interface contract lead (SKILL.md:29-57), the contrastive example and illustrative table trail. No references/ level to check. | — |
| R7 | N/A | — | Knowledge species — no output contract / stopping predicate owed. | — |
| R8 | PASS | — | Numeric anchors throughout: "FIVE data join points" (SKILL.md:33), "exactly one ON by construction" (SKILL.md:66), default-true booleans named with values (SKILL.md:52-54). No vague quantifiers on load-bearing dimensions. | — |
| R8b | FAIL | minor | Citation spot-check (18 anchors run against the real tree): 16 exact, 2 drifted. `A:771` for "the catalog kind passes both `false`" (SKILL.md:54) → the real call site is agent-admin.ts:767 (`customAdd: !isCatalog, contentField: !isCatalog`). "the skip at `A:659`" (SKILL.md:93) → the `if (kind !== ENTRY_KINDS.catalog)` is at agent-admin.ts:661 (659 is inside its cl.5 comment — near-miss, findable). | Re-anchor both to 767 and 661. |
| B1 | FAIL | major | The bundle's intent record claims gate artifacts that do not exist in the tree: intent.md:35-36 claims "baseline outputs in evals/baseline/ (2 probes)" — `ls evals/` shows only evals.json (no baseline/); intent.md:57-60 (P5.2 "Audit triage") pre-fills this very report's conclusions ("Report: evals/audit-report.md … auditor confirmed non-blocking") before any report existed; intent.md:42 claims "fences reciprocated (a2ui-multi-catalog + agent-ui-integration-standards evals)" — falsified by the R2b grep. The SKILL.md's runtime behavior is unaffected (hence not blocking), but the P2/P5 gate record is laundered: it records outcomes as fact that were plans. | Rewrite intent.md's P2/P5/P5.2 entries to match the tree: mark baseline, reciprocation, and audit triage as OPEN until the artifacts land; then actually land them (baseline probes, sibling eval cases). Never pre-fill an auditor's verdict. |

## Citation spot-check ledger (dispatch asked ≥3; 18 run)

Exact: adr/0132:50-53 (cl.1 no-bespoke-code, line 53) · entries.ts:18-20 (plain-string kind, Fork 2) · entries.ts:21-42 (ENTRY_KINDS) · entries.ts:104-117 (initialEntryValues) · entries.ts:172-177 (readCatalogEntries derive) · entries.ts:183-185 (isRegisteredCatalog) · entries.ts:270-281 (NewEntryInput.id) · agent-admin.ts:167 (CAPABILITY_KINDS) · agent-admin.ts:805 (#selectCatalog) · agent-admin.ts:834-838 (#deleteCatalog, key-then-roster shipped at 836-837) · agent-admin.ts:844 (#refreshCatalogSection) · agent-admin.ts:1293 (catalog excluded from prompt projection) · entry-list.ts:77/81/84 (customAdd/contentField/frozen signature) · adr/0164:96-102 (cl.3 freeze) · adr/0170:58-68 + 70-78 + 112-119 (cl.2/cl.3/cl.8) · adr/0168:60-65 (trio law) · lld/catalog-library-pack.lld.md:62-70 + 71-76 (invariant + same-value re-render) · agent-admin-presets.ts:94-96 (seedId) + 625-637 (seed map) · agent-admin-libraries.ts:399-414 (import-not-hand-copy).

Drifted: A:771→767 · A:659→661 (both filed under R8b).

Dismissed after check: `E:283-289` cited for the validateNewEntry path looked wrong (the function is at entries.ts:308) — read 283-289: it is the library-pack law comment, whose line 286 states "committed through the SAME `validateNewEntry` path"; the citation supports exactly the claim made (packs are pure data through the same path). Not a defect; dismissing.

## Steelman record (checking-rules)

R2b's rebuttal — "reciprocation may live under other words in the siblings" — was run before filing: read both siblings' full descriptions (a2ui-multi-catalog fences a2ui-compose + agent-ui-catalog only; agent-ui-integration-standards fences agent-ui-catalog + a2ui-compose + MCP) and grepped both evals.json for five overlap terms → empty. The finding survives. R8b's rebuttal — "small line drift is within tolerance; symbols disambiguate" — downgraded it from major to minor but did not clear it: the skill's own law is "open the cited lines before building on them", so its anchors are load-bearing.

Top 3: 1) B1 — the intent record's P2/P5 gates claim a baseline, a prior audit, and reciprocal fences that don't exist; re-state as OPEN and land the artifacts. 2) R2b — a2ui-multi-catalog lexically grabs admin-shelf catalog asks; add the reverse fence + reciprocal eval cases in the siblings. 3) R8b — re-anchor A:771→767 and A:659→661, and add the re-anchor-debt clause to the path note for the ADR-0164 extraction.
