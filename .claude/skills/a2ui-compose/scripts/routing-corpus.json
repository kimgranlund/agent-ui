{
  "skill": "a2ui-compose",
  "_note": "h13 routing tripwire (decomp a2ui-expert-harness.decomp-v2 node h13). Positives: compose/author/extend/debug an A2UI payload (message stream) against a catalog, incl. 2 reworded paraphrases (#9,#10). Negatives drawn from SIBLING territory the description fences: component-author (ui-* control source), a2ui-builder agent dispatch (package/renderer/validator CODE), docs-author (site pages), + one a2ui-corpus-curate (curation). A tripwire, not a certification — the pass condition is description-lint + a human read of the named misses/grabs (SPEC-N2 v0.2).",
  "positives": [
    "compose an A2UI payload for a settings surface",
    "author the A2UI message stream that renders this generative UI",
    "build the updateComponents adjacency list for this component tree",
    "wire the data bindings and actions on these catalog nodes",
    "extend this A2UI payload with a ChildList list template",
    "debug why the renderer won't paint my A2UI payload against the agent-ui catalog",
    "add validity checks and gate the submit under a FormProvider in my payload",
    "run the compose validate self-correct loop on this payload",
    "I need the server to client message stream that draws a generative UI from the catalog",
    "put together the createSurface and updateDataModel messages for this screen"
  ],
  "negatives": [
    "write the ui-checkbox control source with ElementInternals",
    "my icon-only button isn't square, what padding should the control use",
    "implement the streaming parse in the a2ui package's TypeScript source",
    "fix the zero-dependency renderer code that paints the components",
    "write the docs-site page documenting ui-text-field",
    "add an API doc page to the agent-ui site",
    "import this seed into the corpus and rescore it"
  ]
}
