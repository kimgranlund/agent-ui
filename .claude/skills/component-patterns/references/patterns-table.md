# Component patterns — the full prior-art table

Harvested verbatim from `component-patterns`'s own body (2026-08-15 extraction, GH #930/wave-2
W2-6). Read when: the SKILL.md's compact routing index named a pattern and you need the
mechanism's own description before opening the ADR/source themselves — this table is the
middle rung between the one-line index and the primary sources.

| Problem shape | The fleet's mechanism | ADR · realized home |
|---|---|---|
| Reusable behaviour shared across controls | a **trait**: a free `(host, opts) => cleanup` function called from `connected()` — no registry, no decorators, no `host.use()` | foundational (plan §5 + the `traits/index.ts` header; earliest trait decision ADR-0010) · `packages/agent-ui/components/src/traits/` |
| Shared value-control base plumbing across a control family | the **`controls/_base/`** widget value-control bases (a sub-layer, deliberately NOT traits — the ADR argues why) | ADR-0042 · `packages/agent-ui/components/src/controls/_base/` |
| A floating panel anchored to a trigger (menu, select, tooltip, combo-box) | the **Overlay controller** + platform-owned dismissal (Escape/outside-click are the platform's; anchor focus-restore; `:popover-open`-resilient close) | ADR-0043 + ADR-0045 · `traits/overlay*`, patterned in `controls/select/`, `controls/menu/` |
| Which control owns a modal/docked/anchored surface (the fleet's overlay/docking vocabulary boundary — four cells) | `ui-modal` = centered modal `<dialog>` · `ui-drawer` = edge-docked modal `<dialog>` · popover/menu/select/tooltip = anchored non-modal top-layer · shell side/pane = docked non-overlay layout; a persistent non-scrimmed panel is NEVER a drawer | ADR-0188 · `controls/drawer/` |
| Consistent padding/gaps/sticky regions inside any container surface | the shared **`[data-box]` container box-model** (one spacing layer, nested content levels, adjacent-sibling content gap) | ADR-0046 · `controls/_surface/container-box.css` |
| A container stacking its own children sanely under overlays | **a container is its own z-depth scope** (`isolation: isolate` on `[data-box]`) | ADR-0052 · `controls/_surface/container-box.css` |
| A typed value with a formatted editing surface (currency, unit, percent, date, time) | a **value codec** per `type` (parse/format/step/validity, per-type `AbortSignal`) | ADR-0047 (numeric family) + ADR-0048 (date/time) · `traits/value-codec.ts` + `controls/text-field/` |
| A heavy sub-component only some usages need | **lazy `import()` at the interaction point** so tree-shaking holds (calendar into the text-field overlay) | ADR-0048 · `controls/text-field/` |
| One component needs to know about many descendants (form ⇄ fields) | **connect-time registration event + reactive registry** — the provider/context primitive; guard work on the CONNECTION signal (the bulk-insert cascade lesson) | ADR-0050 · `traits/form-registry.ts` + `controls/form-provider/` |
| A wrapper labels/describes a control it doesn't own | the **field-labelling seam** (`setFieldLabelling`/`applyFieldLabelling`, `formUserInvalid`, reactive error rendering) | ADR-0051 · `controls/field/` + `dom/` |
| Reordering children without losing focus | **`ChildPart.moveBefore`** over native `Node.prototype.moveBefore` — only the native call preserves focus; the seam upgrade is gated | ADR-0022 · `dom/` (repeat) |
| Imperative composition into a template-rendered tree | the public **`mount()` directive-host seam** | ADR-0023 · `dom/` |
| A swappable asset/provider family (icon packs) | **pure core + subpath adapters** in a zero-dep leaf package (default pack vendored at build time as inert TS) | ADR-0065 + ADR-0066 · `packages/agent-ui/icons/` |
| A component that must NEVER be agent-emittable (page/app-owner chrome) | the a2ui **`EXCLUSION_ALLOWLIST`** — a permanent, tested catalog exclusion | ADR-0112 cl.6 (the ToastRegion reasoning — the top-layer host + `show()` singleton stay app chrome permanently; `Toast` itself LEFT the allowlist, next row), applied by ADR-0117 (ThemeProvider) · `packages/agent-ui/a2ui/src/catalog/default/index.test.ts` |
| A ruled catalog EXCLUSION gets reversed by an owner ruling — one half of an excluded pair turns out agent-shaped after all | the **allowlist-drain-by-amendment** path: Kim's outcomes-are-toasts ruling (GH #1184) REVERSED the Toast half of ADR-0112 cl.6 — `Toast` entered the default catalog (PR #1186: bespoke `toastFactory`, `label` → adopt-once `textContent`, `urgent`/`duration` accessors, `action` a curated omission since its `select` event carries no serializable value slot; an agent-emitted Toast renders INLINE where placed and self-removes on close/expiry) and was DRAINED from the code `EXCLUSION_ALLOWLIST`, while **`ToastRegion`'s exclusion stands PERMANENT** for the record's original reasons. The reversal lands as a ratified AMENDMENT on the original ADR (the decision record catching up with the ruled, shipped state), every affected code site citing the ruling issue — never a quiet allowlist edit | ADR-0112 Amendment (2026-08-17, GH #1184, PR #1186) · catalog row + `toastFactory` in `packages/agent-ui/a2ui/src/catalog/default/` |
| A shipped `ui-*` type that IS agent-emittable — but only through ONE persona catalog, never the generic default catalog | the SECOND `EXCLUSION_ALLOWLIST` justification category — **persona-scoped content type** (beside ADR-0112 cl.6's chrome family): the type stays excluded from the DEFAULT catalog because it is *catalogued elsewhere, on the persona surface that teaches it*; a type qualifies iff at claim time it ALREADY carries a shipped persona-catalog row plus a teaching surface (fragment + mini-skills entry) — checked against the claimant's shipped artifacts, never argued by analogy. Reversal path stays allowlist-drain-by-amendment (previous row) if the type ever earns a default row | ADR-0225 cl.2 (`ui-playing-card`, croupier persona; GH #1478) · `PlayingCard` entry in `packages/agent-ui/a2ui/src/catalog/default/index.test.ts` + `personas/croupier/catalog.json` |
| A component depicts a PHYSICAL OBJECT whose colors are pigment, not theme — a scheme-flipped rendering stops reading as the object at all | the **depicted-object color exception**: pigment tokens that deliberately do NOT flip with the color scheme — the face surface pinned LIGHT in BOTH schemes (image-content posture, not a themed UI surface), inks off flat mode-independent PRIMITIVES (the red suit ink steps off the danger ladder's flat primitives as a hue source — explicitly NEVER the `danger` intent role, which claims a semantic that isn't there, and never `on-surface`, which flips); color stays a REDUNDANT identity carrier (shape-distinct glyphs + text carry identity, ADR-0057/ADR-0219 cl.4), contrast ≥4.5:1 browser-gated on the pinned surface in both schemes. Citable by any future depicted-object component (chess pieces, dice) | ADR-0225 cl.3–4 · `--ui-playing-card-ink`/`--ui-playing-card-ink-red`/`--ui-playing-card-face-surface` in `controls/playing-card/playing-card.css` |
| Nesting a SHIPPED control whose connect-time child-move relocates your parts (modal, select, combo-box panels) | the ORDERING constraint: the nested control moves light-DOM children at ITS connect — compose parts before/after that move deliberately, never assume your DOM order survives (the same shape as lazy-import-into-overlay) | ADR-0017 (the child-move pattern) + ADR-0048 (lazy calendar into the overlay) · `controls/modal/`, `controls/combo-box/` — the command-modal intake's recorded footgun |
| A multi-column grid whose columns are POPULATED CONDITIONALLY by props (a card row with an optional action) | pin every fixed-role part to an EXPLICIT `grid-column` — auto-placement is only safe when every child is always present (the phantom-track defect; the measured detail lives in the cited ticket) | TKT-0014 (the toast dismiss-X proof: an 8px mis-inset exactly equal to the column gap) · `controls/toast/toast.css` |
| Ambient theming context (scheme/scale/density) over a subtree | **`ui-theme-provider`** — reflected props re-rooting `color-scheme`; unset means inherit-ambient, never a forced default | ADR-0117 · `controls/theme-provider/` |
| A component-owned SCROLL REGION that must survive disconnect/reconnect | the parts-once `#built` guard alone is NOT enough — pair it with a **live scroll-listener shadow** restored synchronously in `connected()` (engines reset a removed scroll container's offsets; the mechanism detail lives in the cited ticket + probe) | TKT-0067 (measured 40 → 0 in Chromium AND WebKit with identical-node reattach) · `controls/table/table.ts` + the reconnect probe in `table.browser.test.ts` |
| A component with BOTH an imperative API and declarative light-DOM children | **adopt-if-authored-else-create**: `connected()` looks up `:scope >` recognized children and seats whichever exist, else creates the SAME element internally — every method already writes through the seated reference, so the imperative API gains ZERO mode branches (identity under both paths is structural, not promised); NEVER a second imperative surface on the adopted child (engine duplication is the exact two-implementations failure ADR-0129 cl.4 exists to prevent). Third independent instance of the consumer-authors-a-child seam: TKT-0056/0058's composer extraction → GH #666's `setEmptyState` + SPEC-R12's `setContentRenderer` → this contract | ADR-0180 (cl.2/cl.4) · `packages/agent-ui/app/src/controls/conversation/` |
| A second lookup/map must exist over an already-canonical key set | **derive it from the canonical constants, never hand-list a parallel table** — a pure function/spread over the constants the first consumer already reads, pinned TOTAL over the key set and parity-gated against the real DOM/data by its own suite; fail-closed on an unmapped key (no reaction, never a throw). Two hand-maintained enumerations of one truth is the GH #406 silent-divergence class — hit twice, by name: the `persona-patch.ts` hoisting, then `field-location.ts` citing it as precedent | ADR-0181 cl.4 · `controls/agent-admin/persona-patch.ts` + `controls/agent-admin/field-location.ts` (`@agent-ui/app`; the totality gate in `field-location.test.ts`) |
| An OPTIONAL browser API several surfaces want, progressive-enhancement-only (View Transitions) | **ONE shared seam in `components/dom`** — the one home every same-document surface can import without a layering violation: `withViewTransition(mutate, enabled)` + `viewTransitionAvailable()` run the mutate inside the API iff enabled AND the API exists AND `prefers-reduced-motion` is unset, synchronously otherwise; each surface wraps its own swap site behind a **default-`false` boolean prop**, byte-identical when unset. The stated caveat: the transition path runs `mutate` ASYNC (the platform snapshots first) — staleness guards re-check INSIDE the mutate | ADR-0183 · `dom/view-transition.ts`, opted in by `ui-router-outlet`, `ui-super-shell`, `ui-surface-host` |
| A streaming, incrementally-painted host must tell FIRST PAINT from a RE-RENDER (animate one, never strobe the other) | the host's OWN **settled-once boundary**: the first `finalize()` marks the host settled — every `ingest()` before it is first-paint streaming (never transitions), every one after is a re-render (transition-eligible); a reset flips the flag back so a rebuilt surface streams as a first paint again. Burst coalescing is the platform's own update-callback queue, relied on deliberately (at most ~one visible cross-fade per burst, FIFO, no line lost) | ADR-0183 Amendment (GH #742) · `controls/surface-host/surface-host.ts` (`@agent-ui/app`; the `#settledOnce` flag) |
| Cross-DOM element MORPHS (not just cross-fades) on an opted-in View Transitions surface | the fleet's opt-in **named-morph convention**: `viewTransitionName(surface, token)` + `setViewTransitionName(el, name, enabled)` in `dom/view-transition.ts`, names `ui-vt-{surface}-{token}` (`surface` = the owning control's slug, `token` = a PERSISTENT identity — slot name/id/instance counter, never an array index; both halves sanitized to `[a-zA-Z0-9-]`). The pairing law, both halves: (i) every element that can occupy ONE visual role carries the SAME name (the browser morphs outgoing into incoming across different DOM nodes); (ii) a name is unique per DOCUMENT, not per surface INSTANCE — a twice-mountable surface folds an instance discriminator into its token or the transition aborts. Names are applied only behind a surface's OWN opt-in (cl.4's no-DEFAULT-names law stands); first proving surface: `ui-super-shell`'s `viewTransitionNames` boolean, meaningful only alongside `viewTransitions` | ADR-0183 Amendment (2026-08-16, GH #958) · `dom/view-transition.ts` + `controls/super-shell/` (`@agent-ui/app`) |
| Async-stale content: keep showing the LAST SETTLED value, visibly dimmed, while a new answer is in flight | the fleet's async-stale law — the TKT-0062 SHAPE re-applied to a second, orthogonal state axis (async freshness vs entry-control fill): ONE host custom state **`:state(pending)`** (joining the `ready`/`truncated`/`dragging`/`revealed`/`settled` host-state vocabulary; the CONSUMING control applies it from `pendingComputed`'s `pending` signal via `internals.states`, the `trackUserInvalid` split) + ONE token pair **`--ui-pending-duration`** (aliases `--md-sys-motion-duration-fast`) / **`--ui-pending-opacity`** (`0.6`) — a DIMMING, never a role-repoint (TKT-0047's opacity exception generalized: stale content is arbitrary-depth DOM). Precedence `disabled` > `pending` > every TKT-0062 fill/hover/focus state; `pending` COMPOSES under `settled` where both exist, never replaces it | ADR-0191 · `traits/pending-computed.ts` + `shared/src/tokens/dimensions.css`; sibling: ADR-0196 extends the family (the answered state, next row) |
| A settled/confirmed CHOICE must read "answered, correction goes through Edit" — never disabled, never like an unanswered control | the fleet's answered-state law — the THIRD state axis run through the TKT-0062/ADR-0191 shape (fill → TKT-0062, async freshness → ADR-0191, answer settlement → this): ONE host custom state **`:state(answered)`** (a boolean `answered` prop the CONSUMING surface sets; the control mirrors it into `internals.states` — presentation-only, never AX-reflected) + ONE token pair **`--ui-answered-bg`/`--ui-answered-ink`**, both ALIASES (`neutral-container-low` / `neutral-on-surface-variant`, zero new literals) — a ROLE-REPOINT, never an opacity dim: only the UNSELECTED options + frame step back; the selected indicator keeps its selected tokens. Precedence FIXED: **`disabled > pending > answered > focus > hover > filled > default`** (`answered` over hover/focus deliberately — hover/focus repaints are affordance signals the settle flow revoked; the Edit affordance carries "change this"; mutual exclusion via `:not()` guards, never specificity). Scope: the 7 choice controls (`ui-radio-group`/`ui-checkbox`/`ui-switch`/`ui-segmented-control`/`ui-select`/`ui-multi-select`/`ui-combo-box`), explicitly NOT free-text. **Answered is NOT disabled** (replaces GH #805's disable-on-submit — focus and the form lifecycle stay live). Template consumption contract: on submit the card SETTLES (collapses to answer + summary row), never disappears (the Edit-anchor law); Edit re-opens and clears `answered` for the edit's duration; a change APPENDS an amendment turn ("Changed: X → Y") — never rewind/rewrite, and never an 8th event name (ADR-0153's seven stay closed) | ADR-0196 (GH #1065) · choice-control `{name}.css` answered blocks + the A2UI questionnaire card template (build slices follow ratification) |
| A surface that is LIVE and being mutated in place by an in-flight producer turn must read alive — the OPPOSITE message of pending's stale-dim (dimming fresh content would be a false statement; alive needs emphasis) | the fleet's working-state law — the FOURTH state axis run through the TKT-0062/ADR-0191/ADR-0196 shape: ONE host custom state **`:state(working)`** (the EIGHTH vocabulary member: `ready`/`dragging`/`truncated`/`revealed`/`settled`/`pending`/`answered`/`working`; the CONSUMING surface sets a boolean `working` prop, the control mirrors it into `internals.states` — presentation-only, never AX-reflected; the turn's announced face stays the ADR-0146 narration strip) + a token QUARTET **`--ui-working-duration`** (1600ms — the fleet's FIRST loop-motion token, deliberately NOT a `--md-sys-motion-duration-fast` alias: a 300ms breath is a strobe) / **`--ui-working-opacity-min`/`-max`** (0.15/0.55) / **`--ui-working-blur`** (24px), plus the pure color alias `--ui-working-color: var(--md-sys-color-primary)`. Treatment: a breathing diffused INNER-shadow overlay pseudo-element animating only its own OPACITY (compositor-only, the ADR-0095 exemption; shipped on `::before`, not the ruled `::after` — that slot was already ADR-0187's placeholder channel, a documented mechanical deviation). Precedence FIXED: **`disabled > pending > working > answered > …`** — pending (a correctness statement) outranks working (an ambience statement), though the two COMPOSE mechanically (disjoint channels: content dim vs frame overlay). **`prefers-reduced-motion` degrades STATIC, never NOTHING** — `animation: none`, overlay held at `--ui-working-opacity-max`: a NEW fleet precedent for any ambient animation that CARRIES STATE (it must stay legible motionless; only pure transition polish may degrade to none) | ADR-0199 (GH #1104) · `controls/surface-host/surface-host.{ts,css}` + the `conversation.ts` turn-handle wiring (`@agent-ui/app`) + `shared/src/tokens/dimensions.css` |
| General-purpose functionality that fits NONE of components' layers (`reactive ← dom ← traits ← controls`) and would ride every consumer's foundation budget | **mint a new zero-dep SIBLING PACKAGE off `components`** (`shared ← components ← {router, code, data}`), catalog-invisible by construction (`a2ui` never imports it), NEVER grown inside `components` itself — proven three times: router (ADR-0115) → code (ADR-0119) → data (ADR-0192); the ADR-0040 foundation-budget argument is the recurring trigger, and each mint extends every inward `layering.test.ts` scan by one package name | ADR-0115 + ADR-0119 + ADR-0192 · `packages/agent-ui/{router,code,data}/` |
| Which GEOMETRY a new package mint takes — the previous row's sibling shape does NOT fit every mint | TWO distinct package-mint geometries, chosen by what the new package's SUBJECT is: (i) **sibling-off-`components`** (`shared ← components ← {router, code, data}`, ADR-0115/0119/0192) for general-purpose functionality below the catalog — catalog-invisible from BESIDE it, never importing `a2ui`; (ii) **top-tier-above-the-catalog, peer of `app`** (`shared ← components ← a2ui ← {app, devtools}`, plus `a2a ← devtools`, ADR-0200) when the package's SUBJECT is the a2ui turn seam itself — devtools is the FIRST package depending on `a2ui` (+ `a2a`), because a below-a2ui placement would forbid the very imports it exists for and force re-declaring the seam types (the duplication ADR-0192 refused for `AgentTransport`). Both geometries keep the package catalog-invisible (`a2ui` never imports it) — one from beside, one from above — and both extend every inward `layering.test.ts` scan by the new name; peers at the top tier never import each other (`app` ⇹ `devtools`) | ADR-0200 (GH #1122; contrast rows: ADR-0115/0119/0192) · `packages/agent-ui/devtools/` |
| Site glue needs TYPES from two packages neither of which imports the other (an A2A card mapper over an `app` record; shared page chrome; a hoisted stream reader) | the **`site/lib/` cross-package glue module** — the escape valve that AVOIDS widening the DAG: `site/` already sits above every package by construction, so a `site/lib/` module imports both packages' real exports as an ordinary consumer, getting genuine compile-time type-checking at ZERO permanent architectural cost. Rejects both alternatives by name: duck-typed re-declaration inside a package (silent drift the moment the owning type changes, no compiler check at the divergence point) and a ruled DAG-widening edge (a permanent `layering.test.ts` allowlist cost paid for a capability with no in-package consumer). Third worked instance — proven three times: `site/lib/flow-chrome.ts` (ADR-0198) → `site/lib/ndjson-lines.ts` (the `@agent-ui/data/stream` re-export role) → `teamMemberToAgentCard` (ADR-0203 cl.3). DISTINCT from the sibling-package mint two rows up: that mints a real package for reusable functionality; this is site-internal composition glue that must never become a package edge | ADR-0203 cl.3 (GH #1191; precedent ADR-0198) · `site/lib/` (`flow-chrome.ts`, `ndjson-lines.ts`, the team→AgentCard mapper) |
| A layer below `app` needs to persist something (nothing below `app` may import upward, and hand-rolled localStorage touch-points drift) | the **`StorageAdapter` seam in `@agent-ui/shared`** (the DAG bottom, so every layer can reach it): ONE typed async interface — `get`/`set`/`delete`/`keys`, plus optional `subscribe?(listener) => unsubscribe` (absent ⇒ no external-change reactivity) — with two zero-dep, hand-rolled tiers: `createLocalStorageAdapter({namespace})` (namespaced `${namespace}.${key}`, missing `localStorage` degrades to a safe no-op) and `createIndexedDbAdapter({dbName, storeName?, version?})` (hand-wrapped `IDBRequest` Promises — the `idb` PATTERN, never the dependency; missing `indexedDB` rejects with a named Error). Cross-tab notification is opt-in at the LISTENER boundary: native `storage` event (localStorage tier) / `BroadcastChannel` (IndexedDB tier), nothing attached until `subscribe()` is called. `app`'s sync `SettingsStore` is a deliberately different, untouched contract. **Tier-scoped sync-read extension** (ADR-0193 Amendment, GH #1077): the localStorage tier ONLY gains **`SyncReadableStorageAdapter extends StorageAdapter`** with `getSync(key)`/`keysSync()` — per-verb same-tick MIRRORS of the async surface over the SAME live backing store (a `snapshot(): ReadonlyMap` warm cache was REJECTED as the stale-in-memory-mirror failure mode the base ADR's own Alternatives already rejected, and it over-serves a keys-scan-then-read consumer). Capability expression is a NAMED interface extension + a **narrowing factory return type** (`createLocalStorageAdapter` narrows to `SyncReadableStorageAdapter`) — the deliberate INVERSE of `@agent-ui/data`'s `DataSource<T>` optional-member + runtime-probe idiom (ADR-0192 SPEC-R2): probe when arbitrary sources implement arbitrary verb subsets and consumers are generic over unknown sources; narrow statically when the capability is a property of exactly ONE known tier the consumer constructs itself. `StorageAdapter` itself gains NO sync members (async-throughout stands for the base contract; nothing may fake `getSync` over an async store), and after the migration NO `app` module touches `localStorage` directly | ADR-0193 (GH #959 Slice 1) + its 2026-08-17 Amendment (GH #1077, built PR #1095) · `packages/agent-ui/shared/src/storage/` |
| An N-level show-one-hide-rest container whose POSITION is an ARRAY (a drill/stack path), consumable both with and without external wiring | **show-one-hide-rest + controlled-array-prop-duality**: the `ui-tab-panel` hidden-toggle interior (author panels stay SIBLINGS; the host toggles `hidden` on every panel but the resolved active one) paired with `ui-split.sizes`' ADR-0102 prop-as-source-of-truth duality applied to an ARRAY prop — `path: string[] \| undefined` (`prop.json`, `attribute: false` — too structured for an HTML attribute). UNCONTROLLED (`undefined`): an internal signal drives the render AND self-mutates on every commit — zero consumer wiring. CONTROLLED (defined): the host renders the prop and only EMITS the proposed array via `change` (`event.detail: string[]`, ⊂ the closed seven — a position COMMIT, the `ui-split` ratio-commit shape, not a `select` pick), never self-mutating. The array carries its own invariants as public contract: always populated (the FULL chain inclusive of the root key, NEVER empty — uncontrolled seeds `[rootKey]` at connect; a controlled `undefined`/`[]` resolves the same way), forward navigation APPENDS (never recomputes a chain from panel metadata — `parent` is consulted only for the Back label), and a broken/unresolvable controlled array gets a DEFINED repair — walk from the END for the first entry naming a real panel, else the root (the `ui-tabs` overflow fallback-to-pinned precedent), never a throw | ADR-0195 (cl.2–4; the intake's §6 novelty leg) · `controls/drill/drill.ts` (the `path` prop + `#resolve`/`#drillTo`/`#commit`) + `controls/drill/drill-panel.ts` |
| [incident] A flex item auto-sized to content (`align-self: flex-start`) COLLAPSES to a fixed-floor child's min-width instead of filling the column, once a fixed-floor component is composed inside it | drop the content-sizing rule for that role entirely — **`align-self: stretch; max-inline-size: none`** — never re-tune the width cap: the cap was written for a wrapper that sized itself to its text, and on a wrapper carrying a fixed-floor child (`ui-status-stream`'s 16rem-floor narration strip) the wrapper's intrinsic width IS the child's floor, so the whole composed turn (header, pending row, bubble) shrank to ~16rem and hugged the left edge of a real column. Scope the fix to the role that carries the floor (the agent turn); a sibling role without one (the user turn) keeps its cap + `flex-end` deliberately | ADR-0160 Amendment (2026-08-16, GH #1032; Kim's ruling "use the full width") · `packages/agent-ui/app/src/controls/conversation/conversation.css` + the three re-pinned full-column probes in `conversation.browser.test.ts` |
| A load-on-demand affordance is needed BEFORE the real model-side tool-call loop ships (the architecturally-named fix is deferred) | the **exact-match auto-attach stopgap**: client-side, when the user's own typed text names a reachable entry's `label` EXACTLY (case-insensitive, whitespace/punctuation-normalized), resolve it through the SAME path an explicit reference already uses (`resolveTurnReferences` — downstream-indistinguishable from a committed chip, reusing the existing chip rendering, never a new visual); capped at ONE auto-attach per turn (a second exact hit is silently dropped), NEVER fuzzy/description matching (the false-positive risk explicitly declined), and the matcher only ever sees the already-reachable roster. Named as a poor-man's UX repair ON TOP of a stopgap — the real model-side pull (`load_capability(label)`) stays cited, not built, gated on ADR-0132's deferred tool-execution loop | ADR-0190 Amendment (2026-08-16, GH #1030) · `@agent-ui/app` capability tagging + `capability-availability-tagging.spec.md` §13 (SPEC-R16) |
| A public `.` barrel's STATIC re-export of a heavy arm drags its whole module graph into EVERY consumer's entry chunk (a class value cannot be re-exported statically without landing its module eagerly) | the **barrel-lazy-split**: static access moves to SUBPATHS (the `./agent-admin-*` grammar; no module moves on disk) + the barrel gains ONE memoized **`loadX()` dynamic-import accessor** (one in-flight promise; importing defines the tag — fleet self-define idiom; consumers that only mount `await loadX()`, type/value consumers repoint to subpaths — a compile-checked migration). Gated three ways: a Rolldown **bundle-shape test** (the arm absent from the eager closure + a negative control), runtime parity (lazy mount ≡ static-subpath mount, memoization, rejection caught), and the size budget — which after the diet is CLOSED to upward re-bases as a class. Proven three times: dogfood-lazy (GH #354) → markdown-lazy (GH #468) → agent-admin-lazy (ADR-0197); a barrel export-set change is a public-contract change and lands as an ADR (ADR-0139 cl.8's intake discipline), never silently inside a build | ADR-0197 (GH #1092/#1080; lineage ADR-0139 cl.8 · GH #354 · GH #468) · `packages/agent-ui/app/src/index.ts` + `agent-admin-lazy.bundle.test.ts` |
| A capability seems to NEED a third-party runtime dependency in a zero-dep repo | the **ruled runtime-dependency-exception test**, proven twice and reusable verbatim — four gates, in order: (i) *category* — is the capability DISPLAY of static content (hand-rolled-small is real: ADR-0107 charts, ADR-0119 highlight/markdown) or an interactive/INTERPRETIVE runtime (hand-rolled-small is not real: an editor engine, a PDF interpreter of fonts/encodings/content-streams)? (ii) *inert-data* — is the mass a compiled data table vendorable at build time (ADR-0066's icon-pack license)? executable parsing code fails this; (iii) *wire-shortcut* — is there a network protocol to speak instead (ADR-0069/0073's plain-fetch answer)? a local parse has none; (iv) **Kim's call ALONE** — with the prior three exhausted, the exception arrives as a proposed ADR naming its precedent, ratified before the dependency appears in any `package.json`. An earned exception then ships the identical clause-8 gate set: identity (every default root barrel stays dependency-free and byte-identical for non-users), confinement (a grep trip-wire — no static import outside the ONE designated lazy-load module; the runtime arrives via dynamic `import()` only), size (a measured lazy-chunk line-item, never main-graph budget), lockfile hygiene (bumps are ordinary gated dependency PRs). Two worked ADOPTIONS: CodeMirror 6 (ADR-0139, `@agent-ui/code/editor`) → pdfjs-dist (ADR-0202, the app doc-ingestion seam) — and one worked DECLINE: Map (ADR-0222, GH #1376) resolved at gate (iii) — the rendering already lives behind a URL the browser speaks natively via `ui-image`, so adoption never reached gate (iv); a live wire shortcut means the prior alternatives are NOT exhausted. Vendoring executable code instead is "a runtime dependency in costume" (ADR-0107) | ADR-0139 + ADR-0202 (GH #1190) + ADR-0222 (GH #1376, declined) · `code/package.json` (`./editor`) + the doc-ingestion extraction seam; gates: `identity.test.ts` family + confinement grep + `scripts/measure-size.mjs` line-items |
| Eyebrow / section-header / group-label typography (a small uppercase kicker introducing a block) | read the fleet's **KICKER typescale row** — `font-size`/`weight`/`line-height`/`letter-spacing` from `--md-sys-typescale-kicker-{size}-*`, with `text-transform: uppercase` applied by the CONSUMER (the role carries tracking, never casing) — ad hoc font values on an eyebrow-shaped label are the defect, not a style choice; second instance settled it as the class default | GH #370 (the kicker-role ruling; ADR-0078 cl.2b, REV 2026-07-30) + GH #1291/PR #1292 (`ui-select` group-label) · `shared/src/tokens/dimensions.css` kicker rows + `controls/select/select.css` (group-label) |
| A sticky child inside a PADDED scroll container must pin to the container's top edge (a tab strip, a listbox group label) | the **padding-handoff**: both engines pin a sticky child BELOW the scroll container's own block-start padding while scrolled content clips at the PADDING box (measured 2026-08-18, Chromium + WebKit) — so `inset-block-start: 0` alone leaves the child one gutter shy of the top with scrolled content bleeding through the padding band, and a negative-margin/compensating-padding pair on the CHILD measures no better (the pin line is the container's; only the container's own padding edge can move it). The container SHEDS its block-start padding and the sticky child CARRIES it as its own `padding-block-start` — net-zero at rest — plus an OPAQUE surface from the file's OWN token chain and `z-index` above in-flow content; the inline axis stays inset (the ambient-wrapper-gutter idiom — inline full-bleed buys no occlusion) | GH #1318 (Kim's ruling) · PR #1323 · `app`'s `controls/agent-admin/agent-admin.css` (the sticky settings-nav strip; the mechanism's own diff commentary carries the measurements) — family precedent: `controls/select/select.css`'s sticky group label |
| A default-stamped overlay (paddles, carets) floats over a scroll container and content paints/peeks UNDER it | reserve the lane as a **transparent BORDER band on the scroll container, never padding** — a scroll container clips scrolled content at the PADDING box, so a padding lane still lets slides slip under the overlay; a transparent `border-inline` genuinely excludes the band from the scrollport (`--ui-swiper-paddle-band`, sized to the stamped icon-only md button square via `--md-sys-height-md`), gated on the overlay actually being stamped (`:has(> ui-swiper-paddles[data-default])`), with the offset math made snapport-aware (`#alignedOffset`) | GH #1330 · PR #1400 · `controls/swiper/swiper.css` (`--ui-swiper-paddle-band` + the gated track rule) + `swiper.ts` (`#alignedOffset`) |
| A REFLECTING stateful prop needs a native-parity `default*` reset baseline (`defaultChecked`) — but reflection makes the attribute mirror the LIVE value, so neither prop nor attribute is the declared record | **declared-state capture through the reflect-echo lock**: record the attribute channel only — genuine inbound writes (parser-applied markup, an external `setAttribute`/`removeAttribute`) in `attributeChangedCallback` — and EXCLUDE reflect echoes of programmatic prop writes via the props-layer outbound-lock probe `isReflectEcho(host, name)` (an internal props.ts seam, deliberately NOT on the dom barrel); the first-connect snapshot reads that record, never the live prop OR attribute (both polluted by any pre-connect programmatic write — the `ui-radio-group` value-setter direct-match path was the reproducer). Preserves native asymmetry: declared-then-programmatically-unchecked keeps `defaultChecked === true` (the removal is the write's own echo, not an un-declaration). Generalizes to ANY reflecting stateful prop needing a declared-vs-live split | GH #1333 · PR #1390 · `controls/_base/indicator-element.ts` (`#declaredChecked`, all four indicators inherit) + `dom/props.ts` (`isReflectEcho`) |
| CSS Anchor Positioning (the enhanced anchored-overlay path) silently renders a panel detached from its trigger | the measured **two-state failure taxonomy + verify-guard/demote-per-session**: (1) IACVT static-position — an open popup whose `anchor()` references fail to resolve gets all-`auto` insets and renders IN-FLOW beside its trigger, visually detached; (2) the stale-pin freeze — anchor scroll compensation TRANSLATING a baked clamp-bound `max(0px, …)` pin without ever re-running anchor layout (reproduced deterministically under an inner scroller). ONE guard path: `verifyAnchoredPlacement()` synchronously post-open + a ResizeObserver reveal signal + an IntersectionObserver scroll-into-view signal, demoting THAT open session to the JS path on mismatch and re-trying enhanced on the next open — zero scroll/resize listeners kept on the enhanced path. Lineage: supersedes PR #1359's partial `computedInsetsAllAuto()` one-shot detector (false-negative on boxless anchors, blind to the freeze) — one guard, never two competing ones; a boxless popup DEFERS rather than demotes | GH #1339 · PR #1401 (supersedes #1359; lineage record in the trait header) · `traits/overlay.ts` (`verifyAnchoredPlacement`, the `anchoredFallback` session state) |
| Categorical identity in a data-viz mark (slices/segments) where hue-only identity is the trap | **CVD-safe categorical identity**: carry identity on FOUR channels — ORDER (DOM order = data order = clockwise from 12 o’clock) + LABEL + PRINTED VALUE (per-slice percent as real DOM text in a `role=list` key list — the legend IS the data list, never an SVG legend layer) + a single-family LIGHTNESS-ramp fill (six `--ui-pie-chart-slice-{1..6}-ink` tokens over pairwise-distinct, strictly monotone primary TONE primitives per scheme — lightness is the channel deuteranopia keeps, and monotone-with-data-order means a CVD reader still reads "first slice = brightest"); the fill is the FOURTH carrier, never the only one. Generalizes ADR-0057’s non-color-signifier law (intent never travels by color alone) to data-viz categorical identity; a hue-varied palette was rejected — it buys nothing the ramp doesn’t while costing CVD ordering, and the intent families would mis-signal (red = danger). Beware the emphasis-role trap the Amendment fixed: `-bright/-high/…/-muted` roles are mode-RELATIVE (zigzag, duplicates, one a different hue family) — ramp over tone PRIMITIVES, pinned resolved-distinct in both schemes | ADR-0219 cl.4–5 + its 2026-08-19 tone-primitive Amendment (law generalized: ADR-0057) · `controls/pie-chart/pie-chart.css` (slice tokens) + `pie-chart-css.test.ts`/`pie-chart.browser.test.ts` (monotone-distinct pins, both schemes) |
| Native radio-group semantics (mutual exclusion + no self-deselect) needed on composed `ui-radio`s in a host whose content model forbids a `<ui-radio-group>` ancestor (e.g. `<tbody>` accepts only table elements) and whose contract mandates normal tab order — `ui-radio-group`'s `grouped()` hook would work but ALSO applies roving-tabindex demotion | **host-owned capture-phase click guard on the delegation root**: mutual exclusion is already structural where render rebuilds every `checked` from the selection model on commit (a property of the render, not the DOM); the missing half is no-self-deselect — a bare `ui-radio` has checkbox-style toggle semantics, so the host installs its own capture-phase `click` listener on the delegation node it already listens on and stops a click whose target is an ALREADY-CHECKED `[data-part="select"]` before the control's own toggle runs — `ui-radio-group`'s `grouped()` guard technique verbatim, host-owned instead of group-owned, with NO tabindex side effect. No `data-radio-group` marker (pulls in the roving-tabindex demotion), no shared `name` mint (grouped native inputs only) | ADR-0163 Amendment (2026-08-19, ratified) cl.4 re-reading · `controls/table/table.ts` (`connected()` capture guard on `#table`), technique source `controls/radio/radio.ts` (`grouped()`) |
| A ruled dogfood exception ("no native form elements" carve-out, sanctioned raw `<input>`/`<button>` anatomy) whose founding premise has gone stale — the fleet controls it was waiting on now exist | **exception RETIREMENT by amendment, not relaxation** (the first worked example): re-check the carve-out's premise against the shipped fleet — ADR-0163 cl.3's two native exceptions (checkbox/radio selection column, `<button>` sortable-header trigger) were "necessary" only before `ui-checkbox`/`ui-radio`/`ui-button` existed, yet all three shipped BEFORE the ADR itself; the amendment swaps only the TAG NAME of the stamped nodes (`<input>` → composed `<ui-checkbox>`/`<ui-radio>`, `<button>` → `<ui-button inline>`) while every accessibility rationale the exception was minted to preserve (internals-driven `ariaChecked`, native-parity keyboard activation, normal tab-stop) rides the FACE controls' own contracts unchanged — ZERO fleet-wide exceptions remain. The law is never relaxed; the carve-out that predated its own prerequisites is removed, and platform-freebies the natives carried (radio no-self-deselect) get named replacements (the click-guard row above) | ADR-0163 Amendment (2026-08-19, ratified — GH #1445, Kim ruling "ui-table must dogfood; BOTH cl.3 native exceptions fall") · `controls/table/table.ts` (composed `data-part` anatomy) |
