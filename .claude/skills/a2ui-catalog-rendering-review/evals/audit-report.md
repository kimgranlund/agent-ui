Skill: .claude/skills/a2ui-catalog-rendering-review/SKILL.md · Standards: skill-writing-rules · Lint: clean
Verdict: PASS (0 blocking · 2 major · 4 minor · 2 nit)

Depth: FLOOR (check-skill procedure). Lint line, verbatim: `skill-postwrite-invocation-lint · clean · .claude/skills/a2ui-catalog-rendering-review/SKILL.md` (exit 0). `eval_check · clean · evals/evals.json` (11 trigger / 9 no-trigger). Description 696 chars; uppercase NEVER/MUST NOT count 0; body 103 lines. Reviewed 2026-08-18. No file other than this report was written.

| ID | Verdict | Severity | Evidence (file:line) | Fix |
|----|---------|----------|----------------------|-----|
| R1 | PASS | — | SKILL.md:26 (sources read BEFORE any pixel), :53-54 (blind-identify line before title, miss ⇒ B4=1), :59-60 (promote rule + quadrant/owner routing). Deletion test: evals/baseline/attachment-card-review.md (fresh -p, no skill) produced a narrative with no gate table, no anchors, no quadrant/owner, no blind-identify line — each of the three lines is the measured delta (intent.md:64-67 records the same). | — |
| R2 | PASS | — | Description SKILL.md:7-9 carries the verbatim asks "review the a2ui catalog examples", "grab screenshots of each example and review them", "eval every card across the tabs" (= evals t01/t02/t05); fences :9-11 in parseable `NOT for … (owner)` form repel t12-t15. `eval_check` clean. | — |
| R2-a | minor | minor | intent.md:40 fences "site page shell/nav (docs-writer)" and evals t17 relies on it, but the description SKILL.md:9-11 carries no docs-writer fence — t17's no-trigger rests on absence of a match, not on a repellent. | Add `NOT for the page shell/nav (docs-writer)` to the description (re-budget: 696 → ~740 chars, under 1,024). |
| R3 | PASS | — | Procedural species (intent.md:3), dials explicit `disable-model-invocation: false` / `user-invocable: true` (SKILL.md:12-13), name head `-review` (zero-derivation verb) — one story. Not preloaded by any agent (grep of .claude/agents, .claude/skills, .claude/docs: no inbound reference), so preloadability is moot. | — |
| R4 | PASS | — | Load-bearing lines are declarative spec-present: SKILL.md:26 "the review reads them BEFORE", :52 "A gate carries ✓/✗ only", :90-91 "Done when …". 0 hard gates. Counter-example labelled "do not imitate" :101-103. | — |
| R5 | FAIL | major | Drift pairs with the rubric of record: SKILL.md:28-32 (ground-truth table) restates rubric §1 table (`a2ui-catalog-example.md:12-16`); SKILL.md:34-37 (`demonstrable` definition) restates §1.1 (`:21-36`); SKILL.md:55-58 (out-in/in-out gloss) restates §2 (`:38-49`) and §4 (`:82-91`); SKILL.md:59 promote rule restates rubric `:54` "every [gate] green AND no [review] < 3"; SKILL.md:83-85 A3 `open` exemption restates A3 row `:62`. The rubric is `proposed v0.1` (intent.md:51) — the most drift-prone state; the skill already mandates reading it (SKILL.md:20). Steelman ("the body must stand alone in context") does not survive: the rubric is required reading by :20, so the delta is the ORDER (sources → pixels), not the content. | Keep the ordering sentence and dim IDs; replace :28-32 with "sources per rubric §1", :34-37 with "write the §1.1 expected-card record", :59 with "the rubric's gate-to-promote rule", :83-85 with "A3's `open` exemption (rubric A3 row)". |
| R6 | PASS | — | Output contract at SKILL.md:64-77, failure branches :79-88, stopping predicate :90-91 — all inside the first ~1,600 tokens; example in the tail :93-103; references one level deep (rubric, repo runner). | — |
| R7 | PASS | — | Output contract :66-77 (header carries `probes: script|manual`, per-card row, findings, blind-identify log, coverage); 5 named failure branches :81-88; checkable stopping predicate :90-91. | — |
| R8 | FAIL | major | SKILL.md:46 "Its `index.json` row count equals the page header's "(N shown)" for `all`" — the runner pushes one row per theme × card (`scripts/screenshot-a2ui-catalog.mjs:49` outer theme loop, `:102` `rows.push({… theme …})`), so with `--theme both` (offered by the argument-hint :14 and step 2 :44) rows = 2N and the equality check fails on every complete run, minting a phantom coverage mismatch on the path the skill advertises. `(N shown)` = `names.length` (`site/pages/a2ui-catalog.ts:29`), the catalog name count. | Rewrite :46 to "the count of distinct `name` values in `index.json` (per theme) equals "(N shown)"". |
| R8-a | minor | minor | Step 1 scopes by tier key (:41) but the runner has no tier flag (`screenshot-a2ui-catalog.mjs:39` usage: `--base --out --theme --crop --only --scale`) and :62 only exempts the single-component case; a tier run's expected-N and capture path are unstated. | Add: tier scope ⇒ capture `all`, filter `index.json` by `tier`, expected-N = `TIERS`-membership count via `tierOf` (`site/lib/a2ui-catalog-tiers.ts:135`). |
| R8-b | minor | minor | :42 "Dev server: `http://localhost:5174`" — this is the runner's default (`screenshot-a2ui-catalog.mjs:18`); `vite.config.ts` sets no `server.port`, so a lone dev server sits on 5173 and 5174 only when 5173 is busy. The 404 branch (:42-43, :81) catches it, but the first run on a clean host predictably trips it. | State "runner default 5174; vite's own default is 5173 — pass `--base` from the `npm run dev` banner". |
| R5-a | minor | minor | Rubric §4 (`a2ui-catalog-example.md:84`) names the reviewer as "a fresh-context critic — `a2ui-review-agent` class, never the maker"; the skill's step 5 (:55-58) has the invoking session score the review dims itself and states no generator≠critic guard, and nothing in the repo (rubric, `a2ui-review`, any agent) points at this skill. Not a restatement defect but the same drift class: the rubric and the skill disagree on WHO judges. | One line in step 5: "the scorer is not the seat that authored the seeds/knobs under review"; and (owner: rubric maker) let rubric §4 name this skill as the procedure. |
| DM | N/A | — | Body carries no subagent/dispatch/fork/parallel language and no `context: fork` (frontmatter :1-15); "owner seat" (:60, :88) is finding hand-off, not delegation. Lint R1-R3 clean per the lint line. DM-R4/5/6 out of scope. | — |
| nit-1 | nit | nit | argument-hint :14 omits `--base` although step 1 (:42) reads it. | `"[Component | TIER | all] [--theme dark|light|both] [--base URL]"`. |
| nit-2 | nit | nit | intent.md:58 records "SKILL.md 106 lines"; file is 103 lines (`cat -n`). Ledger cosmetic. | Update on next intent touch. |

Verified pointers (runtime over claim): rubric of record exists with §1/§1.1/§2/§3 (A1–C3)/§4/§5/§6 as cited (`a2ui-catalog-example.md:8,21,38,52,82,93,112`); runner exists with `--out/--theme/--only/--base/--crop canvas` (`:7-8,39`), `section.catalog-item`/`.preview-canvas`/`index.json`/`empty-canvas` (`:59,82,93,113`); `scripts/eval-a2ui-catalog.mjs` absent as the skill states (:48-49); `catalog.json`, `A2UI_INITIAL`(:223) / `SAMPLE_TREES`(:283) / `sampleFor`(:580) in `site/lib/component-preview.ts`; `TIERS`/`tierOf`/`seedsUsingType` (`a2ui-catalog-tiers.ts:33,135,155`); `WidgetFactory` (`a2ui/src/catalog/types.ts:19`), `factories.ts` at `catalog/default/`; seats `example-authoring-agent`, `a2ui-build-agent`, `component-build-agent` (`.claude/agents/`), `screens:component-checker` (screens plugin), `docs-writer` (teamwork plugin), `a2ui-review` (repo skill). Dismissal check for the "playwright resolves only from repo root" claim (:45): `screenshot-a2ui-catalog.mjs:44` dynamic `import('playwright')` resolves from the script's own location — claim holds.

Top 3: 1) R8 major — the `index.json` row-count = "(N shown)" check is false under `--theme both` (rows are theme×card); count distinct names per theme (SKILL.md:46). 2) R5 major — five drift pairs with the proposed-v0.1 rubric (SKILL.md:28-37, :55-59, :83-85); keep the ordering + dim IDs, point at rubric §1/§1.1/§2/A3/gate-to-promote for the content. 3) R8-a/R8-b minor — tier-scope capture path and expected-N unstated; 5174 is the runner default not vite's — name both and route through `--base`.

## Re-audit 2026-08-18 (fix leg)

Skill: .claude/skills/a2ui-catalog-rendering-review/SKILL.md · Standards: skill-writing-rules · Lint: clean
Verdict: PASS (0 blocking · 3 major · 6 minor · 3 nit)

Depth: FLOOR (check-skill procedure), scoped to the 2026-08-18 widening (step 7 + `references/catalog-pipeline.md`). Lint line, verbatim: `skill-postwrite-invocation-lint · clean · SKILL.md` (exit 0). `eval_check · clean · evals/evals.json` (now 15 trigger / 9 no-trigger). Description 679 chars (yaml-parsed); uppercase NEVER/MUST NOT in SKILL.md: 0; body 126 lines; reference 169 lines (Grep-first note at :3-5, no TOC — under 200 lines, acceptable). The skill directory is untracked (`git status`: `??`), so this is a pre-commit review. No file other than this report was written.

### Focus (1) — is the fix procedure self-sufficient and correctly ordered?

Mostly yes. SKILL.md:64-74 carries the decision rule (shape by quadrant), the layer order, the gate discipline (regen owed per layer, `check`+`test` by exit code), the finish criterion (held dims ≥ 3/✓ on a NEW capture, :71-72), and the two dispatch boundaries (:73-74); the output contract has a Fixes line (:92-93); failure branches :104-110 cover red-gate → matrix row, control-side dispatch, and the catalog.json ⇒ baseline-recapture pairing; the stopping predicate :112-114 includes the fix leg. The reference holds the file/test/command detail — the correct split (one level deep, Grep-first). Three gaps, filed below: the executor ambiguity between body and reference (major-1), the `card`-quadrant fix path (minor-3), and the internal ordering wobble between §1/body and §5(iii) (minor-4).

### Focus (2) — pointer spot-check (runtime over claim; 30 pointers checked)

HOLD exactly: `factories.ts` `accessorFactory` :246 · `attachmentFactory = mappedAccessorFactory('ui-attachment',{name:'filename'})` :792 · `buttonFactory` :119 · `optionFactory` :395 · `menuItemFactory` :440 · `defaultFactories` :1015-1082 · non-identity invariant :231-232 · `catalog.json` `"Attachment"` :583 with `"mapsTo": "filename"` :585, `"functions"` :733 · `catalog.ts` `validateComponent` :228 / `validateValueMark` :290 / `validatePropDef` :306 · `descriptor-agreement.test.ts` `AGREEMENT_EXCEPTIONS` :139, sole entry `Text.variant` :141-142, residue test :227-228 · `default/index.test.ts` `EXCLUSION_ALLOWLIST` :165, fleet gate :240-256, residue guard :259 · `factories.test.ts` :56/:76, walker ~:300-340 · `registry.test.ts:75-76` `CATALOG_FACTORY_MISSING` · `prompt-equivalence.test.ts` :47-49 baseline compare · `recapture-baseline.test.ts` `RECAPTURE_BASELINE=1` :10/:27 · `examples.test.ts` GH #729 block :396, validate/render :111/:115 · `import-seeds.ts` `SEEDS_BY_MODULE` :239, drift halt :303-310 · `system-prompt.ts` `catalogInventory` :184 · `props-gen-driftwire.test.ts:20` (10 CONVERTED, list identical) · `llms.test.ts:35` · `component-preview.ts` `A2UI_INITIAL` :223 / `SAMPLE_TREES` :283 / `sampleFor` :580 / `COMPONENT_SAMPLE_CHILDREN` :631 / `NO_SLOT_TEXT` :1105 / `STRUCTURAL` :1193 / `SLOT_TEXT_OK` :1207 · `component-preview-catalog.test.ts` GH #978 block :100-142 · `a2ui-catalog-tiers.test.ts` :17-40 · scripts `generate-props.mjs`, `build-dogfood-assets.mjs`, `generate-llms-full.mjs`, `screenshot-a2ui-catalog.mjs` all present; `eval-a2ui-catalog.mjs` absent as stated · all 5 seats in `.claude/agents/`, all 9 method skills in `.claude/skills/`, `node-idioms.md`, both rubrics, SPEC + LLD present · `DISPOSITION_ALLOWLIST` (lives in `corpus/disposition-allowlist.ts`, imported by `admission-coverage.test.ts:29`).
DRIFTED (small, the reference self-declares "line refs drift" :3-4): `mappedAccessorFactory` is :265 (ref :264-270) · `types.ts` `WidgetFactory` :19 (ref :20-46) · `variant.ts` `resolveFactory` :31-36 (ref :33-38) · `descriptor-agreement.test.ts` real-catalog describe is :207-227 (ref :168-227; :118-137 sits inside a helper) · `component-preview-catalog.test.ts` orphan checks are :82-88 (ref :66-77) · `a2ui-catalog-tiers.ts` `NESTED_ONLY` :29 / `TIERS` :33 / `TIER_OF` :45-124 (ref :31 / :35 / :48-137) · `default/index.ts:23` — `CATALOG_MALFORMED` is thrown by `catalog.ts:176` inside `loadCatalog`, called from `default/index.ts:25` (misattributed line, right mechanism).

| ID | Verdict | Severity | Evidence (file:line) | Fix |
|----|---------|----------|----------------------|-----|
| R2 (major-1: description grab surface) | FAIL | major | Description SKILL.md:8-9 now lists "fix the X catalog card / its seeds / its catalog row" as a positive while the ROW-grading fence (intent.md:37 "NOT for grading the catalog ROW — catalog.json/factory/tests (a2ui-review)") is absent from the description; its two fences :9-11 name only "composed payload or corpus record" and "control's own anatomy". Eval t12 ("grade this catalog row … for the new Rating type", no-trigger, evals.json:61-63) now competes with a description that contains "catalog row" and "add a prop or new type to the A2UI catalog" — its no-trigger rests on the verb ("grade") not on a repellent. Steelman: "`a2ui-review` is model-only knowledge preloaded by a seat, never user-routed (a2ui-review/SKILL.md:7)" — true, but that makes THIS skill the only routable surface for "grade this row", i.e. the grab lands here with no competitor. Finding survives. | Add `NOT for grading a catalog row's code — catalog.json/factory/tests (a2ui-review)` to the description (679 → ~760 chars, under 1,024) and keep t12. |
| R7/R5 (major-2: who executes shapes i/ii) | FAIL | major | SKILL.md:73-74 rules the skill performs in-package edits and dispatches ONLY control source (`component-build-agent`) and corpus admission (`a2ui-corpus-curation`) — matching intent.md:40's ruling. The reference contradicts it in three places a fix-leg reader hits first: §5 shape headers assign owners — "(i) … owner example-authoring-agent" (catalog-pipeline.md:132), "(ii) … a2ui-build-agent (+ component-build-agent …)" (:140) — and §4's "Owner seat" table (:113-121) lists `catalog.json`/`factories.ts` → a2ui-build-agent, `A2UI_INITIAL`/`SAMPLE_TREES` → example-authoring-agent, `TIER_OF` → docs-writer, `Seeds` → a2ui-payload-authoring-agent, none marked "the skill edits this itself". A reader following §5(i) literally dispatches example-authoring-agent (whose own description claims `component-preview.ts`, `.claude/agents/example-authoring-agent.md:4-5`); one following SKILL.md:73 edits it inline. Both produce a fix, so not blocking, but the ambiguity is on the exact axis Kim ruled. | One sentence at the top of §4 (and drop the owner tags from §5(i)/(ii) headers): "Owner = the seat that holds this artifact when the fix runs OUTSIDE this skill and the rubric-§6 routing target for review-only runs; on `--fix` this skill edits every a2ui/ + site/ artifact here itself and dispatches only control source (component-build-agent) and corpus admission (a2ui-corpus-curation) — SKILL.md step 7." |
| Behavior check (major-3: fix leg unmeasured) | FAIL | major | skill-writing-rules' tier ladder: a semantic body change owes lint + fresh-context critic + behavior check. `evals/with-skill/` holds only `attachment-card-review.md` (34 lines, review run, no `## Fixes` block); intent.md:49 assertion 6 (fix leg: shape named, files in layer order, regen commands, exit codes, re-graded delta, baseline recapture paired with a catalog.json edit) has no captured evidence; intent.md:71 P5 cites assertions 1-5 only. Steelman: "the reference was mapped from the tree today, so it is correct by construction" — that proves the map, not that a fresh session FOLLOWS the layer order and recaptures the baseline; the intent's own delta paragraph (:52-55) names exactly the omissions only a run would surface. Survives. | Run one fresh `-p` session: `--fix Attachment` (shape i, seed-only, on a throwaway branch); capture the report to `evals/with-skill/attachment-card-fix.md`; check assertion 6 field-by-field; record in intent.md P5. |
| R2 (minor-1: control-fix fence names the wrong owner) | minor | minor | Description fence :10-11 "NOT for the ui-* control's own anatomy (screens:component-checker)" names a CHECKER; the fix leg's near-miss t24 ("fix the control css", evals.json:121-123, no-trigger) needs the BUILDER named as the owner. SKILL.md:73-74 and :107-108 route control-side changes to `component-build-agent`, so the fence and the body disagree on the repellent's owner for a fix ask. | `NOT for the ui-* control's own anatomy or source (screens:component-checker · component-build-agent)`. |
| R2-a carry-over (minor-2) | minor | minor | Still open from the first audit: no docs-writer / page-shell fence in the description (SKILL.md:9-11); intent.md:41 and t17 (evals.json:85-88) rely on it. Now sharper because the fix leg edits `TIER_OF` in shape (iii) step 6 (catalog-pipeline.md:165) — site-adjacent asks ("the tab filter is wrong") sit next to a positive. | Add `NOT for the page shell/nav (docs-writer)` alongside major-1's fence; budget stays under 1,024. |
| R7 (minor-3: `card` quadrant has no fix path) | minor | minor | Step 7 maps L-only → (i), R-only/L↔R → (ii), missing type → (iii) (SKILL.md:65-69); the fourth quadrant `card` (rubric §6 :121 → docs-writer) has no shape and no stated hand-off when `--fix` is on. :77 ("review-only → the owner column is the handoff") does not cover the `--fix` path. | Add to step 7: "quadrant `card` → no fix shape; the finding hands off to docs-writer via the owner column and the Fixes line reads `deferred (card/docs-writer)`." |
| R7 (minor-4: order stated two ways) | minor | minor | Body :69-70 "layer ORDER (control → row → seed → site → regen)" and reference §1 (:7, layers 0-4 "in edit order") put regen last; §5(iii) (:154-168) recaptures the prompt baseline at step 4 BEFORE seed (5) and site (6), and §5(ii) runs control regen at step 1. Dependency-wise both are correct (the baseline depends only on catalog.json), but a reader told "regen last" and then shown "recapture at step 4" cannot tell which is normative. | Reword :69-70 to "dependency order (control → row → seed → site), each layer's owed regen run as that layer lands (§3)". |
| R5 (minor-5: transcribed schema) | minor | minor | catalog-pipeline.md:23-30 transcribes the `catalog.json` component-entry shape (`type/bindable/mapsTo/format/required/rejectFunctionCall`, `children`, `value` slots) — a drift pair with `catalog.ts validatePropDef` :306-323 / `validateValueMark` :290-304 (both cited on the same lines) and LLD C1. A new PropDef field lands in the validator and silently not here. | Replace the block with the one-line pointer already present ("shape per `catalog.ts validatePropDef`/`validateValueMark`; the Attachment row :583-590 is the worked example") and delete the transcription. |
| R5 (minor-6: residual §1 drift pair) | minor | minor | SKILL.md:30-34 ground-truth table still mirrors rubric §1 (`a2ui-catalog-example.md:12-16`), condensed and with the tiers row added; intent.md:71 records this R5 major as FIXED. The load-bearing delta is :28 (sources BEFORE pixels) and :36-38 (write the §1.1 record); the table itself is the rubric's. Downgraded from the first audit's major because the restatement is now 3 rows and adds one source the rubric lacks (`a2ui-catalog-tiers.ts`). | Keep the tiers row (rubric §1 has "U — uses line" but not the tier source); replace the L/R rows with "L and R sources per rubric §1". Or (rubric owner) add the tier source to rubric §1 and drop the table here. |
| R1 | PASS | — | Deletion test on three fix-leg lines: :71-72 "done when the held dims read ≥ 3 / ✓ on the NEW screenshot, not when the edit compiles" (without it a fix closes on green tests — the exact regression intent.md:52-55 names); :109-110 catalog.json ⇒ recapture, "a diff that moves anything beyond inventory lines is a defect to report, not to commit" (without it the recapture launders a prompt change — `recapture-baseline.test.ts:10-17` warns the same); :104-106 "the fix lands in that layer … never in the test" (without it the allowlist/`AGREEMENT_EXCEPTIONS` become the fix). All three survive. | — |
| R3 | PASS | — | Procedural species (intent.md:3), dials explicit SKILL.md:12-13, `-review` head; the widening keeps the species — fix is a step of the procedure, not a command (no side-effect gate needing `disable-model-invocation`). Not preloaded anywhere (grep `.claude/agents` for the handle: none). | — |
| R4 | PASS | — | 0 uppercase hard gates in SKILL.md (grep count 0); the two lowercase "never"s (:106, :108) are parameter-style locks naming the forbidden neighbor (the test / the package). Reference §1 :40 quotes the source invariant's "must NOT" in a code citation, not a gate of its own. Step 7's imperatives are numbered-step register, sanctioned for procedural species. | — |
| R6 | PASS | — | Output contract :79-94, failure branches :96-110, stopping predicate :112-114 — all before the example (:116-126); reference one level deep; description first sentence names the capability. ~1,900 tokens to the end of the stopping predicate. | — |
| R7 | PASS (with minor-3/-4) | — | Fixes line in the contract :92-93 has every field assertion 6 demands; 7 named failure branches (3 fix-specific, :104-110); checkable stopping predicate :112-114 (exit 0, fresh capture, held dims cleared). | — |
| R8 | PASS | — | Numeric anchors on the fix leg: "≥ 3 / ✓" :72; "exit 0" :114; "10 CONVERTED controls" (catalog-pipeline.md:15, verified against `props-gen-driftwire.test.ts:20`); "ONLY inventory lines move" :105. | — |
| DM | N/A (in scope, nothing to score) | — | Body now mentions dispatch (:60-61 critic, :74/:107 component-build-agent), so the gate is in scope. No `context: fork`, no `agent:`/`model:` frontmatter (:1-15) → DM-R4/R5/R6 have no object; lint R1-R3 clean per the lint line. | — |
| Evals coverage (Focus 4) | PASS (with major-1/minor-1) | — | Fix triggers present: t15 seeds (evals.json:75-78), t21 prop on existing type (:105-108), t22 end-to-end (:110-113), t23 new pattern (:115-118) — one per shape (i)/(ii)/(iii)+pattern; near-miss negatives: t24 control css (:120-123), t12 row grading (:60-63), t18 corpus record (:90-93). Missing near-miss negatives on the fix boundary's dispatch targets: "recapture the prompt baseline" (a2ui-prompt-authoring), "admit this seed to the corpus" (a2ui-corpus-curation), "add the Croupier persona catalog" (a2ui-multi-catalog). | Add three no-trigger cases (t25-t27) naming those asks; re-run `eval_check`. |
| nit-1 | nit | nit | intent.md:68 P2 "11 trigger / 9 no-trigger", :69 P3 "SKILL.md 106 lines, description 696 chars", :60 "~25-row gate matrix" — measured today: 15/9, 126 lines, 679 chars, 20 matrix rows (catalog-pipeline.md:75-94). Ledger cosmetic. | Update on the next intent touch (status is `forging`, so a touch is owed anyway). |
| nit-2 | nit | nit | The line-drift list above (7 pointers off by 1-13 lines, one misattributed to `default/index.ts:23`). The reference's own header (:3-4) already discharges the risk. | Correct the 7 refs in the same pass as major-2; consider dropping line ranges from test-file pointers and keeping the `describe` names, which drift slower. |
| nit-3 | nit | nit | argument-hint :14 carries `--fix` but not `--base` (first audit nit-1, still open) though step 1 :44 reads it. | `"[Component | TIER | all] [--theme dark|light|both] [--base URL] [--fix]"`. |

Dismissed after check: "shape (iii) step 1 sends a not-agent-emittable control to a PERMANENT allowlist entry, contradicting the fleet gate" — read `default/index.test.ts:165-215,232-273`: the residue guard permits permanent entries (ToastRegion, ADR-0112 cl.6) and only bites when a seeded key ALSO has a catalog row; the reference states this correctly (catalog-pipeline.md:155-157). Not a finding. "The pipeline omits `catalog.test.ts`/`naming.test.ts` as the malformed-row gate" — they are the first matrix row (:75). Not a finding.

Top 3: 1) major-1 — the description gained "catalog row"/"add a prop or new type" positives without the row-GRADING fence; add `NOT for grading a catalog row's code (a2ui-review)` (+ the docs-writer fence, minor-2) and re-budget. 2) major-2 — SKILL.md:73-74 says the skill edits a2ui/+site/ artifacts itself, but catalog-pipeline.md §4/§5 hand shapes (i)/(ii) to example-authoring-agent/a2ui-build-agent — state the executor rule once at the top of §4 and untag §5. 3) major-3 — the fix leg has no captured behavior evidence (assertion 6); one fresh `-p` `--fix Attachment` run into `evals/with-skill/`.
